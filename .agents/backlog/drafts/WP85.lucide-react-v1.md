# Lucide React 0.x → 1.x

**TaskID**: WP85
**Status**: draft
**Order**: ③ — after the ② batch
**Workflow**: `aexecute` directly — codebase audit already confirmed no brand icons

## Elevator's Pitch

Bump lucide-react from 0.x to 1.x. The v1.0 release removed brand icons (Github, Figma, Slack, etc.) but the project only uses generic icons — confirmed safe by codebase audit.

## Business Gain

Gets off the 0.x pre-release train onto a stable major version. Future minor/patch updates will be within a stable API.

## Current State

- `lucide-react` at `^0.564.0` in package.json, installed 0.514.0
- Latest: 1.8.0
- Project uses ~20 icon imports, all generic: CheckSquare, ChevronRight, Plus, Trash2, GripVertical, ListTodo, Menu, X, Clock, Circle, CheckCircle2, etc.
- `LucideIcon` type import used in `src/42go/layouts/app/types.ts`

## Desired State

- `lucide-react` at `^1.8.0`
- All icon imports resolve
- `LucideIcon` type still works
- `npm run qa` passes

## Definition of Success

- Package updated
- `npm run qa` passes
- No missing icon imports

## Additional Context

Steps:

1. Change `"lucide-react": "^0.564.0"` → `"lucide-react": "^1.8.0"`
2. `npm install`
3. `npm run qa`

v1.0 breaking changes:

- Removed brand icons (Github, Figma, Slack, Discord, etc.)
- Project audit confirmed: zero brand icon usage — all safe

## Constraints

- Depends on PE62 (fix stale lockfile) being done first

## Acceptance Criteria

- [ ] lucide-react at 1.x
- [ ] `npm run qa` passes
- [ ] All icon imports resolve correctly

## Related to

- [UG00] Update Dependencies (Research) — [outcome](../completed/UG00.update-dependencies.outcome.md)
- [PE62] Fix Stale Lockfile (prerequisite)
