# ESLint 9 → 10

**TaskID**: UT41
**Status**: draft
**Order**: ⑤ — after QQ22 (TypeScript 6)
**Workflow**: `arefine` then `aplan` — new recommended rules may flag many violations

## Elevator's Pitch

Upgrade ESLint from 9.x to 10.x. The project already uses flat config, so the main risk is new rules in `eslint:recommended` flagging new violations. Optional — no security urgency.

## Business Gain

Stays on latest ESLint major. Access to new rules and improvements.

## Current State

- `eslint` at `^9.39.2` in package.json, installed 9.32.0
- Latest: 10.2.1
- Project uses flat config (`eslint.config.mjs`)

## Desired State

- ESLint 10.x installed
- All lint rules pass or are intentionally adjusted
- `npm run qa` passes

## Definition of Success

- ESLint 10 installed
- `npm run qa` passes
- No unintentional rule suppressions

## Additional Context

Steps:

1. Change `"eslint": "^9.39.2"` → `"eslint": "^10"`
2. `npm install`
3. Fix any config/rule changes
4. `npm run qa`

ESLint 10 breaking changes:

- eslintrc fully removed (project already uses flat config — should be fine)
- New `eslint:recommended` rules may flag new violations
- New config lookup algorithm
- Some deprecated APIs removed

## Constraints

- Should follow QQ22 (TypeScript 6 upgrade)
- Optional / low priority — defer if other tasks take longer than expected

## Acceptance Criteria

- [ ] ESLint 10.x installed
- [ ] Flat config still works
- [ ] `npm run qa` passes

## Related to

- [UG00] Update Dependencies (Research) — [outcome](../completed/UG00.update-dependencies.outcome.md)
- [QQ22] TypeScript 6 (should be done first)
