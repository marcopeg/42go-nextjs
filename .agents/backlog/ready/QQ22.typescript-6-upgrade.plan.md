# Plan — TypeScript 5 → 6

**TaskID**: QQ22
**Status**: draft

## Goal

Upgrade TypeScript from 5.x to 6.x, migrate tsconfig.json for new compiler defaults, and fix all type errors surfaced by stricter defaults. All four apps (default, quicklist, notes, calendar) must compile cleanly with zero type errors, and `npm run qa` must pass without warnings or errors.

## Milestones

### Milestone 1 — Baseline & Preparation

- [ ] Step 1 — Verify current state is green
  - Achieve: Document baseline before any changes (current TS5.x, all apps compiling)
  - Create: Session notes documenting current error count (should be 0)
  - Modify: None
  - Delete: None
  - Touch points: Terminal verification only
  - Validation: `npm run qa` passes cleanly, no type errors reported
  - Notes: Take baseline measurement for regression detection

- [ ] Step 2 — Create backup branch/state
  - Achieve: Ensure ability to revert if needed
  - Create: None (git is the backup)
  - Modify: None
  - Delete: None
  - Touch points: Git status verification
  - Validation: `git status` shows workspace clean or all changes committed
  - Notes: Ensures we can rollback if major issues arise

### Milestone 2 — TypeScript 6 Installation & tsconfig Migration

- [ ] Step 3 — Run ts5to6 migration tool
  - Achieve: Auto-migrate tsconfig.json for TS6 defaults
  - Create: None (tool updates in-place)
  - Modify: `tsconfig.json` (target, module, lib, possibly strict mode changes)
  - Delete: None
  - Touch points: `tsconfig.json` only
  - Validation: Tool runs without errors; inspect tsconfig.json diff
  - Notes: This handles most of the migration automatically; review changes carefully

- [ ] Step 4 — Add explicit types declaration
  - Achieve: Explicitly declare `"types": ["node"]` (TS6 requirement)
  - Create: None
  - Modify: `tsconfig.json` → compilerOptions.types = ["node"]
  - Delete: None
  - Touch points: `tsconfig.json` compilerOptions
  - Validation: tsconfig.json contains `"types": ["node"]` in compilerOptions
  - Notes: TS6 no longer auto-includes @types packages; must be explicit

- [ ] Step 5 — Update package.json and install
  - Achieve: Bump TypeScript to 6.x and resolve dependencies
  - Create: None
  - Modify: `package.json` → `"typescript": "^6"` in devDependencies
  - Delete: None
  - Touch points: `package.json`, `package-lock.json`
  - Validation: `npm install` completes successfully; `npm list typescript` shows 6.x
  - Notes: `npm install` may trigger lock file updates; commit as part of TS6 upgrade

### Milestone 3 — Type Error Discovery & Triage

- [ ] Step 6 — Compile and capture all type errors
  - Achieve: Generate complete list of TS6-surfaced type errors
  - Create: Execution notes documenting all errors by file and category
  - Modify: None
  - Delete: None
  - Touch points: Compiler output (terminal)
  - Validation: Run `npx tsc --noEmit` to get exhaustive error list; capture in notes
  - Notes: This is critical—capture errors by file, error code, and type; don't try to fix yet

- [ ] Step 7 — Categorize errors by severity and location
  - Achieve: Organize errors into fixable categories (async, strict nullability, module types, etc.)
  - Create: Execution notes with error categories (e.g., "null/undefined checks", "async/await", "import types", "Node types")
  - Modify: None
  - Delete: None
  - Touch points: Error analysis only
  - Validation: All errors mapped to a specific category; no errors left uncategorized
  - Notes: This helps determine fix strategy and order

### Milestone 4 — Fix Type Errors (Largest Milestone)

- [ ] Step 8 — Fix Node.js type errors
  - Achieve: Resolve type issues related to Node.js APIs (missing declarations with explicit types)
  - Create: None
  - Modify: Files importing or using Node APIs (e.g., `src/42go/db`, `src/42go/utils/filesystem`)
  - Delete: None
  - Touch points: Imports of `fs`, `path`, `process`, Node built-ins; any files using Node APIs
  - Validation: `npx tsc --noEmit` shows zero Node-related errors
  - Notes: Use type assertions or import from @types/node if needed

- [ ] Step 9 — Fix strict null/undefined errors
  - Achieve: Add explicit null checks and type guards where TS6 strict mode requires them
  - Create: None
  - Modify: Core files in `src/42go/` that handle data (auth, policy, config, db, utils)
  - Delete: None
  - Touch points: Functions returning nullable values, array/object access without checks
  - Validation: `npx tsc --noEmit` shows zero null/undefined errors
  - Notes: May require adding `!` assertions, `?.` optional chaining, or type narrowing

- [ ] Step 10 — Fix async/await and Promise errors
  - Achieve: Ensure all async functions have proper return types and Promise handling
  - Create: None
  - Modify: API routes, server components, middleware, utility functions using async/await
  - Delete: None
  - Touch points: `src/app/api/**`, `src/middleware.ts`, `src/42go/db/index.ts`, any async helpers
  - Validation: `npx tsc --noEmit` shows zero Promise/async errors
  - Notes: May require explicit `Promise<T>` return types or `void` assertions

- [ ] Step 11 — Fix any/unknown type errors
  - Achieve: Replace `any` with proper types; add explicit types where needed
  - Create: None
  - Modify: Files with implicit `any`, untyped destructuring, or loose component props
  - Delete: None
  - Touch points: Component files, utility functions with loose typing
  - Validation: `npx tsc --noEmit` shows zero `any` usage errors
  - Notes: Search for `any` in error output; prioritize high-risk areas (auth, policy, db)

- [ ] Step 12 — Fix import/module resolution errors
  - Achieve: Ensure all imports resolve correctly under TS6 module rules
  - Create: None
  - Modify: Import statements that may have changed behavior in TS6 (especially relative vs absolute)
  - Delete: None
  - Touch points: All `src/**` files if module handling changed
  - Validation: `npx tsc --noEmit` shows zero module resolution errors
  - Notes: TS6 module defaults may affect bundler/ESM handling; verify with `next build`

- [ ] Step 13 — Fix component and props type errors
  - Achieve: Ensure all React components have explicit prop types
  - Create: None
  - Modify: React component files (`.tsx` files) with untyped or loosely typed props
  - Delete: None
  - Touch points: `src/42go/components/**`, `src/components/**`, `src/app/**` TSX files
  - Validation: `npx tsc --noEmit` shows zero component/props errors
  - Notes: Use React.FC<Props> or explicit return types for components

- [ ] Step 14 — Fix remaining miscellaneous errors
  - Achieve: Address any remaining type errors not covered by previous steps
  - Create: None
  - Modify: Files with residual type issues (edge cases, advanced types, generics)
  - Delete: None
  - Touch points: Utility files, complex generic functions
  - Validation: `npx tsc --noEmit` shows zero remaining errors
  - Notes: Iterate until full zero-error state is achieved

### Milestone 5 — Multi-App Validation

- [ ] Step 15 — Verify default app compiles cleanly
  - Achieve: Ensure default app routes and components have no type errors
  - Create: None
  - Modify: Only if errors found in default app code
  - Delete: None
  - Touch points: `src/config/default/**`, `src/app/` (default routes)
  - Validation: `npm run qa` passes; default app builds without type errors
  - Notes: npm run qa includes `eslint . && next build`; must both pass

- [ ] Step 16 — Verify quicklist app compiles cleanly
  - Achieve: Ensure quicklist routes and components have no type errors
  - Create: None
  - Modify: Only if errors found in quicklist app code
  - Delete: None
  - Touch points: `src/config/quicklist/**`, app-specific quicklist files
  - Validation: `npx tsc --noEmit` shows zero errors for quicklist; Next.js build includes quicklist
  - Notes: Multi-app build should automatically verify all apps through standard build

- [ ] Step 17 — Verify notes app compiles cleanly
  - Achieve: Ensure notes routes and components have no type errors
  - Create: None
  - Modify: Only if errors found in notes app code
  - Delete: None
  - Touch points: `src/config/notes/**`, app-specific notes files
  - Validation: `npx tsc --noEmit` shows zero errors for notes
  - Notes: Shared codebase means one global typecheck validates all apps

- [ ] Step 18 — Verify calendar app compiles cleanly
  - Achieve: Ensure calendar routes and components have no type errors
  - Create: None
  - Modify: Only if errors found in calendar app code
  - Delete: None
  - Touch points: `src/config/calendar/**`, app-specific calendar files
  - Validation: `npx tsc --noEmit` shows zero errors for calendar
  - Notes: Final app validation

### Milestone 6 — Final Verification & Cleanup

- [ ] Step 19 — Run full QA suite
  - Achieve: Ensure linting, type checking, and build all pass without warnings or errors
  - Create: None
  - Modify: None
  - Delete: None
  - Touch points: Terminal execution only
  - Validation: `npm run qa` completes successfully with exit code 0
  - Notes: This is the final gate—all errors must be fixed to pass

- [ ] Step 20 — Test runtime functionality (no regressions)
  - Achieve: Verify that the app runs without runtime errors on all endpoints
  - Create: Execution notes documenting test coverage
  - Modify: None
  - Delete: None
  - Touch points: Manual testing or existing integration test suite if available
  - Validation: App starts, all major routes respond, no 500 errors, data loads correctly
  - Notes: Quick smoke test of key features across all apps; dev server running

- [ ] Step 21 — Final documentation
  - Achieve: Record what errors were fixed, any non-obvious type changes, and lessons learned
  - Create: Execution notes with summary of fixes applied
  - Modify: None
  - Delete: None
  - Touch points: Execution notes file
  - Validation: Notes contain list of major error categories, refactoring done, and outcome
  - Notes: This helps future upgrades and documents technical debt cleared

## Success Criteria

- ✅ TypeScript 6.x installed and active
- ✅ tsconfig.json migrated via ts5to6
- ✅ `"types": ["node"]` explicitly declared
- ✅ **Zero type errors** across entire codebase (`npx tsc --noEmit` output: 0 errors)
- ✅ All four apps (default, quicklist, notes, calendar) compile without type errors
- ✅ `npm run qa` passes (linting + build)
- ✅ No runtime regressions in manual testing
