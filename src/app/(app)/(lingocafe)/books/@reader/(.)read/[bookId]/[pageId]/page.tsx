"use client";

import { BookReadPage } from "@/app/(app)/(lingocafe)/books/_components/BookReaderPage";

const InterceptedBookReaderPage = () => <BookReadPage intercepted />;

export default InterceptedBookReaderPage;
