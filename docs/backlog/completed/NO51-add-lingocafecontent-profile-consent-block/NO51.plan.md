---
taskId: NO51
createdAt: 2026-05-04T16:17:46+02:00
updatedAt: 2026-05-04T18:39:04+02:00
---

# Plan — Add LingoCafe Profile Legal and Consent Blocks

## Goal

Add required Terms and Privacy acknowledgements plus optional Marketing and Early Birds consent controls to LingoCafe profile persistence, `/profile`, and `/books` onboarding. Store minimal consent evidence in `lingocafe.profiles.data` using readable `consent_{key}` objects without collecting IP, User-Agent, screen resolution, or fingerprinting data.

## Milestones

### Milestone 1 — Legal Content and Constants

- [x] Step 1 — Add LingoCafe legal pages
  - Achieve: Provide real `/terms` and `/privacy` targets for the required acknowledgement links in the LingoCafe app.
  - Create: likely `src/config/lingocafe/legal-pages.ts` or inline page configs in `src/config/lingocafe/config.ts`; possibly `contents/lingocafe/terms.md` and `contents/lingocafe/privacy.md` if using markdown blocks.
  - Modify: `src/config/lingocafe/config.ts`.
  - Delete: n/a.
  - Touch points: `public.pages`, `DynamicPage`, `MarkdownBlock`, LingoCafe public metadata.
  - Validation: Visiting `/terms` and `/privacy` under the LingoCafe app resolves public pages instead of 404; links can be referenced from profile and onboarding UI.
  - Notes: Content can be placeholder-but-clear legal copy if final lawyer-approved text is not available; version constants must identify what was shown.

- [x] Step 2 — Define consent keys, versions, and helpers
  - Achieve: Centralize the four consent/acknowledgement keys and their evidence fields so `/profile`, `/books`, and API validation cannot drift.
  - Create: likely `src/app/api/(lingocafe)/lingocafe/_lib/profile-consent.ts` or `src/app/(app)/(lingocafe)/_components/profile-consent.ts` plus shared exported types if needed.
  - Modify: `src/app/api/(lingocafe)/lingocafe/_lib/reader.ts` imports/types as needed.
  - Delete: n/a.
  - Touch points: objects `consent_terms`, `consent_privacy`, `consent_mkt`, `consent_alpha`; params `value`, `date`, `version`, `source`, `method`; source values `profile`, `books-onboarding`; method `checkbox-submit`.
  - Validation: TypeScript compile catches invalid consent key/param names; no duplicated hardcoded consent key lists remain in UI and API code beyond acceptable presentation labels.
  - Notes: Use full ISO timestamps from the server save path.

### Milestone 2 — Profile API Persistence

- [x] Step 3 — Extend the profile payload schema
  - Achieve: Accept required legal acknowledgement booleans and optional consent booleans in `POST /api/lingocafe/profile`.
  - Create: n/a.
  - Modify: `src/app/api/(lingocafe)/lingocafe/_lib/reader.ts`.
  - Delete: n/a.
  - Touch points: `ProfilePayload`, `parseProfilePayload`, zod schema, validation error messages, `getReaderLanguages()`.
  - Validation: Invalid payloads reject missing/false required `terms` and `privacy`; optional `mkt` and `alpha` default safely when absent only where backward compatibility is intentionally allowed.
  - Notes: Preserve existing language validation and keep server-side validation authoritative.

- [x] Step 4 — Merge consent data into `profile.data`
  - Achieve: Save legal and optional consent evidence into existing `profile.data` without overwriting unrelated keys.
  - Create: n/a.
  - Modify: `src/app/api/(lingocafe)/lingocafe/_lib/reader.ts`.
  - Delete: n/a.
  - Touch points: `saveProfile`, current profile row read, current `data` normalization, `onConflict().merge`, no-op detection, `buildProfileUpdateDiff`.
  - Validation: Saving language-only values preserves existing consent keys; saving consent values preserves unrelated `data`; no-op saves do not change dates.
  - Notes: On first profile creation, write explicit false/date/version/source/method for unchecked `mkt` and `alpha`.

- [x] Step 5 — Keep profile-update event coherent
  - Achieve: Ensure existing `profile-update` event behavior does not break when profile `data` changes.
  - Create: n/a.
  - Modify: `src/app/api/(lingocafe)/lingocafe/_lib/reader.ts`.
  - Delete: n/a.
  - Touch points: `buildProfileUpdateDiff`, `trackReaderEvent`, transaction boundaries.
  - Validation: Changed consent values produce a meaningful diff or at minimum do not prevent existing language diffs/events; no-op consent saves do not create noisy events.
  - Notes: Task says consent evidence itself is not stored in `lingocafe.events`; this step is only about not breaking the already existing event behavior.

### Milestone 3 — Profile Blocks

- [x] Step 6 — Build `LingoCafeTermsPrivacy`
  - Achieve: Add a LingoCafe-specific required profile block with Terms and Privacy checkboxes, validation, loading state, and persistence via the global profile save action.
  - Create: `src/app/(app)/(lingocafe)/_components/LingoCafeTermsPrivacy.tsx`.
  - Modify: possibly shared LingoCafe profile client types/helpers under `src/app/(app)/(lingocafe)/_components/`.
  - Delete: n/a.
  - Touch points: `useProfileBlockHandle`, `SimplePanel`, required checkbox validation, `/api/lingocafe/profile`, links to `/terms` and `/privacy`.
  - Validation: `/profile` save is blocked with clear feedback when either required checkbox is false; checked values persist and reload from `profile.data`.
  - Notes: Privacy acknowledgement is not optional marketing consent. Keep wording clear.

- [x] Step 7 — Build optional `LingoCafeContent`
  - Achieve: Add the optional Marketing and Opportunities profile block with separate checkboxes for marketing emails and Early Birds questionnaires.
  - Create: `src/app/(app)/(lingocafe)/_components/LingoCafeContent.tsx`.
  - Modify: `src/config/lingocafe/config.ts`.
  - Delete: n/a.
  - Touch points: `useProfileBlockHandle`, `SimplePanel`, optional consent labels, consent data helpers, `/api/lingocafe/profile`.
  - Validation: Both optional values initialize from `profile.data`, can be toggled independently, and save without blocking when unchecked.
  - Notes: UI section title can be `Marketing & Opportunities`; component name should remain `LingoCafeContent` unless implementation chooses a clearer name and updates the task consistently.

- [x] Step 8 — Register profile blocks in LingoCafe config
  - Achieve: Render required legal block, language preferences, optional content block, account info, and logout in a useful order.
  - Create: n/a.
  - Modify: `src/config/lingocafe/config.ts`.
  - Delete: n/a.
  - Touch points: `app.profile.items`, `LingocafePreferences`, `LingoCafeTermsPrivacy`, `LingoCafeContent`.
  - Validation: `/profile` under LingoCafe host shows all configured blocks in order and the global `Save preferences` action coordinates validation/persistence.
  - Notes: Prefer Terms/Privacy before optional consents so required blockers are visible first.

### Milestone 4 — Books Onboarding

- [x] Step 9 — Extend onboarding state and submit payload
  - Achieve: Add required Terms/Privacy and optional Marketing/Early Birds fields to the `/books` onboarding form.
  - Create: n/a.
  - Modify: `src/app/(app)/(lingocafe)/books/page.tsx`.
  - Delete: n/a.
  - Touch points: local state, form validation, submit button disabled state, `saveProfile`, payload shape, source value `books-onboarding`.
  - Validation: Incomplete-profile onboarding shows all legal/consent checkboxes; submit is blocked until Terms and Privacy are checked; optional checkboxes can remain unchecked and still submit.
  - Notes: Keep the existing language selectors and catalog flow intact.

- [x] Step 10 — Normalize API response data for UI reuse
  - Achieve: Ensure `/books` and profile blocks can safely read consent keys from `profile.data`.
  - Create: possibly a small client helper near LingoCafe components.
  - Modify: `src/app/(app)/(lingocafe)/books/page.tsx`, `src/app/(app)/(lingocafe)/_components/LingocafePreferences.tsx` only if needed, new block components.
  - Delete: n/a.
  - Touch points: `ReaderProfile.data`, unknown JSON normalization, default unchecked behavior.
  - Validation: Missing consent keys display unchecked; existing rows without consent data do not crash `/books` or `/profile`.
  - Notes: Avoid duplicating fragile `unknown` parsing across components if a tiny helper keeps it cleaner.

### Milestone 5 — Validation and Documentation

- [x] Step 11 — Add focused tests or smoke checks
  - Achieve: Cover the server-side consent persistence rules and basic UI flow risks.
  - Create: test files only if the repository has a suitable existing pattern for route/helper tests.
  - Modify: existing tests if present; otherwise document manual smoke checks in execution notes later.
  - Delete: n/a.
  - Touch points: `parseProfilePayload`, `saveProfile`, profile `data` merge behavior, `/books` onboarding payload.
  - Validation: Verify required Terms/Privacy rejection, optional false-plus-date creation, no-op save date stability, unrelated `profile.data` preservation, and missing legacy data tolerance.
  - Notes: If no test harness is practical, execution must at least run API-level smoke checks against local Postgres.

- [x] Step 12 — Run QA and browser/API smoke
  - Achieve: Prove the feature builds and the key authenticated flows still work.
  - Create: n/a.
  - Modify: n/a unless validation finds bugs.
  - Delete: n/a.
  - Touch points: `npm run qa`, `/profile`, `/books`, `/terms`, `/privacy`, `POST /api/lingocafe/profile`.
  - Validation: `npm run qa` passes; public legal pages return 200; authenticated profile save persists consent data; onboarding cannot complete without required acknowledgements.
  - Notes: AppConfig changes may require a dev server restart for browser smoke.
