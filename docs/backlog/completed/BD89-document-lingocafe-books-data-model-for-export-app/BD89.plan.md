---
taskId: BD89
createdAt: 2026-04-28T08:23:42+02:00
updatedAt: 2026-04-28T08:27:23+02:00
---

# Plan — Document LingoCafe Books Data Model for Export App

## Goal

Create `docs/lingocafe/BOOKS_DATA_MODEL.md` as the authoritative implementation-facing contract for generating `content-export.sql` from the LingoCafe content repository into the current reader database schema.

## Milestones

### Milestone 1 — Establish Source Truth

- [x] Step 1 — Inspect authoritative schema and reader usage
  - Achieve: Confirm the implemented table structure, constraints, cascade behavior, and runtime assumptions that the documentation must describe.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `knex/migrations/20260427150000_lingocafe_schema.js`, `src/app/api/(lingocafe)/lingocafe/_lib/reader.ts`, `knex/seeds/20260427151000_init_lingocafe_data.js`.
  - Validation: Verify that the documentation source facts match the migration source, especially `books`, `books_pages`, `books_progress`, composite page identity, unique constraints, and `books_progress_page_fk ON DELETE CASCADE`.
  - Notes: The migration is the source of truth. The database may be inspected only as supporting evidence.

- [x] Step 2 — Identify content-owned versus user-owned data
  - Achieve: Draw a clear boundary between export-owned content tables and user/runtime state that the export must preserve unless stale references are removed by cascade.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `lingocafe.books`, `lingocafe.books_pages`, `lingocafe.books_progress`, `lingocafe.events`, `lingocafe.profiles`.
  - Validation: Confirm the final doc states that `books` and `books_pages` are content export targets, `books_progress` is user state linked to content, and `events` is historical runtime data not managed as replaceable content.
  - Notes: Stale progress cleanup should be explained through the existing cascade when stale pages are deleted.

### Milestone 2 — Draft the Data Model Contract

- [x] Step 1 — Create the LingoCafe documentation file
  - Achieve: Add the documentation artifact requested by the task.
  - Create: `docs/lingocafe/BOOKS_DATA_MODEL.md`.
  - Modify: n/a
  - Delete: n/a
  - Touch points: Documentation folder structure under `docs/`.
  - Validation: Confirm the file exists at exactly `docs/lingocafe/BOOKS_DATA_MODEL.md` and is readable as standalone Markdown.
  - Notes: Create `docs/lingocafe/` if it does not already exist.

- [x] Step 2 — Document tables, columns, keys, and constraints
  - Achieve: Thoroughly document `lingocafe.books`, `lingocafe.books_pages`, and `lingocafe.books_progress` for export authors.
  - Create: n/a
  - Modify: `docs/lingocafe/BOOKS_DATA_MODEL.md`.
  - Delete: n/a
  - Touch points: `books.id`, `books.lang`, `books.level`, `books.title`, `books.description`, `books.author`, `books.tags`, `books.cover`, `books.published_at`, timestamps, `books_pages.book_id`, `books_pages.id`, `books_pages.position`, `books_pages.kind`, `books_pages.prefix`, `books_pages.title`, `books_pages.summary`, `books_pages.content`, `books_progress.user_id`, `books_progress.book_id`, `books_progress.page_id`, `books_progress.progress_bps`, timestamps, `completed_at`.
  - Validation: Cross-check every documented column against the migration. Confirm the document explains `PRIMARY KEY (book_id, id)`, `UNIQUE (book_id, position)`, `UNIQUE (user_id, book_id)`, `progress_bps` range `0..10000`, and the cascade from pages to progress.
  - Notes: Mention `lingocafe.events` only to clarify that it is historical reader telemetry and not content export input.

- [x] Step 3 — Document ID ownership and stability rules
  - Achieve: Explain how `book_id` and page IDs must behave across exports so content changes do not accidentally reset user progress.
  - Create: n/a
  - Modify: `docs/lingocafe/BOOKS_DATA_MODEL.md`.
  - Delete: n/a
  - Touch points: `books.id`, `books_pages.id`, composite page identity, reader links `/books/[bookId]/[pageId]`, `books_progress.page_id`.
  - Validation: Confirm the document states that IDs are owned by the LingoCafe content repository, stable logical books/pages should keep stable IDs, page IDs are scoped by book, and reflowed books may intentionally remove old page IDs and add new ones.
  - Notes: Call out that changing a page ID is a structural content change with progress consequences.

### Milestone 3 — Define Export SQL Semantics

- [x] Step 1 — Specify transaction-safe per-book export flow
  - Achieve: Define the required order of operations for each exported book.
  - Create: n/a
  - Modify: `docs/lingocafe/BOOKS_DATA_MODEL.md`.
  - Delete: n/a
  - Touch points: SQL transaction boundaries, `INSERT ... ON CONFLICT` for `lingocafe.books`, `INSERT ... ON CONFLICT` for `lingocafe.books_pages`, stale-page `DELETE` by `book_id` and exported page ID set.
  - Validation: Confirm the flow says to upsert the book, upsert all exported pages, delete stale pages for that same `book_id`, and never delete books merely because they are absent from the source export.
  - Notes: The flow should be idempotent and scoped to the current export batch.

- [x] Step 2 — Add concrete SQL examples/templates
  - Achieve: Provide SQL examples that a content export generator can adapt into `content-export.sql`.
  - Create: n/a
  - Modify: `docs/lingocafe/BOOKS_DATA_MODEL.md`.
  - Delete: n/a
  - Touch points: PostgreSQL `BEGIN`/`COMMIT`, `VALUES` CTEs or temp CTEs for book/page payloads, `ON CONFLICT`, stale page deletion, PostgreSQL array or CTE comparison for exported page IDs.
  - Validation: Read the examples for syntactic plausibility and confirm they demonstrate scoped replacement without full-table truncation or broad deletion.
  - Notes: Examples should be concrete enough to guide generation, while the documentation remains the contract.

- [x] Step 3 — Explain stale progress handling
  - Achieve: Make progress cleanup behavior explicit and tied to the implemented foreign key.
  - Create: n/a
  - Modify: `docs/lingocafe/BOOKS_DATA_MODEL.md`.
  - Delete: n/a
  - Touch points: `books_progress_page_fk`, `FOREIGN KEY (book_id, page_id) REFERENCES lingocafe.books_pages (book_id, id) ON DELETE CASCADE`.
  - Validation: Confirm the document says stale progress is removed automatically when stale pages are deleted under the current migration, and explicit progress deletion is only needed if a future schema lacks that cascade.
  - Notes: This protects reader progress when page IDs remain stable and removes invalid progress when a reflow deletes old page IDs.

### Milestone 4 — Validate and Polish

- [x] Step 1 — Check documentation against acceptance criteria
  - Achieve: Ensure `BD89.task.md` acceptance criteria are covered.
  - Create: n/a
  - Modify: `docs/lingocafe/BOOKS_DATA_MODEL.md` if gaps are found.
  - Delete: n/a
  - Touch points: `docs/backlog/drafts/BD89-document-lingocafe-books-data-model-for-export-app/BD89.task.md`, `docs/lingocafe/BOOKS_DATA_MODEL.md`.
  - Validation: Manually map each acceptance criterion to a section in the documentation.
  - Notes: This is a documentation task, so checklist coverage matters more than code execution.

- [x] Step 2 — Run lightweight verification
  - Achieve: Confirm the repo remains clean from formatting or link mistakes introduced by the doc.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `docs/lingocafe/BOOKS_DATA_MODEL.md`, optionally `npm run qa` if the operator wants the standard full gate even for docs-only work.
  - Validation: Use shell inspection such as `sed`/`rg` to verify headings, required terms, and references. Run `npm run qa` only if requested or if implementation expands beyond docs.
  - Notes: No application code is expected to change for this task.
