# Plan — Fix Reader Page Navigation Bounceback

## Goal

Make LingoCafe reader page turns stable on the first attempt, especially after Chrome on iPhone resumes the home-screen installed app from idle/background/unloaded state, by fixing stale resume/restore/navigation/progress races and ignoring duplicate page-turn inputs while a target page is loading.

## Milestones

### Milestone 1 — Reproduce and Locate the Race

- [x] Step 1 — Trace reader navigation state
  - Achieve: Identify which state transition causes the visible next page to be replaced by the previous page after PWA wake/resume.
  - Create: Temporary local diagnostics only if needed; remove them before completion.
  - Modify: n/a
  - Delete: Any temporary diagnostics before final validation.
  - Touch points: `src/app/(app)/(lingocafe)/books/[bookId]/[pageId]/page.tsx`, Chrome on iPhone installed app, reader network requests, browser history state.
  - Validation: Capture the order of app wake/resume, `routeFromUrl`, `readerRoute`, URL, fetch start/finish/abort, `bookPage`, and progress POST events during the first failing page turn after idle/background/unload.
  - Notes: Static trace identified two concrete stale-state paths: URL-derived route sync could reassert a stale route after explicit navigation, and progress saving could use the next route while the previous page was still mounted during delayed transition. Direct Chrome iPhone PWA reproduction remains for operator testing.

- [x] Step 2 — Trace resume-last-read sources
  - Achieve: Determine whether persisted last-read state is reasserted after the first explicit page-turn.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `createReadingAction()`, `buildReadPageHref()`, `/api/lingocafe/books`, `/books` book-card links, profile continue-reading `getLastReadingHref()`, `saveBookOpenProgress()`.
  - Validation: Confirm whether `books_progress` resume href, profile/bookshelf continue behavior, or the initial page-open progress write can cause a stale page route to win after app wake.
  - Notes: Resume hrefs are generated from `books_progress`, and opening a reader page calls `saveBookOpenProgress()`. The implementation now makes explicit user navigation update browser history immediately and prevents stale route/progress effects from overriding it.

- [x] Step 3 — Check stale progress and final progress writes
  - Achieve: Determine whether pending progress saves from the source page can overwrite the target page or trigger a stale route restore.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: scroll cleanup effect in `page.tsx`, `apiHref`, `latestProgressRef`, `sendFinalProgress`, `/api/lingocafe/books/[bookId]/pages/[pageId]` POST behavior.
  - Validation: Confirm whether a source-page keepalive or delayed POST fires after the target-page GET has completed, especially after PWA resume.
  - Notes: Fixed a concrete stale-write risk: the progress effect now posts to the loaded `bookPage` route, not the pending `readerRoute`, so an old mounted page cannot save progress to a newly requested page during transition.

### Milestone 2 — Harden Page-Turn State

- [x] Step 4 — Add latest-intent guards to page loads
  - Achieve: Ensure only the latest requested reader route can update `bookPage`, `loading`, `error`, and browser history.
  - Create: n/a
  - Modify: `src/app/(app)/(lingocafe)/books/[bookId]/[pageId]/page.tsx`
  - Delete: n/a
  - Touch points: `navigateToReaderPage`, route-derived state synchronization, GET fetch effect, abort cleanup, `window.history.pushState`, any wake/resume route reconciliation.
  - Validation: Simulated or manual fast navigation cannot let an older fetch completion, stale resume route, or stale URL-derived route replace the latest intended page.
  - Notes: Added latest-route and load-sequence guards. Route sync from `routeFromUrl` is ignored when it does not match the actual browser URL or conflicts with a pending explicit user navigation.

- [x] Step 5 — Ignore duplicate page-turn input while loading
  - Achieve: Prevent Next button clicks and mobile swipes from starting another page-turn command while the current page-turn fetch is in flight.
  - Create: n/a
  - Modify: `src/app/(app)/(lingocafe)/books/[bookId]/[pageId]/page.tsx`, `src/app/(app)/(lingocafe)/books/_components/BookReaderSurfaces.tsx`
  - Delete: n/a
  - Touch points: `ReaderSurfaceProps`, `ReaderNavButton`, `BookProgress`, `BookReaderMobileSurface` swipe handler, `onNavigatePage`.
  - Validation: During a page load, extra bottom Next clicks and swipe gestures are ignored; after load completion, page turns work again.
  - Notes: Reader previous/next controls and mobile swipes now ignore page-turn commands while a page-turn fetch is pending.

- [x] Step 6 — Preserve URL, scroll, and progress semantics
  - Achieve: Keep refresh/share URLs aligned with the visible page, restore target-page progress correctly, and prevent stale source-page progress from overriding the active page.
  - Create: n/a
  - Modify: likely `page.tsx`; API code only if investigation proves server progress behavior needs a guard.
  - Delete: n/a
  - Touch points: `routeFromUrl` effect, `popstate` handler, scroll restore effect, progress save effect, `saveBookOpenProgress`, resume href generation.
  - Validation: Target page stays visible after navigation, refresh opens the visible page, progress restore remains correct, source-page cleanup cannot bounce the reader back, and resume-last-read does not override a newer user page-turn.
  - Notes: Delayed-loading behavior remains. URL updates immediately on explicit page-turn intent; scroll restore only consumes route progress when the route matches the loaded page.

### Milestone 3 — Regression Coverage

- [x] Step 7 — Add focused automated coverage where practical
  - Achieve: Cover the race-prone reader state behavior with the lightest suitable test.
  - Create: A focused unit/component test if existing test tooling can mount the relevant logic without heavy browser setup.
  - Modify: Existing test configuration only if required and low-risk.
  - Delete: n/a
  - Touch points: reader route state helpers, navigation handler behavior, stale fetch guard behavior, resume-route override behavior if it can be isolated.
  - Validation: Test fails against the stale-update or stale-resume pattern and passes with the fix.
  - Notes: No focused automated test was added because the failure depends on Next App Router native history integration plus Chrome iPhone PWA resume lifecycle. This is documented in execution notes; QA/build passed.

- [x] Step 8 — Run quality checks
  - Achieve: Prove the implementation compiles and lint/build gates stay clean.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `npm run qa`
  - Validation: `npm run qa` passes.
  - Notes: `npm run lint` passed. `npm run qa` passed, including production build and TypeScript.

### Milestone 4 — Manual Verification

- [ ] Step 9 — Verify primary mobile failure path
  - Achieve: Confirm Chrome on iPhone no longer bounces back on the first next-page turn.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `https://lc42go.ngrok.app/`, login `john` / `john`, `/books`, multiple books, Chrome on iPhone.
  - Validation: Open the home-screen installed app, let it sit idle/backgrounded or force unload/reopen, then perform the first next-page swipe or bottom Next click. The first attempt lands on and stays on the target page.
  - Notes: Test more than one book because the bug was reported across many books. The first page-turn after resume is the main target.

- [ ] Step 10 — Verify non-regression navigation paths
  - Achieve: Confirm the fix does not break reader navigation paths that were not the primary failing path.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: desktop Chrome, mobile Safari if available, previous-page navigation, table of contents, browser back/forward, refresh on reader route.
  - Validation: All checked paths update the visible page and URL correctly without stuck loading, bounceback, or broken progress restore.
  - Notes: Desktop Chrome is a non-regression check because it did not reproduce the bug during refinement.
