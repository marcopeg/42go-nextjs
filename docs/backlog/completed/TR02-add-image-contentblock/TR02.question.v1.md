# Refinement Questions v1 — Add Image ContentBlock

Answer inline under each question, then return to chat and say `next round`.

## Questions

### 1. What concrete business or product value should this block unlock?

For example: richer landing pages without custom components, reusable marketing sections, documentation pages with illustrations, or another outcome.

Answer:

richer landing page. this should be a content block that integrates with the others (hero, text, ...)

### 2. What should the public configuration API look like?

Please confirm the desired fields and names. A possible shape is:

```ts
{
  type: "image",
  image: {
    src: string;
    alt: string;
    position?: "left" | "right" | "top" | "bottom";
  },
  content?: {
    source: string;
  },
  imageAnimation?: "none" | "fade" | "slide-left" | "slide-right" | "slide-up" | "slide-down";
  contentAnimation?: "none" | "fade" | "slide-left" | "slide-right" | "slide-up" | "slide-down";
}
```

Answer:

good like this, i'd add also a "content.text". text would provide inlined markdown. source is a path to a markdown file to read and render. I'd say, if the user adds the "content" key, then either "text" OR "source" must be provided. only one of those.

### 3. Should side content support only inline markdown, or also markdown file paths like the existing `markdown` block?

The existing `MarkdownBlock` supports either `source` or `path`. Should the image block reuse that same content model for its optional side content?

Answer:

Answered by question n.2

### 4. What layout behavior should the block have on mobile and desktop?

Please specify whether `left` and `right` should stack on mobile, whether `top` and `bottom` are needed, and whether image/content widths should be configurable or fixed.

Answer:

on mobile, both "left" and "right" would behave like "top" (image on top)

### 5. Should the image rendering use Next.js `Image` or a normal `img` element?

This affects whether callers must provide image dimensions or whether the block can accept simple URLs with CSS-controlled sizing.

Answer:

What is best? this is app configuration, not dynamic content. so the developer must be able to get the dimensions.

### 6. Should this be a server-only ContentBlock, or should it also be available in the client ContentBlock renderer?

Current page rendering uses the server ContentBlock for dynamic pages. App/public toolbar actions use smaller client ContentBlock unions that currently only include link and component blocks.

Answer:

this is only server. 
It's for landing page content.

### 7. Should the existing `ScrollAnimation` component be extended, replaced, or used as-is?

It currently supports `fade`, `scale`, and `slideUp`. This task asks for left/right/top/bottom image slide animations and optional content animation.

Answer:

Use ScrollAnimation.
do not extend it now, review the task so that it supports only the effects already implemented in the ScrollAnimation.

### 8. What should count as done for acceptance?

Please list the minimum visible behavior, type coverage, example config, and any tests or manual verification you expect.

Answer:

- add this to the default config's home page with a couple of different examples (one block under the other)
- integrate this in the skill `42go-pages` so that it knows how to use it
- integrate this in the publid documentation
- lint
- build
- typechecks

I will do my final visual test on browser and report my feedback.
