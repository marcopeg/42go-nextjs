# Plan — Add AppLayout Profile Guard Before Slot

## Goal

Add a route-independent `before` profile guard slot that can block the AppLayout shell before sidebar/header/content mount, then move the existing LingoCafe onboarding guard to that slot with a short release delay so the modal closes over the freshly rendered app layout.

## Milestones

### Milestone 1 — Define The Guard Contract

- [x] Step 1 — Extend profile guard types
  - Achieve: Support `slot: "before" | "layout"` in the profile guard config while preserving the existing loader/direct-component shape.
  - Create: n/a
  - Modify: `src/42go/components/ProfileBlock/types.ts`, and exports in `src/42go/components/ProfileBlock/index.ts` only if the exported names change.
  - Delete: n/a
  - Touch points: `TProfileLayoutGuardConfig`, `TProfileLayoutGuardProps`, `TProfileLayoutGuardLoader`.
  - Validation: TypeScript accepts existing `layout` guards and new `before` guards.
  - Notes: Keep names stable if practical. Chuck Norris does not rename a type just to watch imports bleed.

- [x] Step 2 — Add explicit before-guard lifecycle props
  - Achieve: Give before-slot guard components a small way to release the blocked shell, including an optional unmount/close delay around 300ms.
  - Create: n/a
  - Modify: `src/42go/components/ProfileBlock/types.ts`.
  - Delete: n/a
  - Touch points: Guard props should still include `refreshKey`; add a release callback such as `releaseBeforeGuard({ delayMs })` or equivalent.
  - Validation: The contract can express: block by default, release immediately for completed/non-applicable state, release with delay after successful onboarding.
  - Notes: Do not put route information in this contract.

### Milestone 2 — Gate AppLayout Before Shell Mount

- [x] Step 1 — Preserve the current shell as a reusable component
  - Achieve: Extract the existing sidebar/header/main/footer/mobile nav markup and related sidebar/mobile state into an internal AppLayout shell component.
  - Create: Optional `src/42go/layouts/app/AppLayoutShell.tsx` if it keeps `AppLayout.tsx` clean.
  - Modify: `src/42go/layouts/app/AppLayout.tsx`.
  - Delete: n/a
  - Touch points: `SidebarMenu`, `MobileBottomNav`, `Toolbar`, `ProtectComponent`, footer, padding logic, sidebar localStorage behavior.
  - Validation: With no before guard, rendered AppLayout behavior is unchanged.
  - Notes: The before guard must be able to block before the shell component and its layout effects mount.

- [x] Step 2 — Upgrade the guard runner for both slots
  - Achieve: Make the guard runner support `layout` overlay behavior and `before` blocking behavior from the same app config entry.
  - Create: Optional small helper under `src/42go/layouts/app/` if the runner becomes noisy.
  - Modify: `src/42go/layouts/app/ProfileLayoutGuard.tsx`.
  - Delete: n/a
  - Touch points: Dynamic `loader`, direct `component`, fallback rendering, `profile:complete` listener, remount/refresh key.
  - Validation: `slot: "layout"` still renders over the shell; `slot: "before"` starts blocking until released.
  - Notes: Loader fallback for `before` should not reveal the shell. A blank or configured fallback beats a flash.

- [x] Step 3 — Wire before blocking into AppLayout
  - Achieve: `AppLayout` renders only the before guard while it is blocking, renders the shell after release, and can keep the guard mounted above the shell until its release delay ends.
  - Create: n/a
  - Modify: `src/42go/layouts/app/AppLayout.tsx`, `src/42go/layouts/app/ProfileLayoutGuard.tsx`.
  - Delete: n/a
  - Touch points: Guard state, release callback, delayed unmount timer cleanup, normal shell rendering.
  - Validation: When a before guard blocks, sidebar, header, page children, footer, and mobile nav are not visible or mounted.
  - Notes: Clean timers on unmount. No ghosts. Chuck Norris already handled those.

### Milestone 3 — Move LingoCafe Onboarding To Before Slot

- [x] Step 1 — Adapt the onboarding guard lifecycle only
  - Achieve: Keep the existing LingoCafe onboarding fields, copy, profile save, consent handling, and modal surface while adding before-slot release behavior.
  - Create: n/a
  - Modify: `src/app/(app)/(lingocafe)/_components/LingocafeOnboardingGuard.tsx`.
  - Delete: n/a
  - Touch points: `useSession`, `useProfileController`, `Modal open`, `profile:complete`, new release callback.
  - Validation: Completed or unauthenticated states release the before guard without rendering onboarding; incomplete authenticated profiles stay blocked.
  - Notes: After successful complete save, start the modal close state, release the shell with about a 300ms delayed guard unmount, and keep existing persistence/event behavior.

- [x] Step 2 — Change LingoCafe guard config to before
  - Achieve: Configure LingoCafe to use the existing onboarding guard in the new `before` slot.
  - Create: n/a
  - Modify: `src/config/lingocafe/config.ts`.
  - Delete: n/a
  - Touch points: `app.profile.guard.slot`, lazy loader import.
  - Validation: LingoCafe compiles with `slot: "before"` and still lazy-loads the onboarding guard.
  - Notes: Do not add route checks. The guard is the bouncer, not the map.

### Milestone 4 — Validate No-Flicker Behavior

- [x] Step 1 — Run code quality checks
  - Achieve: Verify lint, type, and build gates after the guard contract and AppLayout changes.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `npm run qa`.
  - Validation: `npm run qa` passes.
  - Notes: Also run targeted lint/type commands first if useful while implementing.

- [x] Step 2 — Smoke test guard behavior by state
  - Achieve: Verify before-guard behavior for incomplete and completed LingoCafe profiles.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `https://lc42go.ngrok.app/`, `john` / `john`, `/books`, `/profile`, `/api/profile`.
  - Validation: Incomplete profile initially shows only onboarding; completed profile reaches normal app shell; non-LingoCafe apps without guards are unaffected.
  - Notes: Use existing test flow unless execution discovers a cleaner local authenticated path.

- [x] Step 3 — Visual check desktop and mobile
  - Achieve: Confirm no initial sidebar/header/content flicker and confirm the completion transition renders AppLayout underneath before the modal closes.
  - Create: Screenshots only if useful for notes.
  - Modify: n/a
  - Delete: n/a
  - Touch points: Browser check at desktop and mobile viewport sizes, LingoCafe onboarding modal, `/books` after completion.
  - Validation: Desktop and mobile both show onboarding-only first paint for incomplete profile, then a smooth release to `/books`.
  - Notes: If the public test account is already complete, reset or create a suitable incomplete profile state before visual proof.
