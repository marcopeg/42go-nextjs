---
taskId: BJ58
createdAt: 2026-04-30T15:12:40+02:00
updatedAt: 2026-04-30T15:19:06+02:00
---

# Plan — Improve Books Data Model With Info JSONB

## Goal

Update the LingoCafe books model in the existing schema migration, API normalization, UI metadata rendering, and export documentation so books use `project` plus `info` JSONB, never persist or read `cover`, and optional `words_count` and `chapters_count` render only when valid.

## Milestones

### Milestone 1 — Schema Shape

- [x] Step 1 — Update the current books migration
  - Achieve: Change the authoritative LingoCafe schema so fresh database rebuilds create `lingocafe.books` with `project text NOT NULL` immediately after `id`, no `cover` column, and `info jsonb NOT NULL DEFAULT '{}'::jsonb`.
  - Create: n/a.
  - Modify: `knex/migrations/20260427150000_lingocafe_schema.js`.
  - Delete: n/a.
  - Touch points: `exports.up`, `createTable("books")`, Knex column ordering, existing `profiles.data` JSONB style.
  - Validation: Inspect the migration and later verify rebuilt schema through `make migrate.rebuild`.
  - Notes: Do not create a new migration file and do not add an `alter table` migration path.

- [x] Step 2 — Confirm removed book seed state
  - Achieve: Verify no removed LingoCafe books seed file or seed insert path still writes `cover`, omits `project`, or needs migration.
  - Create: n/a.
  - Modify: n/a.
  - Delete: `knex/seeds/20260429161622.lingocafe.books.js`.
  - Touch points: `knex/seeds/`, references to `lingocafe.books`, seed execution order.
  - Validation: `rg "lingocafe\\.books|books\"\\)|cover|project" knex/seeds -S` shows no stale LingoCafe book seed contract.
  - Notes: The stale generated book seed still existed and was removed because the task says this work should not maintain or recreate book seeds.

### Milestone 2 — API Contract

- [x] Step 1 — Replace persisted cover dependency with deterministic cover resolution
  - Achieve: Remove all server-side `books.cover` types, selects, and override normalization while keeping the client-facing `cover` URL derived from `/images/lingocafe/<book-id>.jpg` plus the existing fallback.
  - Create: n/a.
  - Modify: `src/app/api/(lingocafe)/lingocafe/_lib/reader.ts`.
  - Delete: `normalizePublicCoverUrl` or any equivalent override-only helper if it becomes unused.
  - Touch points: `BookRow`, `BookInfoRow`, `BookPageBookRow`, `resolveBookCover`, `mapBook`, `mapBookPageDetail`, `loadReaderData`, `loadBookInfo`, `loadBookPage`.
  - Validation: TypeScript build rejects any remaining selected `cover` column; `rg "books\\.cover|\"cover\"" src/app/api/(lingocafe) -S` leaves only client payload or deterministic cover naming.
  - Notes: The API may still expose a `cover` field to clients, but it must be computed only from `book.id`.

- [x] Step 2 — Add `project` and `info` to book payload mapping
  - Achieve: Include `project` and `info` in the relevant book row types, database selects, and normalized API payloads for catalog, detail, and read-page book metadata.
  - Create: n/a.
  - Modify: `src/app/api/(lingocafe)/lingocafe/_lib/reader.ts`.
  - Delete: n/a.
  - Touch points: `BookRow`, `BookInfoRow`, `BookPageBookRow`, `mapBook`, `mapBookInfo`, `mapBookPageDetail`, `loadReaderData`, `loadBookInfo`, `loadBookPage`.
  - Validation: API return types expose `info` as an object and `project` as text where full book metadata is returned.
  - Notes: Keep the payload property name `info`, not `data`.

- [x] Step 3 — Normalize optional book info defensively
  - Achieve: Ensure malformed or null database `info` values normalize to `{}` so clients can safely read optional fields without crashing.
  - Create: A small local helper in `reader.ts` only if needed.
  - Modify: `src/app/api/(lingocafe)/lingocafe/_lib/reader.ts`.
  - Delete: n/a.
  - Touch points: JSONB row typing, `mapBook`, `mapBookInfo`, `mapBookPageDetail`.
  - Validation: TypeScript accepts `info` as `Record<string, unknown>` or an equivalent safe shape.
  - Notes: PostgreSQL default prevents null in normal rows, but API normalization should still be defensive.

### Milestone 3 — Client Types And Book Info UI

- [x] Step 1 — Update route-local book types
  - Achieve: Reflect the new API shape in client types, including `project` and `info`, while keeping computed `cover` and `coverFallback` as display fields.
  - Create: n/a.
  - Modify: `src/app/(app)/(lingocafe)/books/_components/book-types.ts`.
  - Delete: n/a.
  - Touch points: `ReaderBook`, `ReaderBookInfo`, `ReaderBookPage.book`, JSON-compatible `info` type.
  - Validation: `npm run qa` catches mismatches between page normalization and component usage.
  - Notes: Keep types route-local unless the same shape is already shared elsewhere.

- [x] Step 2 — Normalize client payloads for `info`
  - Achieve: Ensure `/books`, `/books/[bookId]`, and `/books/[bookId]/[pageId]` client normalizers preserve `info` and still provide deterministic cover fallback behavior.
  - Create: n/a.
  - Modify: `src/app/(app)/(lingocafe)/books/page.tsx`, `src/app/(app)/(lingocafe)/books/[bookId]/page.tsx`, `src/app/(app)/(lingocafe)/books/[bookId]/[pageId]/page.tsx` if needed.
  - Delete: n/a.
  - Touch points: `normalizeReaderData`, `normalizeBookInfo`, read-page payload normalization.
  - Validation: Normalizers treat missing or non-object `info` as `{}` and do not assume `info.cover`.
  - Notes: Catalog cards do not need to display `project` or metrics unless existing components require the shape.

- [x] Step 3 — Render optional words and chapters metrics
  - Achieve: Display `info.words_count` and `info.chapters_count` on the book info page only when each value is present and valid, skipping each independently.
  - Create: n/a unless a route-local helper keeps the presentation clean.
  - Modify: `src/app/(app)/(lingocafe)/books/_components/BookInfoContent.tsx`.
  - Delete: Any old computed chapter-count display only if it conflicts with the new source requirement.
  - Touch points: `MetaItem`, `MobileMetaItem`, current `chapterCount`, optional numeric metadata parser, desktop and mobile metadata rows.
  - Validation: UI renders no placeholder labels for missing or invalid values; valid counts appear on both desktop and mobile metadata surfaces where appropriate.
  - Notes: The existing table-of-contents chapter count can still be used for the contents section unless the UI metric is specifically sourced from `info.chapters_count`.

### Milestone 4 — Documentation And Export Contract

- [x] Step 1 — Update the books data model document
  - Achieve: Bring `docs/lingocafe/BOOKS_DATA_MODEL.md` in line with the new schema and export contract.
  - Create: n/a.
  - Modify: `docs/lingocafe/BOOKS_DATA_MODEL.md`.
  - Delete: n/a.
  - Touch points: `ExportBook` type, source metadata list, `lingocafe.books` column table, SQL templates, field ownership matrix.
  - Validation: The document no longer describes `books.cover`, includes `project`, includes `info`, and explains deterministic cover resolution outside persisted book rows.
  - Notes: SQL templates must upsert `project` and `info`, and must stop writing `cover`.

- [x] Step 2 — Audit remaining references
  - Achieve: Find and address any stale references to persisted `books.cover`, `info.cover`, missing `books.project`, or outdated export examples.
  - Create: n/a.
  - Modify: Any concretely stale docs or code found by the audit.
  - Delete: n/a.
  - Touch points: `src/`, `knex/`, `docs/lingocafe/`, relevant `docs/articles/`, and `contents/` if search finds book model examples there.
  - Validation: `rg "books\\.cover|cover = EXCLUDED.cover|AS cover|info\\.cover" src knex docs/lingocafe docs/articles contents -S` has no stale persisted-cover contract.
  - Notes: Do not rewrite unrelated generic uses of the word `cover`, such as CSS background cover or UI image props, unless they imply database override behavior.

### Milestone 5 — Rebuild And Quality Gates

- [x] Step 1 — Rebuild the local development database
  - Achieve: Destroy and rebuild the local schema from the updated migration.
  - Create: n/a.
  - Modify: Local database state only.
  - Delete: Local database state through the approved rebuild flow.
  - Touch points: `make migrate.rebuild`, PostgreSQL local database, LingoCafe schema.
  - Validation: `make migrate.rebuild` exits successfully.
  - Notes: This is intentionally destructive for the local development database.

- [x] Step 2 — Seed the rebuilt database
  - Achieve: Run current seed set against the rebuilt schema and confirm removed book seeds are not required.
  - Create: n/a.
  - Modify: Local database state only.
  - Delete: n/a.
  - Touch points: `make seed`, `knex/seeds/`.
  - Validation: `make seed` exits successfully.
  - Notes: The seed command should pass without recreating LingoCafe book content.

- [x] Step 3 — Run QA
  - Achieve: Validate linting, type checking, and production build against the schema/API/UI changes.
  - Create: n/a.
  - Modify: n/a.
  - Delete: n/a.
  - Touch points: `npm run qa`, Next.js build, TypeScript, ESLint.
  - Validation: `npm run qa` exits successfully.
  - Notes: If QA exposes unrelated pre-existing failures, record them clearly before stopping.

- [x] Step 4 — Optional smoke checks when data exists
  - Achieve: If the operator has regenerated book data by execution time, verify the key LingoCafe surfaces still load.
  - Create: n/a.
  - Modify: n/a.
  - Delete: n/a.
  - Touch points: `/api/lingocafe/books`, `/api/lingocafe/books/[bookId]`, `/books`, `/books/[bookId]`, `/books/[bookId]/[pageId]`.
  - Validation: Skipped route-level book smoke checks because the rebuilt and seeded database contains zero rows in `lingocafe.books`.
  - Notes: Schema was verified directly through `information_schema.columns`; book-data smoke checks can run after the operator regenerates book content.
