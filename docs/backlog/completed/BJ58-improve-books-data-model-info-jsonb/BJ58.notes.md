---
taskId: BJ58
createdAt: 2026-04-30T15:13:53+02:00
updatedAt: 2026-04-30T15:19:06+02:00
---

# Execution Notes — Improve Books Data Model With Info JSONB
**Task**: ./BJ58.task.md
**Plan**: ./BJ58.plan.md

## Decisions

- Updated the existing LingoCafe schema migration directly, per operator instruction, instead of creating a new migration.
- Removed the stale LingoCafe books seed file because the operator clarified there should be no book seeds in this task and the seed still wrote the removed `cover` column.
- Kept `cover` as a client-facing computed display URL while removing it as a persisted database field.
- Kept deterministic cover rendering as `/images/lingocafe/<book-id>.jpg` with the existing placeholder fallback.
- Added defensive `info` normalization on both server and client so missing or malformed metadata becomes `{}`.
- Rendered `info.words_count` and `info.chapters_count` as optional details-page metrics only when each value is valid.

## Problems Encountered

- The `backlog-execute` skill references a local `scripts/resolve_task.py`, but that script is not present in the skill folder. Execution continued using the already resolved WIP task folder.
- The first sandboxed `make migrate.rebuild` failed with `EPERM` while connecting to PostgreSQL. The approved escalated rerun succeeded.
- `make migrate.rebuild` emitted the existing `pg` deprecation warning about `client.query()` while a client is already executing a query.
- `npm run qa` passed, but Next/Turbopack still emits the existing NFT trace warning from `next.config.ts` through the markdown/content-block import path and repeated static-build `next/headers` fallback logs.

## Deviations From Plan

- Milestone 1 Step 2 expected no modification unless a remaining seed reference was found. A stale books seed file was found, so it was deleted.
- Optional route-level smoke checks were skipped because the rebuilt and seeded database has zero `lingocafe.books` rows.

## Additional Requests

- The operator accepted the plan and requested execution in the same reply.

## Known Limitations

- Smoke checks that require regenerated book data were skipped because no books exist after the rebuild and seed flow.
- The current database schema was verified directly: `lingocafe.books` now has `project text NOT NULL`, `info jsonb NOT NULL DEFAULT '{}'::jsonb`, and no `cover` column.
