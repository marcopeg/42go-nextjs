# Plan - Audit AppConfig Client Secret Exposure

## Goal

Produce `LU73.output.md` with a serious, reproducible security analysis of how AppConfig and secret-bearing configuration can reach client-visible surfaces. The task is analysis-only: it documents findings, evidence, and follow-up remediation, but does not patch behavior.

## Milestones

### Milestone 1 - Source-Level Data Flow Map

- [x] Step 1 - Inventory AppConfig definitions and server-only fields
  - Achieve: Identify the full `TAppConfigItem` shape, all app config files, and all fields that are secret-bearing or plausibly server-only.
  - Create: Initial `LU73.output.md` sections for scope, method, config field classification, and evidence log.
  - Modify: `docs/backlog/wip/LU73-audit-appconfig-client-secret-exposure/LU73.output.md`
  - Delete: n/a
  - Touch points: `src/AppConfig.ts`, `src/config/*/config.ts`, auth provider config types, any config modules containing `secret`, `token`, `key`, `password`, or `credential`.
  - Validation: `rg` evidence lists all secret-bearing config definitions without printing real secret values.
  - Notes: Redact any real `.env` value. Treat OAuth `clientSecret` as confirmed secret-bearing.

- [x] Step 2 - Trace server AppConfig resolution paths
  - Achieve: Map request matching, proxy/header app ID propagation, server config helpers, metadata, PWA, layout, auth, docs, policy, and API consumers.
  - Create: Data-flow section in `LU73.output.md`.
  - Modify: `LU73.output.md`
  - Delete: n/a
  - Touch points: `src/proxy.ts`, `src/42go/config/app-config.ts`, `src/42go/lib/app-id/**`, `src/app/layout.tsx`, `src/app/manifest.ts`, `src/42go/pwa/HeadTags.tsx`, auth provider setup, docs utilities, policy utilities.
  - Validation: Every direct `getAppConfig()` and `getAppInfo()` use is classified as server-render, API, auth, metadata, or utility.
  - Notes: Include line references for important flow boundaries.

- [x] Step 3 - Trace client AppConfig import paths
  - Achieve: Identify all client-side imports of `useAppConfig()`, `useAppID()`, and any direct or indirect import of `src/AppConfig.ts` into client modules.
  - Create: Client import-path section in `LU73.output.md`.
  - Modify: `LU73.output.md`
  - Delete: n/a
  - Touch points: `src/42go/config/use-app-config.tsx`, app layout components, profile blocks, LingoCafe pages, shared components, `src/components/Providers.tsx`, any client component importing types or values from `@/AppConfig`.
  - Validation: Import graph evidence explains whether the full `apps` registry can enter browser bundles.
  - Notes: Distinguish type-only imports from runtime imports. Chuck Norris does not punch type imports unless they punch first.

### Milestone 2 - Client-Visible Surface Audit

- [x] Step 4 - Audit public and protected API responses
  - Achieve: Find API routes that expose raw AppConfig or config-derived data, then classify whether their responses can reveal secrets.
  - Create: API surface section in `LU73.output.md`.
  - Modify: `LU73.output.md`
  - Delete: n/a
  - Touch points: `src/app/api/**/route.ts`, especially routes using `getAppConfig()` or returning `config`, including `src/app/api/todos/route.ts`.
  - Validation: Evidence includes route paths, auth/policy status, response shape, and whether secret-bearing fields can appear.
  - Notes: Do not paste real response secrets. Use redacted or sentinel evidence.

- [x] Step 5 - Audit HTML, injected globals, and hydration payloads
  - Achieve: Determine what AppConfig-derived values are emitted into rendered HTML, `window` globals, Next/React hydration data, metadata, manifest, icons, PWA tags, and layout props.
  - Create: Rendered output section in `LU73.output.md`.
  - Modify: `LU73.output.md`
  - Delete: n/a
  - Touch points: `src/42go/config/InjectAppID.tsx`, `src/app/layout.tsx`, public layout, login page, app layout, manifest route, PWA head tags.
  - Validation: Captured or summarized HTML/runtime evidence shows whether only app ID/public metadata is injected or whether broader config is serialized.
  - Notes: Inspect both public and authenticated surfaces where relevant.

- [x] Step 6 - Audit NextAuth and provider exposure
  - Achieve: Verify how provider IDs and secrets are used by NextAuth, what `/api/auth/*` endpoints expose, and whether provider config leaks through sign-in, session, CSRF, callback, or error endpoints.
  - Create: Auth surface section in `LU73.output.md`.
  - Modify: `LU73.output.md`
  - Delete: n/a
  - Touch points: `src/app/api/auth/[...nextauth]/route.ts`, `src/42go/auth/lib/authOptions.ts`, provider factory modules, callbacks, session shaping, login page provider rendering.
  - Validation: Evidence distinguishes public OAuth client IDs/provider names from server-only client secrets.
  - Notes: Use sentinel values or redaction for secret checks.

### Milestone 3 - Build and Runtime Evidence

- [x] Step 7 - Prepare sentinel build inputs safely
  - Achieve: Define unique sentinel values for OAuth client secrets and other relevant secrets without changing committed config or real `.env`.
  - Create: Reproducible command notes in `LU73.output.md`.
  - Modify: `LU73.output.md`
  - Delete: n/a
  - Touch points: local shell environment, `package.json` build command, `.next` output.
  - Validation: The output documents exact sentinel names searched and confirms no real secrets were printed.
  - Notes: Prefer command-scoped environment variables. Read `.env` only if needed, never modify it.

- [x] Step 8 - Run production build and inspect browser assets
  - Achieve: Build the app with sentinel secret values and search emitted browser assets, server assets, standalone output, source maps if present, and generated static files.
  - Create: Bundle inspection evidence in `LU73.output.md`.
  - Modify: `LU73.output.md`
  - Delete: n/a
  - Touch points: `.next/static/**`, `.next/server/**`, `.next/standalone/**`, source maps if generated.
  - Validation: Searches report where sentinel values appear. Client asset hits are confirmed exposure; server-only hits are classified separately.
  - Notes: `npm run build` already removes `.next/standalone/.env*`; still verify.

- [x] Step 9 - Run targeted runtime checks
  - Achieve: Start or use an app runtime and inspect relevant public routes, protected routes where practical, HTML, `window.__APP_ID__`, hydration payloads, API responses, and auth endpoints.
  - Create: Runtime evidence in `LU73.output.md`.
  - Modify: `LU73.output.md`
  - Delete: n/a
  - Touch points: local built app or existing dev/prod runtime, public pages, login page, `/api/todos`, `/api/auth/*`, profile/books pages if login is practical.
  - Validation: Evidence states which routes were checked, whether auth was required, and whether sentinel or real-secret patterns appeared.
  - Notes: If login/runtime cannot be completed, document the blocker and cover with source/build evidence.

### Milestone 4 - Findings and Follow-Up Recommendations

- [x] Step 10 - Classify protections and gaps
  - Achieve: Explain what currently prevents secret exposure, what does not, and whether each protection is enforced by type, runtime projection, framework build behavior, convention, or accident.
  - Create: Protection analysis and risk classification in `LU73.output.md`.
  - Modify: `LU73.output.md`
  - Delete: n/a
  - Touch points: all evidence gathered above.
  - Validation: Conclusion uses one explicit category: no exposure with enforceable protections, no exposure but fragile protections, or confirmed exposure with affected paths.
  - Notes: Be blunt. If an API returns raw `config`, call it out.

- [x] Step 11 - Recommend remediation without implementing it
  - Achieve: If exposure or fragile boundaries are found, propose concrete follow-up implementation tasks.
  - Create: Follow-up recommendations section in `LU73.output.md`.
  - Modify: `LU73.output.md`
  - Delete: n/a
  - Touch points: possible `PublicAppConfig` projection, server-only config split, auth provider registry, lint/static import guards, bundle sentinel regression tests, API response redaction.
  - Validation: Recommendations are specific enough to draft follow-up backlog tasks.
  - Notes: This milestone must not edit production code.

- [x] Step 12 - Final verification of the analysis artifact
  - Achieve: Confirm `LU73.output.md` satisfies every acceptance criterion and contains no real secrets.
  - Create: Final checklist section in `LU73.output.md`.
  - Modify: `LU73.output.md`
  - Delete: n/a
  - Touch points: `LU73.task.md`, `LU73.question.v1.md`, `LU73.output.md`, `docs/backlog/BACKLOG.md`.
  - Validation: Manual checklist confirms source trace, API check, HTML/global check, bundle check, runtime check, conclusion, and follow-up recommendations are present.
  - Notes: No `npm run qa` is required unless execution introduces code changes.
