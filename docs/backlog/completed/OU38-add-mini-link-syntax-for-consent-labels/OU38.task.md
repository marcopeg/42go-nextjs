---
taskId: OU38
status: completed
createdAt: 2026-05-06T12:03:25+02:00
updatedAt: 2026-05-06T16:10:25+02:00
group: consent
reviewedAt: 2026-05-06T14:09:28+02:00
plannedAt: 2026-05-06T14:11:20+02:00
startedAt: 2026-05-06T14:18:03+02:00
completedAt: 2026-05-06T16:10:25+02:00
---

# Add Mini Link Syntax for Consent Labels

## Elevator's Pitch

Extend the core consent label rendering so a string label can mark one segment as a link using `{{...}}`, with companion `labelLink` config for the destination URL and optional link target. This should stay intentionally narrow and behave like a super-mini markdown feature with just one supported mechanic.

## Business Gain

Apps can keep consent labels simple and configuration-driven while still linking legal text such as Terms or Privacy pages without needing a custom React component for every consent item.

## Current State

Consent items already support `label` as either a plain string or a React component. When `label` is a string, the core renderer displays it as raw text with no inline link syntax or structured link metadata.

## Desired State

If a consent item's `label` is a string and the item also provides `labelLink` metadata, one substring wrapped in `{{...}}` inside the label should render as an anchor tag. The configuration should support a `target` value equivalent to HTML anchor target behavior. `target` is optional and defaults to `_blank`.

Example:

```ts
{
  name: "terms",
  label: "I accept the {{Terms}}",
  labelLink: { href: "/terms" },
}
```

## Definition of Success

An app can configure a consent item using a plain string label plus one `labelLink` object, mark exactly one link segment inside the string with `{{...}}`, and the consent UI renders that segment as a clickable link while leaving the rest of the string as plain text.

## Additional Context

Relevant core files already exist:

- `src/42go/profile/types.ts` defines `TConsentItem`, where `label` currently supports string-like React content or a component.
- `src/42go/profile/ProfileConsent.tsx` renders consent labels and is the likely place to interpret the mini syntax.
- `src/42go/components/ProfileBlock/blocks/Consent.tsx` hosts the profile block that uses `ProfileConsent`.
- Current app configs in `src/config/default/config.ts`, `src/config/quicklist/config.ts`, and `src/config/lingocafe/config.ts` use plain string consent labels today.

The requested behavior is intentionally narrow:

- only one mini-markup mechanic
- only for string labels
- only for rendering a single link segment
- not a general markdown parser

Prior consent work clarified that `statement` is the stable plain string persisted into consent evidence, while `label` is presentation only. This task must preserve that split: the mini link syntax can affect UI rendering, but not the evidence text.

## Assumptions

- React-component labels should keep working unchanged.
- The link target should default to `_blank`.
- The consent evidence `statement` should remain plain persisted text and should not become HTML.

## Constraints

- Keep the feature in the core consent system, not in app-specific consent rendering.
- Do not introduce full markdown support for consent labels.
- Do not require every app to replace string labels with custom React components just to add one link.
- Preserve backwards compatibility for existing consent configurations that use plain string labels with no link metadata.
- Support exactly one `{{...}}` link segment per label. Multiple links are out of scope.
- `labelLink.target` must be optional and default to `_blank`.

## Acceptance Criteria

- [ ] The core consent config supports one new `labelLink` object for string labels.
- [ ] `labelLink` supports `href` and optional `target`.
- [ ] A string label can mark exactly one segment with `{{...}}` and that segment renders as a link.
- [ ] The link target supports same-screen or new-screen behavior.
- [ ] When `labelLink.target` is omitted, the rendered anchor uses `_blank`.
- [ ] A label such as `I accept the {{Terms}}` renders only `Terms` as the clickable text.
- [ ] Consent labels without link metadata keep rendering as plain text.
- [ ] React-component labels keep working unchanged.
- [ ] The feature is implemented as a tiny custom mechanic, not as general markdown parsing.

## Dos

- Do choose a clear property name for the link config, even if the initial idea mentioned both `label link` and `label URL`.
- Do keep the parsing rules explicit and very small.
- Do keep the persisted evidence statement plain and stable.
- Do use `labelLink` as the config property name.
- Do use `{{...}}` as the only supported marker.

## Don'ts

- Don't introduce a general markdown engine for consent labels.
- Don't store rendered HTML in consent evidence.
- Don't break existing consent items that already use string labels or component labels.
- Don't support multiple links in a single string label in this task.

## Open Questions

n/a

## Related to

- [ZO85: Core User Profile and Consent Storage](../../completed/ZO85-core-user-profile-and-consent-storage/ZO85.task.md) - introduced core consent configuration, `ProfileConsent`, `statement` evidence text, and `auth.users.consent` history.
- [NO51: Add LingoCafe Profile Legal and Consent Blocks](../../completed/NO51-add-lingocafecontent-profile-consent-block/NO51.task.md) - earlier LingoCafe legal/consent work needed linked Terms and Privacy labels before the later core consent simplification.
- [FH48: Configurable App Profile Page](../../completed/FH48-configurable-app-profile-page/FH48.task.md) - established the AppConfig-driven profile block system that hosts the core Consent block.
