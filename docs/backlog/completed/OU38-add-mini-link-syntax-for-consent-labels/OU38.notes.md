# Execution Notes — Add Mini Link Syntax for Consent Labels
**Task**: ./OU38.task.md
**Plan**: ./OU38.plan.md

## Decisions

- 2026-05-06T14:18:13+02:00 — Operator accepted the plan and requested immediate implementation.
- 2026-05-06T14:18:13+02:00 — LingoCafe consent labels should use the new link syntax with canonical legal URLs at `lingocafe.app/en/legal/...`.
- 2026-05-06T14:20:19+02:00 — Added `labelLink` typing, one-marker `{{...}}` rendering, `_blank` target default, safe `_blank` rel handling, docs, and LingoCafe label links.
- 2026-05-06T14:23:19+02:00 — `npm run qa` passed after exporting `TConsentLabelLink`.
- 2026-05-06T15:23:02+02:00 — Removed consent item config fields `purpose`, `legalBasis`, `category`, and `statement`; string `label` now renders basic bold/italic markdown plus `\n` line breaks and is converted to the plain persisted evidence statement.
- 2026-05-06T15:33:19+02:00 — Dropped `labelLink` support and replaced it with markdown links in labels. Links render from `[text](url)` and always open in a new tab. LingoCafe legal links now live directly in label strings.
- 2026-05-06T16:00:22+02:00 — Fixed `/books` onboarding profile fill-up by making reader API consent explicit as `profile.consent` instead of overloading `profile.data`, and by reapplying loaded profile/consent state after save.
- 2026-05-06T16:05:29+02:00 — Fixed stale consent readback after the label syntax migration. Stored evidence with legacy `{{...}}` markers now compares against current markdown labels by normalized plain statement, while new saves still write clean plain statements.
- 2026-05-06T16:09:52+02:00 — Final `npm run qa` passed after the stale-readback fix.

## Problems Encountered

- First `npm run qa` attempt failed because `TConsentLabelLink` was added to `types.ts` but not exported from `src/42go/profile/index.ts`. Export added and QA rerun. Later work removed `TConsentLabelLink` entirely.
- `/books` onboarding initially saved consent correctly but read it back as stale because persisted evidence still contained legacy `{{...}}` markers. Normalized statement comparison fixed the frontend/server completeness checks without a data migration.
- The reader API previously overloaded `profile.data` with consent data. This made the onboarding read path fragile; the API now returns explicit `profile.consent`.

## Deviations From Plan

- Scope expanded from one custom `{{...}}` link mechanic to basic label markdown: bold, italic, line breaks, and `[text](url)` links.
- The later operator request removed `labelLink`, `purpose`, `legalBasis`, `category`, and `statement` from consent item configuration.
- A small out-of-task profile logout UI change was made: the logout block now renders as a destructive-colored link-style button.

## Additional Requests

- Add LingoCafe legal URLs:
  - `lingocafe.app/en/legal/terms-and-conditions`
  - `lingocafe.app/en/legal/privacy-policy`
  - `lingocafe.app/en/legal/marketing-consent`
  - `lingocafe.app/en/legal/early-birds-program`
- Support label markdown links directly and always open them in a new tab.
- Fix first-login `/books` profile completion readback after save.

## Known Limitations

- No remaining known limitation from this task. Browser save/readback was operator-exercised through `/books`; repository QA passed after the final fixes.
