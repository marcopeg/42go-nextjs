---
taskId: VU18
createdAt: 2026-04-28T13:40:28+02:00
updatedAt: 2026-04-28T13:45:08+02:00
---

# Plan — Implement Books Page Read Design

## Goal

Update the LingoCafe `/books/[bookId]/[pageId]` read page to match `read-page-design.png` on desktop and mobile while preserving the existing read route, vertical-scroll progress persistence, page-open/page-scroll events, previous/next navigation, and authenticated client-side data flow.

## Milestones

### Milestone 1 — Confirm Reader Boundaries and Layout Path

- [x] Step 1 — Inspect current reader ownership
  - Achieve: Confirm the exact files that own the read route shell, mobile overlay, desktop scroll container, reader presentation, markdown rendering, page normalization, and page API.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `src/app/(app)/(lingocafe)/books/[bookId]/[pageId]/page.tsx`, `src/app/(app)/(lingocafe)/books/_components/BookPageReader.tsx`, `book-types.ts`, `src/app/api/(lingocafe)/lingocafe/_lib/reader.ts`, `src/app/api/(lingocafe)/lingocafe/books/[bookId]/pages/[pageId]/route.ts`.
  - Validation: Verify current behavior still loads the requested page, restores `progress_bps`, records scroll progress, and renders previous/next links through the existing route/API.
  - Notes: Confirmed the read route shell, scroll restore/save, mobile overlay, reader markdown presentation, client normalization, and page API are owned by the planned route-local files. Used `ZE11` as behavior baseline and `NN45`/`DJ89` as precedent for route-local UI refactors, isolated placeholder controls, and avoiding fake first-release behavior.

- [x] Step 2 — Decide desktop layout strategy
  - Achieve: Verify whether the designed desktop left reader column can be implemented cleanly inside the current `AppLayout`, or whether the approved full-screen desktop reader surface fallback should be used.
  - Create: n/a
  - Modify: Possibly `src/app/(app)/(lingocafe)/books/[bookId]/[pageId]/page.tsx`.
  - Delete: n/a
  - Touch points: `AppLayout` props (`hideMobileMenu`, `disablePadding`, `stickyHeader`, `footer`), existing global desktop sidebar, `desktopScrollRef`, `MobileBookPage`.
  - Validation: Decision is recorded in implementation notes later; selected approach does not require broad `AppLayout` refactoring and does not fight the global sidebar.
  - Notes: Chose the approved route-local full-screen desktop reader surface. `AppLayout` has no custom desktop sidebar slot, and the fixed reader surface avoids broad layout refactoring while matching the designed side column.

### Milestone 2 — Extend Reader Data for Designed Navigation

- [x] Step 1 — Add full book page list to the read-page API
  - Achieve: Return ordered page summaries with the read-page payload so the desktop side column can render the contents list and the bottom progress aid can know page position within the book.
  - Create: n/a
  - Modify: `src/app/api/(lingocafe)/lingocafe/_lib/reader.ts`, `src/app/(app)/(lingocafe)/books/_components/book-types.ts`.
  - Delete: n/a
  - Touch points: `BookInfoPageSummary` or a shared page-summary type, `loadBookPage`, `mapBookPageDetail`, `buildReadPageHref`, `ReaderBookPage`.
  - Validation: Valid read-page API responses include the current page, previous/next neighbors, and an ordered `pages` array without full page content for every page.
  - Notes: Extended the read-page payload with ordered page summaries and book cover/language/level metadata. Reused the page-summary idea from `DJ89`; did not fetch all page content for the side column.

- [x] Step 2 — Normalize new reader payload safely on the client
  - Achieve: Extend client normalization so missing or malformed page-list data falls back to an empty array or current previous/next state without crashing.
  - Create: n/a
  - Modify: `src/app/(app)/(lingocafe)/books/[bookId]/[pageId]/page.tsx`, `book-types.ts`.
  - Delete: n/a
  - Touch points: `normalizeBookPage`, `isNeighbor`, new page-summary guard, `ReaderBookPage`.
  - Validation: Existing loading/error behavior remains unchanged; older or partial payloads still render the page body and previous/next controls.
  - Notes: Added defensive client guards for page summaries and expanded book metadata. Missing/malformed page summaries fall back to an empty array.

### Milestone 3 — Rebuild Reader Presentation Components

- [x] Step 1 — Split reader shell from reader content
  - Achieve: Refactor the current read page into route-local presentation pieces so desktop shell, mobile shell, side column, top toolbar, reading content, and bottom progress aid are easy to reason about.
  - Create: Possible helper components such as `BookReaderShell`, `BookReaderToolbar`, `BookReaderSidePanel`, `BookReaderProgress`, and `ReaderPlaceholderActions` under `src/app/(app)/(lingocafe)/books/_components/`.
  - Modify: `src/app/(app)/(lingocafe)/books/[bookId]/[pageId]/page.tsx`, `BookPageReader.tsx`.
  - Delete: n/a
  - Touch points: `BookPageReader`, `ReaderBookPage`, existing `desktopScrollRef` and `mobileScrollRef`, Tailwind responsive classes.
  - Validation: Component split compiles, route behavior is unchanged before styling is completed, and placeholder controls stay isolated enough to remove.
  - Notes: Added `BookReaderSurfaces.tsx` with route-local desktop/mobile surfaces, side panel, toolbar placeholders, progress aid, and navigation helpers.

- [x] Step 2 — Restyle long-form reading content
  - Achieve: Match the design's reading typography, centered content width, chapter prefix, title hierarchy, decorative divider, paragraph spacing, and readable mobile line length.
  - Create: n/a
  - Modify: `BookPageReader.tsx` and/or new reader content helper.
  - Delete: n/a
  - Touch points: `ReactMarkdown` component map, headings, paragraphs, restricted markdown allowed elements.
  - Validation: Markdown still supports only headings, paragraphs, bold, and italic; content remains readable without overlap on desktop and mobile.
  - Notes: Restyled `BookPageReader` for centered long-form typography, chapter label, title hierarchy, divider, and larger serif paragraphs. The restricted markdown allowed elements remain unchanged.

### Milestone 4 — Implement Desktop Reader Surface

- [x] Step 1 — Build the desktop reader surface
  - Achieve: Implement the chosen desktop layout path: preferably designed side column inside the current layout, or the approved full-screen custom reader surface if the layout cannot host it cleanly.
  - Create: Possible `BookReaderDesktopSurface` helper.
  - Modify: `src/app/(app)/(lingocafe)/books/[bookId]/[pageId]/page.tsx`, new/updated reader shell helpers.
  - Delete: n/a
  - Touch points: `AppLayout` usage, `hideMobileMenu`, `disablePadding`, `stickyHeader`, `desktopScrollRef`, desktop-only classes.
  - Validation: Desktop visually matches the design: left book/navigation column, top reader toolbar, centered content, bottom progress aid, and circular previous/next buttons.
  - Notes: Implemented a fixed desktop full-screen reader surface with app padding disabled underneath. No broad `AppLayout` refactor was needed.

- [x] Step 2 — Add desktop side column contents
  - Achieve: Render the designed left column with close/back affordance, cover/title/author, visual progress, tabs as placeholders, and an ordered contents list with the current page highlighted.
  - Create: Possible `BookReaderSidePanel` helper.
  - Modify: Reader shell helpers, `book-types.ts` only if type support is still missing.
  - Delete: n/a
  - Touch points: `ReaderBookPage.pages`, `BookCover` if reused safely, `Link`, current `bookId/pageId`, visual current-page marker.
  - Validation: Contents rows link to `/books/[bookId]/[pageId]`; current page is clearly marked; placeholder tabs do not implement real bookmarks/settings behavior.
  - Notes: Added side panel with back affordance, real cover data, title/author, visual progress, placeholder tabs, ordered contents rows, and current-page marker.

- [x] Step 3 — Add desktop toolbar and bottom progress aid
  - Achieve: Render `Aa`, theme/sun, bookmark, and `...` as visual-only placeholders, plus a bottom book-level progress aid and circular previous/next navigation.
  - Create: Possible `BookReaderToolbar`, `ReaderPlaceholderActions`, `BookReaderProgress` helpers.
  - Modify: Reader shell helpers.
  - Delete: n/a
  - Touch points: Lucide icons, `ReaderBookPage.previous`, `ReaderBookPage.next`, ordered `pages`, current page position.
  - Validation: Placeholder controls do not call APIs, open real menus, persist state, or interfere with navigation; previous/next links still work.
  - Notes: Added visual-only toolbar placeholders and bottom book-level progress with circular previous/next navigation. Placeholders are isolated in `ReaderToolbarPlaceholders`.

### Milestone 5 — Implement Mobile Reader Surface

- [x] Step 1 — Restyle mobile full-screen reader
  - Achieve: Adapt the existing mobile `z-[600]` full-screen read overlay to the design's compact top bar, reading area, bottom visual progress aid, and circular previous/next controls.
  - Create: Possible `BookReaderMobileSurface` helper.
  - Modify: `src/app/(app)/(lingocafe)/books/[bookId]/[pageId]/page.tsx`, reader shell helpers.
  - Delete: n/a
  - Touch points: `MobileBookPage`, `mobileScrollRef`, `backHref`, `height: 100dvh`, mobile overflow constraints.
  - Validation: Mobile still overlays book details, back reveals the detail page underneath, vertical scrolling persists progress, and no horizontal overflow is introduced.
  - Notes: Reworked the existing mobile overlay into the designed compact reader surface while preserving `z-[600]`, full-screen behavior, back navigation, and vertical scroll tracking.

- [x] Step 2 — Move mobile secondary controls into the top-right menu placeholder
  - Achieve: Drop the design's mobile bottom toolbar and expose `Innehåll`, `Bokmärken`, `Inställningar`, and other first-release controls behind the top-right `...` placeholder.
  - Create: Possible `MobileReaderMenuPlaceholder` helper.
  - Modify: Mobile reader shell helper.
  - Delete: n/a
  - Touch points: `MoreHorizontal` icon, accessible label, placeholder menu surface or non-opening placeholder depending on smallest clean implementation.
  - Validation: No bottom toolbar appears on mobile; no real bookmark/settings/contents behavior or persistence is implemented.
  - Notes: Dropped the mobile bottom toolbar and exposed disabled placeholder items through the top-right dropdown menu.

### Milestone 6 — Preserve Behavior and Validate

- [x] Step 1 — Preserve vertical scroll tracking and resume
  - Achieve: Ensure the design refactor keeps `progress_bps` restore and save behavior attached to the actual vertical scroll container on desktop and mobile.
  - Create: n/a
  - Modify: `src/app/(app)/(lingocafe)/books/[bookId]/[pageId]/page.tsx`.
  - Delete: n/a
  - Touch points: `desktopScrollRef`, `mobileScrollRef`, `getScrollProgressBps`, `scrollToProgressBps`, scroll event listeners, `sendProgress`, `sendFinalProgress`.
  - Validation: `progress_bps` in the URL restores vertical scroll; page-scroll POST still sends vertical scroll progress; the bottom horizontal progress aid does not update saved `books_progress.progress_bps`.
  - Notes: Kept the existing `desktopScrollRef`/`mobileScrollRef` restore and scroll-save flow. The bottom horizontal progress aid only reflects book page position and does not write progress.

- [x] Step 2 — Preserve loading, 404, and error states
  - Achieve: Keep non-crashing loading and error states in both desktop and mobile reader shells even though the design does not specify all states.
  - Create: n/a
  - Modify: `src/app/(app)/(lingocafe)/books/[bookId]/[pageId]/page.tsx`, reader shell helpers if created.
  - Delete: n/a
  - Touch points: `loading`, `error`, `normalizeBookPage`, fetch error handling, authenticated session gating.
  - Validation: Loading remains visible; invalid book/page shows the preserved "Book page not found." style; generic failures show "Could not load page."
  - Notes: Preserved existing loading and error copy while rendering it inside the redesigned desktop/mobile shells.

- [x] Step 3 — Run static quality gate
  - Achieve: Verify linting and build pass after the reader refactor.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `npm run qa`.
  - Validation: `npm run qa` passes.
  - Notes: `npm run qa` passed. Existing Turbopack/NFT and repeated `next/headers` fallback warnings remain as baseline warnings.

- [x] Step 4 — Smoke-check read route and responsive layout
  - Achieve: Verify the read page still loads and visually matches the attached design across desktop and mobile.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `/books/gosta-berling-sv-b2/p01`, `/books/gosta-berling-sv-b2/p02?progress_bps=4200`, `/api/lingocafe/books/[bookId]/pages/[pageId]`, authenticated app host if available.
  - Validation: Route returns 200 in local smoke where applicable; unauthenticated API protection remains 401; desktop/mobile have no horizontal overflow, text/control overlap, or broken previous/next navigation.
  - Notes: Local `HEAD /books/gosta-berling-sv-b2/p01` and `HEAD /books/gosta-berling-sv-b2/p02?progress_bps=4200` returned 200 with `Host: lingocafe.localhost:3000`. Unauthenticated `GET /api/lingocafe/books/gosta-berling-sv-b2/pages/p01` returned 401.
