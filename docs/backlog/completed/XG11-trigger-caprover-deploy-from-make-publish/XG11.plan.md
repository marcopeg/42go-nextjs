---
taskId: XG11
createdAt: 2026-05-03T10:00:58+02:00
updatedAt: 2026-05-03T10:18:28+02:00
---

# Plan — Add CapRover Deploy Targets

## Goal

Add non-interactive Makefile deploy targets that publish the existing Docker Hub image tags and then trigger a CapRover image deployment for the `a42go-multi` VPS app.

## Milestones

### Milestone 1 — Confirm CapRover CLI Contract

- [x] Step 1 — Verify installed deploy flags
  - Achieve: Confirm the local or project-selected CapRover CLI accepts image deployment flags compatible with the documented `caprover deploy -i/--imageName <image>` path.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: CapRover CLI executable, likely `caprover deploy --help`; optionally `npx caprover deploy --help` if no global binary is installed.
  - Validation: Help output confirms `--imageName`, app selection, URL or machine selection, and app token support.
  - Notes: `npx --yes caprover deploy --help` confirmed `--imageName`, `--caproverApp`, `--caproverUrl`, and `--appToken`. No real deployment was run.

- [x] Step 2 — Decide executable and auth command shape
  - Achieve: Choose the exact command form used by Makefile targets, preferring token-based non-interactive auth via `CAPROVER_APP_TOKEN` and `CAPROVER_URL`.
  - Create: n/a
  - Modify: `Makefile`
  - Delete: n/a
  - Touch points: Make variables such as `CAPROVER`, `CAPROVER_URL`, `CAPROVER_APP`, `CAPROVER_IMAGE`, `CAPROVER_APP_TOKEN`, and optional `CAPROVER_MACHINE`.
  - Validation: The chosen command can be represented without prompts and with `CAPROVER_APP` override support.
  - Notes: Chosen command shape uses `npx --yes caprover deploy --caproverUrl "$$CAPROVER_URL" --caproverApp "$(CAPROVER_APP)" --imageName "$(CAPROVER_IMAGE)" --appToken "$$CAPROVER_APP_TOKEN"`. Default `CAPROVER_APP` is `a42go-multi`. Default `CAPROVER_IMAGE` is `$(IMAGE):$(VERSION)`.

### Milestone 2 — Implement Makefile Deploy Targets

- [x] Step 1 — Load local deployment environment safely
  - Achieve: Make local `.env` values available to Makefile deploy targets so `CAPROVER_APP_TOKEN` can be sourced without committing secrets.
  - Create: n/a
  - Modify: `Makefile`
  - Delete: n/a
  - Touch points: top-level Make variable section, conditional `.env` include/export behavior, existing `.gitignore` `.env*` rules.
  - Validation: `make -n deploy CAPROVER_APP_TOKEN=fake CAPROVER_URL=https://captain.example.com VERSION=0.0.0` uses the supplied values and does not require an interactive shell prompt.
  - Notes: Added Makefile `.env` include and stripped quotes from CapRover values loaded from `.env`. Dry-run output keeps the token as `$CAPROVER_APP_TOKEN`.

- [x] Step 2 — Add CapRover trigger target
  - Achieve: Add a separately callable target, likely `deploy.caprover`, that deploys `$(CAPROVER_IMAGE)` to `$(CAPROVER_APP)` through CapRover.
  - Create: n/a
  - Modify: `Makefile`
  - Delete: n/a
  - Touch points: CapRover CLI invocation, variable validation guards for required `CAPROVER_URL` and `CAPROVER_APP_TOKEN` when token auth is used, image tag resolution.
  - Validation: `make -n deploy.caprover CAPROVER_APP_TOKEN=fake CAPROVER_URL=https://captain.example.com VERSION=9.9.9` prints a non-interactive CapRover image deploy command that targets `marcopeg/42go-next:9.9.9`.
  - Notes: Added `deploy.caprover` with guards for `CAPROVER_URL` and `CAPROVER_APP_TOKEN`. It deploys `$(CAPROVER_IMAGE)`, defaulting to `$(IMAGE):$(VERSION)`.

- [x] Step 3 — Add full deploy targets
  - Achieve: Add `deploy` and `deploy.nocache` targets that compose existing registry publish targets with the CapRover trigger target.
  - Create: n/a
  - Modify: `Makefile`
  - Delete: n/a
  - Touch points: `publish`, `publish.nocache`, `publish.universal`, new `deploy`, new `deploy.nocache`, new `deploy.caprover`.
  - Validation: `make -n deploy VERSION=9.9.9 CAPROVER_APP_TOKEN=fake CAPROVER_URL=https://captain.example.com` shows publish before CapRover deploy; `make -n deploy.nocache ...` shows no-cache publish before CapRover deploy.
  - Notes: Added `deploy: publish deploy.caprover` and `deploy.nocache: publish.nocache deploy.caprover`. `publish.universal` remains publish-only.

### Milestone 3 — Document Operator Workflow

- [x] Step 1 — Document deployment variables
  - Achieve: Document the CapRover variables needed for deployment and how they can be placed in local `.env`.
  - Create: n/a
  - Modify: `.env.example`, `docs/articles/PRODUCTION_DEPLOYMENT.md`
  - Delete: n/a
  - Touch points: environment variable examples for `CAPROVER_URL`, `CAPROVER_APP`, `CAPROVER_APP_TOKEN`, optional `CAPROVER_MACHINE`, and `CAPROVER_IMAGE`.
  - Validation: Documentation clearly separates non-secret defaults from secret values and does not include a real token.
  - Notes: Added CapRover placeholders to `.env.example` and documented local `.env` usage in the production deployment guide.

- [x] Step 2 — Document command semantics
  - Achieve: Explain when to use `make publish`, `make publish.nocache`, `make publish.universal`, `make deploy`, and `make deploy.nocache`.
  - Create: n/a
  - Modify: `docs/articles/PRODUCTION_DEPLOYMENT.md`
  - Delete: n/a
  - Touch points: existing Publish Targets section, production command summary, troubleshooting section.
  - Validation: The guide states that deploy targets push Docker Hub tags first, then trigger CapRover with `$(IMAGE):$(VERSION)`.
  - Notes: Production deployment docs now describe `publish`, `publish.nocache`, `publish.universal`, `deploy`, and `deploy.nocache`, plus CapRover failure checks.

### Milestone 4 — Validate Without Accidental Deployment

- [x] Step 1 — Dry-run Makefile command expansion
  - Achieve: Validate target ordering and variable substitution without pushing or deploying.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `make -n deploy`, `make -n deploy.nocache`, `make -n deploy.caprover`, `make -n publish.universal`.
  - Validation: Dry runs show the correct publish/deploy order, `CAPROVER_APP=a42go-multi`, `CAPROVER_IMAGE=$(IMAGE):$(VERSION)`, and no accidental universal deploy path.
  - Notes: Dry runs passed for `deploy.caprover`, `deploy`, `deploy.nocache`, and `publish.universal` with placeholder CapRover values. The deploy dry runs print `$CAPROVER_APP_TOKEN`, not the token value.

- [x] Step 2 — Run project quality checks
  - Achieve: Confirm the repository still passes the standard project check after Makefile and docs edits.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `npm run qa`
  - Validation: `npm run qa` passes.
  - Notes: `npm run qa` passed.

- [x] Step 3 — Record live-deploy readiness
  - Achieve: Leave a clear final note on what is needed before the first live `make deploy`.
  - Create: n/a
  - Modify: `docs/articles/PRODUCTION_DEPLOYMENT.md` if missing details are discovered during validation.
  - Delete: n/a
  - Touch points: CapRover app token, CapRover URL, Docker Hub visibility or registry credentials, deployed image tag.
  - Validation: The implementation summary can state whether only dry-run validation was performed or a live deployment was executed.
  - Notes: Production deployment docs state that `CAPROVER_URL`, `CAPROVER_APP_TOKEN`, and CapRover registry pull access are prerequisites before a live `make deploy`.
