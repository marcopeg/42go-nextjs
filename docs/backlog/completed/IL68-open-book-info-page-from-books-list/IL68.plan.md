---
taskId: IL68
createdAt: 2026-04-27T16:05:49+02:00
updatedAt: 2026-04-27T16:12:29+02:00
---

# Plan — Open Book Info Page From Books List

## Goal

Implement a canonical `/books/[bookId]` LingoCafe book info route that loads the selected book from the existing LingoCafe API namespace, displays cover/title/author/tags/description, renders as main page navigation on desktop and full-screen popup-style detail on mobile, and records a `book-info` event in `lingocafe.events`.

## Milestones

### Milestone 1 — Confirm Current LingoCafe Ground Truth

- [x] Step 1 — Verify prerequisite task output
  - Achieve: Confirm `DG66` is completed before implementation starts and identify the cover behavior to reuse.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `docs/backlog/completed/DG66-display-book-covers/DG66.task.md`, `docs/backlog/completed/DG66-display-book-covers/DG66.notes.md`, `public/images/lingocafe/`, `src/app/(app)/(lingocafe)/books/page.tsx`, `src/app/api/(lingocafe)/lingocafe/_lib/reader.ts`.
  - Validation: `DG66` is completed and establishes `/images/lingocafe/<book-id>.jpg`, `/images/lingocafe/placeholder.jpg`, `next/image`, fixed `2:3` cover frames, and card overflow fixes.
  - Notes: This is already evidenced in the codebase and completed backlog, so it is marked done during planning.

- [x] Step 2 — Verify the completed catalog API shape
  - Achieve: Confirm the book list and profile API paths established by `PK51`.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `docs/backlog/completed/PK51-show-language-filtered-lingocafe-books/PK51.notes.md`, `src/app/api/(lingocafe)/lingocafe/books/route.ts`, `src/app/api/(lingocafe)/lingocafe/profile/route.ts`, `src/app/api/(lingocafe)/lingocafe/_lib/reader.ts`.
  - Validation: Existing code uses `/api/lingocafe/books`, `/api/lingocafe/profile`, and `api:lingocafe`; removed `/api/books` must not return.
  - Notes: This is already evidenced in the codebase and completed backlog, so it is marked done during planning.

### Milestone 2 — Extend LingoCafe Reader Data Access

- [x] Step 1 — Add book-detail and event helpers
  - Achieve: Add server-side helpers that load one book by id with description and append a `book-info` event for the authenticated user.
  - Create: n/a
  - Modify: `src/app/api/(lingocafe)/lingocafe/_lib/reader.ts`.
  - Delete: n/a
  - Touch points: Existing `BookRow`, `mapBook`, `resolveBookCover`, `getSessionUserId`, `json`, `getDB`; new detail response type/helper such as `loadBookInfo(bookId)`, and a helper such as `trackReaderEvent({ userId, name, bookId, data, meta })`.
  - Validation: The detail helper selects `id`, `title`, `author`, `tags`, `description`, `cover`, and fallback cover data; missing books are distinguishable from empty catalog states; event insert targets `lingocafe.events` with `name: "book-info"`, authenticated `user_id`, and selected `book_id`.
  - Notes: Keep remote covers ignored per `DG66`; continue returning browser-safe public cover URLs only.

- [x] Step 2 — Add the book-detail API route
  - Achieve: Expose a protected LingoCafe API route for a single book detail and track `book-info` only after a valid book is found for the authenticated user.
  - Create: `src/app/api/(lingocafe)/lingocafe/books/[bookId]/route.ts`.
  - Modify: `src/app/api/(lingocafe)/lingocafe/_lib/reader.ts` if the route reveals helper shape gaps.
  - Delete: n/a
  - Touch points: `protectRoute`, `getSessionUserId`, `json`, `loadBookInfo`, `trackReaderEvent`, `api:lingocafe`, dynamic route params.
  - Validation: Unauthenticated requests return 401 through existing policy/session behavior; unknown books return 404; valid books return detail data including description and cover fields; `lingocafe.events` receives `book-info` rows for successful detail views.
  - Notes: Keep the route under `/api/lingocafe/books/[bookId]`; do not revive `/api/books`.

### Milestone 3 — Build Reusable Book Info UI

- [x] Step 1 — Extract shared LingoCafe book UI primitives
  - Achieve: Avoid duplicating cover and metadata rendering between the list and detail page where a small local abstraction helps.
  - Create: Prefer a scoped component folder such as `src/app/(app)/(lingocafe)/books/_components/` with components like `BookCover.tsx`, `BookTags.tsx`, and `BookInfoContent.tsx` if the current single-file page becomes too dense.
  - Modify: `src/app/(app)/(lingocafe)/books/page.tsx`.
  - Delete: n/a
  - Touch points: Existing `BookCover`, `ReaderBook` type, fixed `2:3` image frame, tag pills, responsive card layout.
  - Validation: The books list still renders the same cards with covers and no horizontal overflow; TypeScript types remain local and explicit.
  - Notes: Keep this pragmatic. If extraction adds noise, keep components local to the route group but avoid copy-pasting the cover fallback logic.

- [x] Step 2 — Implement the detail content presentation
  - Achieve: Render the selected book cover, title, author, tags, and full description in a layout that works in desktop page content and mobile full-screen presentation.
  - Create: Detail presentation component if not created in the previous step.
  - Modify: `src/app/(app)/(lingocafe)/books/_components/*` or the future detail page directly.
  - Delete: n/a
  - Touch points: `next/image`, `Button` only where command actions are needed, tag pills, responsive `2:3` cover frame, readable description typography.
  - Validation: Long titles, author names, tags, and descriptions wrap without overflow on mobile and desktop; missing tags are hidden cleanly.
  - Notes: The first metadata scope is only title, author, tags, description, and cover.

### Milestone 4 — Add Canonical `/books/[bookId]` Route

- [x] Step 1 — Create the client detail page
  - Achieve: Add the authenticated app route at `/books/[bookId]` that fetches detail data browser-side and uses the existing App Router client-page convention.
  - Create: `src/app/(app)/(lingocafe)/books/[bookId]/page.tsx`.
  - Modify: n/a unless shared components/types are extracted.
  - Delete: n/a
  - Touch points: `"use client"`, `useParams`, `useRouter` if needed, `useSession`, `AppLayout`, `backBtn={{ to: "/books" }}`, `policy={{ require: { feature: "page:books", session: true } }}`, `fetch("/api/lingocafe/books/${bookId}")` with `credentials: "same-origin"` and `cache: "no-store"`.
  - Validation: Refreshing `/books/[bookId]` reloads the same selected book; the toolbar back button returns to `/books`; 404/error states are clear and do not crash.
  - Notes: Keep this route a client component. Do not SSR-fetch internal APIs.

- [x] Step 2 — Render desktop as main page navigation and mobile as full-screen popup-style detail
  - Achieve: Use one canonical route with responsive presentation: normal AppLayout page on desktop, full-screen overlay-style visual on mobile.
  - Create: Mobile wrapper component if useful, modeled after QuickList `MobileEditPanel`/`MobileCreatePanel`.
  - Modify: `src/app/(app)/(lingocafe)/books/[bookId]/page.tsx`, related `_components/*`.
  - Delete: n/a
  - Touch points: `AppLayout` `hideMobileMenu`, `disablePadding` if needed, fixed `inset-0`/`z-[500]` mobile container, `100dvh`, safe-area padding, body overflow handling where appropriate, top bar/back button behavior.
  - Validation: Desktop detail behaves as the main page; mobile detail visually covers the app and list chrome; the back button navigates to `/books`; no horizontal scrolling is introduced.
  - Notes: The URL remains `/books/[bookId]` on mobile. The overlay-style component is presentation, not client-only hidden state.

- [x] Step 3 — Link book cards to the detail route
  - Achieve: Make book cards on `/books` clickable and keyboard-accessible, navigating to `/books/[bookId]`.
  - Create: n/a
  - Modify: `src/app/(app)/(lingocafe)/books/page.tsx` and any extracted card component.
  - Delete: n/a
  - Touch points: `next/link`, card article/link structure, accessible labels, existing cover card responsive layout.
  - Validation: Clicking or keyboard-activating a book opens `/books/[bookId]`; onboarding and empty states are unchanged; card text and images remain readable.
  - Notes: Do not add nested interactive elements inside a link unless the structure supports it.

### Milestone 5 — Validate Behavior and Regression Risk

- [x] Step 1 — Run static quality gate
  - Achieve: Verify lint and production build still pass.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `npm run qa`.
  - Validation: `npm run qa` exits successfully.
  - Notes: This is required by the repository workflow.

- [x] Step 2 — Smoke test core LingoCafe flows
  - Achieve: Confirm the implemented flow works against seeded data and does not regress the completed `/books` catalog.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: Local app at `/books`, `/books/nils-holgersson-sv-a2` or another seeded id, `/api/lingocafe/books/[bookId]`, `lingocafe.events`.
  - Validation: Authenticated user sees list, opens detail, refreshes detail URL, uses back button, and a `book-info` event exists with the expected user and `book_id`.
  - Notes: Authenticated curl as LingoCafe `jane` returned the expected detail payload, `/books/[bookId]` returned 200, `/api/lingocafe/books/[bookId]` returned 401 without a session, removed `/api/books/[bookId]` returned 404, and Postgres contained the expected `book-info` event row.

- [ ] Step 3 — Mobile layout verification
  - Achieve: Verify the full-screen mobile detail presentation and the prior DG66 no-horizontal-scroll fix survive.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: Mobile viewport in browser/in-app browser, `/books`, `/books/[bookId]`, mobile bottom navigation/sidebar behavior.
  - Validation: Detail appears above the app UI, back returns to `/books`, content scrolls vertically, body has no horizontal overflow, cover keeps `2:3` aspect without cropping.
  - Notes: Still pending because the browser tool was not available through the current tool surface.
