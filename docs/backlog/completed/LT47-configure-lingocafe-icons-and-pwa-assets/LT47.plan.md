---
taskId: LT47
createdAt: 2026-05-02T06:42:23+02:00
updatedAt: 2026-05-02T06:46:23+02:00
---

# Plan — Configure LingoCafe Icons and PWA Assets

## Goal

Configure LingoCafe to use its own static logo and PWA icon assets through existing 42go configuration, generate the missing 512x512 icon from the best available source if local tooling supports it, verify manifest/head output, and leave shared per-app favicon architecture to `RO05`.

## Milestones

### Milestone 1 — Confirm Existing Icon Contract

- [x] Step 1 — Reconfirm current config surface
  - Achieve: Verify the active LingoCafe config still has `public.pwa`, `startUrl: "/books"`, and `icons.appleTouch180` pointing at the default icon before editing.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `src/config/lingocafe/config.ts`, `src/AppConfig.ts`, `src/42go/pwa/types.ts`.
  - Validation: Source inspection shows the exact fields that need changing and confirms no existing favicon field was added since refinement.
  - Notes: Preserve user edits in `src/config/lingocafe/config.ts`; do not revert unrelated formatting or environment variable changes.

- [x] Step 2 — Reconfirm asset inventory
  - Achieve: Confirm dimensions and formats for `public/lingocafe/lingocafe.png`, `public/lingocafe/logo.png`, and `public/lingocafe/favicon/*`.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `public/lingocafe/`, `public/lingocafe/favicon/`.
  - Validation: `file public/lingocafe/*.png public/lingocafe/favicon/*` or equivalent confirms `lingocafe.png` is 1024x1024 and favicon assets include 180/192/256 sizes.
  - Notes: Confirmed via `file`; ignore `.DS_Store` unless the operator asks for cleanup.

### Milestone 2 — Prepare LingoCafe Icon Assets

- [x] Step 1 — Generate missing 512x512 PWA icon
  - Achieve: Create a 512x512 PNG derived from `public/lingocafe/lingocafe.png` for manifest use.
  - Create: `public/lingocafe/favicon/favicon-512x512.png` or another clearly named LingoCafe 512 icon path under `public/lingocafe/`.
  - Modify: n/a
  - Delete: n/a
  - Touch points: local image tooling such as `sips` on macOS, ImageMagick if available, or another existing local command; `public/lingocafe/lingocafe.png`.
  - Validation: `file` confirms the generated image is PNG and 512x512.
  - Notes: Generated with `sips` from `public/lingocafe/lingocafe.png`.

- [x] Step 2 — Decide maskable icon handling
  - Achieve: Decide whether the generated 512x512 icon is suitable for `maskable512` or should remain a standard manifest icon only.
  - Create: n/a unless a dedicated maskable output is safely generated.
  - Modify: possibly generated image files only if a safe maskable derivative is created.
  - Delete: n/a
  - Touch points: `public/lingocafe/lingocafe.png`, generated 512 icon, PWA manifest icon requirements.
  - Validation: The final config either sets `maskable512` to a deliberate path or leaves it unset with the reason recorded in execution notes/final outcome.
  - Notes: `maskable512` will stay unset because no dedicated safe-area maskable asset was provided.

### Milestone 3 — Wire LingoCafe Configuration

- [x] Step 1 — Configure app logo
  - Achieve: Add the LingoCafe logo to the existing top-level `logo` field if source inspection confirms `AppTitle` consumers render URL logos correctly.
  - Create: n/a
  - Modify: `src/config/lingocafe/config.ts`.
  - Delete: n/a
  - Touch points: `TAppConfigItem.logo`, `src/42go/layouts/public/AppTitle.tsx`, `src/42go/layouts/app/AppTitle.tsx`, `next/image`.
  - Validation: Source inspection confirms `logo: "/lingocafe/logo.png"` or the chosen icon path is used by `AppTitle` and is a valid public URL.
  - Notes: Configured `logo: '/lingocafe/logo.png'`; build/runtime checks remain later validation.

- [x] Step 2 — Configure PWA icon paths
  - Achieve: Replace the default LingoCafe PWA icon path with LingoCafe-specific assets.
  - Create: n/a beyond any generated 512 icon from Milestone 2.
  - Modify: `src/config/lingocafe/config.ts`.
  - Delete: n/a
  - Touch points: `public.pwa.icons.appleTouch180`, `manifest192`, `manifest512`, optionally `maskable512`.
  - Validation: Config points at existing files such as `/lingocafe/favicon/favicon-180x180.png`, `/lingocafe/favicon/favicon-192x192.png`, and the generated 512 path.
  - Notes: Updated `appleTouch180`, `manifest192`, and `manifest512`; kept `startUrl: "/books"` unchanged.

- [x] Step 3 — Document favicon status in task outcome
  - Achieve: Confirm there is no current shared favicon config support and keep the favicon-system work delegated to `RO05`.
  - Create: n/a
  - Modify: no source change required unless a supported favicon field is discovered.
  - Delete: n/a
  - Touch points: `src/AppConfig.ts`, `src/app/layout.tsx`, `src/42go/pwa/HeadTags.tsx`, `RO05.task.md`.
  - Validation: Final outcome states that general website favicon configuration is blocked on `RO05` if no existing support exists.
  - Notes: Source inspection found no shared favicon config field; did not add hardcoded shared-layout favicon tags.

### Milestone 4 — Verify Runtime Metadata

- [x] Step 1 — Verify generated manifest
  - Achieve: Confirm LingoCafe's manifest includes LingoCafe icon paths and still launches at `/books`.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `src/app/manifest.ts`, local dev server if already running, `read.lingocafe.app` or `lingocafe.localhost` host behavior as available.
  - Validation: Use source inspection or `curl` against the running app with the LingoCafe host to confirm `start_url: "/books"` and `icons` contain the LingoCafe paths.
  - Notes: `curl -H 'Host: read.lingocafe.app' http://127.0.0.1:3000/manifest.webmanifest` returned `/books`, the 192 icon, and the generated 512 icon.

- [x] Step 2 — Verify Apple touch head tag
  - Achieve: Confirm the LingoCafe Apple touch icon link resolves to the LingoCafe 180x180 asset.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `src/42go/pwa/HeadTags.tsx`, root layout head rendering, configured `appleTouch180` path.
  - Validation: Source inspection or rendered HTML shows `<link rel="apple-touch-icon">` uses the LingoCafe asset.
  - Notes: Source inspection confirms `HeadTags` reads `pwa.icons.appleTouch180`; the configured 180 asset returns HTTP 200.

- [x] Step 3 — Verify logo rendering path
  - Achieve: Confirm the configured logo path can render in current layout title components without type or image-domain issues.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `src/42go/layouts/app/AppTitle.tsx`, `src/42go/layouts/public/AppTitle.tsx`, `src/config/lingocafe/config.ts`.
  - Validation: Source inspection, browser smoke check, or build confirms the `next/image` local public path is accepted.
  - Notes: Source inspection confirms `AppTitle` renders string logos with `next/image`; `/lingocafe/logo.png` returns HTTP 200.

### Milestone 5 — Quality Gate and Backlog Hygiene

- [x] Step 1 — Run QA
  - Achieve: Ensure the icon config and generated asset changes do not break linting or the production build.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `npm run qa`, Next.js build, TypeScript validation.
  - Validation: `npm run qa` exits successfully.
  - Notes: Passed; existing Turbopack NFT warning and `next/headers` fallback messages still appear.

- [x] Step 2 — Final source review
  - Achieve: Review changed files and confirm the task stayed scoped to LingoCafe assets/config plus backlog references.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `git diff -- docs/backlog src/config/lingocafe public/lingocafe`, `RO05.task.md`.
  - Validation: Diff shows no shared icon schema implementation and no global favicon replacement.
  - Notes: Source review confirmed LT47 stayed scoped to LingoCafe config/assets and backlog updates; RO05 remains the shared icon-system task.
