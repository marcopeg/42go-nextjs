# Plan — Fix Reader Sentence Segmentation Skipping Sentence-Ending Full Stops

## Goal

Give the reader and translation API one language-independent sentence-boundary rule, so normal full stops split clickable targets, one sentence with an abbreviation remains valid, and genuinely multi-sentence input remains rejected.

## Milestones

### Milestone 1 — Create a shared, testable boundary utility

- [x] Step 1 — Add language-independent sentence segmentation
  - Achieve: Provide a pure shared utility that normalizes sentence candidates, returns reader-safe text segments, and determines whether input contains exactly one sentence without relying on the current fixed English abbreviation set.
  - Create: `src/lib/lingocafe/sentence-segmentation.ts`.
  - Modify: n/a.
  - Delete: n/a.
  - Touch points: Unicode whitespace, `Intl.Segmenter` sentence candidates where available, a deterministic Unicode-punctuation fallback, and reconciliation of abbreviation/initial fragments so `Dr. Watson arrived.` remains one sentence rather than two.
  - Validation: The exported API preserves each segment's source text for rendering, recognizes the reported Swedish two-sentence string as two sentences, recognizes an abbreviation sentence as one, and recognizes deliberate multi-sentence input as more than one.
  - Notes: Implemented in `src/lib/lingocafe/sentence-segmentation.ts` with language-neutral platform segmentation, a deterministic fallback applied to every platform chunk, and Unicode-pattern reconciliation for standalone abbreviation/initial fragments. Node 20 raw `Intl.Segmenter` splits `Dr.` from the following words but also misses a full stop before the Swedish dialogue dash; the shared utility repairs both results.

### Milestone 2 — Apply the shared rule to reader and API validation

- [x] Step 1 — Replace the reader-local cursor splitter
  - Achieve: Make `BookPageReader` render one translation target per segment from the shared utility, fixing normal periods after ASCII and Unicode words without changing target ordering, whitespace presentation, or sentence-ID sequencing.
  - Create: n/a.
  - Modify: `src/app/(app)/(lingocafe)/books/_components/BookPageReader.tsx`.
  - Delete: The reader-local `sentenceAbbreviations` set and `splitSentenceSegments()` implementation.
  - Touch points: `renderSentenceText()`, sentence trimming, `data-reader-sentence-id` catalog collection, playback highlight alignment, and word-scope rendering.
  - Validation: The Chapter 20 text renders as two selectable sentence targets; their catalog order and text remain correct for playback and translation.
  - Notes: `BookPageReader` now imports `splitLingoCafeSentences()`; the local abbreviation list and cursor splitter are removed.

- [x] Step 2 — Replace raw full-stop counting in the translation route
  - Achieve: Preserve length, membership, authentication, and authorization checks while validating the submitted text as exactly one detected sentence through the shared utility.
  - Create: n/a.
  - Modify: `src/app/api/(lingocafe)/lingocafe/translate/route.ts`.
  - Delete: The raw full-stop-shape helper and its closing-character regex if they become unused.
  - Touch points: `getFormalValidationIssue()`, normalized request text, existing `400` validation response, and the rule that a valid source sentence must appear in the requested book page.
  - Validation: A request for `Dr. Watson arrived.` passes formal sentence validation, while the reported two-sentence string still receives the existing one-sentence error before any translation-provider call.
  - Notes: `getFormalValidationIssue()` now uses `hasExactlyOneLingoCafeSentence()`, retaining the existing validation error for genuine multi-sentence input.

### Milestone 3 — Lock the regression and run quality gates

- [x] Step 1 — Add focused sentence-boundary regression coverage
  - Achieve: Add an explicit Node test command and fixtures for the shared utility so boundary behavior does not depend on manual reader testing.
  - Create: `tests/lingocafe-sentence-segmentation.test.mts`.
  - Modify: `package.json`.
  - Delete: n/a.
  - Touch points: Node's existing `node:test` pattern, the shared utility, Chapter 20 Swedish source text, abbreviation/initial examples, decimal values, terminal quotes or parentheses, Unicode sentence terminators, and intentional multi-sentence input.
  - Validation: `npm run test:lingocafe-sentence-segmentation` passes and proves the reader/API utility returns consistent one-versus-many sentence results. It must include the exact reported source as a permanent regression fixture.
  - Notes: Added `tests/lingocafe-sentence-segmentation.test.mts` and `npm run test:lingocafe-sentence-segmentation`. Tests are provider-free and database-free, covering the exact Swedish regressions, dialogue dashes, abbreviations/initials, decimal values, closing quotes, Unicode terminators, and multi-sentence rejection.

- [x] Step 2 — Run implementation and hand-off verification
  - Achieve: Confirm code quality and state the operator's remaining manual mobile verification clearly.
  - Create: n/a.
  - Modify: n/a.
  - Delete: n/a.
  - Touch points: Lint, production build, focused Node test, reader sentence catalog, and translation API formal validation.
  - Validation: Run `npm run test:lingocafe-sentence-segmentation` and `npm run qa`. Hand off the reported Chapter 20 case and one abbreviation case for the operator's manual mobile-reader smoke test.
  - Notes: `npm run test:lingocafe-sentence-segmentation` and `npm run qa` passed. The configured ngrok test URL was offline during this execution, so the operator's manual mobile verification remains the hand-off check.
