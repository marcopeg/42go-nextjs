# Refinement Questions v1 — Fix Reader Sentence Segmentation Skipping Sentence-Ending Full Stops

Answer inline under each question, then return to chat and say `next round`.

## Questions

### 1. For this bug fix, should the implementation preserve the existing abbreviation policy exactly, or also add Swedish-language abbreviations now?

The reported content is Swedish, but the current abbreviation set is English-oriented. The narrow recommendation is to preserve the current policy, fix the skipped-full-stop defect, and record any language-expansion work separately.

Answer:

Should be language independent.

### 2. Is the requested verification scope a focused automated regression test plus normal QA, or should this task also require a manual mobile-reader smoke test?

The recommendation is focused automated coverage for the exact Chapter 20 text and punctuation edge cases, plus `npm run qa`. A manual mobile smoke is useful but is not needed to prove the cursor defect is fixed.

Answer:

I will do the manual testing after you're done.
