# Refinement Questions v2 — Fix Reader Sentence Segmentation Skipping Sentence-Ending Full Stops

Answer inline under each question, then return to chat and say `next round`.

## Questions

### 1. Should the server-side translation validator share the language-independent sentence detector, so a single sentence containing an abbreviation is accepted?

For example, a sentence such as `Dr. Watson arrived.` has two periods but is still one sentence. The current API rejects it using a raw period-count check. The recommendation is to share one language-independent boundary utility between the reader and API, retaining the rule that requests with more than one detected sentence are rejected.

Answer:

Yes, if possible, abbreviations should be treated as the same sentence.
