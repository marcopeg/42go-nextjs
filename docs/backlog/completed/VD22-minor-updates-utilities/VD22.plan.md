---
taskId: VD22
createdAt: 2026-04-21T08:50:51+02:00
updatedAt: 2026-04-23T15:27:45+02:00
---

# Plan — Minor Updates — Utilities

**TaskID**: VD22
**Status**: completed

## Goal

Upgrade knex, pg, pg-query-stream, @tanstack/react-query, tailwind-merge, and zod to current minor versions while preserving build stability and runtime behavior for auth/database and quicklist API/UI flows.

## Milestones

### Milestone 1 — Update Utility Dependencies

- [x] Step 1 — Install the six targeted minor updates
  - Achieve: Run a targeted dependency install for the six VD22 packages only.
  - Create: None.
  - Modify: `package.json`, `package-lock.json`.
  - Delete: None.
  - Touch points: npm dependency graph and direct dependency entries.
  - Validation: `npm ls knex pg pg-query-stream @tanstack/react-query tailwind-merge zod` resolves expected latest minors without `invalid` entries.
  - Notes: Keep scope restricted to the six utility packages.

- [x] Step 2 — Inspect dependency diff scope
  - Achieve: Verify changes in manifest and lockfile are consistent with targeted updates and expected transitive movement.
  - Create: None.
  - Modify: `package-lock.json` only if a reinstall is needed to stabilize resolution.
  - Delete: None.
  - Touch points: `package.json`, `package-lock.json`, git diff.
  - Validation: Direct changes match six target packages; any transitive churn is documented in notes.
  - Notes: Direct dependency deltas in `package.json` matched only the six VD22 target packages.

### Milestone 2 — Validate Build And Runtime Behavior

- [x] Step 1 — Run repository QA
  - Achieve: Confirm lint/type/build integrity after dependency updates.
  - Create: None.
  - Modify: Only compatibility fixes if updates reveal regressions.
  - Delete: None.
  - Touch points: `package.json`, `package-lock.json`, `src/42go/db/index.ts`, `src/42go/db/utils.ts`, `src/components/Providers.tsx`, `src/lib/quicklists/hooks/useQuicklists.ts`, `src/app/api/notes/route.ts`, `src/app/api/quicklists/route.ts`, `src/app/api/quicklists/[projectId]/route.ts`, `src/app/api/quicklists/[projectId]/[taskId]/route.ts`, `src/app/api/quicklists/[projectId]/invites/route.ts`.
  - Validation: `npm run qa` exits successfully.
  - Notes: Initial QA failed due to stricter Knex typing at `db.raw(projectsSql, params)` in `src/app/api/quicklists/route.ts`; fixed by changing `params` from `unknown[]` to `Array<string | Date | number>`, then QA passed.

- [x] Step 2 — Run DB/Auth and QuickList smoke checks
  - Achieve: Validate runtime behavior where updated utilities matter most.
  - Create: None.
  - Modify: Only if regressions are confirmed.
  - Delete: None.
  - Touch points: `src/42go/auth/lib/callbacks.ts`, `src/42go/db/index.ts`, `src/components/Providers.tsx`, `src/lib/quicklists/hooks/useQuicklists.ts`, `src/app/api/quicklists/route.ts`, `src/app/api/notes/route.ts`.
  - Validation: Login/session path still works (DB queries via knex/pg), quicklist fetch/mutation path still works (@tanstack/react-query), and one invalid API payload still triggers zod validation failure.
  - Notes: Authenticated smoke checks passed via credentials login (`john/john`), invalid quicklists payload returned 400 with zod errors, valid quicklists payload returned 201, and authenticated quicklists GET returned 200.

### Milestone 3 — Capture Execution State

- [x] Step 1 — Update plan and notes to reflect actual execution
  - Achieve: Keep progress checkboxes accurate and annotate any deviations, problems, or residual limitations.
  - Create: `./VD22.minor-updates-utilities.notes.md` (if not already present).
  - Modify: `./VD22.minor-updates-utilities.plan.md`, `./VD22.minor-updates-utilities.notes.md`.
  - Delete: None.
  - Touch points: Execution artifacts and any code files changed due to compatibility fixes.
  - Validation: Plan state reflects completed steps and notes capture outcomes plus limitations.
  - Notes: Task remains In Progress at session end; final state transition is handled by `acomplete`.
