# Refinement Questions v2 — Add Makefile Database Backup and Restore Commands

Answer inline under each question, then return to chat and say `next round`.

## Questions

### 1. For `make restore from=...`, how should restore determine the mode?

Choose one: infer `full` or `light` from the dump filename; require `mode=<full|light>` in addition to `from`; accept optional `mode` and fail if it does not match the filename.

Answer:

restore should just execute the instructions in the dump.
it's the dump part that must create a good dump sql file.
the restoration could happen manually in a db mgm tool.

### 2. When you wrote that migrations must be executed manually "prior to a dump", did you mean before restore?

Proposed interpretation: backup reads the current connected database as-is; restore assumes the operator has already run `make migrate` manually and must not run migrations automatically.

Answer:

yes, before restore. my bad.

### 3. Should the new `42go-backup` script be a public CLI surface or only a Makefile backend?

Choose one: expose documented script subcommands such as `backup` and `restore`; keep the documented public interface as Makefile only and let the skill/Makefile call the script internally.

Answer:

only makefile plus operable via codex skill
