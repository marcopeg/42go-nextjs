# Refinement Questions v1 — Add Makefile Database Backup and Restore Commands

Answer inline under each question, then return to chat and say `next round`.

## Questions

### 1. What should `make restore` do if the target database already contains data?

Choose one: fail fast unless relevant tables are empty; truncate the target tables first in dependency-safe order; insert/merge into existing data.

Answer:

truncate first. careful with the order.

### 2. Should `knex_migrations` and `knex_migrations_lock` be included in the data-only dump?

The restore target is expected to have already run migrations, so excluding them avoids rewriting migration bookkeeping. Including them could make a dump closer to a full database state.

Answer:

the dump should be data only. migrations must be executed prior to a dump. manualy

### 3. Should `make restore` require `dump=<path>`, or should it default to the newest dump matching `mode`?

Choose one: always require `dump`; default to newest matching dump when omitted; support both but print the selected file before restoring.

Answer:

use 'make restore from=...'

### 4. Which timestamp convention should dump filenames use?

Choose one: UTC, local machine timezone, or another explicit format. The required shape remains `knex/dumps/{timestamp}.dump.{mode}.sql`.

Answer:

utc

### 5. Is it acceptable for the implementation to use a repo-local helper script to generate ordered pg_dump/psql commands?

Makefile-only logic may get brittle because table order and light-mode exclusions matter. A script can query PostgreSQL catalogs or keep a reviewed table order close to the command implementation.

Answer:

yes. but build a 42go-backup skill and put the script there.
it shuld work when ivoked as skill via codex or via makefile.
