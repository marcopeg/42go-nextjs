---
taskId: BW36
createdAt: 2026-04-27T17:04:29+02:00
updatedAt: 2026-04-27T17:42:17+02:00
---

# Plan — Collect Profile Update Event

## Goal

Record a `profile-update` row in `lingocafe.events` only when `/api/lingocafe/profile` successfully changes stored LingoCafe profile values, with event `data` containing a before/after diff of the changed profile fields.

## Milestones

### Milestone 1 — Model the Profile Diff

- [x] Step 1 — Add explicit profile payload and diff types
  - Achieve: Make the LingoCafe profile update shape and event diff shape explicit so later code can compare stored values without loose object handling.
  - Create: n/a
  - Modify: `src/app/api/(lingocafe)/lingocafe/_lib/reader.ts`
  - Delete: n/a
  - Touch points: Profile payload type currently inferred in `parseProfilePayload`/`saveProfile`; existing `ProfileRow`; new diff type such as `ProfileUpdateDiff`.
  - Validation: TypeScript accepts the typed payload and diff structure during `npm run qa`.
  - Notes: Keep field names aligned with API payload names where useful, but compare against DB columns `own_lang`, `target_lang`, and `target_level`.

- [x] Step 2 — Implement before/after diff calculation
  - Achieve: Compare the stored LingoCafe profile row against the submitted validated payload and return only fields whose values actually change.
  - Create: n/a
  - Modify: `src/app/api/(lingocafe)/lingocafe/_lib/reader.ts`
  - Delete: n/a
  - Touch points: New helper such as `buildProfileUpdateDiff`; current profile fields `own_lang`, `target_lang`, `target_level`; no-op detection.
  - Validation: Manual code inspection confirms unchanged values produce an empty diff and changed values produce `{ before, after }` pairs for only changed fields.
  - Notes: Treat a missing profile row as a real change from `null`/missing before-values to the submitted after-values.

### Milestone 2 — Persist Profile Changes and Event Atomically

- [x] Step 1 — Read the existing profile before saving
  - Achieve: Load the current `lingocafe.profiles` row for the authenticated user before the upsert so the real before-state is available.
  - Create: n/a
  - Modify: `src/app/api/(lingocafe)/lingocafe/_lib/reader.ts`
  - Delete: n/a
  - Touch points: `saveProfile`, `getDB`, `lingocafe.profiles`, authenticated `userId`.
  - Validation: Existing profile saves still return `loadReaderData(userId)`; no browser-supplied user id is introduced.
  - Notes: Keep the public response shape unchanged unless implementation proves a new internal return shape is needed.

- [x] Step 2 — Save profile and track event only on real change
  - Achieve: After a successful upsert, insert `profile-update` through `trackReaderEvent` only when the computed diff is non-empty.
  - Create: n/a
  - Modify: `src/app/api/(lingocafe)/lingocafe/_lib/reader.ts`
  - Delete: n/a
  - Touch points: `saveProfile`, `trackReaderEvent`, `lingocafe.events_prepare_partitions()`, `lingocafe.events`, event `name: "profile-update"`, event `data`.
  - Validation: A changed profile produces one `profile-update` event with diff data; repeating the same request produces no new event.
  - Notes: Prefer wrapping the profile upsert and event insert in one transaction if the local Knex/event helper shape allows it cleanly. If `trackReaderEvent` stays non-transactional, keep the event after the successful save and document that choice in execution notes.

- [x] Step 3 — Keep failure paths quiet
  - Achieve: Preserve current validation and error behavior so invalid payloads, unauthenticated requests, and failed saves do not write events.
  - Create: n/a
  - Modify: `src/app/api/(lingocafe)/lingocafe/profile/route.ts` only if route-level handling needs to pass through richer save results; otherwise n/a.
  - Delete: n/a
  - Touch points: `parseProfilePayload`, `updateProfile`, `protectRoute`, current 400/401/500 responses.
  - Validation: Invalid profile payload returns 400 and does not insert `profile-update`; unauthenticated requests still return 401 through existing behavior.
  - Notes: Keep the task strictly scoped to `/api/lingocafe/profile`.

### Milestone 3 — Verify Event Behavior

- [x] Step 1 — Run quality gate
  - Achieve: Prove lint and production build still pass after the backend change.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `npm run qa`
  - Validation: `npm run qa` exits successfully.
  - Notes: This repo treats `npm run qa` as the required post-change gate. Chuck Norris does not negotiate with lint.

- [x] Step 2 — Smoke test profile event writes
  - Achieve: Verify the behavior against the running local app and database using an authenticated LingoCafe session or an equivalent local smoke path.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `POST /api/lingocafe/profile`, `lingocafe.profiles`, `lingocafe.events`, seeded LingoCafe user such as John or Jane.
  - Validation: First changed save writes one `profile-update` event with before/after diff; repeated identical save writes zero additional events; invalid payload writes zero events.
  - Notes: If local authenticated smoke cannot be run in the current environment, record that limitation during execution and rely on `npm run qa` plus static inspection.

- [x] Step 3 — Inspect event payload shape
  - Achieve: Confirm stored event `data` contains only the changed LingoCafe profile fields with before/after values.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: SQL query against `lingocafe.events` filtered by `name = 'profile-update'`; changed fields such as `ownLang`, `targetLang`, `targetLevel`.
  - Validation: Event data matches the accepted task contract and does not include generic account profile fields or browser-provided identity.
  - Notes: The final payload shape should be easy to read from JSONB during later analytics work.
