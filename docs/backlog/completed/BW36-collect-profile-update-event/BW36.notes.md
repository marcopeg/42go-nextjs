---
taskId: BW36
createdAt: 2026-04-27T17:39:55+02:00
updatedAt: 2026-04-27T17:42:17+02:00
---

# Execution Notes — Collect Profile Update Event
**Task**: ./BW36.task.md
**Plan**: ./BW36.plan.md

## Decisions

- Treat the operator's `yes and execute` response as plan acceptance plus execution.
- Move `BW36` into WIP before source changes.
- Keep the implementation inside the existing LingoCafe reader API helper because `saveProfile` already owns profile persistence for `/api/lingocafe/profile`.
- Add explicit profile payload and profile diff types so event data is shaped intentionally.
- Compute the before/after diff from the stored `lingocafe.profiles` row before upserting.
- Skip the profile upsert and event insert when the submitted values match the stored profile values.
- Extend `trackReaderEvent` to accept an optional Knex connection or transaction so `profile-update` can be inserted in the same transaction as the profile upsert.
- Keep the profile response shape unchanged.
- Clean up the local smoke-test event row and restore Jane's seeded `target_level = a2` profile value after validation.

## Problems Encountered

- The WIP transition script reordered one completed-log entry unrelated to `BW36`; that completed-log change was restored.
- The first authenticated curl attempt failed because shell variable and pipe usage did not behave well under command segmentation. Retried with explicit temp files and direct commands.

## Deviations From Plan

- n/a

## Additional Requests

- n/a

## Known Limitations

- n/a

## Validation

- `npm run qa` passed.
- Authenticated curl smoke as LingoCafe `jane` returned 200 for `GET /api/lingocafe/profile`.
- Changing Jane's `targetLevel` from `a2` to `b1` returned 200 and inserted one `profile-update` event.
- SQL verified the event payload as `{"diff": {"targetLevel": {"before": "a2", "after": "b1"}}}`.
- Repeating the same `targetLevel = b1` save kept the `profile-update` event count at one.
- Invalid profile payload returned 400 and kept the `profile-update` event count at one.
- Local smoke cleanup restored Jane's `target_level = a2` and deleted the test `profile-update` event.
