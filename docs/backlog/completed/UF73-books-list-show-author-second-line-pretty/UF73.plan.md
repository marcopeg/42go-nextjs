---
taskId: UF73
createdAt: 2026-05-01T08:20:12+00:00
updatedAt: 2026-05-01T08:24:05+00:00
---

# Plan — Books List: Show Author as Stylish Second Line

## Goal

Implement a subtle, polished second-line `by {author}` subtitle on `/books` list items when author data exists, while preserving current list behavior and theme consistency.

## Milestones

### Milestone 1 — Locate rendering path and define display contract

- [x] Step 1 — Find `/books` list item rendering component and data shape
  - Achieve: Identify the exact component(s) rendering book row title text and the author field source.
  - Create: n/a
  - Modify: likely `src/app/.../books/...` page/component files and/or shared list item components if documentation comments are needed.
  - Delete: n/a
  - Touch points: `/books` page module, list row component, TypeScript book item interface/type definitions.
  - Validation: Verify in code that author is optional and can be conditionally rendered without data-fetch changes.
  - Notes: Keep change scoped to `/books`; avoid collateral UI refactors.

- [x] Step 2 — Decide and document long-author behavior
  - Achieve: Resolve open question: truncate single line with ellipsis vs wrap to two lines.
  - Create: n/a
  - Modify: task artifact only if decision is product-driven during implementation handoff.
  - Delete: n/a
  - Touch points: `/books` row typography classes and layout rules.
  - Validation: Decision is reflected in implementation and visually verified with long author names.
  - Notes: Default recommendation for subtle/clean style is single-line truncation.

### Milestone 2 — Implement subtitle UI and conditional rendering

- [x] Step 3 — Render `by {author}` as second line only when author exists
  - Achieve: Add second-line subtitle directly under title; omit subtitle when author is missing.
  - Create: n/a
  - Modify: likely the book list row JSX/TSX file and related type guards.
  - Delete: n/a
  - Touch points: row markup, conditional render logic, author formatting helper (if introduced).
  - Validation: Manual check with mixed dataset (with/without author) confirms no placeholder text is shown.
  - Notes: Keep semantic structure accessible (e.g., grouped text container).

- [x] Step 4 — Apply subtle/clean visual styling consistent with design system
  - Achieve: Style subtitle with smaller muted text, balanced spacing, and non-competitive hierarchy under title.
  - Create: n/a
  - Modify: Tailwind class list / token usage in row component; optional shared style utility only if already pattern-consistent.
  - Delete: n/a
  - Touch points: typography classes, spacing classes, dark/light theme contrast behavior.
  - Validation: Visual pass on supported themes and responsive widths confirms readability and alignment.
  - Notes: Avoid custom one-off CSS if existing utility classes suffice.

### Milestone 3 — Validate behavior and prevent regressions

- [x] Step 5 — Regression-check list interactions and layout stability
  - Achieve: Ensure selection/navigation/actions on `/books` rows still behave exactly as before.
  - Create: n/a
  - Modify: only if minor class/layout adjustments are needed after validation.
  - Delete: n/a
  - Touch points: interactive row wrappers, click handlers, focus states.
  - Validation: Manual interaction checks across desktop/mobile breakpoints.
  - Notes: No behavior changes are intended; this is presentation-only.

- [x] Step 6 — Run project QA gate and summarize outcomes
  - Achieve: Run repo QA to catch lint/build issues and document results for handoff.
  - Create: n/a
  - Modify: n/a unless fixes are required.
  - Delete: n/a
  - Touch points: `npm run qa` output and any directly related remediation files.
  - Validation: QA command passes; if not, failures are documented with scope and cause.
  - Notes: Required by repo workflow before implementation completion.
