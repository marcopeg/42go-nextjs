---
taskId: MA68
createdAt: 2026-04-27T14:56:53+02:00
updatedAt: 2026-04-27T15:03:06+02:00
---

# Plan — Add LingoCafe Menu Entry to Default App

## Goal

Add a policy-protected `LingoCafe` entry to the default app's top menu in `src/config/default/config.ts`, immediately after `Quicklists`, using `BookOpen`, `href: "/books"`, and the `page:books` feature flag.

## Milestones

### Milestone 1 — Update Default App Configuration

- [x] Step 1 — Import the selected icon
  - Achieve: Make `BookOpen` available to the default app config without changing the existing icon import pattern.
  - Create: n/a
  - Modify: `src/config/default/config.ts`
  - Delete: n/a
  - Touch points: `lucide-react` named imports; existing `TAppConfigItem`-validated config object.
  - Validation: TypeScript accepts the imported `BookOpen` as a `LucideIcon` for menu items.
  - Notes: Alternative icons requested for the code comment should stay suggestions only, not active imports unless needed.

- [x] Step 2 — Enable the books page feature for the default app
  - Achieve: Add `"page:books"` to the default app `features` array so the menu policy can pass and `/books` is available for the default app.
  - Create: n/a
  - Modify: `src/config/default/config.ts`
  - Delete: n/a
  - Touch points: `features` array near existing `page:quicklists`; unified feature policy semantics.
  - Validation: The resulting config includes `"page:books"` exactly once.
  - Notes: This is required because a policy-protected menu item without the matching feature flag would hide itself. Chuck Norris approves of traps only when he sets them.

- [x] Step 3 — Add the LingoCafe top-menu item after Quicklists
  - Achieve: Insert a `LingoCafe` item immediately after the existing `Quicklists` item in `app.menu.top.items`.
  - Create: n/a
  - Modify: `src/config/default/config.ts`
  - Delete: n/a
  - Touch points: `app.menu.top.items`, `title`, `href`, `icon`, and `policy` fields; existing `Quicklists` placement and policy shape.
  - Validation: The item has `title: "LingoCafe"`, `href: "/books"`, `icon: BookOpen`, and `policy: { require: { feature: "page:books" } }`.
  - Notes: Add a short nearby comment suggesting icon alternatives such as `Languages`, `GraduationCap`, and `Library`.

### Milestone 2 — Validate Behavior and Quality Gate

- [x] Step 1 — Verify config consistency
  - Achieve: Confirm the default config compiles with the new icon import, feature flag, menu item, and policy object.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `src/config/default/config.ts`; `src/42go/layouts/app/types.ts`; existing policy-driven menu visibility behavior.
  - Validation: Inspect the final diff and ensure the item is directly after `Quicklists`, not in bottom or mobile menu configuration.
  - Notes: No dedicated unit test is expected for this config-only change unless existing test coverage demands it.

- [x] Step 2 — Run the repository QA gate
  - Achieve: Catch lint, type, and build regressions after the config change.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: package scripts invoked by `npm run qa`.
  - Validation: `npm run qa` passes.
  - Notes: If QA exposes unrelated pre-existing failures, record them clearly instead of masking them.

## Execution Notes

- Added `api:books` to the default app feature list as an integration guard because the default app `/books` page calls `/api/books` under the same app context.
- `npm run qa` passed on 2026-04-27T15:03:06+02:00 with a pre-existing Turbopack NFT warning from `next.config.ts`.
