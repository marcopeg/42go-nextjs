---
taskId: PE62
status: completed
createdAt: 2026-04-23T15:27:45+02:00
updatedAt: 2026-04-23T15:27:45+02:00
reviewedAt: 2026-04-20T20:56:54+02:00
plannedAt: 2026-04-20T20:56:54+02:00
startedAt: 2026-04-20T20:56:54+02:00
completedAt: 2026-04-20T20:56:54+02:00
---

# Fix Stale Lockfile + Remove Dead Dependencies

**Order**: ① — do first, everything else depends on this
**Workflow**: `aexecute` directly — steps are clear, no refinement needed

## Elevator's Pitch

The lockfile is out of sync with package.json — 7 packages declare ranges that don't match what's installed. Additionally, 4 OCI/Azure SDK packages have zero imports anywhere in the codebase. Remove the dead deps, run `npm install` to sync the lockfile, and validate everything still works.

## Business Gain

Restores lockfile integrity. Removes ~50MB of unused OCI/Azure dependency tree. Unblocks all other dependency update tasks.

## Current State

- `package.json` declares `@dnd-kit/sortable: ^10.0.0` but lockfile pins 8.0.0
- Same mismatch for: `@types/bcrypt`, `@types/node`, `dotenv`, `eslint-config-next`, `react-syntax-highlighter`, `uuid`
- `oci-objectstorage`, `oci-secrets`, `@azure/app-configuration`, `@azure/keyvault-secrets` have zero imports in `src/`, `knex/`, or `scripts/`

## Desired State

- All lockfile entries match package.json ranges
- Dead OCI/Azure packages removed from package.json and lockfile
- `npm run qa` passes
- Dev server starts and basic auth flow works
- QuickList drag-and-drop works (dnd-kit 8→10 jump)

## Definition of Success

- `npm ls` shows no `invalid` entries
- `npm run qa` passes
- No runtime regressions in dev mode

## Additional Context

From UG00 research. The dnd-kit jump (8→10) is the main risk — test QuickList drag-and-drop carefully. All other stale packages are type defs or dev tools with backwards-compatible APIs.

Steps:

1. `npm uninstall oci-objectstorage oci-secrets @azure/app-configuration @azure/keyvault-secrets`
2. `npm install`
3. `npm run qa`
4. Test dev server startup, auth flow, QuickList drag-and-drop

## Assumptions

- The dnd-kit/sortable v10 API is compatible with current usage (SortableContext, useSortable, arrayMove)
- uuid v13 ESM-only export works with Next.js bundler

## Constraints

- Must be done before all other dependency update tasks
- Do not change any application code — only package.json and lockfile

## Acceptance Criteria

- [x] Dead OCI/Azure packages removed
- [x] `npm ls` shows no `invalid` entries
- [x] `npm run qa` passes
- [x] Dev server starts cleanly
- [ ] QuickList drag-and-drop still works

## Related to

- [UG00] Update Dependencies (Research) — [outcome](../UG00-update-dependencies/UG00.outcome.md)
