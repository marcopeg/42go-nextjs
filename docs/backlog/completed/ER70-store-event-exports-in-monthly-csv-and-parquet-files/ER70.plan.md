# Plan — Store Event Exports in Monthly CSV and Parquet Files

## Goal

Change `42go-events-export` from timestamped batch files to a raw monthly archive that mirrors the PostgreSQL `events.events_YYYYMM` partition naming strategy under `.local/42go-events`.

## Milestones

### Milestone 1 — Monthly Archive Contract

- [x] Step 1 — Update exporter path and naming model
  - Achieve: Make `.local/42go-events` the default archive root and resolve monthly filenames as `events_YYYYMM.csv` and `events_YYYYMM.parquet`.
  - Create: Helper functions for month keys, monthly paths, and affected-month metadata.
  - Modify: `.agents/skills/42go-events-export/scripts/export_events.py`.
  - Delete: n/a.
  - Touch points: `DEFAULT_ARCHIVE_DIR`, `Paths`, output path construction.
  - Validation: `export_events.py --help` shows `.local/42go-events` and no public `--app-id` option.
  - Notes: Match `knex/migrations/20260511143000_core_events_schema.js` partition naming.

### Milestone 2 — Atomic Monthly Merge

- [x] Step 2 — Merge fetched rows into monthly files
  - Achieve: Group fetched rows by `created_at` month, merge with existing monthly rows, de-duplicate by `id`, and atomically rewrite each touched CSV and Parquet file.
  - Create: CSV and Parquet read helpers, de-duplication helpers, and monthly smoke validation.
  - Modify: `.agents/skills/42go-events-export/scripts/export_events.py`.
  - Delete: n/a.
  - Touch points: CSV writer, Parquet writer, DuckDB smoke read, state commit ordering.
  - Validation: Fixture merge updated existing May files, created June files, and left May with two de-duplicated rows.
  - Notes: The cursor advances only after every touched monthly file is written and validated.

### Milestone 3 — Legacy Batch Cleanup and Manifest

- [x] Step 3 — Delete local legacy batch files and record monthly updates
  - Achieve: Remove old local `batch-*.csv` and `batch-*.parquet` files from the archive after successful monthly writes, and record touched month files in `manifest.jsonl`.
  - Create: Legacy cleanup helper and manifest payload for monthly updates.
  - Modify: `.agents/skills/42go-events-export/scripts/export_events.py`.
  - Delete: Local generated archive files only at runtime, never repository files and never database rows.
  - Touch points: `manifest.jsonl`, `events/csv`, `events/parquet`.
  - Validation: Fixture cleanup removed `batch-old.csv` and `batch-old.parquet` after monthly writes.
  - Notes: Cleanup is local archive migration, not source event retention.

### Milestone 4 — Reader and Documentation

- [x] Step 4 — Update local analysis and skill docs
  - Achieve: Make `analyze_events.py` read the monthly Parquet layout and document the new archive root, filenames, merge behavior, and cleanup behavior.
  - Create: n/a.
  - Modify: `.agents/skills/42go-events-export/scripts/analyze_events.py`, `.agents/skills/42go-events-export/SKILL.md`, `.agents/skills/42go-events-export/README.md`.
  - Delete: References that describe timestamped batch files as current behavior.
  - Touch points: Archive layout docs, setup paths, examples, analysis glob.
  - Validation: `analyze_events.py --help`, empty-archive `wau`, and fixture `wau` all passed.
  - Notes: Raw archive remains time-partitioned only; app-specific ETL is out of scope.

### Milestone 5 — Validation and Backlog Evidence

- [x] Step 5 — Run focused validation
  - Achieve: Prove the scripts compile, CLI help works, docs are aligned, and the monthly merge path works with local fixtures.
  - Create: n/a.
  - Modify: `ER70.plan.md`, `ER70.notes.md`.
  - Delete: n/a.
  - Touch points: Python compile/help commands, temporary fixture archive under `/private/tmp`.
  - Validation: `py_compile`, exporter/analyzer help, analyzer empty archive, Parquet merge fixture, fixture `wau`, and `npm run qa` passed.
  - Notes: The repo base Python lacks `pyarrow`; fixture validation used the existing `.local/42go-events-analytics/.venv` environment. PyArrow emitted sandbox-only `sysctlbyname` warnings while still passing.
