---
taskId: ZD09
createdAt: 2026-05-03T07:36:57+02:00
updatedAt: 2026-05-03T08:18:58+02:00
---

# Plan — Fix Auth Users Name Unique Constraint

## Goal

Split credential usernames from OAuth display names while keeping the local migration history clean and providing a manual SQL patch for the existing deployed database.

## Milestones

### Milestone 1 — Reconcile the Working Tree

- [x] Step 1 — Remove the superseded Knex migration
  - Achieve: Align the open changes with the refined decision to rewrite the original auth migration in place instead of adding a new Knex migration.
  - Create: n/a
  - Modify: n/a
  - Delete: `knex/migrations/20260502083000_auth_username.js`
  - Touch points: Git status, migration ordering, task constraints.
  - Validation: `git status --short` no longer shows the superseded migration file.
  - Notes: The root `migrate.sql` will replace this file as the production patch path.

- [x] Step 2 — Keep the clean local auth schema rewrite
  - Achieve: Preserve the already-started change to the original auth migration so local rebuilds create the intended final `auth.users` schema.
  - Create: n/a
  - Modify: `knex/migrations/20240320_auth.js`
  - Delete: n/a
  - Touch points: `auth.users.username`, `auth.users.name`, `users_app_id_username_unique`, `users_app_id_email_unique`.
  - Validation: Inspect the migration to confirm `username` exists, `(app_id, username)` is unique, `(app_id, email)` remains unique, and `(app_id, name)` is not unique.
  - Notes: This is intentionally destructive for local rebuilds because the operator will destroy and rebuild the local database.

### Milestone 2 — Add Manual Production Patch

- [x] Step 1 — Create root `migrate.sql`
  - Achieve: Provide a manual SQL patch that brings the existing deployed database into the same final schema shape as a local rebuild.
  - Create: `migrate.sql`
  - Modify: n/a
  - Delete: n/a
  - Touch points: `auth.users`, `username` column, `users_app_id_name_unique`, `users_app_id_username_unique`, `users_app_id_email_unique`.
  - Validation: Read the SQL and verify it is transaction-wrapped, adds `username` only when needed, backfills password users from `name`, drops the name unique constraint, and adds the username unique constraint.
  - Notes: This file is an operational patch, not a Knex migration.

- [x] Step 2 — Make dirty data fail loudly
  - Achieve: Ensure the manual patch aborts if password-user `name` values would produce duplicate `(app_id, username)` values.
  - Create: n/a
  - Modify: `migrate.sql`
  - Delete: n/a
  - Touch points: PostgreSQL `DO $$` validation block, duplicate grouping by `app_id` and `name` for users with `password IS NOT NULL`.
  - Validation: SQL contains an explicit `RAISE EXCEPTION` path before the backfill and constraint creation.
  - Notes: Do not auto-generate usernames.

### Milestone 3 — Update Credentials Flow

- [x] Step 1 — Use username for password login
  - Achieve: Make credentials login resolve users by `username` or `email`, not display `name`.
  - Create: n/a
  - Modify: `src/42go/auth/lib/providers/get-providers.ts`
  - Delete: n/a
  - Touch points: NextAuth credentials provider, `authorize`, `auth.users` lookup, bcrypt password check.
  - Validation: Inspect query for `username ILIKE credentials.username OR email ILIKE credentials.username`; run QA after all changes.
  - Notes: The current open change already matches this direction and should be retained unless tests expose a problem.

- [x] Step 2 — Preserve OAuth display-name behavior
  - Achieve: Confirm OAuth providers continue to map provider profile names into `name` and do not require `username`.
  - Create: n/a
  - Modify: n/a unless inspection finds OAuth writing username.
  - Delete: n/a
  - Touch points: GitHub provider profile mapping, Google provider profile mapping, OAuth `signIn` callback insert/update.
  - Validation: Inspect `src/42go/auth/lib/providers/get-providers.ts` and `src/42go/auth/lib/callbacks.ts` for no OAuth username requirement.
  - Notes: OAuth users may have duplicate display names.

### Milestone 4 — Split Demo User Seeds

- [x] Step 1 — Split default app test users from auth/role seed
  - Achieve: Move default demo user creation into a clearer seed file while preserving auth cleanup and role/grant setup.
  - Create: `knex/seeds/20240523_default_test_users.js`
  - Modify: `knex/seeds/20240522_init_auth_data.js`
  - Delete: n/a
  - Touch points: `auth.users`, bcrypt hashing, `username`, display `name`, emails, default app roles/grants dependencies.
  - Validation: Seed order still creates default users before later seeds that depend on `john.doe@example.com`; QA and optional seed smoke check.
  - Notes: Keep demo credentials `admin/admin`, `john/john`, and `jane/jane`.

- [x] Step 2 — Split Quicklist test users from Quicklist demo data
  - Achieve: Move Quicklist app user creation into a clearer Quicklist test-user seed and leave Quicklist projects/tasks/collabs in the Quicklist demo-data seed.
  - Create: `knex/seeds/20250815090000_quicklist_test_users.js`
  - Modify: `knex/seeds/20250815_init_quicklist_data.js`
  - Delete: n/a
  - Touch points: `auth.users` rows for `app_id = "quicklist"`, existing Quicklist tables, existing default-user references by email.
  - Validation: Seed order creates needed users before demo data; inspect for duplicate inserts on repeated seed runs.
  - Notes: The current Quicklist data seed creates Quicklist users but demo projects reference default users. Preserve behavior deliberately or simplify only if inspection proves Quicklist app users are unused.

- [x] Step 3 — Split Lingocafe test users or document none are needed
  - Achieve: Make Lingocafe test-user setup explicit.
  - Create: `knex/seeds/20260427150500_lingocafe_test_users.js`
  - Modify: `knex/seeds/20260427151000.lingocafe.data.js`
  - Delete: n/a
  - Touch points: `auth.users` rows for `app_id = "lingocafe"`, Lingocafe profile seed, existing default-user dependency.
  - Validation: Lingocafe seed either creates app-scoped users intentionally or clearly continues using the default John user for profile data without deleting nonexistent Lingocafe users.
  - Notes: Current Lingocafe seed deletes `app_id = "lingocafe"` users but then attaches profile data to the default app John user. Inspect before changing behavior.

### Milestone 5 — Validate Schema and Login Behavior

- [x] Step 1 — Run static QA
  - Achieve: Confirm TypeScript, linting, and production build pass after code and seed cleanup.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `npm run qa`.
  - Validation: `npm run qa` exits with code 0.
  - Notes: This was already passing before final seed/migration cleanup; rerun after the actual execution.

- [x] Step 2 — Verify database paths
  - Achieve: Prove the local rebuild path and manual patch path both produce the intended constraints.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `make migrate`, `make seed`, manual `migrate.sql` review or local scratch application, PostgreSQL constraint inspection.
  - Validation: Confirm `auth.users` has `username`; `(app_id, email)` and `(app_id, username)` are unique; `(app_id, name)` is not unique.
  - Notes: Only apply `migrate.sql` to a safe local/scratch database during execution unless the operator explicitly asks to patch production.

- [x] Step 3 — Smoke test auth outcomes
  - Achieve: Confirm password login still works and OAuth can tolerate duplicate display names.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: credentials provider, seeded users, `auth.users` duplicate display-name insert scenario.
  - Validation: Credentials lookup works for `john` or `john.doe@example.com`; database permits two users in one app with the same `name` and different emails; email uniqueness still blocks duplicate emails.
  - Notes: Browser OAuth end-to-end may need provider credentials; use direct DB/API smoke tests if OAuth credentials are unavailable.
