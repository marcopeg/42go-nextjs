---
taskId: ZD09
createdAt: 2026-05-03T07:49:10+02:00
updatedAt: 2026-05-03T08:30:37+02:00
---

# Execution Notes — Fix Auth Users Name Unique Constraint
**Task**: ./ZD09.task.md
**Plan**: ./ZD09.plan.md

## Decisions

- Accepted the plan and moved the task to WIP on 2026-05-03.
- Kept the in-place edit to `knex/migrations/20240320_auth.js` because the operator explicitly wants a local destructive rebuild path.
- Replaced the superseded Knex migration with a root-level `migrate.sql` manual production patch.
- Split app test users into dedicated seed files for default, Quicklist, and LingoCafe.
- Kept OAuth profile handling unchanged; OAuth still writes provider real names into `auth.users.name` and does not require `username`.
- Corrected the LingoCafe seed path to use LingoCafe-scoped Jane for demo profile data, matching the earlier OG12 decision that John/Jane are LingoCafe app users.
- `npm run qa` passed after the migration, auth, SQL patch, and seed split changes.
- The operator confirmed the local path works.
- The operator confirmed the intended production posture: after manually applying `migrate.sql`, the production auth-user structure should match the structure produced by a local destructive `make migrate.rebuild` for this change, excluding expected differences such as column order and Knex migration history.
- The operator applied `migrate.sql` to the production database and moved the SQL file into this task folder for memory reference.

## Problems Encountered

- The repo-local `backlog-execute` skill does not ship a `resolve_task.py`; the transition script was available and successfully moved the task to WIP.
- Unrelated untracked `docs/cv/` content exists in the working tree and is intentionally ignored.
- The database-changing validation steps were not run automatically by the agent because applying `migrate.sql`, `make migrate`, or `make seed` can alter local data; the operator later confirmed the local path works.

## Deviations From Plan

- The plan allowed documenting no LingoCafe test users if inspection proved none were needed. Inspection found prior OG12 requirements for LingoCafe-scoped John/Jane, so execution created `knex/seeds/20260427150500_lingocafe_test_users.js` and updated LingoCafe profile data to use Jane.

## Additional Requests

- The operator accepted the plan and requested immediate execution.
- After completion, the operator reported that the manual production patch had been applied and archived the SQL patch with the task.

## Known Limitations

- Production was patched manually with `migrate.sql`; this is intentionally outside Knex migration tracking.
- `migrate.sql` only aligns the auth-users structure for this change. It assumes the deployed database has the other expected migrations already applied.
