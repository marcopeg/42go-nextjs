---
taskId: ZJ19
createdAt: 2026-05-07T19:10:31+02:00
updatedAt: 2026-05-07T20:43:32+02:00
---

# Plan — After GM77, Add Mobile Reader Table of Contents Popup

## Goal

Replace the reader's placeholder and permanent desktop contents rail with one `Modal`-driven table-of-contents flow that opens from a dedicated top-bar button, renders as a full-screen mobile overlay and right-side desktop panel, highlights the current page, and closes immediately after navigation.

## Milestones

### Milestone 1 — Reshape the reader surface around a TOC trigger

- [x] Step 1 — Replace the old desktop contents rail with a shared TOC trigger path
  - Achieve: Remove the permanent left desktop contents column and expose a dedicated visible TOC button in both reader headers so mobile and desktop both enter the same navigation flow from the top bar.
  - Create: n/a
  - Modify: `src/app/(app)/(lingocafe)/books/_components/BookReaderSurfaces.tsx`
  - Delete: any dead left-rail-only reader markup that becomes obsolete inside `BookReaderSurfaces.tsx`
  - Touch points: `ReaderSidePanel`, desktop/mobile header actions, `Button`/icon imports, reader layout widths and spacing.
  - Validation: Visual smoke check in the reader on mobile and desktop confirms a visible TOC trigger exists and the old left contents column is gone.
  - Notes: Completed by removing the permanent left rail, moving desktop back navigation into the top bar, and adding a shared TOC trigger path on both headers.

- [x] Step 2 — Add explicit TOC open state to the reader page flow
  - Achieve: Track a dedicated TOC surface state separately from reader preferences so opening contents does not collide with the appearance panel.
  - Create: n/a
  - Modify: `src/app/(app)/(lingocafe)/books/[bookId]/[pageId]/page.tsx`, `src/app/(app)/(lingocafe)/books/_components/BookReaderSurfaces.tsx`
  - Delete: n/a
  - Touch points: `BookReadPage` state hooks, reader surface prop types, top-bar trigger callbacks.
  - Validation: Manual interaction check confirms the TOC and preferences surfaces can open and close independently.
  - Notes: Completed with a dedicated `isTableOfContentsOpen` state in the reader page and explicit open/change props on both reader surfaces.

### Milestone 2 — Build the shared reader TOC surface on top of `Modal`

- [x] Step 3 — Extract a reusable reader TOC component that consumes existing page summaries
  - Achieve: Introduce a dedicated TOC surface component that renders the existing `bookPage.pages` data, highlights the current page, and includes the bottom-only `Back to book info` affordance.
  - Create: likely `src/app/(app)/(lingocafe)/books/_components/BookReaderTableOfContents.tsx`
  - Modify: `src/app/(app)/(lingocafe)/books/_components/BookReaderSurfaces.tsx`, possibly `src/app/(app)/(lingocafe)/books/_components/book-types.ts` if display helpers need stronger typing
  - Delete: n/a
  - Touch points: `ReaderBookPage`, `ReaderBookPageSummary`, TOC row rendering, current-page indicator styling, `Link` targets for page navigation and `backHref`.
  - Validation: Manual check confirms the TOC content mirrors the previous contents list, the active page is obvious, and the bottom action links back to book info.
  - Notes: Completed via `BookReaderTableOfContents.tsx`, which renders the existing page summaries, current-page highlight, book summary block, and bottom `Back to book info` action.

- [x] Step 4 — Present the TOC through the shared `Modal` primitive with responsive behavior
  - Achieve: Use `Modal` as the only overlay/surface owner, with mobile full-screen behavior and desktop `presentation="panel"` anchored to the right.
  - Create: n/a
  - Modify: `src/app/(app)/(lingocafe)/books/_components/BookReaderTableOfContents.tsx` or `BookReaderSurfaces.tsx`, potentially `src/42go/components/modal/Modal.tsx` only if a real reusable gap is discovered
  - Delete: any bespoke overlay logic introduced during experimentation
  - Touch points: `Modal` props (`presentation`, `anchor`, `size`, `title`, `footer`), responsive class names, header/footer composition.
  - Validation: Browser check confirms mobile uses the GM77 full-screen modal behavior and desktop opens a right-side TOC panel without custom overlay code.
  - Notes: Completed with `Modal presentation="panel" anchor="right"` so desktop gets the right-side panel and mobile inherits the shared full-screen behavior.

### Milestone 3 — Wire navigation and verify reader behavior

- [x] Step 5 — Close the TOC immediately after page selection
  - Achieve: Ensure clicking or tapping a TOC row navigates to the selected reader page and closes the active surface without leaving stale open state behind.
  - Create: n/a
  - Modify: `src/app/(app)/(lingocafe)/books/_components/BookReaderTableOfContents.tsx`, `src/app/(app)/(lingocafe)/books/_components/BookReaderSurfaces.tsx`
  - Delete: n/a
  - Touch points: `next/link` navigation handlers, open-state callbacks, any helper used to determine current page.
  - Validation: Manual navigation check confirms the route changes and the TOC is no longer open after selection on both mobile and desktop.
  - Notes: Completed by closing the TOC in the row `Link` click handler before route navigation continues.

- [ ] Step 6 — Run QA and targeted reader smoke checks
  - Achieve: Prove the new TOC flow does not break the reader route, reader preferences panel, or the shared modal implementation.
  - Create: n/a
  - Modify: n/a unless fixes are required
  - Delete: n/a
  - Touch points: `npm run qa`, the LingoCafe reader route, TOC trigger/open/close behavior, preferences surface behavior.
  - Validation: `npm run qa` passes, plus manual smoke checks cover desktop TOC open/close, mobile TOC open/close, page navigation from TOC, current-page highlighting, and `Back to book info`.
  - Notes: `npm run qa` passed on 2026-05-07 before the hardening pass, at 2026-05-07T20:26:50+02:00 after the reader-as-modal refactor, and at 2026-05-07T20:43:32+02:00 after the header button order/label adjustment. Browser/device smoke checks are still pending, so this step stays open.

### Milestone 4 — Harden reader modal nesting for iOS/WebKit

- [x] Step 7 — Move the reader route shell under the shared `Modal`
  - Achieve: Stop treating the reader page as a bespoke fixed overlay and render it as an always-open `Modal` panel instead, with mobile full-screen behavior and desktop full-width right-anchored panel behavior.
  - Modify: `src/app/(app)/(lingocafe)/books/[bookId]/[pageId]/page.tsx`, `src/app/(app)/(lingocafe)/books/_components/BookReaderSurfaces.tsx`
  - Notes: Completed after the operator pointed out that the reader being an overlay is the actual architectural smell. The reader surfaces now fill the `Modal` body instead of owning `fixed inset-0`.

- [x] Step 8 — Restore independent child overlay state and support stacked modal levels
  - Achieve: Let reader child surfaces such as preferences and TOC open as nested modal/panel layers instead of forcing one global reader overlay slot.
  - Modify: `src/42go/components/modal/Modal.tsx`, `src/app/(app)/(lingocafe)/books/[bookId]/[pageId]/page.tsx`
  - Notes: Completed by adding modal stack context/z-index levels to the shared `Modal` and rendering the reader child modals inside the reader modal body. Chuck Norris stacks panels. The panels say thank you.

- [ ] Step 9 — Confirm on real iPhone WebKit
  - Achieve: Verify the reader route, preferences panel, TOC panel, nested close behavior, and page navigation on the actual failing mobile Chrome/iPhone path.
  - Validation: Real-device iPhone Chrome check is still required because desktop and simulated mobile were already false comfort.
