# Plan — Replace Books Filter Text With Round Language Flag

## Goal

Replace the `Catalog filtered for ...` subtitle on `/books` with a compact round language flag on the right side of the `Books` header row, keep the flag hidden when no active learning language exists, and route taps on the flag to `/profile` without expanding into the broader `BL01` switcher flow.

## Milestones

### Milestone 1 — Add a reusable books-header language flag action

- [x] Step 1 — Define the active-language flag presentation
  - Achieve: Decide the lightweight rendering approach for the round flag badge so the books page can derive a visible flag from the existing `targetLang` code.
  - Create: `src/app/(app)/(lingocafe)/books/_components/BooksHeaderLanguageFlag.tsx`
  - Modify: `src/config/lingocafe/profile-options.ts` only if a small shared language-to-flag label map belongs there; otherwise keep the mapping local to the new component.
  - Delete: none.
  - Touch points: `targetLang`, LingoCafe language option codes, compact badge styles, `/profile` link target, accessible label text.
  - Validation: Static review confirms the component can render a round flag for supported target languages, exposes a clear tap target to `/profile`, and can return `null` when no active language exists.
  - Notes: Prefer a narrow local component and avoid creating a general-purpose flag system unless execution proves it necessary.

### Milestone 2 — Wire the books page header to use the flag instead of subtitle text

- [x] Step 2 — Feed the books header through the existing right-side toolbar actions slot
  - Achieve: Stop using the `Catalog filtered for ...` subtitle when an active language exists, and inject the new flag action into the right side of the `Books` header row.
  - Create: none, unless a tiny helper is needed next to the new flag component.
  - Modify: `src/app/(app)/(lingocafe)/books/page.tsx`
  - Delete: none.
  - Touch points: `AppLayout` `title`/`subtitle`/`actions` props, `targetLabel`, `showProfileForm`, current header copy branches, `TComponentBlock` action shape for `ToolbarActions`.
  - Validation: Code inspection confirms the page now shows `Books` plus right-aligned flag for active-language state, shows no flag during incomplete-profile state, and no longer renders the old filter sentence in the flagged state.
  - Notes: Prefer reusing the existing `actions` slot instead of changing shared `AppLayout` or `Toolbar` APIs unless execution proves the slot cannot meet the alignment requirement.

### Milestone 3 — Stabilize layout and verify state coverage

- [ ] Step 3 — Tighten mobile header behavior and fallback states
  - Achieve: Ensure the header remains visually stable on mobile and desktop, with the flag vertically centered on the `Books` row and no awkward spacing when the flag is absent.
  - Create: none.
  - Modify: `src/app/(app)/(lingocafe)/books/_components/BooksHeaderLanguageFlag.tsx`; `src/42go/layouts/app/ToolbarActions.tsx` only if a small spacing tweak is strictly needed; `src/42go/layouts/app/Toolbar.tsx` only as a last resort.
  - Delete: none.
  - Touch points: toolbar right-side alignment, gap sizing, compact touch target sizing, visual balance when subtitle is absent, profile-incomplete fallback state.
  - Validation: Manual UI check on mobile-width and desktop-width confirms right-edge alignment, vertical centering, clean spacing, and no overlap with title or back button.
  - Notes: Keep shared layout changes minimal. If the existing action slot works cleanly, avoid editing the shared toolbar. The code path stayed within the existing action slot, but the manual visual pass is still pending because this session does not expose the local browser automation tool.

- [ ] Step 4 — Verify regression boundaries for `/books`
  - Achieve: Prove the change stays scoped to header presentation and does not break onboarding, loading, or catalog rendering behavior.
  - Create: none.
  - Modify: no additional files expected beyond fixes found during verification.
  - Delete: none.
  - Touch points: `/books` loading state, incomplete-profile onboarding branch, active-language loaded state, `/profile` navigation, lint/build gate if practical.
  - Validation: Check these states explicitly: incomplete profile shows no flag and keeps onboarding; complete profile with `targetLang` shows the round flag and no old subtitle; tapping the flag reaches `/profile`; existing books grid and error/loading branches still behave normally. `npm run qa` passed on 2026-05-08, and the remaining manual state checks are pending.
  - Notes: This task should not pull in `BL01`, `GM77`, or profile-saving changes.
