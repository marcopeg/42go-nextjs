# Plan — Expose Listening Options in the Reader Customization Panel

## Goal

Expose the canonical playback preferences inside the reader preferences panel with capability-aware Reading and Listening tabs, prewarm device voice discovery, and apply `Auto-pause on settings` consistently to reader preferences and book navigation.

## Milestones

### Milestone 1 — Prewarm and expose playback capability resolution

- [x] Step 1 — Add reusable device voice-discovery state
  - Achieve: Start device voice enumeration before the preferences panel opens and distinguish pending discovery from resolved playback unavailability.
  - Create: A small shared voice-discovery service only if keeping it inside the device speech provider would mix lifecycle concerns.
  - Modify: `src/app/(app)/(lingocafe)/books/_components/reader-playback/device-speech-provider.ts`, `src/app/(app)/(lingocafe)/books/_components/reader-playback/types.ts`, `src/app/(app)/(lingocafe)/books/_components/reader-playback/useReaderPlayback.ts`.
  - Delete: Nothing.
  - Touch points: `speechSynthesis.getVoices()`, `voiceschanged`, fallback resolution, `playback.canPlay`, and a new capability-pending controller field.
  - Validation: Unsupported devices resolve unavailable immediately; empty asynchronous voice enumeration reports pending and later resolves; direct reader entry still starts discovery.
  - Notes: Implemented module-level prewarming with a 1.5-second fallback resolution; language, matching voice, and sentence checks remain in `useReaderPlayback`.

- [x] Step 2 — Prewarm discovery from the default LingoCafe client entry
  - Achieve: Begin voice discovery while the authenticated books client boots, before a reader preferences interaction is possible.
  - Create: Nothing.
  - Modify: `src/app/(app)/(lingocafe)/books/page.tsx`.
  - Delete: Nothing.
  - Touch points: Initial client effect and the shared voice-discovery service.
  - Validation: The books page starts discovery once without creating speech playback or blocking book loading.
  - Notes: `/books` prewarms on mount; `createDeviceSpeechProvider()` remains the direct-reader fallback.

### Milestone 2 — Share the playback-preferences editor

- [x] Step 3 — Extract one canonical playback-preferences editor
  - Achieve: Render speed and all six playback toggles from one shared editor used by both the playback-bar popover and reader preferences.
  - Create: `src/app/(app)/(lingocafe)/books/_components/BookReaderPlaybackPreferencesEditor.tsx`.
  - Modify: `src/app/(app)/(lingocafe)/books/_components/BookReaderPlaybackSettings.tsx`.
  - Delete: Duplicated option markup from the popover shell.
  - Touch points: `READER_PLAYBACK_SPEEDS`, canonical labels, switches, playback setters, compact and panel spacing.
  - Validation: The playback popover retains its current controls and behavior; the panel variant uses the same preference state and setters.
  - Notes: Added `BookReaderPlaybackPreferencesEditor` with compact and panel presentation variants over the same controller.

### Milestone 3 — Add capability-aware Reading and Listening tabs

- [x] Step 4 — Add accessible panel navigation and stable capability behavior
  - Achieve: Open preferences on Reading, show a loader while capability is pending, show tabs only when full `playback.canPlay` resolves true, and keep an exposed Listening tab stable until close.
  - Create: Nothing beyond small local presentation helpers.
  - Modify: `src/app/(app)/(lingocafe)/books/_components/BookReaderPreferencesPanel.tsx`, `src/app/(app)/(lingocafe)/books/[bookId]/[pageId]/page.tsx`.
  - Delete: Nothing.
  - Touch points: Modal props, tablist/tab/tabpanel semantics, arrow/Home/End keyboard control, Reading preview placement, shared Listening editor, capability snapshot lifecycle.
  - Validation: Every opening selects Reading; pending shows a contained loader; resolved false renders the existing reading-only panel; resolved true exposes both tabs; later capability loss does not eject the open Listening view.
  - Notes: Kept the shared Modal; tabs implement arrow, Home, and End keyboard navigation and reset to Reading on each open.

### Milestone 4 — Generalize settings auto-pause across reader panels

- [x] Step 5 — Track named settings surfaces in the playback controller
  - Achieve: Let playback settings, reader preferences, and table-of-contents/book navigation share one settings auto-pause reason without one surface prematurely clearing another.
  - Create: Nothing.
  - Modify: `src/app/(app)/(lingocafe)/books/_components/reader-playback/types.ts`, `src/app/(app)/(lingocafe)/books/_components/reader-playback/useReaderPlayback.ts`, `src/app/(app)/(lingocafe)/books/[bookId]/[pageId]/page.tsx`.
  - Delete: Boolean-only internal settings-open tracking where superseded.
  - Touch points: Named settings surfaces, pause/resume ownership, live changes to `Auto-pause on settings`, panel open/close handlers, playback close cleanup.
  - Validation: Preferences and contents pause/resume only when enabled; tab switching does not create another pause cycle; translation remains controlled independently.
  - Notes: Playback, preferences, and contents now share one named-surface set behind the existing settings pause reason.

### Milestone 5 — Verify and document

- [x] Step 6 — Run static and targeted quality gates
  - Achieve: Catch lint, type, production-build, formatting, and obvious capability-state regressions.
  - Create: Focused tests only if the existing test harness supports the extracted pure behavior without adding a new framework.
  - Modify: Test or package files only if a narrow existing pattern applies.
  - Delete: Nothing.
  - Touch points: `git diff --check`, focused tests if added, `npm run qa`.
  - Validation: All applicable focused checks and `npm run qa` pass.
  - Notes: Targeted ESLint, scoped `git diff --check`, and the full `npm run qa` gate passed. No new testing framework was added.

- [x] Step 7 — Perform LingoCafe visual and interaction checks
  - Achieve: Verify the panel at mobile and desktop widths in capable, pending/unavailable where practical, and active-playback states.
  - Create: Nothing.
  - Modify: Execution notes with the observed result.
  - Delete: Nothing.
  - Touch points: `https://lc42go.ngrok.app/`, `john` / `john`, `/books`, a reader page, light/dark mode, keyboard focus, reader preferences, contents, and playback popover.
  - Validation: Tabs, loader/fallback, shared values, persistence, keyboard navigation, modal scrolling, and settings auto-pause behave as specified.
  - Notes: The operator reviewed the implementation on a live mobile reader, supplied screenshots, and directed the navigation-versus-setting corrections. Browser automation could not authenticate for the remaining capability-state matrix, and the operator explicitly accepted completion after the final shared-divider correction.
