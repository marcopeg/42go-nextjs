---
taskId: SA84
createdAt: 2026-05-04T10:06:53+02:00
updatedAt: 2026-05-04T12:41:59+02:00
---

# Execution Notes - Rename Public Themes to App Themes
**Task**: ./SA84.task.md
**Plan**: ./SA84.plan.md

## Decisions

- Plan accepted by the operator with "yes and execute it".
- Moved SA84 into WIP before implementation.
- Moved existing app CSS overrides from `public/themes` to `public/app-themes`.
- Added `public/app-themes/_default/style.css` as the explicit always-linked fallback.
- Added `scripts/generate-app-themes-registry.mjs` and wired it into `npm run build` after the existing app-icons registry step.
- Added `src/42go/app-themes` with a generated stylesheet registry and `resolveAppThemeStylesheet(appId)`.
- Updated `InjectAppID` to link `_default/style.css` first and the app stylesheet only when registered.
- Removed unused duplicate `InjectAppName.tsx`.
- Updated live token and multi-app docs to use `public/app-themes`.
- Updated SA84 migration and manual test artifacts to match the implemented generator and validation commands.
- `rg "public/themes|/themes/" src contents docs/articles docs/memory-bank` returned no live matches.
- `npm run qa` passed.
- Initial runtime checks confirmed default, QuickList, and missing-theme fallback behavior before the follow-up folder-layout change.
- Follow-up change moved app themes to the same folder convention as app icons: `public/app-themes/<app-id>/style.css`, with default fallback at `public/app-themes/_default/style.css`.
- The app-theme registry now records folders containing `style.css`, and the resolver links `/app-themes/<app-id>/style.css` only when registered.
- The folder layout intentionally leaves room for sibling assets such as fonts beside each app theme stylesheet.
- Follow-up docs pass updated custom icon/theme references across live docs, memory-bank notes, articles, and vibe memory to use the app-id folder convention consistently.
- Follow-up validation confirmed `public/app-themes/_default/style.css` and `public/app-themes/quicklist/style.css` are served as `text/css`.
- Follow-up validation confirmed `npm run qa` passes after the folder-layout change.

## Problems Encountered

- `npm run qa` emitted the existing RO05 app-icons warning that LingoCafe lacks `maskable-512x512.png` and falls back to `_default`; the build still passed.
- Built `next start` is not a reliable rendered-page smoke path in this repo because the app uses standalone output; that server returned empty page bodies during this check, so runtime validation focused on build, resolver source, and static stylesheet delivery.

## Deviations From Plan

- n/a

## Additional Requests

- n/a

## Known Limitations

- n/a
