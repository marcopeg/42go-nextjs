# Migration - SA84 Rename Public Themes to App Themes

## Goal

Move app-specific CSS token overrides from `public/themes` to `public/app-themes` and make stylesheet injection resolver-driven.

## Legacy Layout

```text
public/themes/<app-id>.css
```

Legacy runtime link:

```html
<link rel="stylesheet" href="/themes/<app-id>.css">
```

## New Layout

```text
public/app-themes/_default/style.css
public/app-themes/<app-id>/style.css
```

New runtime links:

```html
<link rel="stylesheet" href="/app-themes/_default/style.css">
<link rel="stylesheet" href="/app-themes/<app-id>/style.css">
```

The app-specific link is emitted only when the app-specific file exists in the generated registry.

## Steps

1. Create `public/app-themes/_default/style.css`.
2. Move every `public/themes/*.css` file into `public/app-themes/<app-id>/style.css`.
3. Delete the legacy `public/themes` folder once all files are moved.
4. Add a shared resolver such as `resolveAppThemeStylesheet(appId)`.
5. Add `scripts/generate-app-themes-registry.mjs` and a build-time registry of existing files under `public/app-themes`.
6. Update `src/42go/config/InjectAppID.tsx` to use the resolver output.
7. Audit `src/42go/config/InjectAppName.tsx`; remove it if obsolete.
8. Replace all live `public/themes` and `/themes/` documentation references with `public/app-themes` and `/app-themes/`.
9. Run `rg "public/themes|/themes/" src contents docs/articles docs/memory-bank` and confirm no live references remain.
10. Run `npm run qa`.

## App File Mapping

```text
public/themes/app1.css       -> public/app-themes/app1/style.css
public/themes/app2.css       -> public/app-themes/app2/style.css
public/themes/calendar.css   -> public/app-themes/calendar/style.css
public/themes/quicklist.css  -> public/app-themes/quicklist/style.css
public/themes/lingocafe.css  -> public/app-themes/lingocafe/style.css
```

## No Compatibility Layer

Do not keep a `/themes` route, symlink, rewrite, or fallback. This project is early. The old path should die cleanly.
