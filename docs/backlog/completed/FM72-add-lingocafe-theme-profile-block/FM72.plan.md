---
taskId: FM72
createdAt: 2026-05-06T09:50:55+02:00
updatedAt: 2026-05-06T13:15:13+02:00
---

# Plan — Add Core Theme Profile Block and Enable It in LingoCafe

## Goal

Add a reusable core profile block for theme preference that stays local-storage-based, participates in the shared profile save lifecycle and dirty UX, and is enabled in LingoCafe after the language preferences block.

## Milestones

### Milestone 1 — Extend the shared profile block lifecycle for local-only save state

- [x] Step 1 — Add block-level dirty state and save lifecycle hooks
  - Achieve:
    Extend the shared profile block runtime so a core block can report local-only dirty state, participate in the same save flow as database-backed profile blocks, and clear or preserve its dirty state based on save outcome.
  - Create:
    None.
  - Modify:
    `src/42go/components/ProfileBlock/types.ts`
    `src/42go/components/ProfileBlock/ProfileBlockRuntime.tsx`
    `src/42go/components/ProfileBlock/ProfilePageRenderer.tsx`
  - Delete:
    None.
  - Touch points:
    `TProfileBlockHandle`, runtime registration, aggregate dirty-state calculation, `onDirtyChange`, and save success/error callbacks.
  - Validation:
    TypeScript compiles with the new handle shape, and the renderer can distinguish local-only dirty state from persisted profile dirtiness without breaking existing blocks.
  - Notes:
    Implemented by extending the runtime handle contract with block-level dirty state and aggregating it in `ProfilePageRenderer`.

- [x] Step 2 — Keep unsaved-change UX aligned with the new aggregate dirty state
  - Achieve:
    Ensure the profile page header button label and leave-warning behavior still reflect the full dirty picture once local-only block state is introduced.
  - Create:
    None.
  - Modify:
    `src/42go/profile/client.tsx`
    `src/app/(app)/profile/page.tsx`
    `src/42go/components/ProfileBlock/ProfilePageRenderer.tsx`
  - Delete:
    None.
  - Touch points:
    `useProfileController`, `onDirtyChange`, the current `beforeunload` and internal navigation guard behavior, and the `SavePreferencesAction` dirty label.
  - Validation:
    A local-only block change makes the page show unsaved state before save and stops doing so after a successful save or explicit reset.
  - Notes:
    The existing guard in `useProfileController` was reused by feeding it `externalDirty`; `src/app/(app)/profile/page.tsx` did not need changes.

### Milestone 2 — Implement the reusable core theme block

- [x] Step 3 — Add a core `ThemePreference` profile block
  - Achieve:
    Create a shared profile block that exposes `light`, `dark`, and `system`, reads the current theme safely from the existing theme system, tracks local edits, and applies the selected theme only after save success.
  - Create:
    `src/42go/components/ProfileBlock/blocks/ThemePreference.tsx`
  - Modify:
    `src/42go/components/ProfileBlock/ProfileBlock.tsx`
    `src/42go/components/ProfileBlock/types.ts`
    `src/42go/config/ThemeProvider.tsx`
  - Delete:
    None.
  - Touch points:
    New block discriminant/type, shared block renderer dispatch, `useTheme()` integration, and any small helper needed to normalize `light | dark | system`.
  - Validation:
    The block renders without LingoCafe-specific wiring, shows the current selection, and does not call `setTheme()` until the shared save action succeeds.
  - Notes:
    The block uses the existing `useTheme()` API and a draft-plus-committed-override model to avoid effect-driven state syncing.
    Follow-up UI pass replaced the native select with a segmented tab-style control using Auto, Light, and Dark options with icons.

- [x] Step 4 — Ensure the core block stays out of database-backed profile validation
  - Achieve:
    Keep the theme preference outside the AJV-backed profile schema and `/api/profile` persistence payload while still letting the block use the shared save choreography.
  - Create:
    None.
  - Modify:
    `src/42go/components/ProfileBlock/ProfilePageRenderer.tsx`
    `src/42go/profile/client.tsx`
    Any related shared typing or save payload shaping file discovered during implementation.
  - Delete:
    None.
  - Touch points:
    Profile save payload composition, validation entry points, and any logic that currently assumes dirty state always maps to persisted profile keys.
  - Validation:
    Saving a theme-only change does not extend `auth.users.profile` and does not require schema updates to pass validation.
  - Notes:
    No payload-shaping change was required because the theme block never enters the shared profile store; `/api/profile` remains only the save checkpoint.

### Milestone 3 — Enable the block in LingoCafe

- [x] Step 5 — Wire the new core block into LingoCafe profile configuration
  - Achieve:
    Add the shared theme block to `app.profile.items` immediately after `LingocafePreferences`, without changing the LingoCafe profile schema for persisted user data.
  - Create:
    None.
  - Modify:
    `src/config/lingocafe/config.ts`
    `src/config/lingocafe/profile-options.ts` only if execution discovers cleanup or comments are needed, not for a new schema field.
  - Delete:
    None.
  - Touch points:
    `app.profile.items` ordering, profile block config typing, and any LingoCafe copy passed into the block.
  - Validation:
    LingoCafe `/profile` renders Language Preferences first and Theme Preferences second, both under the shared save action.
  - Notes:
    Stop at LingoCafe for this task. Other apps can adopt the block later.

### Milestone 4 — Verify behavior and guard against regressions

- [ ] Step 6 — Exercise the user flows that define success
  - Achieve:
    Verify the exact behavior the task promises: dirty UX before save, theme application after save, persistence across reload, and no accidental database-backed profile writes.
  - Create:
    None.
  - Modify:
    Automated tests only if the existing test surface makes that practical in-scope; otherwise document manual verification through the execution notes later.
  - Delete:
    None.
  - Touch points:
    LingoCafe `/profile`, the browser theme state, network payload inspection for `/api/profile`, and any profile-save feedback UI.
  - Validation:
    Confirm:
    1. selecting a new theme marks the page dirty;
    2. leaving the page before save triggers the normal warning;
    3. clicking save applies the selected theme;
    4. reloading keeps the theme;
    5. the profile payload does not gain a persisted theme field.
  - Notes:
    This task has a strong UX contract. Manual verification is not optional.

- [x] Step 7 — Run the repository QA gate
  - Achieve:
    Run the standard project quality gate after implementation to catch type, lint, or build regressions introduced by the new shared block and dirty-state changes.
  - Create:
    None.
  - Modify:
    None.
  - Delete:
    None.
  - Touch points:
    `npm run qa`
  - Validation:
    `npm run qa` completed successfully on 2026-05-06 after the implementation changes.
  - Notes:
    This repo already says to run `npm run qa` after code changes. Obey it like gravity.
