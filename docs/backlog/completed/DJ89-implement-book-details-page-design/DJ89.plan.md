---
taskId: DJ89
createdAt: 2026-04-28T11:51:23+02:00
updatedAt: 2026-04-28T13:39:37+02:00
---

# Plan — Implement Book Details Page Design

## Goal

Update the LingoCafe `/books/[bookId]` detail page to match the attached desktop and mobile design while preserving the existing route, mobile full-screen container, cover fallback, reading action, `book-info` tracking, and authenticated client-side data flow.

## Milestones

### Milestone 1 — Confirm Detail Boundaries and Reuse Shelf Decisions

- [x] Step 1 — Inspect current detail page ownership
  - Achieve: Confirm the exact files that own the detail route, detail content, shared cover/tags components, client payload normalization, and detail API response.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `src/app/(app)/(lingocafe)/books/[bookId]/page.tsx`, `src/app/(app)/(lingocafe)/books/_components/BookInfoContent.tsx`, `BookCover.tsx`, `BookTags.tsx`, `book-types.ts`, `src/app/api/(lingocafe)/lingocafe/_lib/reader.ts`, `src/app/api/(lingocafe)/lingocafe/books/[bookId]/route.ts`.
  - Validation: Verify the current page already loads cover, title, author, tags, description, reading action, and error/loading states from `/api/lingocafe/books/[bookId]`.
  - Notes: Confirmed the current detail route owns mobile shell/loading/error behavior, `BookInfoContent` owns the detail presentation, shared `BookCover`/`BookTags` already exist, and the detail API is implemented through `loadBookInfo`. Use `NN45` as precedent for cover-first visual design, isolated placeholder actions, basic English labels, and not recreating cover artwork with HTML overlays.

- [x] Step 2 — Confirm table-of-contents data source
  - Achieve: Identify the smallest real `books_pages` query needed to render the design's "Contents" rows.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `lingocafe.books_pages`, `BookPageRow`, `BookPageDetailRow`, `buildReadPageHref`, `loadBookInfo`, `ReaderBookInfo`.
  - Validation: Confirm existing schema provides `book_id`, `id`, `position`, `kind`, `prefix`, `title`, `summary`, and `content`; no migration is needed.
  - Notes: Confirmed `books_pages` has the ordered fields needed for Contents rows and no migration is needed. Page count per row is not a real column, so implementation should avoid fake per-row page counts unless a simple display label is derived from available rows.

### Milestone 2 — Extend Detail API Payload for Contents

- [x] Step 1 — Add page summary types and mapper
  - Achieve: Add a compact detail-page page summary shape that the client can render as the "Contents" list without exposing full page content.
  - Create: n/a
  - Modify: `src/app/api/(lingocafe)/lingocafe/_lib/reader.ts`, `src/app/(app)/(lingocafe)/books/_components/book-types.ts`.
  - Delete: n/a
  - Touch points: New server type such as `BookInfoPageRow` or `BookInfoPageSummary`, new client type such as `ReaderBookInfoPage`, `buildReadPageHref`.
  - Validation: TypeScript represents page `bookId`, `pageId`, `position`, `prefix`, `title`, `href`, and optional display metadata without weakening existing book list types.
  - Notes: Added `BookInfoPageRow`/`BookInfoPageSummary` server types and `ReaderBookInfoPage` client type. Kept this scoped to the existing detail endpoint.

- [x] Step 2 — Load contents in `loadBookInfo`
  - Achieve: Query `lingocafe.books_pages` for the selected book, order by `position`, map rows into clickable read-page summaries, and include them in the detail response.
  - Create: n/a
  - Modify: `src/app/api/(lingocafe)/lingocafe/_lib/reader.ts`.
  - Delete: n/a
  - Touch points: `loadBookInfo`, `mapBookInfo`, `books_pages`, `buildReadPageHref`, existing first-page reading action query.
  - Validation: Unknown books still return `null`; valid books include an ordered `pages` or `contents` array; books with no pages still return an empty array and keep the existing unavailable reading action behavior.
  - Notes: `loadBookInfo` now loads ordered `books_pages` rows with id, position, kind, prefix, and title, then maps them to read-page hrefs. It does not fetch full content or invent per-row page counts.

- [x] Step 3 — Normalize new payload safely on the client
  - Achieve: Extend client normalization so malformed or missing contents data falls back to an empty array instead of crashing.
  - Create: n/a
  - Modify: `src/app/(app)/(lingocafe)/books/[bookId]/page.tsx`, `src/app/(app)/(lingocafe)/books/_components/book-types.ts`.
  - Delete: n/a
  - Touch points: `normalizeBookInfo`, `ReaderBookInfo`, page-summary type guards or lightweight shape checks.
  - Validation: Existing loading/error behavior remains unchanged; detail pages with missing `pages` data still render without the Contents rows.
  - Notes: Added a lightweight client shape guard so missing or malformed `pages` data falls back to an empty array. Browser-side fetching remains unchanged.

### Milestone 3 — Rebuild Detail Content Presentation

- [x] Step 1 — Restructure `BookInfoContent` into design sections
  - Achieve: Rework the detail content into a cover-first/mobile and cover-left/desktop layout matching the design's hierarchy.
  - Create: Optional route-local helper components inside `_components`, such as `BookInfoMeta`, `BookInfoActions`, `BookInfoAbout`, and `BookInfoContents`, if the single file becomes dense.
  - Modify: `src/app/(app)/(lingocafe)/books/_components/BookInfoContent.tsx`.
  - Delete: n/a
  - Touch points: `BookCover`, `BookTags`, `ReaderBookInfo`, Tailwind responsive classes.
  - Validation: Desktop shows cover and metadata side by side; mobile shows cover first; title, author, tags, description, and reading action remain visible and stable.
  - Notes: Rebuilt `BookInfoContent` as a client component with desktop breadcrumb, cover-left hero, mobile cover-first layout, metadata section, About section, and Contents section. Reused `NN45` cover direction: cover image remains as-is with no HTML artwork overlays.

- [x] Step 2 — Add compact metadata and primary action treatment
  - Achieve: Render the language/level pill, title/author text, adaptation/source copy using available fields, compact metadata rows for level/language/page count, and the designed black primary reading button.
  - Create: n/a
  - Modify: `BookInfoContent.tsx`, possibly small local helpers in the same component folder.
  - Delete: n/a
  - Touch points: `BookReadingAction`, `ReaderBookReadingAction`, `book.lang`, `book.level`, `book.pages`, basic English labels.
  - Validation: "Read now", "Continue reading", and "No pages available" states continue to render correctly; the primary action link still points to the current read route.
  - Notes: Added language/level badge, compact level/language/page-count metadata, basic English labels, and a prominent primary reading button. Did not implement app-level i18n.

- [x] Step 3 — Add removable secondary visual actions
  - Achieve: Add bookmark, favorite, and more visual controls in an isolated way, matching the detail design while remaining easy to comment out or remove.
  - Create: Possibly local helpers such as `BookInfoSecondaryActions`.
  - Modify: `BookInfoContent.tsx`.
  - Delete: n/a
  - Touch points: Lucide icons such as `Bookmark`, `Heart`, `MoreHorizontal`, placeholder helper components.
  - Validation: Placeholder controls do not trigger persistence, API calls, menus, popovers, or nested link conflicts; `npm run qa` has no unused import/helper warnings after final placeholder state is chosen.
  - Notes: Added bookmark, favorite, and more placeholders as isolated visual-only spans. They do not call APIs, open menus, or persist anything, and are contained in one helper for easy removal.

- [x] Step 4 — Add About and Contents sections
  - Achieve: Render "About this book" and "Contents" below the hero metadata, with desktop full sections and mobile initially compact sections.
  - Create: Optional `BookInfoContents` helper component.
  - Modify: `BookInfoContent.tsx`.
  - Delete: n/a
  - Touch points: `book.description`, `book.pages` or `book.contents`, `Link`, chevron icons, basic English labels, route hrefs from API.
  - Validation: Contents rows use real book-page data where available, link to `/books/[bookId]/[pageId]`, and render an empty/non-crashing state when no rows exist.
  - Notes: Added "About this book" with mobile read-more behavior and "Contents" with real linked page rows plus simple show-all toggling.

### Milestone 4 — Preserve Mobile Container and Page States

- [x] Step 1 — Keep mobile full-screen container behavior
  - Achieve: Preserve the existing mobile fixed full-screen detail shell and only adjust the content and top-bar copy/layout as needed for the design.
  - Create: n/a
  - Modify: `src/app/(app)/(lingocafe)/books/[bookId]/page.tsx`.
  - Delete: n/a
  - Touch points: `MobileBookInfo`, `z-[500]`, `hideMobileMenu`, back link to `/books`, `BookInfoContent`.
  - Validation: Mobile detail still covers the app UI, scrolls vertically, back returns to `/books`, and no horizontal overflow is introduced.
  - Notes: Preserved the existing fixed `MobileBookInfo` shell, `z-[500]`, `hideMobileMenu`, scroll container, and back link. Only the top-bar label and inner content changed.

- [x] Step 2 — Preserve loading, 404, and error states
  - Achieve: Keep current clear non-crashing states, changing only minimal styling needed so they still fit the redesigned page.
  - Create: n/a
  - Modify: `src/app/(app)/(lingocafe)/books/[bookId]/page.tsx`.
  - Delete: n/a
  - Touch points: `loading`, `error`, `normalizeBookInfo`, fetch error handling.
  - Validation: 404 still shows "Book not found."; other failures show "Could not load book."; loading state remains visible on desktop and mobile.
  - Notes: Existing loading, 404, and generic error handling are preserved with minimal shell-copy adjustment.

### Milestone 5 — Validate Responsive Behavior and Quality

- [x] Step 1 — Run static quality gate
  - Achieve: Verify linting and build pass after API and UI changes.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `npm run qa`.
  - Validation: `npm run qa` passes.
  - Notes: `npm run qa` passed after fixing the Lucide placeholder icon type. The build still emits the existing Turbopack NFT warning and repeated `next/headers` fallback messages.

- [x] Step 2 — Smoke-check detail route behavior
  - Achieve: Verify the detail page still loads, the API still protects unauthenticated access, and read links still resolve.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `/books/[bookId]`, `/api/lingocafe/books/[bookId]`, `/books/[bookId]/[pageId]`, existing seeded books.
  - Validation: Valid detail route renders; unknown book shows the preserved error state; contents rows navigate to read pages; opening detail still triggers `book-info` through the existing API route.
  - Notes: Local `HEAD /books/gosta-berling-sv-b2` and `HEAD /books/gosta-berling-sv-b2/p01` returned 200 with `Host: lingocafe.localhost:3000`; unauthenticated `GET /api/lingocafe/books/gosta-berling-sv-b2` returned 401 with the same host.

- [x] Step 3 — Inspect desktop and mobile layout
  - Achieve: Compare the implemented detail page against `book-card-design.png` at desktop and mobile widths.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: Desktop `/books/[bookId]`, mobile `/books/[bookId]`, cover dimensions, metadata wrapping, secondary action placeholders, Contents rows.
  - Validation: No horizontal scroll; cover, title, tags, metadata, actions, about text, and contents rows do not overlap; mobile content remains inside the existing full-screen container.
  - Notes: Operator reviewed desktop and mobile screenshots during execution and requested follow-up alignment fixes. Those fixes were applied. The in-app browser tool was not exposed in this session's available tool surface, so no agent-driven browser screenshot review was performed.
