# Execution Notes — Core User Profile and Consent Storage
**Task**: ./ZO85.task.md
**Plan**: ./ZO85.plan.md

## Decisions

- 2026-05-05T15:46:18+02:00 — Plan accepted with execute request. Task moved to WIP.
- 2026-05-05T17:04:07+02:00 — Implemented core `auth.users.profile` and `auth.users.consent` storage, `/api/profile`, AJV validation, consent evidence history, ProfilePage central store, core `Consent`/`ProfileConsent`, editable account info, LingoCafe migration, docs, skills, and `migrate.sql`.
- 2026-05-05T17:04:07+02:00 — LingoCafe now stores profile keys as `ownLang`, `targetLang`, and `targetLevel` in `auth.users.profile`; required and optional consent evidence goes to `auth.users.consent.{name}[]`.
- 2026-05-05T17:04:07+02:00 — Default, Quicklist, and LingoCafe now explicitly configure profile blocks and consent items. No app receives invented default profile blocks.
- 2026-05-06T09:19:46+02:00 — Simplified LingoCafe consent configuration after operator review: removed the temporary `/terms` and `/privacy` page registrations, changed LingoCafe consent labels to plain text, inlined consent text/version/method values in `src/config/lingocafe/config.ts`, and deleted the old `profile-consent` helper.
- 2026-05-06T09:19:46+02:00 — Simplified the core Logout profile block to a single full-width red outlined button with no panel wrapper or explanatory copy.

## Problems Encountered

- `ps` was blocked by the sandbox while checking for the dev server. `curl` confirmed the app is already reachable at `http://localhost:3000`.

## Deviations From Plan

- Focused helper behavior was validated through `npm run qa`, `make migrate.rebuild`, `make seed`, SQL verification, and unauthenticated API smoke checks. No standalone unit test runner exists in this repo, so no separate unit test files were added.
- Follow-up simplifications were validated with `npm run qa` on 2026-05-06.

## Additional Requests

- Operator confirmed LingoCafe config/profile keys should be `ownLang`, `targetLang`, and `targetLevel` before execution.
- Operator requested that LingoCafe legal pages move to the future public website; current consent items must use simple text labels only.
- Operator requested LingoCafe consent data be defined inline in `config.ts` instead of imported from `profile-consent`.
- Operator requested the Logout block be simplified to a full-width red outlined button.

## Known Limitations

- Browser interaction smoke for authenticated `/profile` and `/books` is left for the operator. The app server is already running at `http://localhost:3000`.
- The manual production patch intentionally does not copy legacy `lingocafe.profiles` data, matching the refinement decision.
