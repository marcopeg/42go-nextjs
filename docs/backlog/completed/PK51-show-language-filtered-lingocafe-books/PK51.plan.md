---
taskId: PK51
createdAt: 2026-04-27T14:56:07+02:00
updatedAt: 2026-04-27T15:06:23+02:00
---

# Plan — Show Language-Filtered LingoCafe Books

## Goal

Replace the fake LingoCafe `/books` list with a real authenticated reader flow that loads the user's profile and available books in one request, filters the catalog by `profile.target_lang` when present, and shows onboarding when the profile is missing.

## Milestones

### Milestone 1 — Configure LingoCafe Reader Options

- [x] Step 1 — Add typed LingoCafe language configuration
  - Achieve: Add a typed place for app-specific LingoCafe reader options so onboarding language lists live in configuration instead of page-local arrays.
  - Create: n/a
  - Modify: `src/AppConfig.ts`, `src/config/lingocafe/config.ts`
  - Delete: n/a
  - Touch points: `TAppConfigItem`, LingoCafe config object, `own_lang` option list, `target_lang` option list
  - Validation: TypeScript accepts the new config shape; LingoCafe config exposes a broad major-European-language `own_lang` list and a target list limited to English, Italian, Spanish, German, and Swedish.
  - Notes: Superseded during execution. The operator asked to avoid app-specific AppConfig type pollution and keep these static lists in `src/app/api/books/route.ts` for now.

- [x] Step 2 — Enable the LingoCafe books policy surface
  - Achieve: Ensure the page and API can be protected through the existing policy system without returning missing-feature 404s.
  - Create: n/a
  - Modify: `src/config/lingocafe/config.ts`
  - Delete: n/a
  - Touch points: `features`, `page:books`, `api:books`
  - Validation: Policy evaluation can allow authenticated access to `/books` and `/api/books` for the LingoCafe app.
  - Notes: Keep the route authenticated; do not create a public reader catalog.

### Milestone 2 — Build the Books/Profile API

- [x] Step 1 — Add authenticated `GET /api/books`
  - Achieve: Return the current user's LingoCafe profile, the effective language options, and the books list in one API response.
  - Create: `src/app/api/books/route.ts`
  - Modify: n/a
  - Delete: n/a
  - Touch points: `protectRoute`, `getServerSession`, `getAuthOptions`, `getDB`, `getAppConfig`, `lingocafe.profiles`, `lingocafe.books`
  - Validation: Unauthenticated requests return 401; authenticated requests return `{ profile, books, languages }`; books are filtered by `profile.target_lang` when it exists and otherwise return the available catalog.
  - Notes: Do not accept a user ID from the request. Derive the user from the session every time.

- [x] Step 2 — Add onboarding profile persistence
  - Achieve: Let the onboarding form create or update the current user's LingoCafe profile using configured language codes.
  - Create: n/a
  - Modify: `src/app/api/books/route.ts`
  - Delete: n/a
  - Touch points: `POST /api/books`, request body validation, language-code allowlists from LingoCafe config, `lingocafe.profiles`
  - Validation: Valid onboarding payloads persist `own_lang` and `target_lang` for the session user; invalid codes or malformed payloads return 400; successful saves return the updated profile or refreshed `{ profile, books, languages }` shape.
  - Notes: This is only onboarding save. It is not the future manual language switch.

- [x] Step 3 — Normalize API response data
  - Achieve: Return stable, client-friendly objects with dates serialized and optional fields handled consistently.
  - Create: n/a
  - Modify: `src/app/api/books/route.ts`
  - Delete: n/a
  - Touch points: book row mapping, `published_at`, `created_at`, `updated_at`, `cover`, `tags`, `profile: null`
  - Validation: The client can render seeded English and Swedish books without defensive crashes, including the no-profile John path and the Swedish-profile Jane path.
  - Notes: Keep the response shape ready for a later language switch by including profile and configured language options.

### Milestone 3 — Replace the `/books` Page UI

- [x] Step 1 — Fetch real reader data from the browser
  - Achieve: Replace the hardcoded book list with browser-side fetch state for `/api/books`.
  - Create: n/a
  - Modify: `src/app/(app)/books/page.tsx`
  - Delete: n/a
  - Touch points: `useEffect`, `useState`, `fetch("/api/books", { credentials: "same-origin" })`, loading state, error state
  - Validation: The page follows the `(app)` route convention and no longer renders the fake static list.
  - Notes: Keep `"use client"` and `AppLayout`.

- [x] Step 2 — Render onboarding when profile is missing
  - Achieve: Show an onboarding form asking for `own_lang` and `target_lang` when the API returns `profile: null`.
  - Create: n/a
  - Modify: `src/app/(app)/books/page.tsx`
  - Delete: n/a
  - Touch points: form state, configured language options from API, submit handler, `POST /api/books`, validation messages
  - Validation: John, who is seeded without a LingoCafe profile, sees the onboarding form and can submit configured languages.
  - Notes: Use normal form controls or existing UI primitives already present in the repo; do not hardcode the option arrays in the component.

- [x] Step 3 — Render filtered books for profiled users
  - Achieve: Show the returned books list with concise book metadata and profile-aware context.
  - Create: n/a
  - Modify: `src/app/(app)/books/page.tsx`
  - Delete: n/a
  - Touch points: book cards/list items, `title`, `author`, `level`, `lang`, `description`, `tags`, empty state
  - Validation: Jane, seeded as English-to-Swedish, sees only Swedish books; users without `target_lang` still see available books.
  - Notes: Do not add a manual language switch in this task. Chuck Norris filters by intent, not by committee.

### Milestone 4 — Verification and Backlog State

- [ ] Step 1 — Run focused implementation checks
  - Achieve: Verify the API and page behavior against the seeded LingoCafe data paths.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: local migrated/seeded database, John no-profile path, Jane Swedish-profile path, authenticated API calls
  - Validation: API responses match the intended profile/catalog behavior and the page renders without runtime errors.
  - Notes: If a full browser smoke check is not practical, record that limitation during execution.

- [x] Step 2 — Run repository QA
  - Achieve: Catch lint, type, and build regressions before completing the task.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `npm run qa`
  - Validation: `npm run qa` passes, or any failure is documented with the failing command and reason.
  - Notes: Do not run `npm dev`.

## Execution Notes

- Implemented the API/page/config path and validated it through `npm run qa`.
- Operator changed direction on language list storage: removed the app-specific AppConfig extension and moved static language lists into the books API.
- Did not run an authenticated browser/database smoke test for John/Jane during this session; that remains the only open verification step.
- `npm run qa` passed again on 2026-04-27T15:06:23+02:00 after moving language lists into the API. The pre-existing Turbopack NFT warning from `next.config.ts` remains.
