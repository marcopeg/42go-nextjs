# Plan — Docker Image Security Review

## Goal

Audit the production Docker build, image artifact, and Compose runtime posture, then publish `TK89.output.md` with evidence, per-area verdicts, release-gate status, and ordered actionable hardening steps.

## Milestones

### Milestone 1 — Establish The Audit Baseline

- [x] Step 1 — Confirm task scope and source inputs
  - Achieve: Lock the review to the current `Dockerfile`, `.dockerignore`, `docker-compose.prod.yml`, `scripts/check-docker-security.sh`, `Makefile` Docker targets, and the resulting `42go-next:latest` image.
  - Create: `docs/backlog/wip/TK89-docker-image-security-review/TK89.output.md` during execution.
  - Modify: `TK89.output.md` only.
  - Delete: n/a
  - Touch points: `Dockerfile`, `.dockerignore`, `docker-compose.prod.yml`, `scripts/check-docker-security.sh`, `Makefile`, `docs/backlog/wip/TK89-docker-image-security-review/TK89.output.md`.
  - Validation: Output file records the exact files, image tag, local platform, Docker/buildx context, and Trivy version used for the audit.
  - Notes: Do not remediate Dockerfile or Compose issues in this task unless the task is explicitly expanded. Chuck Norris reviews first, then kicks the door in later.

- [x] Step 2 — Recheck official guidance used as the review standard
  - Achieve: Compare the repository against current official Next.js standalone output guidance, Docker Next.js containerization guidance, Docker build best practices, and Trivy usage docs already linked in the task.
  - Create: n/a
  - Modify: `TK89.output.md`.
  - Delete: n/a
  - Touch points: Task research links, `next.config.ts`, `Dockerfile`, `.dockerignore`.
  - Validation: `TK89.output.md` names the guidance consulted and states the resulting allowed-content policy.
  - Notes: The accepted content policy allows traced `.next/standalone`, `.next/static`, required `public/` assets, production runtime dependencies, `package.json`, app icons/themes, and minimal runtime support only.

- [x] Step 3 — Define the review table and release gate
  - Achieve: Create the output structure before collecting evidence so every checklist area gets a verdict: pass, issues found, or blocked.
  - Create: `TK89.output.md`.
  - Modify: `TK89.output.md`.
  - Delete: n/a
  - Touch points: Twelve checklist areas from `TK89.task.md`, accepted vulnerability gate, accepted content policy.
  - Validation: Output file contains sections for evidence, per-area verdicts, release-gate decision, and ordered actionable steps.
  - Notes: Critical/high vulnerabilities with plausible production exploit path block release unless fixed or formally accepted. Mediums become actions unless they touch auth/session, runtime command execution, exposed services, or known exploited CVEs.

### Milestone 2 — Source-Level Security Review

- [x] Step 1 — Review build context hygiene
  - Achieve: Verify `.dockerignore` excludes local secrets, VCS state, agent state, docs/backlog, caches, build outputs, logs, dumps, editor files, and private machine files.
  - Create: n/a
  - Modify: `TK89.output.md`.
  - Delete: n/a
  - Touch points: `.dockerignore`, repository root file list, final-image content policy.
  - Validation: Output states whether `.dockerignore` passes or lists concrete missing exclusions.
  - Notes: Pay attention to source maps, fixtures, seed data, generated exports, and any files matching secret-like names.

- [x] Step 2 — Review Dockerfile copy strategy and standalone wiring
  - Achieve: Check whether the multi-stage build copies only what standalone runtime needs, and verify standalone mode is wired correctly for this app.
  - Create: n/a
  - Modify: `TK89.output.md`.
  - Delete: n/a
  - Touch points: `Dockerfile`, `next.config.ts`, `.next/standalone` expectations, `.next/static`, `public/`, `node_modules`, `package.json`, `CMD ["node", "server.js"]`.
  - Validation: Output explicitly answers whether the final image copy strategy is minimal and whether standalone mode is a suspected functional/security risk.
  - Notes: Cross-reference the earlier standalone concern because middleware may not execute under current standalone `node server.js` behavior.

- [x] Step 3 — Review runtime user, permissions, and base image choices
  - Achieve: Verify the final stage runs as non-root, owns runtime files correctly, uses a defensible base image, and avoids unnecessary shell/package surface where practical.
  - Create: n/a
  - Modify: `TK89.output.md`.
  - Delete: n/a
  - Touch points: `Dockerfile` `FROM`, `RUN addgroup/adduser`, `COPY --chown`, `USER nextjs`, `EXPOSE`, `HEALTHCHECK` status.
  - Validation: Output records user ID/group, ownership posture, and any base-image or package-surface findings.
  - Notes: Document digest pinning, base image update cadence, Alpine musl tradeoffs, and disabled Dockerfile healthcheck as actions or accepted choices.

- [x] Step 4 — Review Compose runtime posture
  - Achieve: Evaluate runtime env injection, bind mounts, exposed ports, database exposure, network boundaries, health checks, restart policy, resource limits, and hardening options.
  - Create: n/a
  - Modify: `TK89.output.md`.
  - Delete: n/a
  - Touch points: `docker-compose.prod.yml`, `env_file: .env`, `./contents:/app/contents`, app `ports`, db `ports`, `extra_hosts`, networks, health checks, deploy resource limits.
  - Validation: Output distinguishes image-artifact risks from runtime deployment risks and lists any concrete breach paths.
  - Notes: Runtime hardening gaps block only when they create a breach path; otherwise they become ordered actions.

### Milestone 3 — Build And Inspect The Image Artifact

- [x] Step 1 — Build a fresh local audit image
  - Achieve: Build `42go-next:latest` from the current Dockerfile so inspection targets the actual artifact under review.
  - Create: n/a
  - Modify: `TK89.output.md`.
  - Delete: n/a
  - Touch points: `make prod.build` or equivalent Docker build command, Docker build logs, image ID/digest if available.
  - Validation: Build command succeeds, or `TK89.output.md` records the blocker and stops artifact-dependent verdicts as blocked.
  - Notes: Prefer the clean no-cache path for audit confidence. If Docker sandbox permissions block the command, request escalation like a responsible grown-up with brass knuckles.

- [x] Step 2 — Inspect image history and metadata
  - Achieve: Look for suspicious build arguments, embedded values, oversized layers, unexpected copy layers, root defaults, exposed ports, and labels.
  - Create: n/a
  - Modify: `TK89.output.md`.
  - Delete: n/a
  - Touch points: `docker history 42go-next:latest`, `docker image inspect 42go-next:latest`.
  - Validation: Output records relevant history/inspect findings without dumping noisy full command output.
  - Notes: Any secret-like layer content or unexpected build metadata is a release-gate issue.

- [x] Step 3 — Inspect final filesystem content
  - Achieve: Verify the container filesystem excludes prohibited files and includes only justified runtime content.
  - Create: n/a
  - Modify: `TK89.output.md`.
  - Delete: n/a
  - Touch points: `docker run --rm --entrypoint sh 42go-next:latest`, filesystem `find`, app workdir listing, `.next`, `public`, `node_modules`, `contents`.
  - Validation: Output includes the exact sensitive-name search patterns and pass/issues-found verdict.
  - Notes: Search for `.env`, secret, key, token, backup, dump, `.npmrc`, source maps, docs/backlog, `.agents`, fixtures, tests, and private content indicators.

- [x] Step 4 — Verify runtime identity and standalone behavior
  - Achieve: Confirm the image runs as the expected non-root user and verify the standalone server can start far enough to expose expected runtime files or failure signals.
  - Create: n/a
  - Modify: `TK89.output.md`.
  - Delete: n/a
  - Touch points: `docker run --rm --entrypoint id 42go-next:latest`, optional app container smoke checks, earlier middleware findings.
  - Validation: Output states the runtime user and whether standalone wiring appears correct, broken, or blocked.
  - Notes: This is not a middleware fix. If middleware execution remains suspect, document it as an action or dependency.

### Milestone 4 — Scan Vulnerabilities And Supply-Chain Posture

- [x] Step 1 — Run Trivy image scan
  - Achieve: Scan the built image with local `trivy` and classify findings against the accepted release gate.
  - Create: n/a
  - Modify: `TK89.output.md`.
  - Delete: n/a
  - Touch points: `trivy image 42go-next:latest`, optional severity filters, Trivy version.
  - Validation: Output includes summary counts by severity, release-gate interpretation, and critical/high details when present.
  - Notes: If Trivy fails unexpectedly, record the command failure and mark the scan verdict blocked.

- [x] Step 2 — Check misconfiguration, secret, and SBOM options where practical
  - Achieve: Determine whether Trivy can add useful config, secret, or SBOM evidence for this repo without turning the review into a new tooling project.
  - Create: n/a
  - Modify: `TK89.output.md`.
  - Delete: n/a
  - Touch points: `trivy config`, `trivy image --scanners vuln,secret,misconfig`, possible SBOM command if available.
  - Validation: Output states which Trivy scanner modes were run and which were skipped with rationale.
  - Notes: Do not block the whole task on optional scanner modes if the main image vulnerability scan succeeds.

- [x] Step 3 — Review Docker Hub publishing hygiene
  - Achieve: Evaluate tags, latest/version usage, platform target, public-registry risk, signing/provenance/SBOM expectations, and whether publishing should wait on any blockers.
  - Create: n/a
  - Modify: `TK89.output.md`.
  - Delete: n/a
  - Touch points: `Makefile` `publish`, `publish.nocache`, `publish.universal`, `IMAGE`, `VERSION`, `PUBLISH_PLATFORM`, Docker Hub target.
  - Validation: Output includes a Docker Hub publishing verdict and action list for tagging, signing/provenance, SBOM, and public/private decision.
  - Notes: Do not push images during this review unless explicitly asked.

### Milestone 5 — Produce The Review Output

- [x] Step 1 — Fill the twelve-area verdict table
  - Achieve: Complete pass/issues-found/blocked verdicts for every checklist area with evidence references.
  - Create: n/a
  - Modify: `TK89.output.md`.
  - Delete: n/a
  - Touch points: All evidence collected in milestones 1-4.
  - Validation: No checklist item is left blank.
  - Notes: Keep command excerpts short. The point is evidence, not a scroll of doom.

- [x] Step 2 — Write ordered actionable hardening steps
  - Achieve: Convert findings into a prioritized action list with blocker/follow-up labels.
  - Create: n/a
  - Modify: `TK89.output.md`.
  - Delete: n/a
  - Touch points: Dockerfile issues, Compose runtime issues, vulnerability findings, image-content findings, publishing hygiene findings.
  - Validation: Every issue has an owner-ready action or an explicit accepted/no-action rationale.
  - Notes: Separate remediation tasks should be recommended for changes outside the review scope.

- [x] Step 3 — Make the release recommendation
  - Achieve: State whether the current image is acceptable for Docker Hub publishing, acceptable with documented exceptions, blocked, or blocked pending standalone remediation.
  - Create: n/a
  - Modify: `TK89.output.md`.
  - Delete: n/a
  - Touch points: Release gate, vulnerability gate, secret/content inspection, Compose posture, standalone behavior.
  - Validation: Recommendation directly follows the accepted gate and cites the decisive evidence.
  - Notes: If the image is blocked, name the shortest path to unblock it. Chuck Norris does not leave smoke in the hallway.
