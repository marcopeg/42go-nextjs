# Execution Notes — Docker Image Security Review
**Task**: ./TK89.task.md
**Plan**: ./TK89.plan.md

## Decisions

- Accepted plan moved `TK89` into WIP for execution.
- Review scope remains audit-only: findings and actionable remediation steps go into `TK89.output.md`; Dockerfile/Compose remediation is not performed in this task.
- Docker Hub publication is blocked by the review gate.
- Secret values observed via Compose expansion are intentionally not repeated in the output.

## Problems Encountered

- Initial Trivy image scan failed in the sandbox while downloading the vulnerability database. Retried with escalated permission and the scan succeeded.
- `trivy config --no-progress` failed because `trivy config` does not support `--no-progress` in Trivy 0.70.0. Reran without that flag.
- `docker history` once hit Docker socket permission denial from the sandbox. Reran with escalated Docker daemon access and captured the relevant final-stage history.

## Deviations From Plan

- Runtime standalone behavior was checked with host-specific API smoke tests instead of a full browser/app walkthrough. This was enough to verify proxy execution and app ID resolution for the audit.

## Additional Requests

- Operator clarified that `docker-compose.prod.yml` is a local production environment: it is intended to run the production build locally and mimic production shape for engineers, not define the real production deployment topology.
- Based on that clarification, Compose `env_file`, local DB port exposure, host-gateway, and bind mounts are documented as local-production warnings rather than Docker Hub publishing blockers.
- Operator requested applying the standalone-only runtime image change after a successful trial. The production `Dockerfile` now removes the redundant final full `node_modules` copy.
- Operator bumped the Docker base image from `node:22-alpine` to `node:26-alpine` in all Dockerfile stages to test whether a fresher base clears Docker image vulnerability warnings.
- Operator confirmed local production works and deployed the image online for final verification.
- A fresh `42go-security-check` assessment against `42go-next:latest` completed on 2026-05-12 with `0` blocking findings.
- Fresh Trivy image scan reported `0` critical, `0` high, `0` medium, and `0` low runtime vulnerabilities.
- The remaining work was reduced to real follow-ups in `action-points.md`: source-map policy, local dump/token hygiene, real production env allowlist, health-check ownership, Docker Hub supply-chain policy, and final authenticated remote smoke testing.
- Source maps are now removed from `.next/standalone` and `.next/static` during the Docker builder stage before the runner image copies production files.
- Rebuilt `42go-next:latest` after the source-map change; verification found `0` `*.map` files under `/app`.
- `docker-compose.prod.yml` remains explicitly local production only; it is no longer tracked as a real production action point.
- Docker-ignored dump files are not a Docker image/public-image blocker; they remain only a local repository hygiene concern unless they leave this machine or enter shared infrastructure.
- Operator decided local dump hygiene should come later as part of a backup-system review, likely moving backup/dump outputs into `.local/` following the events dump pattern.
- Operator deployed and tested the production image online successfully.
- Full fresh-build `42go-security-check` run completed after the Dockerfile source-map comment/update: build `ok`, `npm run qa` `ok`, Trivy image vulnerabilities `0`, final image source maps `0`, blocking findings `0`.
- Follow-up backlog cleanup completed after TK89:
  - `QS23: Fix Docker Security Gate Findings` was deleted as fully superseded by TK89 remediation and the fresh clean security gate.
  - `AEM: Fix Docker build [aem]` was deleted as fully superseded by ADE/TK89 and the successful standalone production deployment.
  - Dead `AEM` cross-references were removed from active/completed backlog files.
  - Backlog doctor reported `0` issues after cleanup.
- Remaining security-adjacent active backlog assessment:
  - `HZ45: Docker Release Quality and Hardening v2` remains valid and owns future non-blocking release hardening.
  - `QW92: Split AppConfig Into Public and Server-Only Configuration` remains valid; TK89 reduced source-map exposure but did not solve the AppConfig client/server boundary.
  - `FN30: Remove Google Fonts Build Network Dependency` remains valid; Docker builds passed, but offline/sandbox font independence is still not proven.

## Known Limitations

- The review did not push or inspect a remote Docker Hub image.
- The latest assessment inspected the local `42go-next:latest` image, not the remote registry digest.
