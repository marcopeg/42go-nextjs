---
taskId: WP59
createdAt: 2026-05-02T06:08:49+02:00
updatedAt: 2026-05-02T06:16:15+02:00
---

# Plan — Document and Configure Lingocafe iOS Home Screen Launch Behavior

## Goal

Make Lingocafe's installed iOS Home Screen launch path explicit through `public.pwa.startUrl: "/books"` and document the AppConfig PWA options in the default documentation site so future apps know how to configure install metadata and launch behavior.

## Milestones

### Milestone 1 — Confirm PWA Configuration Surface

- [x] Step 1 — Verify current PWA runtime behavior
  - Achieve: Confirm the current metadata and manifest pipeline derives installed-app behavior from `config.public.pwa` and falls back to `/` when `startUrl` is missing.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `src/app/manifest.ts`, `src/app/layout.tsx`, `src/42go/pwa/HeadTags.tsx`, `src/42go/pwa/types.ts`.
  - Validation: Read the implementation and record that `manifest.ts` uses `pwa?.startUrl || "/"`, while layout/head tags only emit PWA metadata when `public.pwa` exists.
  - Notes: This step must not add an authenticated `/` redirect; WP59 is config-only for runtime behavior.

- [x] Step 2 — Compare Lingocafe and Quicklist app configs
  - Achieve: Confirm the exact config delta that explains the reported behavior: Quicklist has `public.pwa.startUrl`, Lingocafe does not.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `src/config/lingocafe/config.ts`, `src/config/quicklist/config.ts`.
  - Validation: Verify Quicklist keeps `startUrl: "/quicklists"` and Lingocafe still uses `app.default.page: "/books"` as the intended authenticated entry point.
  - Notes: This comparison drives both the config fix and the documentation example.

### Milestone 2 — Add Lingocafe PWA Config

- [x] Step 1 — Add Lingocafe `public.pwa`
  - Achieve: Configure Lingocafe so its generated manifest and iOS head tags describe the installed app and launch to `/books`.
  - Create: n/a
  - Modify: `src/config/lingocafe/config.ts`.
  - Delete: n/a
  - Touch points: `TAppConfigItem`, `TPWAConfig`, `public.pwa.name`, `shortName`, `description`, `themeColor`, `backgroundColor`, `startUrl`, `scope`, `display`, and `icons.appleTouch180`.
  - Validation: Inspect the generated manifest route or code path to confirm Lingocafe resolves `start_url` to `/books`.
  - Notes: Custom Lingocafe icon design is out of scope. Use an existing default icon path if no Lingocafe-specific icon exists.

- [x] Step 2 — Preserve non-Lingocafe behavior
  - Achieve: Keep Quicklist and other apps unaffected by making the change only in Lingocafe config.
  - Create: n/a
  - Modify: n/a beyond the Lingocafe config file from the previous step.
  - Delete: n/a
  - Touch points: `src/config/quicklist/config.ts`, `src/AppConfig.ts` only if inspection reveals registration impact.
  - Validation: Confirm Quicklist still declares `public.pwa.startUrl: "/quicklists"` and Lingocafe declares `"/books"` without changing shared PWA fallback semantics.
  - Notes: Do not modify `src/app/manifest.ts` unless validation exposes a real defect unrelated to the planned config-only fix.

### Milestone 3 — Add PWA Documentation

- [x] Step 1 — Create the dedicated PWA docs page
  - Achieve: Add a public documentation page explaining the supported `public.pwa` configuration and why `startUrl` matters for installed app launch behavior.
  - Create: `contents/default/docs/pwa/README.md`.
  - Modify: n/a
  - Delete: n/a
  - Touch points: `TPWAConfig` fields from `src/42go/pwa/types.ts`, generated manifest behavior in `src/app/manifest.ts`, iOS head tag behavior in `src/42go/pwa/HeadTags.tsx`.
  - Validation: Review the page for concrete coverage of `name`, `shortName`, `description`, `themeColor`, `backgroundColor`, `statusBarStyle`, `display`, `scope`, `startUrl`, and icon fields.
  - Notes: The docs should explicitly say that missing `startUrl` falls back to `/`, which can reopen an installed app to public home.

- [x] Step 2 — Include a practical AppConfig example
  - Achieve: Show a small config example for an authenticated app that starts at its app entry route, using Lingocafe/Quicklist-style semantics without hardcoding policy advice into shared runtime code.
  - Create: n/a
  - Modify: `contents/default/docs/pwa/README.md`.
  - Delete: n/a
  - Touch points: Example `public.pwa` block with `startUrl: "/books"`, `scope: "/"`, `display: "standalone"`, and `icons.appleTouch180`.
  - Validation: Ensure the example is TypeScript-friendly, ASCII-safe, and does not imply that custom icon generation is part of WP59.
  - Notes: Mention that app-specific icon assets can be added later while a default icon can cover temporary development use.

- [x] Step 3 — Link PWA docs in the sidebar
  - Achieve: Make the new PWA documentation discoverable from the default docs navigation.
  - Create: n/a
  - Modify: `contents/default/docs/SIDEBAR.md`.
  - Delete: n/a
  - Touch points: Existing sidebar sections under Features or UI & Experience.
  - Validation: Confirm the sidebar link points to `./pwa/README.md` and matches the docs project's relative-link convention.
  - Notes: Prefer a simple top-level docs page instead of nesting under `multiple-apps`.

### Milestone 4 — Validate and Record Outcome

- [x] Step 1 — Run automated quality checks
  - Achieve: Prove the config and docs changes do not break linting, typing, or production build.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `npm run qa`.
  - Validation: `npm run qa` passes, or any failure is documented with the exact blocking reason.
  - Notes: If build reaches out to Google Fonts and the sandbox blocks network, record that as an environment failure and run any available narrower validation.

- [x] Step 2 — Smoke-check manifest values
  - Achieve: Confirm the generated manifest exposes Lingocafe `start_url: "/books"` and does not change Quicklist's `start_url`.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: Running dev server if available, `curl` with Lingocafe and Quicklist host headers, or direct code inspection if no server is running.
  - Validation: Lingocafe manifest reports `/books`; Quicklist manifest still reports `/quicklists`.
  - Notes: Do not start `npm dev`; use the existing dev server if present or `make app.start` if a server is needed.

- [x] Step 3 — Capture manual iOS validation guidance
  - Achieve: Leave deterministic manual validation steps for the operator to verify the installed Home Screen behavior on the affected iPhone.
  - Create: n/a
  - Modify: `contents/default/docs/pwa/README.md` if the steps belong in docs, or final execution notes during execution.
  - Delete: n/a
  - Touch points: iOS Safari Add to Home Screen flow, Google login, force-close/reopen behavior.
  - Validation: The documented steps verify that a valid Google-authenticated Lingocafe session reopens to `/books` and Quicklist remains unchanged.
  - Notes: The exact iOS launch URL is still an open observation, but it does not block implementation.
