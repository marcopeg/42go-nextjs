---
taskId: WP59
createdAt: 2026-05-02T06:13:46+02:00
updatedAt: 2026-05-02T06:16:15+02:00
---

# Execution Notes — Document and Configure Lingocafe iOS Home Screen Launch Behavior
**Task**: ./WP59.task.md
**Plan**: ./WP59.plan.md

## Decisions

- Accepted WP59 and moved it from draft to WIP before implementation.
- Confirmed the runtime fix stays config-only: no authenticated `/` redirect will be added.
- Added Lingocafe `public.pwa` with `startUrl: "/books"` so the generated manifest launches installed Lingocafe into the books flow.
- Added dedicated PWA documentation at `contents/default/docs/pwa/README.md` and linked it from the docs sidebar.
- Fixed a narrow pre-existing `/books` type error found by QA: `BookCard` now narrows `readingAction.href` to a string before passing it to `next/link`.
- Live manifest smoke checks returned Lingocafe `start_url: "/books"` and Quicklist `start_url: "/quicklists"`.

## Problems Encountered

- `public/images/icons` does not currently exist, even though Quicklist, Notes, and the PWA head tag fallback reference `/images/icons/...` paths.
- The first `npm run qa` attempt failed on the existing `BookCard` `href` type issue. After the narrow fix, `npm run qa` passed.
- A sandboxed `ps` check for running Node/Next processes failed with `operation not permitted`; this did not block validation because localhost manifest `curl` checks succeeded.

## Deviations From Plan

- Added a small `BookCard` type-narrowing fix so the accepted QA gate could pass. This is adjacent to WP59 because `/books` is the configured Lingocafe launch target.

## Additional Requests

- Operator chose `contents/default/docs/pwa/README.md` as the documentation target.

## Known Limitations

- Custom Lingocafe icon assets are not available yet and remain future work.
- The configured default icon path still needs a real asset before a polished iOS install; WP59 intentionally does not create custom icon artwork.
- `npm run qa` still reports an existing Turbopack NFT warning for `next.config.ts` and repeated static generation messages about falling back when `next/headers` is unavailable. The command exits successfully.
