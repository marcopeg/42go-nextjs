---
taskId: TW33
createdAt: 2026-05-04T08:27:43+02:00
updatedAt: 2026-05-04T08:38:10+02:00
---

# Execution Notes — Reader Back Links Return To Bookshelf
**Task**: ./TW33.task.md
**Plan**: ./TW33.plan.md

## Decisions

- Treat the operator's "yes and execute it" as plan acceptance plus execution approval.
- Move `TW33` from Drafts to WIP before source changes.
- Keep desktop/AppLayout reader back navigation pointed at `/books/[bookId]`.
- Change only the mobile reader header back link target to `/books`.
- Split the read page hrefs into `bookDetailsHref` and `bookshelfHref` instead of reusing one shared `backHref`.
- Update the mobile reader header link accessible label to `Back to books`.
- Operator expanded the rule after initial execution: desktop/AppLayout reader back navigation should also go to `/books`.
- Removed the separate `bookDetailsHref` and now pass `bookshelfHref` to AppLayout, mobile reader, and desktop reader.
- Updated the desktop reader side-panel back link accessible label to `Back to books`.

## Problems Encountered

- The first transition script run used a skill-relative `docs/backlog` path and failed to find `TW33`; reran it with the absolute backlog root.

## Deviations From Plan

- n/a

## Additional Requests

- Operator confirmed before planning that only the mobile reader header back icon should go to `/books`, while desktop/AppLayout stays on `/books/[bookId]`.
- Operator later requested the same rule for desktop as well.

## Known Limitations

- Static verification passed for the first pass: mobile receives `/books`, desktop/AppLayout receive `bookDetailsHref`, and desktop keeps `Back to book details`.
- Expanded static verification passed: AppLayout, mobile reader, and desktop reader all receive `/books`; both reader labels say `Back to books`.
- Browser smoke was not run in this execution pass.

## Validation

- `npm run qa` passed.
- `npm run qa` passed again after applying the bookshelf rule to desktop.
