---
taskId: TW33
createdAt: 2026-05-04T08:25:49+02:00
updatedAt: 2026-05-04T08:38:10+02:00
---

# Plan — Reader Back Links Return To Bookshelf

## Goal

Change the visible LingoCafe reader back links so mobile, desktop, and AppLayout reader back navigation all go to `/books`, while leaving reader route, data loading, progress, and event behavior untouched.

## Milestones

### Milestone 1 — Route Reader Back Targets To Bookshelf

- [x] Step 1 — Use bookshelf target value
  - Achieve: Replace the old book-detail reader back target with a bookshelf target.
  - Create: n/a
  - Modify: `src/app/(app)/(lingocafe)/books/[bookId]/[pageId]/page.tsx`
  - Delete: n/a
  - Touch points: `bookshelfHref`, `AppLayout` `backBtn`, `BookReaderMobileSurface`, `BookReaderDesktopSurface`
  - Validation: Source inspection shows the only reader back target value is `/books`.
  - Notes: Keep route shape `/books/[bookId]/[pageId]`; do not touch API fetch or progress logic.

- [x] Step 2 — Pass the right href to each surface
  - Achieve: Wire `BookReaderMobileSurface`, `BookReaderDesktopSurface`, and AppLayout reader back navigation to the bookshelf href.
  - Create: n/a
  - Modify: `src/app/(app)/(lingocafe)/books/[bookId]/[pageId]/page.tsx`
  - Delete: n/a
  - Touch points: JSX props for `backBtn`, `BookReaderMobileSurface`, `BookReaderDesktopSurface`
  - Validation: Mobile, desktop, and AppLayout reader back prop paths are visibly `/books`.
  - Notes: This is a targeted prop wiring change. No reader UI redesign.

### Milestone 2 — Update Back Link Semantics

- [x] Step 1 — Update mobile accessible label
  - Achieve: Make the mobile reader header back link label match the bookshelf target.
  - Create: n/a
  - Modify: `src/app/(app)/(lingocafe)/books/_components/BookReaderSurfaces.tsx`
  - Delete: n/a
  - Touch points: `BookReaderMobileSurface`, `Link`, `aria-label`
  - Validation: Mobile reader link no longer says `Back to book details`; it uses a label such as `Back to books`.
  - Notes: Keep the icon unchanged unless the existing code requires no extra work.

- [x] Step 2 — Update desktop accessible label
  - Achieve: Make the desktop reader side-panel back link label match the bookshelf target.
  - Create: n/a
  - Modify: `src/app/(app)/(lingocafe)/books/_components/BookReaderSurfaces.tsx`
  - Delete: n/a
  - Touch points: `ReaderSidePanel`, `BookReaderDesktopSurface`, desktop back `Link`
  - Validation: Desktop reader link no longer says `Back to book details`; it uses `Back to books`.
  - Notes: Desktop now follows the same bookshelf rule as mobile.

### Milestone 3 — Validate The Narrow Fix

- [x] Step 1 — Static verification
  - Achieve: Confirm the code reflects the intended mobile/desktop split.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `rg` or direct source inspection around `backHref`, `BookReaderMobileSurface`, `BookReaderDesktopSurface`, and `aria-label`
  - Validation: Checks show mobile, desktop, and AppLayout reader back hrefs are `/books`, and no progress/event/fetch code was changed.
  - Notes: The task is small; static verification should catch the main regression risk.

- [x] Step 2 — Run QA
  - Achieve: Ensure lint and build still pass after the navigation change.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `npm run qa`
  - Validation: `npm run qa` passes.
  - Notes: Existing baseline warnings may still appear; record only new failures as blockers.

- [ ] Step 3 — Optional browser smoke check if server/browser is available
  - Achieve: Verify the visible reader back links land on `/books` in a running app.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `/books/[bookId]/[pageId]` at mobile and desktop viewports, visible reader back controls, `/books`
  - Validation: Activating the mobile and desktop reader back controls navigates to `/books`.
  - Notes: Browser smoke remains optional.
