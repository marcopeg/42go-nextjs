# Execution Notes — Remove Google Fonts Build Network Dependency
**Task**: ./FN30.task.md
**Plan**: ./FN30.plan.md

## Decisions

- Accepted plan with operator request: `yes and execute it`.
- Transitioned FN30 from `refined` to `planned`, then to `wip` before implementation.
- Chose the refined system-font approach: remove `next/font/google` instead of vendoring font files or adding runtime Google CDN links.
- Confirmed the only live Google font path before editing was `src/app/layout.tsx`.
- Confirmed LingoCafe reader font options are web-safe stacks only: Georgia, Palatino, Arial, Verdana, Trebuchet MS, and Tahoma.
- Updated `src/app/tailwind.css` so `font-sans` resolves to a deterministic system sans-serif stack.
- Documented the font strategy in `docs/articles/THEMING.md`.
- Verified live source has no `next/font/google`, Google Fonts CDN, Google Fonts CSS import, or Google-generated `--font-inter` path after edits. Remaining Google Fonts mentions are documentation only.
- `npm install` passed: dependencies were already up to date; npm reported 3 moderate audit findings and synchronized the root `package-lock.json` version from `0.0.41` to `0.0.43`.
- `npm run build` passed with Next.js 16.2.6 / Turbopack, generating 33 static pages and no Google Fonts fetch failure.
- `npm run qa` passed: ESLint completed and the production build passed.
- Follow-up operator request changed the final typography choice from OS-dependent system stack to locally vendored Inter.
- Added Inter normal and italic variable TTF files from Google Fonts under `src/app/fonts/inter`.
- Added `OFL.txt`, `METADATA.pb`, and `README.md` beside the vendored font files.
- Rewired `src/app/layout.tsx` to use `next/font/local` with the local Inter normal and italic variable fonts.
- Rewired `src/app/tailwind.css` so `font-sans` uses `var(--font-inter)` with system fallbacks.
- Final local-font verification passed: no live `next/font/google`, Google Fonts CDN, or Google Fonts CSS import remained in source.
- Final `npm run build` passed with local Inter assets.
- Final `npm run qa` passed with local Inter assets.

## Problems Encountered

- `npm install` reported 3 moderate audit findings. No package change was made because dependency remediation is outside FN30.

## Deviations From Plan

- The accepted plan originally implemented the refined system-font stack. The operator later requested a local Google font asset implementation for consistent typography, so Milestone 5 was added and executed.

## Additional Requests

- Operator asked whether Terms and Conditions or Privacy Policy need updates because of local font vendoring.

## Known Limitations

n/a
