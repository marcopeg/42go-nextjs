# Execution Notes — Store Database Dumps Under .local
**Task**: ./NQ76.task.md
**Plan**: ./NQ76.plan.md

## Decisions

- Used `.local/42go-backups/` as the single default dump directory.
- Kept explicit readable restore paths unchanged.
- Kept bare restore fallback limited to `.local/42go-backups/` by changing the shared `DUMPS_DIR`.

## Problems Encountered

- Existing generated dump SQL files remain in `knex/dumps/`; they are operator-local data and were left untouched.

## Deviations From Plan

- `.dockerignore` already excluded `.local`; added an explicit `.local/42go-backups` entry anyway to document the backup-specific exclusion.
- Kept a legacy `.gitignore` guard for `knex/dumps/*.sql` so existing operator-local dump files do not become untracked. Bare restore fallback still does not search `knex/dumps`.

## Additional Requests

- Operator requested `.gitignore` and `.dockerignore` updates during refinement.
- Operator later requested dedicated database URL environment variables: backup must require `BACKUP_DATABASE_URL`, restore must require `RESTORE_DATABASE_URL`, and events export must require `EVENTS_DATABASE_URL` without falling back to `DATABASE_URL`.

## Known Limitations

- Destructive restore validation was not run against a disposable database during this pass.

## Validation

- `node .agents/skills/42go-backup/scripts/backup.mjs --help` passed.
- `node --check .agents/skills/42go-backup/scripts/backup.mjs` passed.
- `make backup mode=light` passed and created `.local/42go-backups/20260513T122653Z.dump.light.sql`.
- `git check-ignore .local/42go-backups/20260513T122653Z.dump.light.sql` passed.
- `git check-ignore knex/dumps/20260511T124911Z.dump.light.sql` passed for the legacy ignore guard.
- Bare legacy restore lookup was checked with `node .agents/skills/42go-backup/scripts/backup.mjs restore --from 20260511T124911Z.dump.light.sql`; it failed before restore execution and only tried `.local/42go-backups/20260511T124911Z.dump.light.sql`.
- `npm run qa` passed.
- `node --check .agents/skills/42go-backup/scripts/backup.mjs` passed after splitting backup and restore database URL variables.
- `python3 -m py_compile .agents/skills/42go-events-export/scripts/export_events.py` passed with `PYTHONPYCACHEPREFIX=/private/tmp/42go-pycache`.
- With only `DATABASE_URL` set, backup failed with `BACKUP_DATABASE_URL is required.`
- With only `DATABASE_URL` set, restore failed with `RESTORE_DATABASE_URL is required.`
- With `BACKUP_DATABASE_URL` set and `RESTORE_DATABASE_URL` unset, restore failed with `RESTORE_DATABASE_URL is required.`
- With only `DATABASE_URL` set, events export failed with `EVENTS_DATABASE_URL is required.`
- `npm run qa` passed after the dedicated env var changes.
- `npm run qa` passed after splitting restore to `RESTORE_DATABASE_URL`.
