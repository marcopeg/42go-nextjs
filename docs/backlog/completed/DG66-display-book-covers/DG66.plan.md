---
taskId: DG66
createdAt: 2026-04-27T15:37:01+02:00
updatedAt: 2026-04-27T16:02:15+02:00
---

# Plan — Display Book Covers

## Goal

Render stable, optimized LingoCafe book covers on `/books` using committed static assets under `public/images/lingocafe/`, infer default cover URLs from `book.id`, honor explicit `books.cover` overrides, and fall back to a generic placeholder image when a book-specific cover is missing.

## Milestones

### Milestone 1 — Lock Down Cover Asset Contract

- [x] Step 1 — Inventory existing cover assets
  - Achieve: Confirm the committed cover assets that already exist and document how they map to seeded book IDs.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `public/images/lingocafe/gosta-berling-sv-b2.jpg`, `public/images/lingocafe/nils-holgersson-sv-a2.jpg`, `knex/seeds/20260427151000_init_lingocafe_data.js`
  - Validation: Existing files are confirmed as JPEG covers named after valid `lingocafe.books.id` values.
  - Notes: Both existing covers are 500x750 JPEGs. Their filenames match seeded Swedish books. Their visible printed titles do not match the current seed titles, so implementation should use filename matching and not mutate operator-provided image content during this task.

- [x] Step 2 — Add a generic committed placeholder cover
  - Achieve: Create a reusable generic cover image for any book whose inferred or explicit cover cannot be rendered.
  - Create: `public/images/lingocafe/placeholder.jpg`
  - Modify: n/a
  - Delete: n/a
  - Touch points: static public assets, 2:3 cover ratio, visual fallback for missing book-specific covers
  - Validation: The placeholder is a browser-renderable image file under `/images/lingocafe/placeholder.jpg` and has stable dimensions suitable for `next/image`.
  - Notes: Keep it generic. Do not create book-specific covers for the two English seeded books unless separate assets are supplied.

### Milestone 2 — Resolve Cover URLs in the Reader API

- [x] Step 1 — Add cover URL resolution helper
  - Achieve: Centralize cover URL rules so the client receives browser-safe public URLs and does not construct filesystem paths.
  - Create: n/a
  - Modify: `src/app/api/(lingocafe)/lingocafe/_lib/reader.ts`
  - Delete: n/a
  - Touch points: `BookRow`, `mapBook`, a new helper such as `resolveBookCover(book)`, `cover`, `coverUrl`
  - Validation: Books with no explicit `cover` receive `/images/lingocafe/<book-id>.jpg` as their default cover URL; books with explicit public `cover` values use that override.
  - Notes: Keep the API response free of local filesystem paths. Only public URLs should leave the server.

- [x] Step 2 — Expose fallback metadata without filesystem leakage
  - Achieve: Provide enough cover data for the UI to switch to the placeholder when image loading fails.
  - Create: n/a
  - Modify: `src/app/api/(lingocafe)/lingocafe/_lib/reader.ts`
  - Delete: n/a
  - Touch points: reader response shape, client `ReaderBook` type, `cover`, optional `coverUrl`, optional `coverFallback`
  - Validation: API payload for each book contains a cover URL and a fallback URL, or a single resolved URL strategy that the client can render with a fallback.
  - Notes: Avoid checking the filesystem on every API request unless the implementation can do it cheaply and safely. Browser `onError` fallback is acceptable.

- [x] Step 3 — Keep seed data compatible
  - Achieve: Preserve existing seed behavior while making cover rendering work from book IDs.
  - Create: n/a
  - Modify: `knex/seeds/20260427151000_init_lingocafe_data.js` only if explicit `cover` values are needed for override coverage.
  - Delete: n/a
  - Touch points: `books.cover`, existing `cover: null` rows, two existing image filenames
  - Validation: Reseeded books still work with null `cover` values because the API infers public cover URLs from `id`.
  - Notes: Prefer leaving `cover: null` for default-path coverage unless a small override example is useful.

### Milestone 3 — Render Covers in the Books Page

- [x] Step 1 — Add `next/image` cover rendering
  - Achieve: Render each book cover in the `/books` grid using stable 2:3 dimensions and Next.js image optimization.
  - Create: n/a
  - Modify: `src/app/(app)/(lingocafe)/books/page.tsx`
  - Delete: n/a
  - Touch points: `Image` from `next/image`, `ReaderBook` type, card layout, 500x750 cover ratio, `sizes`, `alt`
  - Validation: Book cards show cover images without layout shift on desktop and mobile widths.
  - Notes: Use the title in `alt` text. Keep cards scan-friendly and avoid oversized decorative layout.

- [x] Step 2 — Implement image-load fallback
  - Achieve: Swap missing or failing book-specific covers to the generic placeholder image.
  - Create: n/a
  - Modify: `src/app/(app)/(lingocafe)/books/page.tsx`
  - Delete: n/a
  - Touch points: small presentational cover component or local fallback state, `onError`, `/images/lingocafe/placeholder.jpg`
  - Validation: English seeded books without committed cover files render the placeholder instead of broken images.
  - Notes: Keep fallback behavior local to cover rendering so the rest of the book card stays simple.

- [x] Step 3 — Preserve onboarding and empty states
  - Achieve: Add covers without regressing the existing profile-completion flow from `PK51`.
  - Create: n/a
  - Modify: `src/app/(app)/(lingocafe)/books/page.tsx`
  - Delete: n/a
  - Touch points: `showProfileForm`, loading state, empty catalog state, language-filtered book list
  - Validation: Incomplete profiles still show the profile form only; complete profiles still show the filtered book list.
  - Notes: Do not show book cards behind the profile form.

### Milestone 4 — Verify and Record Behavior

- [x] Step 1 — Validate static asset serving
  - Achieve: Confirm the new and existing images are reachable from their public URLs.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `/images/lingocafe/gosta-berling-sv-b2.jpg`, `/images/lingocafe/nils-holgersson-sv-a2.jpg`, `/images/lingocafe/placeholder.jpg`
  - Validation: Local static file checks or browser checks confirm the images return successfully.
  - Notes: If no dev server is running, verify file presence and run browser smoke after starting the approved app server only if needed.

- [x] Step 2 — Smoke the catalog UI with seeded data
  - Achieve: Verify the LingoCafe book grid renders covers for the Swedish seeded books and placeholder covers for books without specific assets.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: seeded Jane profile path, `/books`, `/api/lingocafe/books`, browser rendering
  - Validation: The catalog shows real covers for `nils-holgersson-sv-a2` and `gosta-berling-sv-b2`; missing English-specific images do not produce broken-image UI.
  - Notes: Authenticated in-app browser smoke on `http://localhost:3000/books` as `admin/admin` confirmed the catalog renders the supplied covers and no longer has horizontal scroll after the drawer mount fix.

- [x] Step 3 — Run repository QA
  - Achieve: Catch lint, type, and build regressions before completing the task.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `npm run qa`
  - Validation: `npm run qa` passes, or any failure is documented with the command and root cause.
  - Notes: Do not run `npm dev`; use the existing app server or `make app.start` if a server must be started.
