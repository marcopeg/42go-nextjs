# Refinement Questions v1 — Design Reusable Overlay Surface Component and Agent Skill

Answer inline under each question, then return to chat and say `next round`.

## Questions

### 1. What final naming direction should GM77 target for the component family and skill?

Useful choices: `OverlaySurface`, `Surface`, `Overlay`, `Layer`, `OverlayPanel`, or a different name. Include the skill name too if you have a preference, for example `42go-overlay-surface`.

Answer:

I'd say "Modal" could be a name. I see there is the 42go/components/panel. You need to check what this does and if there is any overlapping.

As for the skill, `42go-ui-modal` would be a good name as well

### 2. What foundation should the implementation use for dialog accessibility and lifecycle behavior?

Choose one direction: add/use a Radix Dialog or shadcn dialog-style primitive under the hood; build a custom portal/focus/scroll-lock layer; or use a hybrid. If you choose custom, say what accessibility behavior must be included in GM77.

Answer:

Use radix as much as possible. If the Dialog is the correct base, then maybwe we should call this "dialog" instead of "modal".

### 3. How should the public API expose header, body, and footer composition?

Choose the primary caller experience: slot props such as `title`, `subtitle`, `actions`, `footer`; compound components such as `<OverlaySurface.Header>` and `<OverlaySurface.Body>`; or both with a simple wrapper plus primitives like the existing `Panel` system.

Answer:

I like the props slot version.
The main content is the body.

Example:

```tsx
<Modal title="foobar" actions={['hey', 'you']}>
  this is the main body
</Modal>
```

### 4. How should responsive behavior and sizing be expressed in the API?

Useful choices: one responsive presentation prop such as `presentation={{ mobile: "fullscreen", desktop: "panel" }}`; separate `mobile` and `desktop` props; CSS-only responsive defaults; size presets with raw min/max escape hatches; or raw sizing props only.

Answer:

In mobile it's always full-screen, no need to do props there.
In desktop it can be either centered in the screen, or a side panel.

Make another proposal as a followup question so we can reason more.

### 5. What should count as the concrete reusability proof inside GM77 itself?

Choose one: document mappings for ABX/ZJ19/BL01 without migrating them; add a small demo/example consumer; migrate one existing bespoke surface in GM77; or something else. If migration is included, name the exact surface.

Answer:

while implementing, use it to substitute the current modal/popup in the lingocafe reader page where it's possible to change the reading appearance preferences
