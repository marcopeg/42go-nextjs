---
taskId: GM77
createdAt: 2026-05-07T15:35:34+02:00
updatedAt: 2026-05-07T16:58:32+02:00
---

# Execution Notes — Design Reusable Overlay Surface Component and Agent Skill
**Task**: ./GM77.task.refined.md
**Plan**: ./GM77.plan.md

## Decisions

- The operator accepted the plan with "yes and iplement"; treated as accept + execute.
- The task was moved from Drafts to WIP using `backlog_tool.py transition GM77 --to-state wip`.
- Implementation will follow the accepted plan milestone order unless code inspection proves a safer sequence.
- `Panel` remains a static page-flow content shell. `Modal` owns overlay lifecycle and only matches the visual language where useful.
- `Modal` uses Radix Dialog primitives for portal, focus, Escape, outside interaction, title/description semantics, and close behavior.
- Modal animation is inferred from `presentation` and `anchor`. No public animation prop was added.
- Modal sizing is limited to presets: `sm`, `md`, `lg`, `xl`, `full`.
- LingoCafe reader preference state and controls stayed in the reader feature; only the bespoke overlay mechanics were replaced.

## Implementation Log

- Added `@radix-ui/react-dialog`.
- Added the shared modal exports under `src/42go/components/modal/`.
- Added a shadcn-style low-level dialog wrapper at `src/components/ui/dialog.tsx`.
- Added authenticated default-app demo routes at `/demo-panel` and `/demo-modal`.
- Added default app feature flags and menu links for the demo routes.
- Migrated `BookReaderPreferencesPanel` to use `Modal` for panel/full-screen behavior, close button, backdrop, Escape handling, and footer.
- Added viewport matching inside `BookReaderPreferencesPanel` because the reader mounts mobile and desktop surfaces at the same time; portal-based overlays must not render from both hidden parents.
- Added `.agents/skills/42go-ui-modal/SKILL.md` with trigger guidance, API recipes, and migration rules.
- Follow-up: removed the shared mobile bottom-slide animation from desktop panels so each desktop panel anchor slides from its own edge only.
- Follow-up: added `/demo-modal` examples for all panel anchors: left, right, top, bottom.
- Follow-up: added `/demo-modal` examples for all centered modal sizes: sm, md, lg, xl, full.
- Follow-up: fixed desktop panel edge attachment by resetting the opposite edge per anchor, such as `left-auto` for right panels and `bottom-auto` for top panels.
- Follow-up: fixed `/demo-modal` anchor example close animations by keeping the selected anchor stable while the panel closes instead of clearing it to the right-anchor fallback.
- Follow-up: matched panel header height and spacing to the app toolbar with `h-16 px-6 py-4`.
- Follow-up: reader preferences panel now overrides the shared panel default with `md:h-[68px] md:px-8` to match `BookReaderDesktopSurface`, whose header is explicitly `h-[68px] px-8`.

## Problems Encountered

- In-app browser navigation occasionally timed out while the app still responded on `localhost`; guarded retries succeeded for the demo checks.
- Early reader-route browser verification was blocked by an incomplete LingoCafe profile in the local session; later checks used a reachable reader URL and verified the migrated reader preferences panel.

## Deviations From Plan

- None yet.

## Additional Requests

- Operator requested follow-up fixes for panel anchor entry/exit direction, right-edge attachment, top-panel close direction, demo coverage for all anchors and sizes, and reader header alignment.

## Verification

- `npm run qa` passed.
- Browser checked `/demo-modal` rendering.
- Browser opened and closed the centered modal.
- Browser opened and closed the right-side panel.
- Browser opened and closed every panel anchor example.
- Browser opened and closed every modal size example.
- Browser verified the right panel carries right-edge positioning classes and the top panel keeps top-edge classes.
- Browser verified the panel header and app toolbar both use `h-16`.
- Browser verified the reader preferences panel header carries `md:h-[68px] md:px-8`, matching the reader desktop header rule.
- Browser checked `/demo-panel` rendering.

## Known Limitations

- None known after the final reader preferences browser check and `npm run qa`.
