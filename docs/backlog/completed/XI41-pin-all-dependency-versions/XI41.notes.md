---
taskId: XI41
createdAt: 2026-04-23T14:55:49+02:00
updatedAt: 2026-04-23T15:27:45+02:00
---

# Execution Notes — Pin All Dependency Versions

**TaskID**: XI41
**Task**: ./XI41.pin-all-versions.md
**Plan**: ./XI41.pin-all-versions.plan.md

## Decisions

- Execution started after explicit `accept + execute` from the operator.
- XI41 proceeds on the currently validated ESLint 9 baseline even though UT41 remains paused.
- If UT41 later changes the dependency set, XI41 must be re-verified or rerun so pins stay aligned with the final manifest.
- Cold-install validation is required for this task; a warm lockfile-only `npm install` was not treated as sufficient proof.
- The manifest itself is now treated as authoritative, so dependency pins must satisfy peer constraints without relying on an older lockfile's resolved tree.

## Problems Encountered

- UT41 is paused pending upstream ESLint 10 plugin support, so XI41 cannot truly be the forever-final dependency task unless UT41 is either completed later or permanently abandoned.
- The first cold install from `package.json` alone failed with `ERESOLVE`: `@dnd-kit/sortable@10.0.0` requires `@dnd-kit/core@^6.3.0`, but the pinned manifest still had `@dnd-kit/core@6.1.0`.
- The first cold `yarn qa` after reinstall surfaced six real lint/type failures in `src/42go/config/ThemeProvider.tsx`, `src/app/(app)/quicklists/[id]/page.tsx`, `src/app/(app)/quicklists/[id]/info/page.tsx`, and `src/lib/quicklists/hooks/useQuicklistData.ts`.
- Shell startup in the environment had a broken `PATH`, so reinstall validation had to be run with an explicit path prefix for core system utilities and package-manager binaries.

## Deviations From Plan

- Proceeded on the current validated baseline instead of waiting for UT41 to resume.
- Step 2 required one numeric version correction (`@dnd-kit/core` to `6.3.1`) because the previously declared version was not actually installable without the old lockfile.
- Step 5 expanded into a small repair slice because the clean reinstall exposed existing code patterns that now fail the current lint/type gate.

## Additional Requests

- Operator accepted the plan and requested immediate execution.
- Operator requested a stronger proof: remove dependencies, cache artifacts, and the lockfile, then reinstall from `package.json` alone and verify the repo still works.

## Known Limitations

- A future UT41 resume could require XI41 to be rerun to re-pin any changed ESLint-related versions.
- Final QA still emits the known baseline warnings: the Turbopack NFT tracing warning for `next.config.ts` and repeated `Cannot access next/headers, falling back to environment matching only` build output.