---
taskId: XI31
createdAt: 2026-05-01T07:24:38+02:00
updatedAt: 2026-05-01T07:24:38+02:00
---

# Execution Notes — Fix Reader Scroll Position Progress
**Task**: ./XI31.task.md
**Plan**: ./XI31.plan.md

## Decisions

- Operator accepted the plan and asked to execute in the same turn, then later explicitly requested completion.
- Use URL `progress_bps` as an explicit restore override; otherwise restore persisted same-page progress returned by the page API.
- Preserve `page-open` event writes on every page open.
- Added `loadBookProgress` and `saveBookOpenProgress` in the LingoCafe reader helper so page opens can read the existing row before deciding whether to write progress.
- Returned effective progress in the page API response and taught the client to restore from it when the URL does not provide an explicit progress value.
- Added bounded animation-frame restore retries so desktop and mobile scroll containers have time to become measurable before restore gives up.

## Problems Encountered

- The backlog lifecycle state was found back under Drafts during completion, while the source changes were still present. The task was moved back to WIP before completing so the completion helper could operate on the expected storage layout.
- `npm run qa` printed the existing Turbopack NFT warning and repeated `next/headers` static fallback messages, but exited successfully.

## Deviations From Plan

- n/a

## Additional Requests

- Operator asked to complete `XI31` after execution.

## Known Limitations

- Operator handles final desktop and mobile manual smoke testing.

## Manual Smoke Checklist

- Desktop: open a book page with an existing same-page `books_progress` row and no `progress_bps` query; confirm the reader restores to the saved position.
- Desktop: scroll, wait for the 10-second collection window or navigate away, then reopen; confirm progress resumes near the last scroll position.
- Mobile: repeat same-page reopen and scroll-save checks in the mobile reader overlay.
- Same-page open: confirm `page-open` event is created and `books_progress` is not reset to `0`.
- Different-page open: open a different page in the same book and confirm `books_progress.page_id` moves to that page with `progress_bps = 0`.
