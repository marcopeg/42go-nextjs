---
taskId: NN45
createdAt: 2026-04-28T11:21:42+02:00
updatedAt: 2026-04-28T11:44:51+02:00
---

# Plan — Implement Books Shelf Design

## Goal

Update the LingoCafe `/books` card and responsive grid to match the attached shelf design for desktop and mobile while preserving the existing catalog data flow, cover fallback behavior, and navigation into `/books/[bookId]`.

## Milestones

### Milestone 1 — Inspect Current Card Surface

- [x] Step 1 — Confirm component and data boundaries
  - Achieve: Identify the exact route-local files that own the books shelf, card, cover, tags, and reader book type shape.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `src/app/(app)/(lingocafe)/books/page.tsx`, `src/app/(app)/(lingocafe)/books/_components/BookCard.tsx`, `BookCover.tsx`, `BookTags.tsx`, `book-types.ts`.
  - Validation: Verify the current card still receives `title`, `author`, `lang`, `level`, `tags`, `cover`, and `coverFallback`; confirm no API change is needed.
  - Notes: Confirmed the current route-local files own the card/grid surface and the existing payload already provides the fields needed by the design. No API change is needed.

### Milestone 2 — Rebuild Book Card Presentation

- [x] Step 1 — Convert card to cover-first layout
  - Achieve: Rework `BookCard` so the cover dominates the top of the card, metadata sits below it, and the card visually follows the attached design.
  - Create: n/a
  - Modify: `src/app/(app)/(lingocafe)/books/_components/BookCard.tsx`.
  - Delete: n/a
  - Touch points: `BookCard`, `BookCover`, `BookTags`, `ReaderBook`.
  - Validation: Desktop and mobile card markup keeps a stable cover area, does not overlap title/author/tags, and the link still navigates to `/books/[bookId]`.
  - Notes: Reworked `BookCard` to place the existing `BookCover` first at full card width, with metadata below. The cover image is still rendered as-is through the existing cover component.

- [x] Step 2 — Add removable placeholder actions
  - Achieve: Add a heart control and circular three-dot control as visual placeholders only, isolated enough to comment out or remove easily before first release.
  - Create: Possibly a small local presentation helper inside `BookCard.tsx` if it keeps placeholder actions isolated.
  - Modify: `src/app/(app)/(lingocafe)/books/_components/BookCard.tsx`.
  - Delete: n/a
  - Touch points: Lucide icons if already available, semantic button attributes, card click/link behavior.
  - Validation: Placeholder buttons do not trigger favorite persistence, API calls, popovers, or menus; they do not break card navigation or keyboard focus.
  - Notes: Added isolated `MorePlaceholder` and `FavoritePlaceholder` helpers as non-functional visual spans, then commented out their render calls at the operator's request. No favorite persistence, API calls, popovers, or nested interactive controls were introduced.

- [x] Step 3 — Align metadata and tags
  - Achieve: Match the design's hierarchy for title, author/adaptation text, language/level pill, and tags using existing payload fields.
  - Create: n/a
  - Modify: `BookCard.tsx`, possibly `BookTags.tsx` only if the shared tag style must change for this card.
  - Delete: n/a
  - Touch points: `BookTags`, Tailwind classes, text wrapping behavior.
  - Validation: Long titles wrap cleanly, tags fit or wrap without horizontal overflow, and language/level remains visible.
  - Notes: Reused current `title`, `author`, `lang`, `level`, and `tags` fields. No new payload fields were added.

### Milestone 3 — Update Responsive Shelf Grid

- [x] Step 1 — Implement one-column mobile and two-to-three-column desktop grid
  - Achieve: Adjust the `/books` shelf grid so mobile renders one card per row and desktop renders two to three cards per row depending on available width.
  - Create: n/a
  - Modify: `src/app/(app)/(lingocafe)/books/page.tsx`.
  - Delete: n/a
  - Touch points: Existing loading, onboarding, empty, and book-list render branches.
  - Validation: Grid does not introduce horizontal scrolling on mobile; cards remain readable at common mobile widths and fill desktop space without becoming too wide.
  - Notes: Updated the existing books grid to one column by default, two columns from `md`, and three columns at `2xl`.

### Milestone 4 — Validate and Record

- [x] Step 1 — Run static quality gate
  - Achieve: Verify the implementation compiles and passes the repository quality gate.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `npm run qa`.
  - Validation: `npm run qa` passes.
  - Notes: `npm run qa` passed. The build still prints the existing Turbopack NFT warning and repeated `next/headers` fallback messages.

- [x] Step 2 — Smoke-check responsive behavior
  - Achieve: Confirm the card and grid match the accepted design direction across desktop and mobile.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `/books`, existing authenticated app route, dev server if available.
  - Validation: Inspect desktop and mobile layouts for 2-3 columns on desktop, one card per row on mobile, no horizontal scroll, stable text wrapping, and working navigation to `/books/[bookId]`.
  - Notes: Operator visually smoke-checked the first pass and found the cards were gigantic, then clarified desktop cards should be much smaller and wrap side-by-side into new rows. Kept mobile as a single-card flow, changed the shelf from stretched grid tracks to a wrapping flex row, reduced desktop cards from `md:w-64` to `md:w-56`, and adjusted card internals so cards in a row share a consistent height.
