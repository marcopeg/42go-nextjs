---
taskId: HU36
createdAt: 2026-04-23T16:15:57+02:00
updatedAt: 2026-04-23T16:19:50+02:00
---

# Plan — Create LingoCafe App Config

## Goal

Add a new `LingoCafe` app configuration that resolves on `lingocafe.app`, enables credentials plus Google login, and serves a minimal home page with a basic hero whose action routes users to `/login`.

## Milestones

### Milestone 1 — Register the app in the multi-app config

- [x] Step 1 — Add the LingoCafe config module
  - Achieve: Create a dedicated app config file for LingoCafe that follows the existing per-app pattern and defines hostname matching, public metadata, theme defaults, auth providers, and a minimal public page set.
  - Create: `src/config/lingocafe/config.ts`
  - Modify: `src/AppConfig.ts`
  - Delete: n/a
  - Touch points: `TAppConfigItem`, `apps` registry, `match.url`, `auth.providers`, `public.meta`, `public.toolbar`, `public.pages`
  - Validation: `npm run qa` passed after the config was imported and registered; host matching now includes `lingocafe.app` plus a local `lingocafe.localhost` convenience pattern.
  - Notes: Implemented in `src/config/lingocafe/config.ts` and registered in `src/AppConfig.ts`.

### Milestone 2 — Define the login-oriented public home page

- [x] Step 2 — Add a minimal hero-driven home page
  - Achieve: Configure the public home page so it presents a basic hero for LingoCafe and uses `/login` as the primary action.
  - Create: `src/config/lingocafe/home-page.tsx` if a dedicated page config improves clarity; otherwise `n/a`
  - Modify: `src/config/lingocafe/config.ts`
  - Delete: n/a
  - Touch points: `Page` / `TDynamicPage` config, hero block, CTA/link action config, public toolbar copy if needed
  - Validation: The dedicated `HomePage` config contains a single hero block and its primary action points to `/login`.
  - Notes: Implemented in `src/config/lingocafe/home-page.tsx` to keep the app config small and explicit.

### Milestone 3 — Wire auth and login behavior through existing flows

- [x] Step 3 — Configure credentials and Google for LingoCafe
  - Achieve: Ensure the new app exposes exactly the requested authentication mix so the shared `/login` page shows Google and credentials for LingoCafe.
  - Create: n/a
  - Modify: `src/config/lingocafe/config.ts`
  - Delete: n/a
  - Touch points: `TAuthProviders`, provider entries with `type: "credentials"` and `type: "google"`, logout config if needed
  - Validation: LingoCafe now declares `credentials` and `google`, matching the provider types consumed by `src/app/(public)/login/page.tsx` without any login-page changes.
  - Notes: Reused the existing shared Google env var pattern (`GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET`).

- [x] Step 4 — Verify callback and landing behavior stay coherent
  - Achieve: Confirm LingoCafe either relies on existing default post-login behavior or sets an app default page intentionally, without breaking the requested `/login` entry path.
  - Create: n/a
  - Modify: `src/config/lingocafe/config.ts` if `app.default.page` should be explicit
  - Delete: n/a
  - Touch points: `app.default.page`, `safeInternalPath`, shared `/login` page callback handling
  - Validation: The hero CTA routes to `/login`, and `app.default.page` is set to `/dashboard`, which fits the shared login page `callbackUrl` validation logic.
  - Notes: Chose an explicit safe internal destination instead of relying on the default fallback by implication.

### Milestone 4 — Prove the config works and does not break the shared app surface

- [ ] Step 5 — Run repo validation and host-specific smoke checks
  - Achieve: Validate the new app config against lint/build gates and verify the LingoCafe host resolves to the intended public and login surfaces.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `npm run qa`, app resolution in middleware, `/` on `lingocafe.app`, `/login` provider rendering
  - Validation: `npm run qa` passed. Direct host-header smoke checks against `http://localhost:3000` could not run because no local server was listening during execution.
  - Notes: Build initially failed in the sandbox when Next tried to fetch Google Fonts; rerunning `npm run qa` with network access resolved that environment issue. Host-routed smoke checks remain pending.
