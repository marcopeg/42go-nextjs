# Execution Notes — Restore Draft-Centric Backlog Metadata and Lifecycle States
**Task**: ./CO54.task.md
**Plan**: ./CO54.plan.md

## Decisions

- Execution started from the accepted plan on 2026-05-10.
- Current implementation must preserve unrelated dirty work in backlog skills, LingoCafe reader files, and existing backlog task movements.
- Migrated active backlog tasks to canonical `<taskid>.task.md` files and status folders.
- Added `backlog-doctor` with dry-run and repair modes.
- Updated repo-local backlog skills to describe canonical task files, frontmatter-free sidecars, status folders, status-local indexes, and the `planned` lifecycle.
- Final verification passed for Python compilation, doctor dry-run, index link existence, sidecar frontmatter removal, and fixture draft scaffolding.

## Problems Encountered

- The worktree already contained unrelated changes before CO54 execution. Treat them as user work.
- `backlog-doctor` initially reported a false positive for a completed historical task with stale `status: draft`; discovery now trusts completed/archived folders as historical states.

## Deviations From Plan

- The implementation kept historical completed/archived folders out of active doctor repair, matching the accepted scope.
- Existing refined sidecar files are preserved as content-only artifacts instead of being deleted. The canonical `<taskid>.task.md` file is the active metadata owner.

## Additional Requests

- The earlier UTC timestamp requirement was explicitly removed from CO54 as a mistaken prompt for another chapter.

## Known Limitations

- `npm run qa` is not required unless app/TypeScript code changes are part of this task. CO54 changed Python scripts, skill docs, and backlog markdown.
- Historical completed and archived tasks were not migrated to the new canonical file model.
