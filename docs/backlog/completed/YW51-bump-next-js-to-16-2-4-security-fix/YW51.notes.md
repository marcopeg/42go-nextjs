---
taskId: YW51
createdAt: 2026-04-21T11:15:15+02:00
updatedAt: 2026-04-23T15:27:45+02:00
---

# Execution Notes — Bump Next.js to 16.2.4 — Security Fix

**TaskID**: YW51
**Task**: ./YW51.nextjs-security-bump.md
**Plan**: ./YW51.nextjs-security-bump.plan.md

## Decisions

- Treated operator reply "yes, execute" as accept + execute per aplan skill semantics.
- Kept scope strictly to `next` and `eslint-config-next` version pin changes only.
- Used same cookie-jar curl pattern as PE62/DQ53/KB55 for smoke checks.
- Kept the CSP fix dev-only so production headers remain strict.

## Problems Encountered

- Post-upgrade regression: the browser console reported `eval() is not supported in this environment` because the global CSP in `next.config.ts` blocked React/Turbopack development tooling under Next 16.2.4.

## Deviations From Plan

- Added a minimal compatibility fix in `next.config.ts`: development CSP now includes `'unsafe-eval'` in `script-src` and permits dev HMR connections via `connect-src 'self' ws: wss: http: https:`. Production CSP is unchanged.

## Additional Requests

- None.

## Known Limitations

- 5 remaining audit advisories (1 moderate, 4 high) are in transitive dependencies (picomatch via tinyglobby), not in Next.js. These are outside YW51 scope.
- Audit summary before bump: 6 high from next + others. After bump: 0 next advisories, 5 residual unrelated.

## Results

- `next` and `eslint-config-next` pinned to `16.2.4`.
- `npm ls` confirmed both at `16.2.4`; `next-auth` peer deduped cleanly.
- `npm run qa` passed (Turbopack build, 1 non-blocking warning, no errors).
- `npm audit`: 0 next-related advisories (GHSA-ggv3-7p47-pfv8, GHSA-3x4c-7xq6-9pq8, GHSA-h27x-g6w4-24gq, GHSA-mq59-m269-xvcx, GHSA-jcc7-9wpm-mj36, GHSA-q4gf-8mx6-v5v3 all resolved).
- Smoke checks: login:200, auth:200, quicklists_create:201, quicklists_get:200.
- Dev responses now emit a development-only CSP that allows React/Turbopack eval + HMR; production still uses the stricter CSP.
