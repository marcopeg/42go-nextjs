---
taskId: YW51
createdAt: 2026-04-21T11:15:15+02:00
updatedAt: 2026-04-23T15:27:45+02:00
---

# Plan — Bump Next.js to 16.2.4 — Security Fix

**TaskID**: YW51
**Status**: completed

## Goal

Change the exact `next` and `eslint-config-next` pins in `package.json` from `16.1.6` to `16.2.4`, run `npm install`, validate with `npm run qa`, confirm `npm audit` is clean for Next-related advisories, and pass auth + quicklists runtime smoke checks.

## Milestones

### Milestone 1 — Update Version Pins

- [x] Step 1 — Bump `next` and `eslint-config-next` in package.json
  - Achieve: Change exact pin `"next": "16.1.6"` → `"next": "16.2.4"` and `"eslint-config-next": "16.1.6"` → `"eslint-config-next": "16.2.4"` so both stay version-aligned.
  - Create: None.
  - Modify: `package.json` — two version fields only.
  - Delete: None.
  - Touch points: `package.json` `dependencies` entry for `next`; `devDependencies` entry for `eslint-config-next`.
  - Validation: `grep '"next"' package.json` and `grep 'eslint-config-next' package.json` both show `16.2.4`.
  - Notes: Both fields use exact (no-caret) pins; keep them that way.

- [x] Step 2 — Run `npm install` to apply the pin change
  - Achieve: Regenerate `package-lock.json` with `next@16.2.4` resolved.
  - Create: None.
  - Modify: `package-lock.json`.
  - Delete: None.
  - Touch points: npm lockfile resolution for `next`, `eslint-config-next`, and their transitive peers.
  - Validation: `npm ls next eslint-config-next` shows both at `16.2.4` with no `invalid` or `deduped` conflicts.
  - Notes: Expect transitive peer churn within the Next.js subtree; only flag entries marked `invalid`.

### Milestone 2 — Validate Build and Audit

- [x] Step 1 — Run `npm run qa`
  - Achieve: Confirm lint, type-check, and production build all pass after the version bump.
  - Create: None.
  - Modify: Compatibility fixes only if QA reveals a regression introduced by 16.2.4.
  - Delete: None.
  - Touch points: `next.config.ts`, `src/middleware.ts`, `src/app/`, `src/42go/`, any file flagged by the build or type-checker.
  - Validation: `npm run qa` exits 0; no new errors beyond the existing baseline `Cannot access next/headers…` non-blocking warnings.
  - Notes: QA stayed green. A later dev-only regression required a minimal compatibility fix in `next.config.ts` so the CSP allows React/Turbopack eval and HMR connections in development while keeping production CSP unchanged.

- [x] Step 2 — Verify `npm audit` is clean for Next advisories
  - Achieve: Confirm none of the 6 GHSA advisories attributed to `next < 16.2.3` appear in the audit output.
  - Create: None.
  - Modify: None.
  - Delete: None.
  - Touch points: `npm audit` JSON output filtered for `next` package findings.
  - Validation: `npm audit --json | grep -i '"next"'` returns no high-severity advisories for the `next` package. Unrelated advisories (flatted, lodash, etc.) may remain.
  - Notes: Expected GHSA IDs to be gone: GHSA-ggv3-7p47-pfv8, GHSA-3x4c-7xq6-9pq8, GHSA-h27x-g6w4-24gq, GHSA-mq59-m269-xvcx, GHSA-jcc7-9wpm-mj36, GHSA-q4gf-8mx6-v5v3.

### Milestone 3 — Runtime Smoke Checks

- [x] Step 1 — Start dev server and run auth + quicklists smoke test
  - Achieve: Verify that the middleware, auth routes, and API layer still work correctly after the Next.js bump.
  - Create: None.
  - Modify: None (unless a runtime regression is found).
  - Delete: None.
  - Touch points: `src/middleware.ts`, `src/app/api/auth/`, `src/app/api/quicklists/route.ts`, `src/42go/auth/`.
  - Validation: `GET /login` → 200; authenticated `GET /api/auth/session` → 200; `POST /api/quicklists` valid payload → 201; `GET /api/quicklists` → 200.
  - Notes: Used the same cookie-jar curl pattern established in PE62/DQ53/KB55. Smoke checks remained green after the CSP fix.

### Milestone 4 — Capture Execution State

- [x] Step 1 — Update plan and notes with outcomes and any deviations
  - Achieve: Keep plan checkboxes accurate and document problems or compatibility fixes applied.
  - Create: `./YW51.nextjs-security-bump.notes.md` if not present.
  - Modify: `./YW51.nextjs-security-bump.plan.md`, `./YW51.nextjs-security-bump.notes.md`.
  - Delete: None.
  - Touch points: Task artifacts; any source files touched by compatibility fixes.
  - Validation: All completed steps are checked; notes capture deviations and residual limitations.
  - Notes: Leave task In Progress at session end; final transition via `acomplete`.
