# Refinement Questions v1 — 42go Events Query Session

Answer inline under the question, then return to chat and say `next round`.

## Questions

### 1. What should happen to events without `user_id`?

Pick one: exclude them, group them under an anonymous user, or something else.

Answer:
Ignore events without `user_id`. They are not expected to exist in normal data.
