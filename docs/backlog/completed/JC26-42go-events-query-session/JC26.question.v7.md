# Refinement Questions v7 — 42go Events Query Session

Answer inline under the question, then return to chat and say `next round`.

## Questions

### 1. Should the command include every event name in sessions, or only user activity events?

Pick one: all events, activity events only, or configurable later.

Answer:
For now, include every event for the same user. Cluster by event time. Each session belongs to one single user and must never mix users.
