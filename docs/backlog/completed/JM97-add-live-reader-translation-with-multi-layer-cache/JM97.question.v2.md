# Refinement Questions v2 — Add Live Reader Translation With Multi-Layer Cache

Answer inline under each question, then return to chat and say `next round`.

## Questions

### 1. How should the new `translate` profile boolean be initialized for existing and newly onboarded LingoCafe users? Choose or correct one: A) required schema field defaulting to `false`, B) optional schema field treated as `false` when missing, C) required/default `true` for all LingoCafe users, D) admin/seed-controlled only.

Answer:

do not initialize it at all. the key can be completely missing and that mean the feature is not available.

you can tweak the seeding for the test user that has a profile already (i believe it's john) and enable it for that profile because it's a local test profile.

### 2. Should `translate` be editable by the user in the profile UI, silently enabled as a capability, or controlled only by seed/admin/config?

Answer:

not editable at all. and it should not be overridden by the current profile capability.

### 3. Should the database cache store the raw original source sentence alongside the normalized hash input and translated text for debugging/audit, or should it store only hash, normalized fields, languages, translation, and metadata?

Answer:

yes: hash (pk), from, to, text, translation

## CHANGE OF PLAN

Instead of using the "translate:true" into the profile, modify the existing migration so that the users table also has a "feature_flags" jsonb field and move the "translate:true" logic into there.

As we did previously, create a `migration.sql` file in the task to alter the existing table so that i will run this query manually on the prod server.
