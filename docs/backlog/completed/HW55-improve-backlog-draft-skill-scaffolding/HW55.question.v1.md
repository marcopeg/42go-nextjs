---
taskId: HW55
round: 1
createdAt: 2026-05-07T15:23:02+02:00
updatedAt: 2026-05-07T15:23:02+02:00
answeredAt: 2026-05-07T15:32:38+02:00
---

# Refinement Questions v1 — Improve Backlog Draft Skill Scaffolding

Answer inline under each question, then return to chat and say `next round`.

## Questions

### 1. Where should the scaffold boundary end?

Should the Python scaffold script only create the folder and draft file, or should it also update `docs/backlog/BACKLOG.md` and enforce the "task appears in exactly one section" rule?

Answer:

if it's possible to append the new task to the "drafts" section of the backlog programmatically, it's even better. the more programmatic work we can do the better.

### 2. What should the scaffold command accept as input?

Should the script require a title up front, accept an optional title and generate a fallback placeholder when missing, or accept a raw operator prompt and let the agent edit/rename afterward?

Answer:

if the scaffold can accept the contents of each block:

- title
- pitch
- business value
- ... the other blocks

Then it would be better if the AI first takes the user input, does the research and manipulates it so to convert it into the proper blocks, and then is calls the scaffold script to generate the task on disk programmatically.

Make an assessment of which way is more efficient from the agentic platform point of view.

### 3. What output contract do you want from the scaffold script?

Should the script print JSON, key-value lines, or plain text? Include whether callers should get at least `taskId`, `draftPath`, and `taskFolder`.

Answer:

reason: what output is needed by the agentic platform to complete the task?

But I'd say that taskId and file path for "click to open" is very much needed.

### 4. How aggressive should the skill description cleanup be?

Should invocation rules move mostly into the frontmatter `description`, leaving the body almost entirely operational, or should the body keep a short "when to use" section as a backup for human readers?

Answer:

the skill is not for human readers.
if you want to add a "human help" then it should be a comment that doesn't get into the llm context, or even a README.md dedicated to humal level documentation.

### 5. Which skill copies are in scope?

Should this task update only the repo-local `.agents/skills/backlog-draft`, or also check and update the global fallback skill under `/Users/marcopeg/.agents/skills/backlog-draft` if it diverges?

Answer:
only the local, i will copy over when I'm satistfied
