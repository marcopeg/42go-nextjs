# Plan - Validate Translation Requests Against Book Page Content

## Goal

Harden `/api/lingocafe/translate` so valid cache hits still return fast, while uncached provider calls only happen for one bounded sentence that belongs to the requested LingoCafe book page.

## Milestones

### Milestone 1 - Backend Validation Boundaries

- [x] Step 1 - Add request limit and sentence-shape helpers
  - Achieve: Centralize cheap formal validation before any cache, page, or provider work.
  - Create: Small helper functions in the translation backend module or a focused sibling helper if that keeps `route.ts` readable.
  - Modify: `src/app/api/(lingocafe)/lingocafe/translate/route.ts`, possibly `src/app/api/(lingocafe)/lingocafe/_lib/translation.ts`.
  - Delete: n/a.
  - Touch points: `LC_MAX_TRANSLATE_LENGTH`, default 500, required `bookId` and `pageId`, strict v1 full-stop guard, Zod payload schema.
  - Validation: Direct code checks or focused smoke calls prove malformed payloads, oversized text, and internal full stops return `400` before page loading or provider use.
  - Notes: Kept `pageId`; added `LC_MAX_TRANSLATE_LENGTH` with default 500 and an unsafe upper guard of 5000; added strict v1 internal-full-stop rejection.

- [x] Step 2 - Keep error semantics explicit
  - Achieve: Make route responses match the refined task: `400` validation, `403` permission, `404` unknown book/page, provider errors unchanged where appropriate.
  - Create: n/a.
  - Modify: `src/app/api/(lingocafe)/lingocafe/translate/route.ts`.
  - Delete: n/a.
  - Touch points: `json()`, `TranslationProviderError`, `LC_ENABLE_TRANSLATE`, no-store response shape.
  - Validation: Route branches are easy to inspect and smoke; client-safe messages do not leak page content.
  - Notes: Preserved the current successful response shape; validation errors return client-safe field issues. Added uncached language validation errors for profile and book-language mismatches. Replaced the user feature flag gate with `LC_ENABLE_TRANSLATE=true`.

### Milestone 2 - Cache Flow Split

- [x] Step 1 - Expose cache lookup separately from provider translation
  - Achieve: Allow the route to check memory/database caches after cheap validation and before page-membership validation.
  - Create: Exported cache lookup helper if needed, such as `getCachedTranslation()`.
  - Modify: `src/app/api/(lingocafe)/lingocafe/_lib/translation.ts`.
  - Delete: n/a.
  - Touch points: `buildTranslationHash`, `getMemoryCache`, `getDatabaseCache`, `setMemoryCache`, `TranslationResult`, `TranslationInput`.
  - Validation: Existing `translateText()` behavior remains compatible, and the route can distinguish cache hit vs miss without calling Google.
  - Notes: Added `getCachedTranslation()` and kept hash basis unchanged: normalized `text`, `from`, and `to`.

- [x] Step 2 - Add provider-only translation path for validated misses
  - Achieve: After page membership passes, call the upstream provider and persist the result without repeating cache lookup logic in the route.
  - Create: Exported helper if needed, such as `translateAndCacheText()`.
  - Modify: `src/app/api/(lingocafe)/lingocafe/_lib/translation.ts`, `src/app/api/(lingocafe)/lingocafe/translate/route.ts`.
  - Delete: n/a.
  - Touch points: Google provider factory, database cache upsert, server memory cache set, normalized languages, provider config error mapping.
  - Validation: Provider is not invoked for cache hits or invalid uncached requests; valid uncached requests still store database and memory cache entries.
  - Notes: Added `translateAndCacheText()` and kept `translateText()` as a compatibility wrapper.

### Milestone 3 - Source Page Membership Check

- [x] Step 1 - Load and validate the requested page on uncached misses
  - Achieve: Ensure uncached translations prove the requested text belongs to the requested `bookId` and `pageId`.
  - Create: Page membership helper if useful, such as `isTranslationTextInBookPage()`.
  - Modify: `src/app/api/(lingocafe)/lingocafe/translate/route.ts`, possibly `src/app/api/(lingocafe)/lingocafe/_lib/reader.ts` or a new backend-only helper under `_lib`.
  - Delete: n/a.
  - Touch points: `loadBookPage(bookId, pageId)`, page title, summary, content, `bookPage.page`.
  - Validation: Unknown book/page returns `404`; uncached text absent from the page returns `400`; neither path calls the provider or writes cache rows.
  - Notes: The source check runs only after cheap validation, cache miss, and profile language validation; unknown book/page returns `404`. The requested `from` language must also match the loaded book language before provider use.

- [x] Step 2 - Implement loose normalized text matching
  - Achieve: Match normal reader sentence text against stored title, summary, and markdown body without requiring perfect rendered-text reconstruction.
  - Create: Focused normalization helper if useful.
  - Modify: Backend helper chosen in Step 1.
  - Delete: n/a.
  - Touch points: Unicode normalization, whitespace collapse, markdown marker stripping or neutralization, simple punctuation normalization.
  - Validation: Valid examples from title, summary, and body match; harmless markdown/spacing differences do not reject normal reader sentences.
  - Notes: Matching now normalizes links, markdown markers, whitespace, curly quotes, and dash variants without adding a natural-language parser.

### Milestone 4 - Verification And QA

- [x] Step 1 - Add focused validation coverage
  - Achieve: Cover the security behavior without requiring a live Google API key.
  - Create: Small repo-local check script only if there is no existing practical test harness for these server helpers.
  - Modify: n/a unless adding exported pure helpers makes direct checks cleaner.
  - Delete: n/a.
  - Touch points: Max length, multi-sentence/internal full-stop rejection, missing identifiers, unknown page, text not found, cache hit behavior, valid uncached helper flow.
  - Validation: Checks prove invalid paths do not invoke the provider and cache hits do not load the page.
  - Notes: Used local authenticated curl smoke checks through `Host: lc42go.ngrok.app`; covered unauthenticated `401`, no-permission `403`, internal-full-stop `400`, unknown page `404`, text-not-found `400`, valid provider translation, memory cache short-circuit with a bogus page, uncached profile-language mismatch, and book-language mismatch.

- [x] Step 2 - Run repository QA and practical smoke checks
  - Achieve: Confirm the implementation compiles, lint passes, and core API behavior still works.
  - Create: n/a.
  - Modify: n/a.
  - Delete: n/a.
  - Touch points: `npm run qa`, unauthenticated `/api/lingocafe/translate`, optional authenticated LingoCafe reader/API smoke if the dev environment is running.
  - Validation: `npm run qa` passes; smoke checks confirm 401 unauthenticated behavior and validation branches where practical.
  - Notes: `npm run lint`, `npm run qa`, and `git diff --check` passed before the follow-up language gate; `npm run lint` and `npm run qa` passed again after it. Runtime upstream translation worked in the local environment for the smoke sentence.

### Milestone 5 - Global Translation Enablement Switch

- [x] Step 1 - Replace user flag runtime gate with env switch
  - Achieve: Control LingoCafe translation as a product-level capability instead of per-user `feature_flags.translate`.
  - Create: n/a.
  - Modify: `src/app/api/(lingocafe)/lingocafe/_lib/translation.ts`, `src/app/api/(lingocafe)/lingocafe/translate/route.ts`, `src/app/api/(lingocafe)/lingocafe/books/[bookId]/pages/[pageId]/route.ts`.
  - Delete: n/a.
  - Touch points: `LC_ENABLE_TRANSLATE`, reader payload `translation.enabled`, backend `403` when disabled.
  - Validation: `npm run lint`, `npm run qa`, and smoke checks for disabled/enabled route behavior.
  - Notes: `LC_ENABLE_TRANSLATE` defaults off unless set exactly to `true`.

- [x] Step 2 - Document env and cleanup SQL
  - Achieve: Document the new env var and provide manual SQL for removing stale `translate: true` user feature flags without changing other flags.
  - Create: n/a.
  - Modify: `.env.example`, `AL66.notes.md`.
  - Delete: n/a.
  - Touch points: LingoCafe translation env docs, `auth.users.feature_flags` cleanup SQL.
  - Validation: Manual SQL only targets rows where `feature_flags.translate` is currently `true`.
  - Notes: The cleanup SQL is documented in the task notes instead of executed.
