---
taskId: ZE11
createdAt: 2026-04-27T16:51:20+02:00
updatedAt: 2026-04-27T17:00:51+02:00
---

# Plan — Book Page Read UI

## Goal

Implement the LingoCafe read-page route at `/books/[bookId]/[pageId]`, including a protected page-content API, server-side `page-open` tracking and progress creation, client-side scroll tracking with 10-second last-position reporting, limited markdown rendering, bottom previous/next navigation, and desktop/mobile presentation compatible with the completed book-detail flow.

## Milestones

### Milestone 1 — Extend Reader API Helpers

- [x] Step 1 — Add page row and response types
  - Achieve: Model the book page payload needed by the read UI, including current page data, book summary fields for headers/back links, and neighboring page metadata.
  - Create: n/a
  - Modify: `src/app/api/(lingocafe)/lingocafe/_lib/reader.ts`, `src/app/(app)/(lingocafe)/books/_components/book-types.ts`
  - Delete: n/a
  - Touch points: New server-side types such as `BookPageDetailRow`, `BookPageNeighbor`, `BookPageDetail`; client types such as `ReaderBookPage`, `ReaderBookPageNeighbor`, `ReaderBookPageResponse`.
  - Validation: TypeScript can represent page `bookId`, `pageId`, `position`, `kind`, `prefix`, `title`, `summary`, `content`, and optional previous/next links without weakening existing `ReaderBookInfo` types.
  - Notes: Keep existing book list/detail response shapes stable for `IL68` and `PR58`.

- [x] Step 2 — Load one book page with neighbors
  - Achieve: Add a helper that validates the requested `bookId` and `pageId`, loads the current page from `lingocafe.books_pages`, and finds previous/next pages by `position`.
  - Create: n/a
  - Modify: `src/app/api/(lingocafe)/lingocafe/_lib/reader.ts`
  - Delete: n/a
  - Touch points: `getDB`, `lingocafe.books`, `lingocafe.books_pages`, `buildReadPageHref`, helper such as `loadBookPage(bookId, pageId, userId)`.
  - Validation: Unknown book or page returns `null`; valid `nils-holgersson-sv-a2/p02` returns page content plus previous `p01` and next `p03`; first/last pages have only the available neighbor.
  - Notes: Reuse `/books/<bookId>/<pageId>` href construction so next/previous links match `PR58`.

- [x] Step 3 — Upsert saved progress
  - Achieve: Add a reusable progress upsert helper that writes `lingocafe.books_progress` for the authenticated user and clamps `progress_bps` to `0..10000`.
  - Create: n/a
  - Modify: `src/app/api/(lingocafe)/lingocafe/_lib/reader.ts`
  - Delete: n/a
  - Touch points: `books_progress` unique key on `(user_id, book_id)`, composite page FK `(book_id, page_id)`, helper such as `saveBookProgress({ userId, bookId, pageId, progressBps })`.
  - Validation: Upsert creates a row when missing, updates `page_id`, `progress_bps`, and `updated_at` when present, and never accepts a client-supplied user id.
  - Notes: Use Knex `insert(...).onConflict(["user_id", "book_id"]).merge(...)`.

### Milestone 2 — Add Protected Page APIs

- [x] Step 1 — Add page detail API
  - Achieve: Expose `GET /api/lingocafe/books/[bookId]/pages/[pageId]` to load page content, record `page-open`, and create/update saved progress at `progress_bps = 0`.
  - Create: `src/app/api/(lingocafe)/lingocafe/books/[bookId]/pages/[pageId]/route.ts`
  - Modify: `src/app/api/(lingocafe)/lingocafe/_lib/reader.ts`
  - Delete: n/a
  - Touch points: `protectRoute`, `getSessionUserId`, `json`, `loadBookPage`, `trackReaderEvent`, `saveBookProgress`, policy `{ feature: "api:lingocafe", session: true }`.
  - Validation: Unauthenticated requests return 401, unknown pages return 404, valid requests return page payload, insert a `page-open` event, and save progress with `progress_bps = 0`.
  - Notes: Track `page-open` only after the requested page exists.

- [x] Step 2 — Add scroll event/progress API
  - Achieve: Expose a protected endpoint that accepts the current page scroll payload, validates `progress_bps`, records `page-scroll`, and updates saved progress.
  - Create: Either `src/app/api/(lingocafe)/lingocafe/books/[bookId]/pages/[pageId]/scroll/route.ts` or a `POST` handler in the page route if cleaner.
  - Modify: `src/app/api/(lingocafe)/lingocafe/_lib/reader.ts`
  - Delete: n/a
  - Touch points: `zod` payload parser, `progress_bps` number validation, `trackReaderEvent`, `saveBookProgress`, authenticated session user.
  - Validation: Invalid JSON or out-of-range progress returns 400; unknown page returns 404; valid payload writes one `page-scroll` event and updates `books_progress`.
  - Notes: The client batching prevents event floods, but the server still validates every request like it owes Chuck Norris money.

- [x] Step 3 — Keep existing APIs compatible
  - Achieve: Ensure the completed `/api/lingocafe/books/[bookId]` detail API and `readingAction.href` behavior from `PR58` remain unchanged except for progress rows naturally changing after read activity.
  - Create: n/a
  - Modify: `src/app/api/(lingocafe)/lingocafe/_lib/reader.ts` only if shared helpers require small refactors.
  - Delete: n/a
  - Touch points: `loadBookInfo`, `createReadingAction`, `buildReadPageHref`.
  - Validation: A saved progress row points detail action to `/books/<bookId>/<pageId>?progress_bps=<value>`; no-pages behavior remains `No pages available`.
  - Notes: Avoid moving read action UI into this milestone.

### Milestone 3 — Build Read Route and State Logic

- [x] Step 1 — Add `/books/[bookId]/[pageId]` client route
  - Achieve: Create the authenticated App Router page that loads the page API browser-side and wraps the experience in `AppLayout`.
  - Create: `src/app/(app)/(lingocafe)/books/[bookId]/[pageId]/page.tsx`
  - Modify: n/a
  - Delete: n/a
  - Touch points: `"use client"`, `useParams`, `useSearchParams`, `useSession`, `AppLayout`, `fetch(..., { credentials: "same-origin", cache: "no-store" })`, policy `{ feature: "page:books", session: true }`.
  - Validation: Refreshing `/books/nils-holgersson-sv-a2/p02` reloads page `p02`; unauthenticated state follows the existing app layout policy behavior; API errors render clear non-crashing states.
  - Notes: Use `backBtn={{ to: "/books/[bookId]" }}` equivalent with the concrete encoded book id.

- [x] Step 2 — Normalize route/API payload
  - Achieve: Add client-side normalization and fallback handling for the page payload without trusting malformed API data.
  - Create: n/a
  - Modify: `src/app/(app)/(lingocafe)/books/[bookId]/[pageId]/page.tsx`, `src/app/(app)/(lingocafe)/books/_components/book-types.ts`
  - Delete: n/a
  - Touch points: `ReaderBookPage`, `ReaderBookPageNeighbor`, fallback empty `content`, fallback title, nullable previous/next neighbors.
  - Validation: Missing optional fields do not crash; missing required page data produces an error state; first and last pages handle `null` neighbors.
  - Notes: Keep route-local normalization if it is only used here.

- [x] Step 3 — Restore initial scroll from `progress_bps`
  - Achieve: Parse `progress_bps` from the URL, clamp it to `0..10000`, and scroll the reader container to the requested vertical percentage after content is rendered.
  - Create: n/a
  - Modify: `src/app/(app)/(lingocafe)/books/[bookId]/[pageId]/page.tsx` or a route-local hook such as `useReaderProgress.ts`
  - Delete: n/a
  - Touch points: `useSearchParams`, `useEffect`, scroll container ref, `scrollHeight`, `clientHeight`, initial restoration guard.
  - Validation: `/books/nils-holgersson-sv-a2/p02?progress_bps=4200` starts around 42% of scrollable content; missing or invalid values start at top.
  - Notes: Avoid repeated scroll restoration after the user starts scrolling or after state updates.

- [x] Step 4 — Track scroll with 10-second last-position reporting
  - Achieve: Observe scroll activity, compute current `progress_bps`, and send only the last observed value after the user stops or at the end of a 10-second collection window.
  - Create: Optional route-local hook such as `src/app/(app)/(lingocafe)/books/[bookId]/[pageId]/useReaderScrollTracking.ts`
  - Modify: `src/app/(app)/(lingocafe)/books/[bookId]/[pageId]/page.tsx`
  - Delete: n/a
  - Touch points: scroll listener, timeout cleanup, `AbortController`, POST to scroll endpoint, `credentials: "same-origin"`, final flush on unmount where practical.
  - Validation: Continuous scrolling produces at most one POST per 10-second window and sends the latest `progress_bps`; unmount cleanup does not leave active timers.
  - Notes: Keep failure silent or low-noise in UI; reading should not be blocked by event telemetry failure.

### Milestone 4 — Build Reader Presentation

- [x] Step 1 — Create reader content components
  - Achieve: Render book/page metadata and markdown body in a focused reader surface that feels related to book detail but optimized for reading.
  - Create: `src/app/(app)/(lingocafe)/books/_components/BookPageReader.tsx`, optionally `BookPageMarkdown.tsx`
  - Modify: `src/app/(app)/(lingocafe)/books/_components/book-types.ts`
  - Delete: n/a
  - Touch points: restricted use of `ReactMarkdown` or existing `Markdown` component with overrides, components for `h1`, `h2`, `h3`, `p`, `strong`, `em`, and disabled/neutral renderers for unsupported HTML-heavy features.
  - Validation: Paragraphs, italic, bold, and titles render; arbitrary HTML is not executed or rendered unsafely.
  - Notes: Prefer reusing `react-markdown` and `rehype-sanitize`; keep the allowed surface narrow.

- [x] Step 2 — Add bottom previous/next controls
  - Achieve: Render previous and next buttons at the end of page content only when neighbor pages exist.
  - Create: Optional `BookPageNav.tsx`
  - Modify: `BookPageReader.tsx`
  - Delete: n/a
  - Touch points: `Button`, `Link`, `ChevronLeft`, `ChevronRight`, neighbor `href`, labels from neighbor title/prefix.
  - Validation: `p01` shows next only, middle pages show both, last page shows previous only; no broken links render.
  - Notes: Navigation should use clean page URLs without carrying the old page's `progress_bps`.

- [x] Step 3 — Match desktop and mobile presentation requirements
  - Achieve: Desktop renders as an internal app page with explicit back-to-book affordance; mobile renders fixed full-screen above the book detail layer.
  - Create: Optional `MobileBookPageReader.tsx` if the route page becomes dense.
  - Modify: `src/app/(app)/(lingocafe)/books/[bookId]/[pageId]/page.tsx`, reader components
  - Delete: n/a
  - Touch points: `AppLayout` title/subtitle/back button, `hideMobileMenu`, mobile fixed overlay z-index higher than current book detail `z-[500]`, scroll container sizing with `100dvh`.
  - Validation: Desktop shows normal app chrome and back link to `/books/[bookId]`; mobile read surface sits above detail and the back link returns to the book detail URL.
  - Notes: Use a z-index above the existing mobile detail popup, e.g. `z-[600]`, and keep text/layout from overlapping at small widths.

### Milestone 5 — Validation and Smokes

- [x] Step 1 — Run static quality gate
  - Achieve: Verify lint and production build after implementation.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `npm run qa`
  - Validation: Command exits successfully.
  - Notes: `npm run qa` passed. Existing Turbopack NFT and static `next/headers` warnings still appear.

- [ ] Step 2 — API smoke checks
  - Achieve: Verify route protection, valid page loading, event writes, and progress writes against seeded LingoCafe data.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `/api/lingocafe/books/nils-holgersson-sv-a2/pages/p02`, scroll endpoint, `lingocafe.events`, `lingocafe.books_progress`.
  - Validation: Unauthenticated API returns 401; authenticated page open returns payload and writes `page-open` plus progress `p02/0`; scroll POST writes `page-scroll` plus progress such as `p02/4200`.
  - Notes: Unauthenticated curl returned 401 for the page API. Authenticated event/progress database smoke was not run in this session.

- [ ] Step 3 — Browser smoke checks
  - Achieve: Verify desktop and mobile read UI behavior in the running app.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `/books/nils-holgersson-sv-a2/p02?progress_bps=4200`, `/books/little-prince-en-a2/p01`, desktop viewport, mobile viewport.
  - Validation: Refresh preserves page; initial scroll restores; previous/next controls work; mobile back reveals `/books/[bookId]`; markdown renders correctly and unsafe HTML is inert.
  - Notes: Browser smoke was not run in this session.
