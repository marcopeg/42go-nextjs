---
taskId: UT41
createdAt: 2026-04-21T17:37:10+02:00
updatedAt: 2026-04-23T15:27:45+02:00
---

# Plan — ESLint 9 → 10

**TaskID**: UT41
**Status**: in-progress — paused; do not resume before 2026-07-15

## Goal

Upgrade ESLint from 9.x to 10.x with flat-config compatibility preserved, resolve any new lint findings in code (no new suppressions), and validate with lint/build plus smoke checks and warning-delta reporting.

Execution pause note: a local ESLint 10 + `@eslint/compat` path was validated and then reverted by operator decision. Resume only when native upstream support looks ready or when the not-before date is reached.

## Milestones

### Milestone 1 — Baseline and Upgrade

- [x] Step 1 — Capture pre-upgrade lint/build baseline
  - Achieve: Record a reliable before-state for lint output, build output, and warning profile to compare after ESLint 10.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: package.json scripts (`lint`, `qa`), eslint CLI output, Next.js build output.
  - Validation: Run `yarn lint` and `yarn qa`; capture warning/error counts and key warning signatures.
  - Notes: Baseline comparison is mandatory for UT41 acceptance (`lint/warning baseline changes are captured`).

- [x] Step 2 — Bump ESLint major and install dependencies
  - Achieve: Move `eslint` from `^9.39.4` to `^10` and refresh lockfile/install state.
  - Create: n/a
  - Modify: package.json, yarn.lock or package-lock.json (whichever is project-authoritative in current branch).
  - Delete: n/a
  - Touch points: dependency graph rooted at `eslint`, transitive compatibility with `eslint-config-next` (`16.2.4`).
  - Validation: Successful dependency installation with no unresolved peer/dependency failures.
  - Notes: Initial direct bump failed due to older transitive plugin APIs in `eslint-config-next`. Second attempt succeeded by adding `@eslint/compat` and wrapping the imported Next flat configs with `fixupConfigRules()`, allowing `eslint@10.2.1` to install and run while upstream plugins remain behind on peer range declarations.

### Milestone 2 — Config and Code Compliance

- [x] Step 3 — Confirm flat-config compatibility on ESLint 10
  - Achieve: Ensure `eslint.config.mjs` remains valid and discovered correctly by ESLint 10.
  - Create: n/a
  - Modify: eslint.config.mjs (only if needed for ESLint 10 compatibility).
  - Delete: n/a
  - Touch points: `eslint-config-next/core-web-vitals`, `eslint-config-next/typescript`, `ignores` array in eslint config.
  - Validation: `yarn lint` runs using flat config without config-resolution errors.
  - Notes: Do not introduce broad disables; keep intent of existing ruleset.

- [x] Step 4 — Resolve new ESLint 10 findings in source code
  - Achieve: Fix all newly surfaced violations by changing code, not by adding suppression comments or relaxing rules globally.
  - Create: n/a
  - Modify: only files directly flagged by lint (likely in `src/**`, `knex/**`, or `scripts/**` depending on findings).
  - Delete: n/a
  - Touch points: flagged symbols/functions/components per lint output; possible React/TS patterns affected by updated recommended rules.
  - Validation: `yarn lint` passes with zero errors and no new suppression directives introduced.
  - Notes: If a targeted rule adjustment is absolutely required, pause and request operator approval first (current policy is code-fix only).

### Milestone 3 — Full Verification and Reporting

- [x] Step 5 — Run QA and verify no regressions
  - Achieve: Confirm full project quality gate passes after lint changes.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: Next.js build pipeline, App Router compilation, policy-protected pages referenced by app routes.
  - Validation: `yarn qa` completes successfully.
  - Notes: Track any warning shifts versus baseline and classify as expected/unexpected.

- [ ] Step 6 — Execute smoke checks per app and report deltas
  - Achieve: Validate runtime startup and one protected page per app (`default`, `quicklist`, `notes`, `calendar`) and summarize lint/build warning deltas.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: running dev server via `make app.start`, routes under `src/app/(app)/**/page.tsx`, policy flow via app layout protections.
  - Validation: App starts; each selected protected page responds successfully under expected host/app context; delta summary produced.
  - Notes: Use Host-header or configured matcher approach consistent with prior dependency-upgrade validation. This step is deferred while UT41 is paused.
