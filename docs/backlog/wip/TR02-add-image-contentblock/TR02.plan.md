# Plan — Add Image ContentBlock

## Goal

Add a server-only `image` ContentBlock for public composable pages, backed by strict Next.js `Image` dimensions, reusable markdown side content, generic remote image allowlisting, default-page examples, and public/agent documentation.

## Milestones

### Milestone 1 — Confirm Integration Surface

- [x] Step 1 — Reconfirm ContentBlock registry and renderer behavior
  - Achieve: Verify the implementation hooks into the current server ContentBlock only.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `src/42go/components/ContentBlock/server.tsx`, `src/42go/components/ContentBlock/render-component.ts`, `src/42go/components/ContentBlock/client.tsx`.
  - Validation: Source inspection confirms public pages use server `ContentBlock`; client renderer remains limited to toolbar-safe blocks.
  - Notes: Do not add `image` to `ContentBlock/client.tsx`.

- [x] Step 2 — Reconfirm markdown and animation helpers
  - Achieve: Identify the smallest reuse path for side markdown and existing scroll reveal behavior.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `src/42go/components/ContentBlock/blocks/MarkdownBlock.tsx`, `src/42go/components/Markdown`, `src/components/ui/scroll-animation.tsx`.
  - Validation: Implementation plan preserves `source`/`path` semantics and only uses `none`, `fade`, `scale`, `slideUp`.
  - Notes: If direct `MarkdownBlock` reuse creates unwanted outer spacing, extract or locally reuse only the file-read/content-render pattern.

### Milestone 2 — Generalize Remote Image Configuration

- [x] Step 1 — Replace LingoCafe-specific remote image config
  - Achieve: Make `next.config.ts` accept a generic environment-driven list of Next.js image remote patterns instead of `LC_ASSETS_BASE_PATH`.
  - Create: n/a
  - Modify: `next.config.ts`.
  - Delete: n/a
  - Touch points: `NextConfig.images.remotePatterns`, current `getLingoCafeAssetsRemotePattern()`.
  - Validation: `next.config.ts` builds a valid `remotePatterns` array from the configured list and handles empty/invalid entries predictably.
  - Notes: Prefer a clear name such as `CONTENT_IMAGE_REMOTE_PATTERNS`; document the chosen name in the same milestone.

- [x] Step 2 — Update environment documentation
  - Achieve: Document the new generic remote image allowlist and remove the old LingoCafe-only variable guidance.
  - Create: n/a
  - Modify: `.env.example`.
  - Delete: n/a
  - Touch points: old `LC_ASSETS_BASE_PATH` section, new content image remote pattern examples.
  - Validation: `.env.example` shows how to include `https://assets.lingocafe.app/**` explicitly for LingoCafe deployments.
  - Notes: Keep wording clear that remote image hosts are explicit allowlist entries, not automatic arbitrary external loading.

- [x] Step 3 — Preserve LingoCafe asset URL generation
  - Achieve: Ensure LingoCafe book-cover URL construction still has its own asset base path if runtime code still needs it, while Next image allowlisting becomes generic.
  - Create: n/a
  - Modify: `src/app/api/(lingocafe)/lingocafe/_lib/reader.ts` only if needed after inspecting the current `LC_ASSETS_BASE_PATH` dependency.
  - Delete: n/a
  - Touch points: `LC_ASSETS_BASE_PATH`, `assets.lingocafe.app`, book cover URL helper.
  - Validation: Existing LingoCafe cover URLs remain deterministic; Next image remote allowlisting no longer depends on the LingoCafe URL helper.
  - Notes: Do not break current LingoCafe cover behavior while removing the LingoCafe-specific Next config coupling.

### Milestone 3 — Implement ImageBlock

- [x] Step 1 — Add typed block component
  - Achieve: Create the server-only `ImageBlock` with the final API and runtime-safe defaults.
  - Create: `src/42go/components/ContentBlock/blocks/ImageBlock.tsx`.
  - Modify: n/a
  - Delete: n/a
  - Touch points: `next/image`, `ScrollAnimation`, markdown rendering utilities, `cn` utility.
  - Validation: TypeScript requires `image.src`, `image.alt`, `image.width`, and `image.height`; `sizes` remains optional.
  - Notes: Prefer explicit exported types. Use arrow functions where practical, but match local server block conventions if needed.

- [x] Step 2 — Implement layout alignments
  - Achieve: Support `left`, `right`, `top`, and `bottom` image alignments with stable responsive behavior.
  - Create: n/a
  - Modify: `src/42go/components/ContentBlock/blocks/ImageBlock.tsx`.
  - Delete: n/a
  - Touch points: Tailwind layout class maps for desktop and mobile.
  - Validation: Desktop alignments place image relative to content as configured; mobile maps `left` and `right` to image above content.
  - Notes: Keep section styling aligned with existing public page blocks and avoid nested card-style layout.

- [x] Step 3 — Implement optional markdown side content
  - Achieve: Render optional side content from either `content.source` or `content.path`, never both.
  - Create: n/a
  - Modify: `src/42go/components/ContentBlock/blocks/ImageBlock.tsx`.
  - Delete: n/a
  - Touch points: `MarkdownBlock` behavior, file path resolution from `process.cwd()`, missing-file warning behavior.
  - Validation: Inline markdown renders; file-backed markdown renders; missing file reports a visible warning instead of throwing.
  - Notes: Keep type and runtime checks aligned so misconfigured content fails visibly and politely.

- [x] Step 4 — Implement animation wrappers
  - Achieve: Apply `imageAnimation` and `contentAnimation` only when not `"none"`, defaulting both to `"none"`.
  - Create: n/a
  - Modify: `src/42go/components/ContentBlock/blocks/ImageBlock.tsx`.
  - Delete: n/a
  - Touch points: `src/components/ui/scroll-animation.tsx`.
  - Validation: Omitted animation props do not wrap or animate; configured `fade`, `scale`, and `slideUp` use existing `ScrollAnimation`.
  - Notes: Do not extend `ScrollAnimation`.

- [x] Step 5 — Register block in server renderer
  - Achieve: Add `image` to the server ContentBlock union and blocks map.
  - Create: n/a
  - Modify: `src/42go/components/ContentBlock/server.tsx`.
  - Delete: n/a
  - Touch points: `ContentBlockItem`, `blocksMap`.
  - Validation: Public page config can include `{ type: "image" }` without TypeScript errors; client ContentBlock remains unchanged.
  - Notes: This is the main integration point.

### Milestone 4 — Add Examples And Assets

- [x] Step 1 — Add committed local demo image
  - Achieve: Provide a browser-renderable local image asset for default page examples.
  - Create: `public/images/content-blocks/` and a demo image file inside it.
  - Modify: n/a
  - Delete: n/a
  - Touch points: `public/images/**`.
  - Validation: The image path is reachable from the browser as `/images/content-blocks/...`.
  - Notes: Use a small, inspectable asset suitable for docs/examples; do not commit oversized media.

- [x] Step 2 — Add default home page image examples
  - Achieve: Demonstrate the image block with multiple examples stacked one after another.
  - Create: n/a
  - Modify: `src/config/home-page.tsx`.
  - Delete: n/a
  - Touch points: `HomePage.items`.
  - Validation: Examples compile, include local and remote image cases, include `sizes`, and show at least two layout alignments.
  - Notes: Remote example must use a URL allowed by the new generic remote pattern config.

### Milestone 5 — Document Public And Agent Usage

- [x] Step 1 — Add public image-block documentation
  - Achieve: Document the `image` block API, `sizes`, dimensions, content source/path, alignments, animations, local images, and remote image allowlisting.
  - Create: `contents/default/docs/composable-pages/image-block/README.md`.
  - Modify: `contents/default/docs/composable-pages/README.md`.
  - Delete: n/a
  - Touch points: composable pages docs index and block docs.
  - Validation: Docs include copy-pasteable config examples that match the implemented types.
  - Notes: Be explicit that `width` and `height` are required for config string URLs.

- [x] Step 2 — Update 42go-pages skill reference
  - Achieve: Teach future agents how to choose and configure the image block.
  - Create: `.agents/skills/42go-pages/references/core-image.md`.
  - Modify: `.agents/skills/42go-pages/SKILL.md`, `.agents/skills/42go-pages/references/core-blocks-index.md`.
  - Delete: n/a
  - Touch points: block index table and focused image reference.
  - Validation: Skill reference lists `image` as a registered server block and documents dimensions, `sizes`, source/path, animation, and remote allowlist constraints.
  - Notes: Keep the skill concise; do not duplicate the full public docs.

### Milestone 6 — Validate And Smoke Check

- [x] Step 1 — Run static quality gate
  - Achieve: Catch type, lint, and production build regressions.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: package scripts.
  - Validation: Run `npm run qa`.
  - Notes: If the repo splits lint/build/typecheck internally, `npm run qa` is the required umbrella gate.

- [x] Step 2 — Smoke test default public page
  - Achieve: Confirm the new block renders in the default home page without broken local or remote images.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: default app public home page, static asset URL, remote image URL.
  - Validation: Use the running dev server or `make app.start` if needed; inspect `/` for image layout, markdown content, and no obvious console/render errors.
  - Notes: AppConfig changes may require server restart.

- [x] Step 3 — Smoke test docs page
  - Achieve: Confirm new public documentation is navigable and renders markdown correctly.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `/docs/composable-pages/image-block` or the generated docs route for the new file.
  - Validation: Browser or HTTP check confirms the docs route renders.
  - Notes: The operator will do the final browser visual review, but implementation should still catch broken routes.
