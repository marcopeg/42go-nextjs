---
taskId: JM97
createdAt: 2026-05-09T08:49:03+02:00
updatedAt: 2026-05-09T10:54:38+02:00
---

# Plan — Add Live Reader Translation With Multi-Layer Cache

## Goal

Add authorized sentence translation to the LingoCafe reader with per-user `auth.users.feature_flags.translate` gating, Google Cloud Translation Basic v2 behind a provider abstraction, persistent/server/client caches, sentence highlighting, and a manual production patch.

## Milestones

### Milestone 1 — Schema, Seeds, And Feature-Flag Foundation

- [x] Step 1 — Add `auth.users.feature_flags`
  - Achieve: Extend auth-user storage with a nullable `feature_flags` JSONB document used for user-level capabilities.
  - Create: `docs/backlog/wip/JM97-add-live-reader-translation-with-multi-layer-cache/migrate.sql`.
  - Modify: `knex/migrations/20240320_auth.js`.
  - Delete: n/a.
  - Touch points: `auth.users`, nullable JSONB defaults, manual production SQL patch with verification queries.
  - Validation: Local schema rebuild path includes the column; `migrate.sql` adds the same column for deployed databases without touching profile/consent.
  - Notes: Added `feature_flags jsonb default null` to the auth migration and the manual `migrate.sql` patch.

- [x] Step 2 — Add persistent translation-cache schema
  - Achieve: Add a LingoCafe translation cache table keyed by hash and storing source/target languages, original text, translation, and timestamps needed for lookup/debugging.
  - Create: n/a.
  - Modify: `knex/migrations/20260427150000_lingocafe_schema.js`, `docs/backlog/wip/JM97-add-live-reader-translation-with-multi-layer-cache/migrate.sql`.
  - Delete: n/a.
  - Touch points: `lingocafe.translation_cache` or equivalent table, primary key `hash`, columns `from`, `to`, `text`, `translation`, `created_at`, `updated_at`, `last_used_at`.
  - Validation: Migration creates/drops the table cleanly; manual patch includes the production add-table SQL.
  - Notes: Added `lingocafe.translation_cache` with `hash`, quoted `from`/`to`, source `text`, `translation`, and timestamps.

- [x] Step 3 — Seed local translation capability
  - Achieve: Enable translation only for the existing complete LingoCafe test user so local verification can exercise the feature.
  - Create: n/a.
  - Modify: `knex/seeds/20260427151000.lingocafe.data.js` and possibly `knex/seeds/20260427150500_lingocafe_test_users.js` if upsert merge behavior must preserve feature flags.
  - Delete: n/a.
  - Touch points: Jane Doe LingoCafe seed, `feature_flags: { translate: true }`, no profile-schema changes.
  - Validation: Seeded Jane has `auth.users.feature_flags.translate = true`; users without the key remain unauthorized.
  - Notes: Seeded Jane's `feature_flags.translate = true`; current repo data shows Jane, not John, has the complete LingoCafe profile.

### Milestone 2 — Translation Backend And Cache Stack

- [x] Step 1 — Add translation normalization and hashing utilities
  - Achieve: Define deterministic cache keys from normalized source text, source language, and target language.
  - Create: `src/app/api/(lingocafe)/lingocafe/_lib/translation.ts` or focused sibling modules.
  - Modify: n/a.
  - Delete: n/a.
  - Touch points: Unicode normalization, whitespace normalization, lowercasing where acceptable, language-code normalization, Node `crypto` hashing.
  - Validation: Unit-like direct checks or build-time type checks prove equivalent text inputs produce one hash.
  - Notes: Added server normalization and SHA-256 hashing in the LingoCafe translation helper.

- [x] Step 2 — Add server memory and database cache helpers
  - Achieve: Implement the API-side cache order after the client miss: server memory, database, Google; record last-used data and cache-hit source.
  - Create: Cache helper module under `src/app/api/(lingocafe)/lingocafe/_lib/`.
  - Modify: n/a.
  - Delete: n/a.
  - Touch points: module-level Map cache, size estimation, `LC_TRANSLATION_MEMORY_CACHE_MB` default 20, `lingocafe` table reads/upserts.
  - Validation: Cache helper returns source labels for `memory`, `database`, and `google`; eviction removes oldest/least-recently-used entries when over cap.
  - Notes: Added memory cache, database lookup/update, last-used tracking, and server cache-source labels.

- [x] Step 3 — Add Google provider abstraction
  - Achieve: Call Google Cloud Translation Basic v2 REST with an API key while isolating provider-specific code behind a small interface/factory.
  - Create: Provider module under `src/app/api/(lingocafe)/lingocafe/_lib/`.
  - Modify: n/a.
  - Delete: n/a.
  - Touch points: `LC_GOOGLE_TRANSLATE_API_KEY` or similarly scoped env var, `fetch`, provider interface, explicit errors for missing config.
  - Validation: Missing API key produces a controlled server error; provider response is normalized to a stable translation result type.
  - Notes: Added Google Cloud Translation Basic v2 provider behind a server-side function using `LC_GOOGLE_TRANSLATE_API_KEY`.

- [x] Step 4 — Add protected translation API route
  - Achieve: Expose a LingoCafe API that validates request payloads, checks session, app policy, user feature flags, and then resolves translation through the cache/provider stack.
  - Create: `src/app/api/(lingocafe)/lingocafe/translate/route.ts`.
  - Modify: `src/config/lingocafe/config.ts` if a distinct `api:lingocafe:translate` or equivalent feature flag is chosen.
  - Delete: n/a.
  - Touch points: `protectRoute`, `getSessionUserId`, feature-flag lookup on `auth.users.feature_flags`, Zod payload validation, no-store JSON responses.
  - Validation: Unauthorized users receive 403; invalid payloads receive 400; authorized users receive translated text plus development-safe cache-source diagnostics.
  - Notes: Added `/api/lingocafe/translate` with session, app-policy, user feature-flag, and payload checks.

### Milestone 3 — Reader Sentence Interaction And Client Cache

- [x] Step 1 — Extend reader payload/types with translation capability
  - Achieve: Tell the client whether the current user can translate without exposing unrelated feature flag data.
  - Create: n/a.
  - Modify: `src/app/api/(lingocafe)/lingocafe/_lib/reader.ts`, `src/app/api/(lingocafe)/lingocafe/books/[bookId]/pages/[pageId]/route.ts`, `src/app/(app)/(lingocafe)/books/_components/book-types.ts`, `src/app/(app)/(lingocafe)/books/[bookId]/[pageId]/page.tsx`.
  - Delete: n/a.
  - Touch points: `ReaderBookPage`, `normalizeBookPage`, current user feature-flag helper, hidden UI when disabled.
  - Validation: Users without `feature_flags.translate` do not see or trigger translation UI; Jane can.
  - Notes: Reader page payload now exposes only `translation.enabled/from/to`.

- [x] Step 2 — Tokenize and render clickable sentences
  - Achieve: Wrap sentence text in stable inline targets and highlight the active sentence when selected.
  - Create: Sentence utility or route-local component under `src/app/(app)/(lingocafe)/books/_components/`.
  - Modify: `BookPageReader.tsx`.
  - Delete: n/a.
  - Touch points: restricted ReactMarkdown components, paragraph/heading text children, stop-to-stop sentence splitting, `hasActiveTextSelection()` compatibility, active sentence id/hash.
  - Validation: Clicking/tapping a sentence selects the full sentence, not an arbitrary character subset; existing markdown subset still renders safely.
  - Notes: `BookPageReader` wraps chapter title, summary, and markdown string sentence segments in clickable spans when translation is enabled; translatable spans now show a pointer cursor on hover.

- [x] Step 3 — Add client local-storage cache
  - Achieve: Resolve translations from client storage before calling the API, with the same hash basis and 1 MB cap.
  - Create: `src/app/(app)/(lingocafe)/books/_components/reader-translation-cache.ts`.
  - Modify: Reader page/client components as needed.
  - Delete: n/a.
  - Touch points: localStorage key, last-used timestamps, deterministic size estimate, eviction, shared normalization logic or compatible client copy.
  - Validation: Re-clicking the same sentence can resolve from client cache without network; cache evicts older entries over 1 MB.
  - Notes: Added 1 MB local-storage cache with matching hash normalization and last-used eviction.

- [x] Step 4 — Add popover state and placement
  - Achieve: Display translated text near the selected sentence and choose above/below placement based on viewport space.
  - Create: Translation popover component under reader components.
  - Modify: `BookPageReader.tsx`, `BookReaderSurfaces.tsx`, `books/[bookId]/[pageId]/page.tsx`.
  - Delete: n/a.
  - Touch points: selected sentence rect, loading/error states, outside click close, desktop/mobile layout, reader CSS variables, optional minimal diagnostics.
  - Validation: Popover keeps source sentence and translation visible on desktop/mobile; second outside click closes and clears highlight.
  - Notes: Added reader-article-anchored popover with loading/error/success states, resize anchor sync, an abbreviated absolute-positioned cache-source marker, popover/outside-click close, background-only sentence highlight, and direct sentence-to-sentence switching.

### Milestone 4 — Validation, QA, And Progress Tracking

- [ ] Step 1 — Add focused verification paths
  - Achieve: Verify schema SQL, API authorization, cache behavior, and reader UI behavior with practical local checks.
  - Create: n/a unless a small helper test script is justified.
  - Modify: n/a.
  - Delete: n/a.
  - Touch points: `make migrate`/safe schema inspection if available, API route checks, browser reader smoke, database cache rows.
  - Validation: Unauthorized API fails, authorized request works with configured key or fails safely without it, cache-source diagnostics distinguish layers.
  - Notes: JS syntax checks passed for edited migration/seed files. HTTP smoke passed for LingoCafe `/books`, reader route, and unauthenticated `/api/lingocafe/translate` returning 401. Full browser click smoke is blocked because Playwright has no bundled browser installed and system Chrome aborts under sandbox control.

- [x] Step 2 — Run repository QA
  - Achieve: Confirm lint and production build pass after implementation.
  - Create: n/a.
  - Modify: n/a.
  - Delete: n/a.
  - Touch points: `npm run qa`.
  - Validation: `npm run qa` passes, or failures are documented with exact cause if external config such as a missing Google key blocks only runtime translation.
  - Notes: `npm run qa` passed.
