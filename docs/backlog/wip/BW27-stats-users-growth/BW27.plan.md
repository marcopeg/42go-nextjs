# Plan — Stats Users Growth

## Goal

Add `42go events query users growth`, backed by local Parquet analytics and app-scoped `.local/42go-stats/{app-id}/users/growth/` aggregate caches, so repeated runs update or read user-growth metrics without querying production PostgreSQL.

## Milestones

### Milestone 1 — Map the Existing CLI Surface

- [x] Step 1 — Confirm command tree and extension points
  - Achieve: Identify the cleanest way to add the nested command path `42go events query users growth` without breaking existing `42go events query stats`.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `cli/src/fortytwogo_cli/events/cli.py`, `cli/src/fortytwogo_cli/events/query.py`, `cli/tests/test_cli.py`.
  - Validation: Existing help behavior is understood before edits; no current command names are silently replaced.
  - Notes: Converted `stats` into a Typer sub-app with a callback that preserves bare `42go events query stats`; added nested `query users growth`.

- [x] Step 2 — Confirm event schema assumptions against local Parquet
  - Achieve: Verify real app IDs, timestamp types, event names, consent payload shape, and whether DuckDB JSON functions can extract `data.next.mkt[]` reliably.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `.local/42go-events/events/parquet/*.parquet`, `src/config/lingocafe/config.ts`, `src/42go/profile/server.ts`.
  - Validation: A local DuckDB smoke query confirms app IDs, reading events, consent events, and `next.mkt` evidence extraction.
  - Notes: Confirmed `lingocafe`, 1,352 events, 25 users, reading events, consent events, and `data.next.mkt[]` extraction shape.

### Milestone 2 — Build User Growth Analytics Core

- [x] Step 3 — Add users-growth data model
  - Achieve: Define typed Python structures for app IDs, bucket granularities, metric rows, aggregate metadata, and command options.
  - Create: `cli/src/fortytwogo_cli/events/users_growth.py`.
  - Modify: n/a
  - Delete: n/a
  - Touch points: existing `EventStats` style in `cli/src/fortytwogo_cli/events/query.py`.
  - Validation: Unit-level imports and simple constructors work without requiring local Parquet files.
  - Notes: Added `GrowthMetricRow`, app result, source fingerprint, output format, constants, and result serialization in `users_growth.py`.

- [x] Step 4 — Implement bucket calendar helpers
  - Achieve: Generate day, ISO-week, month, and year bucket boundaries in `Europe/Rome`, with labels suitable for output and aggregate rows.
  - Create: helper functions in `cli/src/fortytwogo_cli/events/users_growth.py` or a small dedicated module if cleaner.
  - Modify: n/a
  - Delete: n/a
  - Touch points: `pytz` dependency already used by the CLI, DuckDB timestamp output behavior.
  - Validation: Focused tests prove day, ISO Monday week, month, and year labels around sample dates.
  - Notes: Implemented day, ISO-week, month, and year buckets in `Europe/Rome`; current partial buckets use the latest available event time for rolling-window metrics.

- [x] Step 5 — Compute base user timelines
  - Achieve: Compute known users over time per app and bucket from first observed user-scoped event timestamps.
  - Create: DuckDB query helpers in `users_growth.py`.
  - Modify: n/a
  - Delete: n/a
  - Touch points: `read_parquet(?)`, `app_id`, `user_id`, `event_at`, all user-scoped rows.
  - Validation: A fixture with users first seen on different dates produces cumulative known-user counts by day/week/month/year.
  - Notes: Uses first observed user-scoped event per app and excludes null `user_id` values.

- [x] Step 6 — Compute subscription timelines
  - Achieve: Compute subscribed users over time per app and bucket from `user.consent.created` and `user.consent.updated`, using the latest `data.next.mkt[]` evidence entry by `changedAt` at each data point.
  - Create: consent extraction SQL/Python helper in `users_growth.py`.
  - Modify: n/a
  - Delete: n/a
  - Touch points: DuckDB JSON extraction, event `data` JSON string, consent event names.
  - Validation: Fixture covers true, false, true-to-false, and multiple `mkt` entries in one event.
  - Notes: Parses `data` as JSON in Python and chooses the latest `data.next.mkt[]` evidence entry by `changedAt`.

- [x] Step 7 — Compute active and inactive timelines
  - Achieve: Compute weekly active, monthly active, and inactive users per app and bucket.
  - Create: rolling-window helpers in `users_growth.py`.
  - Modify: n/a
  - Delete: n/a
  - Touch points: reading events `page.open`, `page.scroll`, `page.translate`.
  - Validation: Fixture proves trailing 7-day WAU, trailing 30-day MAU, and inactive equals known users minus MAU.
  - Notes: Counts only `page.open`, `page.scroll`, and `page.translate`; inactive users equal known users minus trailing-30-day active users.

### Milestone 3 — Add App-Scoped Aggregate Cache

- [x] Step 8 — Resolve stats cache paths
  - Achieve: Resolve `.local/42go-stats/{app-id}/users/growth/` safely for every app ID present in local Parquet.
  - Create: cache path helpers, likely in `users_growth.py` or a reusable events stats paths module.
  - Modify: n/a
  - Delete: n/a
  - Touch points: repository root resolution, existing event archive path handling in `cli/src/fortytwogo_cli/events/paths.py`.
  - Validation: Tests prove app IDs produce isolated directories and reject or sanitize unsafe path values.
  - Notes: Added app ID validation and isolated `.local/42go-stats/{app-id}/users/growth/` cache directories.

- [x] Step 9 — Write aggregate data and metadata
  - Achieve: Persist computed metric rows plus state metadata under each app-scoped cache directory.
  - Create: output files such as `metrics.parquet` and `state.json` under `.local/42go-stats/{app-id}/users/growth/`.
  - Modify: n/a
  - Delete: n/a
  - Touch points: PyArrow or DuckDB Parquet writing, JSON state.
  - Validation: First run creates files; a second run can read them back and produce identical output.
  - Notes: Writes `metrics.parquet` and `state.json` with schema version, source fingerprints, generated timestamp, source event range, timezone, and metric definitions.

- [x] Step 10 — Implement no-new-data fast path
  - Achieve: Detect unchanged source Parquet inputs and load existing aggregates instead of recomputing history.
  - Create: source fingerprint comparison logic.
  - Modify: aggregate state handling.
  - Delete: n/a
  - Touch points: raw Parquet file paths, sizes, mtimes, optional content hashes if needed.
  - Validation: Test or smoke run proves the second command run reports or behaves as cache-backed without recomputing all history.
  - Notes: Uses source Parquet path, size, and mtime fingerprints; cached reads use DuckDB to avoid PyArrow sandbox warnings in terminal output.

- [x] Step 11 — Implement incremental or bounded recompute update
  - Achieve: On changed source data, update affected app aggregates without unnecessary full recomputation when practical.
  - Create: touched-app/touched-range detection and bounded recompute logic.
  - Modify: aggregate refresh workflow.
  - Delete: n/a
  - Touch points: rolling 7-day and 30-day windows, source event range, state metadata.
  - Validation: Fixture or integration test adds new rows and proves updated buckets change while stable historical buckets remain stable.
  - Notes: Uses the bounded recompute fallback from the plan: unchanged inputs read cache; changed inputs recompute affected app histories from local Parquet. Correctness beats theater.

- [x] Step 12 — Implement `--reset`
  - Achieve: Remove existing users-growth aggregate cache and recompute from all available local Parquet data.
  - Create: reset workflow inside the users-growth service.
  - Modify: aggregate refresh workflow.
  - Delete: app-scoped cache contents only when `--reset` is passed.
  - Touch points: `.local/42go-stats/{app-id}/users/growth/`.
  - Validation: Test or smoke check proves reset deletes stale aggregate files and rebuilds them from raw Parquet.
  - Notes: Reset removes only the app-scoped users-growth metric directory before rebuilding.

### Milestone 4 — Wire CLI Output

- [x] Step 13 — Add nested `users growth` command
  - Achieve: Expose `42go events query users growth` with `--reset`, `--app`, `--archive-dir`, and optional `--format`.
  - Create: Typer sub-app or command wiring in `cli/src/fortytwogo_cli/events/cli.py`.
  - Modify: `cli/src/fortytwogo_cli/events/cli.py`, possibly `cli/src/fortytwogo_cli/events/query.py`.
  - Delete: n/a
  - Touch points: existing `stats` command and help contract.
  - Validation: `42go events query stats --help`, `42go events query users --help`, and `42go events query users growth --help` all work.
  - Notes: Added `42go events query users growth` with `--reset`, `--app`, `--archive-dir`, and `--format`.

- [x] Step 14 — Format human-readable terminal output
  - Achieve: Print clear app-scoped metric summaries and tables for day, week, month, and year.
  - Create: formatter functions in `users_growth.py` or `query.py`.
  - Modify: command handler output.
  - Delete: n/a
  - Touch points: terminal width-safe text output, metric row ordering.
  - Validation: Smoke output includes total users, subscribed users, weekly active users, monthly active users, inactive users, and all four granularities.
  - Notes: Prints app cache status plus day, week, month, and year metric tables.

- [x] Step 15 — Add optional JSON output
  - Achieve: Support `--format json` if it fits cleanly, returning metric metadata and rows in a machine-readable structure.
  - Create: JSON serialization helper.
  - Modify: command options and output handling.
  - Delete: n/a
  - Touch points: dataclass serialization, datetime/date string formatting.
  - Validation: `--format json` returns parseable JSON and honors `--app`.
  - Notes: Added `--format json`; it returns source metadata, app cache status, and metric rows.

### Milestone 5 — Tests and Documentation

- [x] Step 16 — Add focused users-growth tests
  - Achieve: Cover metric definitions, consent extraction, bucket generation, cache first-run/no-new-data/reset behavior, and app filtering.
  - Create: `cli/tests/test_users_growth.py`.
  - Modify: existing test fixtures/helpers if useful.
  - Delete: n/a
  - Touch points: temporary Parquet fixtures, DuckDB, Typer command runner.
  - Validation: `.local/42go-events/.venv/bin/python -m pytest cli/tests` passes.
  - Notes: Added synthetic Parquet tests covering consent flips, active/inactive math, cache first-run/cached/reset behavior, app filtering, and unsafe app IDs.

- [x] Step 17 — Update `42go-cli` skill
  - Achieve: Document the new command, options, cache path, metric definitions, and help navigation.
  - Create: optional reference doc under `.agents/skills/42go-cli/` if the flat index becomes crowded.
  - Modify: `.agents/skills/42go-cli/SKILL.md`.
  - Delete: n/a
  - Touch points: command help text and task metric definitions.
  - Validation: Skill lists `42go events query users growth`, `--reset`, `--app`, default archive/cache paths, and metric definitions.
  - Notes: Updated `.agents/skills/42go-cli/SKILL.md` with command index, options, cache paths, and metric definitions.

- [x] Step 18 — Verify help and local smoke behavior
  - Achieve: Prove the command is discoverable and works against the current local Parquet archive.
  - Create: n/a
  - Modify: help strings if gaps appear.
  - Delete: n/a
  - Touch points: `42go --help`, `42go events --help`, `42go events query --help`, `42go events query stats --help`, `42go events query users growth --help`.
  - Validation: Local command run creates `.local/42go-stats/lingocafe/users/growth/` and prints human-readable metrics; second run uses existing aggregate; `--reset` rebuilds it.
  - Notes: Verified help, archive stats compatibility, reset rebuild, cached second run, JSON output, missing app filter, and generated local cache files. Generated `.local` files remain untracked.

### Milestone 6 — Quality Gates

- [x] Step 19 — Run CLI test suite
  - Achieve: Catch Python regressions before repository QA.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: all `cli/tests`.
  - Validation: `.local/42go-events/.venv/bin/python -m pytest cli/tests` passes.
  - Notes: `.local/42go-events/.venv/bin/python -m pytest cli/tests` passed with 9 tests.

- [x] Step 20 — Run repository QA
  - Achieve: Confirm the broader project still passes its standard gate.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: TypeScript/Next.js build and lint surface.
  - Validation: `npm run qa` passes.
  - Notes: `npm run qa` passed, including ESLint and production build.
