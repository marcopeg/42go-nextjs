# Refinement Questions v1 - Audit AppConfig Client Secret Exposure

Answer inline under each question, then return to chat and say `next round`.

## Questions

### 1. Should this task stay analysis-only, or should it include an immediate hotfix if confirmed client exposure is found?

Answer:

analysis only.

the result of the findings go into {taskid}.output.md

### 2. What is the expected final artifact shape inside the task folder?

For example: one `LU73.analysis.md`, one `LU73.evidence.md`, copied command outputs, screenshots, or a concise report with embedded evidence.

Answer:

the result of the findings go into {taskid}.output.md

### 3. Should the evidence include a production build with sentinel secret values, or is source-level and existing-build inspection enough?

Using sentinel values gives stronger proof but may require temporary local environment values and a build run.

Answer:

do a build and inspect it.
but also assess if any secret can leak to the frontend exposed by an api call, or injected into the produce html (we do pass something via window.APP_ID or similar).

### 4. Which runtime surfaces must be checked before this can be called done?

Examples: rendered HTML, browser JS bundles, source maps if present, `window` globals, React hydration payloads, NextAuth endpoints, public app routes, protected app routes after login, and API responses.

Answer:

anything relevant. i want a serious security check for possible leaking secrets.

### 5. What should count as a secret-bearing AppConfig field for this audit?

The draft already names OAuth `clientSecret`. Should this also include API keys, webhook secrets, database URLs, private feature flags, provider tokens, internal domains, or anything matching `secret|token|key|password|credential`?

Answer:

take a look at the config definition. for sure the oauth secrets qualify. check the other possible keys and make an evaluation.

also just having a sound idea of which information reach the frontend is a good outcome.

### 6. If protections are missing but no real leak is confirmed, should the conclusion still create follow-up implementation tasks?

Examples: public/server AppConfig split, `PublicAppConfig` projection, server-only auth provider registry, lint/static import guard, and bundle secret regression checks.

Answer:

so far, highlight the potential treat in the outcome

### 7. Should this task inspect only the 42GO core, or should it also audit LingoCafe-specific config and authenticated LingoCafe pages because they consume `useAppConfig()`?

Answer:

do a complete codebase analysis.
if there is a vulnerability, it doesn't matter where does it come from.

### 8. Are there any real deployment constraints for this audit?

Examples: avoid touching `.env`, avoid running production build, use ngrok public test URL, avoid login, or do not inspect current production assets.

Answer:

you can read .env if you need, but don't change it and be careful because it contains sensitive data (db urls!!!)
