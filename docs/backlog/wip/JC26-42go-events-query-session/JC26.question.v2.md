# Refinement Questions v2 — 42go Events Query Session

Answer inline under the question, then return to chat and say `next round`.

## Questions

### 1. How should `duration` be output?

Pick one: seconds, ISO duration, or both.

Answer:
Store duration as seconds in data/cache/JSON output. Format duration as human-readable text in console output.
