# Refinement Questions v5 — 42go Events Query Session

Answer inline under the question, then return to chat and say `next round`.

## Questions

### 1. What output formats should the command support?

Pick one: text only, text and JSON, or text plus JSON plus CSV.

Answer:
For local cache storage, choose the best implementation format after looking at the existing event query commands. Console output should be human-readable. JSON remains the full-fidelity output format for full event arrays.
