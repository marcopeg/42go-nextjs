# Refinement Questions v3 — 42go Events Query Session

Answer inline under the question, then return to chat and say `next round`.

## Questions

### 1. Should default console output show full `events[]`, or only a session summary?

Pick one: full events, summary only, or summary in console plus full events in JSON.

Answer:
Show a summary in console output and full events in JSON output. Limit visible output to the last 20 sessions by default, with an arbitrary `--limit 100` style override.
