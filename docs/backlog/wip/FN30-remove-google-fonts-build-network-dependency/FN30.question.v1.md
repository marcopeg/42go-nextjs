# Refinement Questions v1 — Remove Google Fonts Build Network Dependency

Answer inline under each question, then return to chat and say `next round`.

## Questions

### 1. What font outcome is acceptable for the main app shell?

Choose the target for regular UI typography: preserve the exact current Google font with local vendored files, switch to a system font stack, or use another local font already available in the repo.

Answer:

Keep the font, download the font files in the codebase so there is no runtime download needed.
Question: is it possible to link the fonts so that the css links to google cdn instead of embedding the font files?

### 2. What font outcome is acceptable for the LingoCafe reader?

The reader has its own font preferences and visual expectations. Should FN30 only remove build-time Google Fonts from the app shell, or should it also audit and document reader font behavior as part of the fix?

Answer:

the reader was instructed to use only web safe fonts.
double check it pls. there should not be the need to download additional fonts.

### 3. Should font files be committed to the repository?

If preserving the exact font matters, the likely fix is vendoring font files under a public/app-theme or framework font folder. If asset footprint matters more, a system stack is smaller and simpler. Which tradeoff wins?

Answer:

For now, let's go with the system stack. 
Single apps don't require custom font just yet.

### 4. Which verification level is required before planning this task?

Is `npm install` plus `npm run build` enough, or should the plan also include an explicit offline/sandbox build proof after dependencies are installed?

Answer:

just make sure there is no network dependencies pulled at build time.
this seems to be the issue in the sandbox.

you can try to install and build in your sandbox just to be sure.

### 5. Where should the font strategy be documented?

Pick the expected documentation target: a short note in `docs/articles/THEMING.md`, a memory-bank update, comments near the implementation, or another specific file.

Answer:

in the theming is ok.