# Plan - Extract LingoCafe Profile Onboarding Overlay

## Goal

Move LingoCafe incomplete-profile onboarding out of `/books` and into an app-configured `AppLayout` layout guard that renders a blocking full-screen onboarding modal until the profile is complete.

## Milestones

### Milestone 1 - Define The Layout Guard Contract

- [x] Step 1 - Add typed app profile guard config
  - Achieve: Extend the profile/AppConfig types with a single app-scoped layout guard entry.
  - Create:
  - Modify: `src/42go/components/ProfileBlock/types.ts`, `src/AppConfig.ts`, and supporting profile guard types if a small new file keeps the contract cleaner.
  - Delete:
  - Touch points: `TProfileConfig`, `TAppConfigItem`, app profile config typing.
  - Validation: TypeScript accepts apps without guards and LingoCafe with one `slot: "layout"` guard.
  - Notes: Do not implement `slot: "before"` here. `NH52` owns that future work.

- [x] Step 2 - Decide and document lazy loading shape in the plan notes
  - Achieve: Pick the smallest code-splitting approach that fits the current client-side `useAppConfig()` model.
  - Create:
  - Modify: `YA00.plan.md` notes during execution if the chosen implementation needs a tradeoff note.
  - Delete:
  - Touch points: LingoCafe app config, guard component reference, Next dynamic import or React lazy if appropriate.
  - Validation: The chosen guard reference compiles and does not force guard execution for apps without `app.profile.guard`.
  - Notes: Implemented `loader` support in profile guard config so LingoCafe can use a dynamic import and avoid hard-referencing the onboarding component in config.

### Milestone 2 - Add The AppLayout Guard Runner

- [x] Step 1 - Render configured layout guard over AppLayout
  - Achieve: `AppLayout` invokes the configured profile layout guard after the normal shell exists and lets it render over the shell.
  - Create: A small guard runner component if it keeps `AppLayout.tsx` readable.
  - Modify: `src/42go/layouts/app/AppLayout.tsx`, `src/42go/layouts/app/types.ts`, and any new guard helper exports.
  - Delete:
  - Touch points: `useAppConfig()`, shell z-index, mobile menu/sidebar/footer layering, policy-wrapped page content.
  - Validation: Apps without a guard render exactly as before; LingoCafe guard can render above sidebar, toolbar, footer, and mobile nav.
  - Notes: Keep this runner generic. Chuck Norris does not let LingoCafe move into the platform basement.

- [x] Step 2 - Add `profile:complete` re-evaluation support
  - Achieve: The guard runner can re-evaluate or remount guard state after a `profile:complete` browser event.
  - Create:
  - Modify: Guard runner component.
  - Delete:
  - Touch points: `window.addEventListener("profile:complete", ...)`, cleanup on unmount, guard remount/reload key.
  - Validation: Dispatching `profile:complete` causes the guard to re-check completion and disappear when complete.
  - Notes: Keep the event client-only and scoped to browser state.

### Milestone 3 - Build The LingoCafe Onboarding Guard

- [x] Step 1 - Extract onboarding form UI from `/books`
  - Achieve: Create a LingoCafe-owned onboarding component that contains the required profile fields and consent controls.
  - Create: `src/app/(app)/(lingocafe)/_components/LingocafeOnboardingGuard.tsx` or a small folder if splitting UI/hook makes the file too large.
  - Modify:
  - Delete: Inline onboarding form code from `/books` later in Milestone 5.
  - Touch points: `ProfileConsent`, `getLingoCafeReaderLanguages`, `Button`, LingoCafe profile schema fields.
  - Validation: Component renders the required own language, target language, target level, and configured consent items.
  - Notes: Do not add a `/profile` link. A small logout action at the bottom is allowed if implementation keeps it simple.

- [x] Step 2 - Load and save profile through existing profile controller/API
  - Achieve: The guard loads current profile state, blocks when incomplete, saves through `/api/profile`, and dispatches `profile:complete` only after the returned profile is complete.
  - Create:
  - Modify: LingoCafe onboarding guard and any profile helper needed for a client-safe completeness check.
  - Delete:
  - Touch points: `useProfileController`, `/api/profile`, `isProfileComplete` or client-safe equivalent, consent evidence handling.
  - Validation: Save errors remain visible; successful complete save closes the overlay without a full route reload.
  - Notes: Existing profile/consent APIs own persistence and event logging. Do not add a second event path.

- [x] Step 3 - Use shared Modal for the blocking overlay
  - Achieve: Render the onboarding surface as a full-screen blocking modal on mobile and desktop.
  - Create:
  - Modify: LingoCafe onboarding guard, possibly `Modal` props only if a tiny capability is truly needed.
  - Delete:
  - Touch points: `src/42go/components/modal/Modal.tsx`, `size="full"`, `showClose={false}`, `closeOnOverlayClick={false}`, accessible title/subtitle.
  - Validation: Overlay cannot be dismissed while profile is incomplete; content scrolls without hiding actions; mobile and desktop both cover the viewport.
  - Notes: Use the shared modal. No homemade fixed overlay nonsense.

### Milestone 4 - Wire LingoCafe Configuration

- [x] Step 1 - Configure LingoCafe profile layout guard
  - Achieve: Add the LingoCafe onboarding guard under `app.profile.guard` with `slot: "layout"`.
  - Create:
  - Modify: `src/config/lingocafe/config.ts`.
  - Delete:
  - Touch points: LingoCafe `app.profile.schema`, `app.profile.items`, `app.consent.items`, guard config.
  - Validation: LingoCafe activates the guard; other app configs remain unchanged and unguarded.
  - Notes: Keep the config readable beside existing profile configuration.

- [x] Step 2 - Confirm bundle/lazy behavior
  - Achieve: Verify the chosen guard component reference compiles in production build and preserves the intended loading behavior as much as practical.
  - Create:
  - Modify: Guard config or guard loader if build constraints appear.
  - Delete:
  - Touch points: Next build output, TypeScript types, dynamic import boundaries.
  - Validation: `npm run qa` reaches the build stage without guard import/type failures.
  - Notes: `npm run qa` passed with the guard loader dynamic import shape.

### Milestone 5 - Clean Up `/books`

- [x] Step 1 - Remove books-owned onboarding state and form
  - Achieve: `/books` no longer owns language/level/consent form state or posts `/api/profile`.
  - Create:
  - Modify: `src/app/(app)/(lingocafe)/books/page.tsx`.
  - Delete: Local onboarding form markup, `saveProfile`, consent state that only supported the inline form, and unused imports.
  - Touch points: `showProfileForm`, header subtitle/actions, error/loading branches.
  - Validation: Incomplete profile never renders book catalog or inline onboarding form.
  - Notes: Keep `/books` focused on books. That page had one job and got ambitious.

- [x] Step 2 - Add incomplete-profile fallback and completion reload
  - Achieve: `/books` shows a plain incomplete-profile note and listens for `profile:complete` to reload/continue.
  - Create:
  - Modify: `src/app/(app)/(lingocafe)/books/page.tsx`.
  - Delete:
  - Touch points: existing books load effect, `loadBooks` extraction if needed, event listener cleanup.
  - Validation: When profile is incomplete, no catalog renders; after `profile:complete`, books data reloads and catalog can render.
  - Notes: Use copy close to `Profile incomplete, can't show books now`.

### Milestone 6 - Verify Behavior

- [x] Step 1 - Run static quality gates
  - Achieve: Catch type, lint, and production build failures.
  - Create:
  - Modify:
  - Delete:
  - Touch points: Full repo quality gate.
  - Validation: `npm run qa` passes.
  - Notes: `npm run lint`, `git diff --check`, and `npm run qa` passed after exporting the new guard types.

- [ ] Step 2 - Perform LingoCafe visual/runtime checks
  - Achieve: Verify guard behavior in the actual LingoCafe app flow.
  - Create:
  - Modify:
  - Delete:
  - Touch points: `https://lc42go.ngrok.app/`, `john` / `john`, `/books`, `/profile` or another authenticated AppLayout route.
  - Validation: Incomplete profile opens blocking full-screen onboarding on desktop and mobile; completed profile removes overlay; `/books` does not render catalog while incomplete.
  - Notes: Runtime HTTP smoke against `https://lc42go.ngrok.app/` passed for public routes and authenticated APIs. Browser visual verification was not completed because the in-app browser tool was not exposed and Playwright is not installed in this checkout.

- [x] Step 3 - Record implementation notes
  - Achieve: Capture any lazy-loading tradeoff, browser verification result, and known limitation for future `NH52`.
  - Create: `YA00.notes.md` during execution if not already present.
  - Modify:
  - Delete:
  - Touch points: `docs/backlog/.../YA00.notes.md`.
  - Validation: Notes identify whether guard loading used dynamic import or direct component reference.
  - Notes: Planning ends here. Execution can swing the hammer next.
