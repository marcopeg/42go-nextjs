# Plan — HeroBlock: Add Alignment Option

## Goal

Add a typed `alignment` option to `HeroBlock` that controls text, actions, content origin, and background image origin while preserving the current centered default.

## Milestones

### Milestone 1 — Confirm HeroBlock Surface

- [x] Step 1 — Reconfirm current HeroBlock API and call sites
  - Achieve: Verify the implementation starts from the actual shared `HeroBlock` and does not miss existing usages.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `src/42go/components/ContentBlock/blocks/HeroBlock.tsx`, app configs that use `type: "hero"`, Hero Block docs under `contents/default/docs/composable-pages/hero-block/README.md`.
  - Validation: Run focused `rg` scans for `type: "hero"`, `type: 'hero'`, and `HeroBlock`.
  - Notes: Existing usages without `alignment` must remain valid and visually centered.

- [x] Step 2 — Define explicit alignment mappings
  - Achieve: Decide the exact classes/styles for left, center, and right before changing JSX.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: Section text alignment, content max-width alignment, action row justification, background position.
  - Validation: Mapping covers all three values and has center as the fallback.
  - Notes: Prefer readable lookup objects over scattered conditional class strings.

### Milestone 2 — Implement Alignment Support

- [x] Step 1 — Extend the HeroBlock type
  - Achieve: Add `alignment?: "left" | "center" | "right"` to `THeroBlock`.
  - Create: n/a
  - Modify: `src/42go/components/ContentBlock/blocks/HeroBlock.tsx`.
  - Delete: n/a
  - Touch points: `THeroBlock` and any local destructuring/default logic.
  - Validation: TypeScript accepts existing and new HeroBlock config shapes.
  - Notes: Do not make `alignment` required.

- [x] Step 2 — Apply alignment to content composition
  - Achieve: Make `alignment` drive title/subtitle text alignment, content column origin, and action row justification.
  - Create: n/a
  - Modify: `src/42go/components/ContentBlock/blocks/HeroBlock.tsx`.
  - Delete: n/a
  - Touch points: `<section>`, inner `.hero-block`, title classes, subtitle classes, action row classes.
  - Validation: `alignment` values map to left/start, center, and right/end layouts without changing current center output.
  - Notes: Keep existing scroll animations, markdown rendering, button variants, and overlay behavior.

- [x] Step 3 — Apply alignment to background image origin
  - Achieve: Make `backgroundImage` use an alignment-appropriate `backgroundPosition`.
  - Create: n/a
  - Modify: `src/42go/components/ContentBlock/blocks/HeroBlock.tsx`.
  - Delete: n/a
  - Touch points: Inline style object for `backgroundImage`.
  - Validation: Left uses left origin, center uses center origin, right uses right origin.
  - Notes: Keep `backgroundSize: "cover"` and the existing dark overlay.

### Milestone 3 — Document the Option

- [x] Step 1 — Update Hero Block docs
  - Achieve: Document `alignment`, its allowed values, and the default.
  - Create: n/a
  - Modify: `contents/default/docs/composable-pages/hero-block/README.md`.
  - Delete: n/a
  - Touch points: Example config and params section.
  - Validation: Docs include at least one non-center example.
  - Notes: Keep the docs concise. The option should be obvious in one read.

- [x] Step 2 — Add or adjust sample config only if useful
  - Achieve: Provide a practical example if docs alone are not enough for discoverability.
  - Create: n/a
  - Modify: Existing sample config only if it improves verification or documentation.
  - Delete: n/a
  - Touch points: `src/config/*` hero entries.
  - Validation: Any sample config still matches the page intent and compiles.
  - Notes: Do not force a live app page to change visually unless it is deliberate.

### Milestone 4 — Verify Behavior and Quality

- [x] Step 1 — Run source and type validation
  - Achieve: Catch broken imports, type regressions, and build errors.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: Whole repo quality gate.
  - Validation: Run `npm run qa`.
  - Notes: If QA fails for unrelated existing issues, record the failure clearly.

- [x] Step 2 — Visually verify the hero alignment
  - Achieve: Confirm left, center, and right produce sane layouts in the browser.
  - Create: n/a
  - Modify: Temporary local config only if needed for visual checks, then remove it before completion.
  - Delete: Temporary verification-only edits before completion.
  - Touch points: Public dynamic page route rendering a HeroBlock.
  - Validation: Use the project visual verification flow or a local/public test page to inspect all three alignments, including at least one background-image case if a suitable image is available.
  - Notes: Preserve current center rendering as the baseline.

### Milestone 5 — Review Related HeroBlock Drafts

- [x] Step 1 — Re-read `AK87` and `FE78`
  - Achieve: Decide whether the alignment outcome changes either draft's refinement needs.
  - Create: n/a
  - Modify: Related task files only if a concrete follow-up note is needed.
  - Delete: n/a
  - Touch points: `docs/backlog/draft/AK87-heroblock-optional-fields-title-subtitle-or-actions-only/AK87.task.md`, `docs/backlog/draft/FE78-heroblock-icon-support/FE78.task.md`.
  - Validation: Final implementation notes state whether related drafts were updated or left unchanged.
  - Notes: Do not implement optional fields or icon support in this task.
