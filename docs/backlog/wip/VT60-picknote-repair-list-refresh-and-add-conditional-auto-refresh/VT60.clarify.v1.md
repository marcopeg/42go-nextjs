# Clarify Round v1 — PickNote: Repair List Refresh and Add Conditional Auto-Refresh

## Questions

### 1. Should each list's auto-refresh setting follow the user across browsers/devices, or is browser-local persistence enough?

## Answers

### 1.

This is an account setting. Whenever the app loads in any browser, it should fetch the setting and apply it locally.

## Resolution Summary

- The auto-refresh preference is server-backed account data, not browser-only state.
- The app fetches the preference when it loads and applies the selected behavior locally.
