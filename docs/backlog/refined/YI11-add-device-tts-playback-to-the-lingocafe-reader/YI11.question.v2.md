# Refinement Questions v2 — Add Device TTS Playback to the LingoCafe Reader

Answer inline under each question, then return to chat and say `next round`.

## Questions

### 1. Which playback-speed presets should the first release offer, and which is the default?

Proposed language-learning set: `0.5×`, `0.75×`, `1×` (default), and `1.25×`. The user only requested slow playback, so this keeps the control compact.

Answer:

I'd say one single button that circles around the 4 proposed speed. each time the user clicks, the next speed is applied, and it is circular fro from 1.25 the next would be 0.5. default is 1x.

### 2. When should the low-volume `audio.play` event be recorded?

Proposed behavior: record it only when the user first starts a playback session from the FAB. Do not record it on pause/resume or percentage seeking, because those are continuations of the same session. Starting playback again after closing or finishing creates a new event.

Answer:

no, i'd say each sentence will record the event so that we can compute a duration of a playback session by clustering adjacent events (the metrics calculations are in a different repositiory. don't think about computing metrics in this task)

### 3. Which identifier should the `audio.play` payload use for the current chapter?

The existing reader data model calls this value `page_id` (`bookPage.page.pageId`), while the requested event calls it a chapter ID. Proposed payload: `{ language, book_id, page_id }`, treating the reader page as the chapter unit. If analytics requires the literal key `chapter_id`, confirm that instead.

Answer:

store:

- page_id
- book_id
