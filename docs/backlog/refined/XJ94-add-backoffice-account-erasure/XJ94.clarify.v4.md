# Clarify Round v4 - Add Backoffice Account Erasure

## Questions

### 1. Should account deletion use the existing `users:list` grant, or should this task add a stricter `users:delete` grant?

## Answers

### 1.

Add a strict `users:delete` grant. Roles must explicitly include this grant before account deletion is enabled.

## Resolution Summary

- Resolved: account deletion requires a dedicated `users:delete` grant in addition to the backoffice role/session requirements.
