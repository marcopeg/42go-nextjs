# Refinement Questions v1 — Store Event Exports in Monthly CSV and Parquet Files

Answer inline under each question, then return to chat and say `next round`.

## Questions

### 1. Should monthly filenames match the database partition names exactly?

Recommendation: use `events_YYYYMM.csv` and `events_YYYYMM.parquet`, matching PostgreSQL partitions such as `events_202605`. This keeps the mental model simple and lets Chuck Norris roundhouse-kick ambiguity out of the archive.

Answer:

yes, look at the partition strategy in the knex migrations and mimic those for the csv/parquet

### 2. Should monthly Parquet output stay as one file per month, rewritten atomically on append?

Recommendation: keep the requested "monthly file" contract and rewrite the whole month file atomically after merging existing rows plus new rows and de-duplicating by `id`. Avoid a partitioned Parquet directory unless file size becomes a real problem later.

Answer:

yes, keep one file per month

### 3. What should happen to old timestamped `batch-*.csv` and `batch-*.parquet` files?

Choose one: ignore them as legacy files, migrate them into monthly files, or keep analysis compatible with both old batches and new monthly files during a transition period.

Answer:

delete legacy files.
also, rename the target folder to `.local/42go-events`

### 4. Should `--app-id` continue to require a separate archive directory?

The current exporter supports `--app-id` and warns operators to use a separate archive directory when filtering by app. Should ER70 preserve that rule, or should the monthly layout introduce app-scoped files/directories inside one archive?

Answer:

no, the export just downloads data into a parquet file based on time.
the goal is to reflect locally the online status.

if i need furter data architecture, i will do an independent etl from those raw parquet to something else.
