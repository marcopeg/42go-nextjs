# Refinement Questions v1 — Docker Image Security Review

Answer inline under each question, then return to chat and say `next round`.

## Questions

### 1. What image should be audited as the release candidate?

Specify the exact source of the image: current `42go-next:latest` from `make prod.build`, a registry image, or another tag/build target.

Answer:

it's the build process via dockerfile and its resulting artifact.
start by checking the dockerfile and see if you find anything security related that can be tighntened up.

### 2. Is this strictly an image artifact audit, or should runtime Compose config be included?

The current `docker-compose.prod.yml` uses `env_file: .env` and mounts `./contents:/app/contents`. Those are not baked into the image, but they affect the deployed container posture. Decide whether this review includes runtime Compose settings or only the built image layers/filesystem.

Answer:

both.

### 3. What vulnerability scanner is acceptable if `trivy` is unavailable?

Pick the fallback path: install/use `trivy`, use Docker Scout, use Grype, use another scanner, or mark the vulnerability scan as blocked. Also state whether installing a scanner is in scope for this task.

Answer:

what is it? how do i install for you to use it?

### 4. What registry and publishing policy should be evaluated?

Name the target registry if known: Docker Hub, GHCR, private registry, or not decided. Also state whether public distribution is intended and whether tagging, signing, provenance, or SBOM requirements should be part of the release gate.

Answer:

docker hub

### 5. What content is allowed in the final image?

Define the policy for `public/`, app configs, built JavaScript bundles, `contents/`, docs, seed data, test fixtures, app icons, app themes, and any other project content. This gives the reviewer a hard line instead of a fog bank to punch.

Answer:

research what is good/bad for nextjs docker build.

### 6. What counts as blocking versus follow-up?

Confirm the release gate. Secrets, private content, and unjustified root runtime are already listed as critical. Should high or medium CVEs block release? Should read-only filesystem, dropped Linux capabilities, no-new-privileges, or other runtime hardening items be required or only recommended?

Answer:

anything that can lead to a security breach once the image is running in production

### 7. Where should the review evidence and outcome summary live?

Choose the output artifact: `TK89.notes.md`, `TK89.output.md`, a docs article, or another file. Also confirm whether remediation tasks should be drafted immediately for each issue found, or only summarized for later triage.

Answer:

TK89.output.md

and in that file i want to see a list of actionable steps
