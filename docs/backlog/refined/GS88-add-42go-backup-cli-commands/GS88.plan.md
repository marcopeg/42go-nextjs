# Plan - Add 42go Backup and Restore CLI Commands

## Goal

Move 42Go backup and restore operations into the Python `42go` CLI, keep the existing Makefile targets as thin wrappers, preserve current dump and restore behavior, and verify both direct CLI and wrapper command surfaces.

## Milestones

### Milestone 1 - Map and preserve current behavior

- [ ] Step 1 - Audit the existing backup script
  - Achieve: Capture the behavior that must survive the move into the CLI.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `.agents/skills/42go-backup/scripts/backup.mjs`, `.agents/skills/42go-backup/SKILL.md`, `Makefile`.
  - Validation: Static inspection confirms current environment variables, dump directory, mode rules, table exclusions, dependency ordering, restore path resolution, notes-table stripping, event partition handling, and `psql` invocation.
  - Notes: Do this before editing. Chuck Norris does not migrate behavior by memory.

### Milestone 2 - Port backup and restore logic into the CLI package

- [ ] Step 1 - Add a Python backup module
  - Achieve: Implement the existing backup data flow in the CLI package with the same full/light table selection, dependency-safe ordering, SQL rendering, and dump output behavior.
  - Create: `cli/src/fortytwogo_cli/backup/`.
  - Modify: n/a
  - Delete: n/a
  - Touch points: `cli/src/fortytwogo_cli/backup/__init__.py`, `cli/src/fortytwogo_cli/backup/core.py`, optional focused helper modules.
  - Validation: Unit tests cover mode validation, timestamped filename shape where practical, table exclusion rules, dependency sorting, dump path resolution, and SQL rendering helpers without requiring a live database.
  - Notes: Reuse Python dependencies already available in `cli/pyproject.toml`, especially `psycopg`. Keep the current default dump directory `.local/42go-backups`.

- [ ] Step 2 - Add restore support in the CLI package
  - Achieve: Implement restore behavior equivalent to the current Node script: resolve explicit paths or bare filenames, prepare older dumps when needed, then execute `psql --set ON_ERROR_STOP=1`.
  - Create: n/a
  - Modify: `cli/src/fortytwogo_cli/backup/core.py` or a restore-focused helper module.
  - Delete: n/a
  - Touch points: restore path resolution, dynamic `notes.notes_*` stripping, event partition statement injection, temporary filtered dump handling, subprocess `psql` call.
  - Validation: Unit tests cover dump path resolution, missing file failures, notes-table stripping, event partition injection from old dumps, and subprocess argument construction through mocking.
  - Notes: Do not run destructive restore validation against a real database unless a disposable target is configured.

### Milestone 3 - Add Typer commands and interactive prompts

- [ ] Step 1 - Register `42go backup`
  - Achieve: Add a top-level backup command with `--full` and `--light` flags.
  - Create: `cli/src/fortytwogo_cli/backup/cli.py`.
  - Modify: `cli/src/fortytwogo_cli/cli.py`.
  - Delete: n/a
  - Touch points: Typer command registration, flag validation, mode prompt when no flag is provided.
  - Validation: `42go backup --help` shows both flags; `--full --light` exits non-zero; no flags triggers a prompt that can be tested with `CliRunner` input.
  - Notes: Use a simple two-choice prompt. No positional `full|light` command surface.

- [ ] Step 2 - Register `42go restore`
  - Achieve: Add a top-level restore command with `--from <dump-path-or-name>` and interactive newest-first backup selection when omitted.
  - Create: n/a
  - Modify: `cli/src/fortytwogo_cli/backup/cli.py`, `cli/src/fortytwogo_cli/cli.py`.
  - Delete: n/a
  - Touch points: backup listing from `.local/42go-backups`, newest-to-oldest sorting, restore prompt, explicit `--from` path.
  - Validation: `42go restore --help` documents `--from`; tests confirm explicit `--from` dispatches restore; tests confirm no `--from` lists available dumps newest first and accepts a selection; empty backup list exits with a useful error.
  - Notes: Sorting can use filename timestamp first, with file mtime as a fallback if needed.

### Milestone 4 - Convert Makefile targets into CLI wrappers

- [ ] Step 1 - Rewrite `make backup`
  - Achieve: Make `make backup mode=full` call `42go backup --full`, `make backup mode=light` call `42go backup --light`, and `make backup` call `42go backup`.
  - Create: n/a
  - Modify: `Makefile`.
  - Delete: direct `node .agents/skills/42go-backup/scripts/backup.mjs backup ...` invocation from the Makefile target.
  - Touch points: `backup` target.
  - Validation: Static inspection and command dry checks confirm no fallback wrapper is present and missing `42go` would fail hard.
  - Notes: Keep Makefile shell logic minimal. It is a wrapper, not a throne.

- [ ] Step 2 - Rewrite `make restore`
  - Achieve: Make `make restore from=<dump>` call `42go restore --from <dump>` and `make restore` call `42go restore`.
  - Create: n/a
  - Modify: `Makefile`.
  - Delete: direct `node .agents/skills/42go-backup/scripts/backup.mjs restore ...` invocation from the Makefile target.
  - Touch points: `restore` target.
  - Validation: Static inspection confirms the Makefile delegates to `42go` and does not keep restore logic.
  - Notes: Preserve the operator-facing `make restore from=...` interface.

### Milestone 5 - Update docs and skill guidance

- [ ] Step 1 - Update backup skill documentation
  - Achieve: Make `.agents/skills/42go-backup/SKILL.md` describe the CLI-first public surface and Makefile wrapper behavior.
  - Create: n/a
  - Modify: `.agents/skills/42go-backup/SKILL.md`.
  - Delete: stale language that says the Makefile is the public command surface or that the Node script is the preferred internal path.
  - Touch points: public commands, behavior, validation, internal implementation notes.
  - Validation: Static read confirms docs mention `42go backup --full`, `42go backup --light`, `42go restore --from`, interactive selection behavior, and wrapper Makefile commands.
  - Notes: Keep generated backup files out of source control as currently expected.

- [ ] Step 2 - Decide the old Node script fate
  - Achieve: Remove, retire, or leave a tiny compatibility shim only if needed by existing docs or external automation.
  - Create: n/a
  - Modify: `.agents/skills/42go-backup/scripts/backup.mjs` if keeping a compatibility shim.
  - Delete: `.agents/skills/42go-backup/scripts/backup.mjs` only if no references need it.
  - Touch points: repository references found by `rg "backup.mjs|42go-backup/scripts"`.
  - Validation: No stale Makefile or skill docs point to a dead implementation path.
  - Notes: Prefer one source of truth. If the shim remains, it should call `42go` and say so.

### Milestone 6 - Verify behavior

- [ ] Step 1 - Run CLI unit tests
  - Achieve: Prove help output, flag validation, prompts, path resolution, and mocked restore dispatch work.
  - Create: n/a
  - Modify: `cli/tests/test_cli.py` and focused backup tests as needed.
  - Delete: n/a
  - Touch points: `cli/tests/`.
  - Validation: Run the CLI test suite from `cli/`, using the project’s existing Python test command or `pytest` if that is the established local path.
  - Notes: Avoid live database dependency in unit tests.

- [ ] Step 2 - Exercise a real or safe command path
  - Achieve: Validate at least one non-destructive command path and, when configured safely, one real backup generation path.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `42go backup --help`, `42go restore --help`, `42go backup --full --light`, optional `42go backup --light`.
  - Validation: Help commands exit successfully, invalid conflicting flags fail, and optional backup generation produces a dump when `BACKUP_DATABASE_URL` is configured.
  - Notes: Do not run restore against anything but a disposable migrated database.

- [ ] Step 3 - Run repository QA
  - Achieve: Catch TypeScript, lint, and build regressions after Makefile/docs/CLI changes.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: whole repository.
  - Validation: `npm run qa` exits successfully, or any existing baseline warning is recorded.
  - Notes: The task demands QA after code changes. Chuck Norris runs the check. The check apologizes.
