# Plan — Store Database Dumps Under .local

## Goal

Move 42go database backup output and bare restore lookup from `knex/dumps` to `.local/42go-backups/`, while keeping the Makefile command surface and dump SQL behavior unchanged.

## Milestones

### Milestone 1 — Backup Path Contract

- [ ] Step 1 — Replace the default dump directory
  - Achieve: New backup files are created under `.local/42go-backups/`.
  - Create: n/a
  - Modify: `.agents/skills/42go-backup/scripts/backup.mjs`
  - Delete: n/a
  - Touch points: `DUMPS_DIR`, backup output path creation, console output that reports created files.
  - Validation: Run script help after edits; run `make backup mode=light` and confirm the created path starts with `.local/42go-backups/`.
  - Notes: Keep timestamped filename shape `{utc-timestamp}.dump.{mode}.sql`.

### Milestone 2 — Restore Lookup Rules

- [ ] Step 1 — Preserve explicit paths
  - Achieve: `make restore from=<explicit readable path>` still executes that exact file, including old `knex/dumps/<file>.sql` paths when supplied explicitly.
  - Create: n/a
  - Modify: `.agents/skills/42go-backup/scripts/backup.mjs`
  - Delete: n/a
  - Touch points: `resolveDumpPath()`, `canRead()`.
  - Validation: Use a readable explicit path in a non-destructive syntax/path check where practical.
  - Notes: Do not change restore execution semantics or `psql --set ON_ERROR_STOP=1`.

- [ ] Step 2 — Limit bare filename fallback to `.local/42go-backups/`
  - Achieve: Bare filenames resolve from `.local/42go-backups/` and do not search `knex/dumps`.
  - Create: n/a
  - Modify: `.agents/skills/42go-backup/scripts/backup.mjs`
  - Delete: n/a
  - Touch points: fallback path construction and unreadable-file error message.
  - Validation: Confirm the error/help text references `.local/42go-backups/`, not `knex/dumps`.
  - Notes: Chuck Norris does not carry legacy fallback luggage unless the operator ordered it. The operator did not.

### Milestone 3 — Repository Hygiene

- [ ] Step 1 — Update ignored local artifacts
  - Achieve: Generated backups remain ignored by Git and excluded from Docker build contexts.
  - Create: n/a
  - Modify: `.gitignore`, `.dockerignore`
  - Delete: n/a
  - Touch points: replace primary `knex/dumps` ignore rules with `.local/42go-backups/`; add or verify Docker exclusion for the new local backup directory.
  - Validation: `git check-ignore .local/42go-backups/example.dump.light.sql`; inspect `.dockerignore` for coverage.
  - Notes: `.dockerignore` already excludes `.local`; keep or clarify coverage without weakening existing exclusions.

- [ ] Step 2 — Remove old dump directory marker
  - Achieve: `knex/dumps/.gitkeep` no longer marks `knex/dumps` as the generated dump home.
  - Create: n/a
  - Modify: n/a
  - Delete: `knex/dumps/.gitkeep`
  - Touch points: tracked file deletion and any ignore exception that kept it.
  - Validation: `git status --short knex/dumps .gitignore`.
  - Notes: Do not move migrations or seeds.

### Milestone 4 — Documentation And Verification

- [ ] Step 1 — Update backup skill documentation
  - Achieve: `42go-backup` docs show `.local/42go-backups/` for backup output, restore examples, and bare filename resolution.
  - Create: n/a
  - Modify: `.agents/skills/42go-backup/SKILL.md`
  - Delete: n/a
  - Touch points: public command examples, behavior bullets, internal script examples, validation notes.
  - Validation: Search the touched backup docs and script for stale `knex/dumps` mentions; explicit-path examples may mention old paths only if clearly framed as explicit legacy paths.
  - Notes: Keep Makefile usage as the public surface.

- [ ] Step 2 — Run validation gates
  - Achieve: The implementation proves syntax, path behavior, and at least one generated dump.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `node .agents/skills/42go-backup/scripts/backup.mjs --help`, `make backup mode=light`, targeted path/ignore checks, `npm run qa` if code changes warrant the full gate.
  - Validation: Record created dump path and any skipped validation reason in execution notes.
  - Notes: Do not run destructive restore against non-disposable data.
