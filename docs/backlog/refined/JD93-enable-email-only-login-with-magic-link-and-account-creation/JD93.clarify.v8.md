# Clarify Round v8 — Enable Email-Only Login With Magic Link and Account Creation

## Questions

### 1. Should v1 log audit/events for email requested, email resent, code verified, and email login failed?

## Answers

### 1.

Yes. Configuration should allow cherry-picking which events to log, with each event exposed as a Boolean key. All event logging keys should default to `true`.

## Resolution Summary

- v1 should log audit/events for email requested, email resent, code verified, and email login failed.
- Event logging should be configurable per event through Boolean keys.
- All event logging keys default to `true`.
