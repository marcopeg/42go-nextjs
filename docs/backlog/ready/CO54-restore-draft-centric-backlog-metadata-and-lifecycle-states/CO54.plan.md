---
taskId: CO54
createdAt: 2026-05-10T07:17:33Z
updatedAt: 2026-05-10T07:17:33Z
---

# Plan — Restore Draft-Centric Backlog Metadata and Lifecycle States

## Goal

Refactor the repo-local backlog system so `<taskid>.task.md` is the single canonical task file, task status drives physical status folders and indexes, `backlog-migrate` can convert existing backlog data safely, and a new `backlog-doctor` skill can verify and repair folder/index drift.

## Milestones

### Milestone 1 — Baseline and Model Contract

- [ ] Step 1 — Inventory current backlog behavior
  - Achieve: Establish the exact scripts, skills, and active backlog files that currently own task discovery, status transitions, migration, and index generation.
  - Create: Nothing.
  - Modify: Nothing.
  - Delete: Nothing.
  - Touch points: `.agents/skills/backlog-*/SKILL.md`, `.agents/skills/backlog-core/scripts/backlog_lib.py`, `.agents/skills/backlog-core/scripts/backlog_tool.py`, `.agents/skills/backlog-migrate/scripts/migrate_backlog.py`, `docs/backlog/BACKLOG.md`, representative active task folders.
  - Validation: Record the current file naming conventions, current state directories, frontmatter owners, and any dirty-worktree files that must not be overwritten.
  - Notes: Treat unrelated existing modifications as user work. Chuck Norris steps around landmines. The landmines apologize.

- [ ] Step 2 — Define the canonical backlog contract in shared code
  - Achieve: Encode the new model in one shared place: canonical task filename, supported statuses, status directories, status-local index filenames, and artifact/frontmatter expectations.
  - Create: Shared constants or helper functions as needed.
  - Modify: `.agents/skills/backlog-core/scripts/backlog_lib.py`.
  - Delete: No files.
  - Touch points: `STATE_DIRS`, `ACTIVE_SECTION_ORDER`, task filename helpers, active task selection, metadata parsing, index rendering helpers.
  - Validation: Shared helpers can resolve a task whose main file is `<taskid>.task.md` and whose folder is under a status-specific directory.
  - Notes: Keep completed and archived compatibility unless implementation proves a narrow adjustment is required.

### Milestone 2 — Core Status, Folder, and Index Behavior

- [ ] Step 1 — Update task discovery and metadata ownership
  - Achieve: Make discovery read task metadata from `<taskid>.task.md` and treat other markdown files as sidecar artifacts without task lifecycle authority.
  - Create: Helper functions for canonical task lookup if needed.
  - Modify: `.agents/skills/backlog-core/scripts/backlog_lib.py`, `.agents/skills/backlog-core/scripts/backlog_tool.py`.
  - Delete: No files.
  - Touch points: `TaskRecord`, `select_active_task_file`, `task_artifact_rank`, `discover_tasks`, `resolve_task`, `detect_task_kind`, `write_markdown`.
  - Validation: A fixture task with `<taskid>.task.md`, plan, notes, and questions resolves with metadata from the task file only.
  - Notes: Preserve legacy read support long enough for migration to operate.

- [ ] Step 2 — Implement status transitions against canonical task files
  - Achieve: Ensure every status transition updates `<taskid>.task.md` and moves the whole task folder into the matching status subfolder.
  - Create: Any narrow helper for moving folders by canonical status.
  - Modify: `.agents/skills/backlog-core/scripts/backlog_lib.py`, `.agents/skills/backlog-core/scripts/backlog_tool.py`, `.agents/skills/backlog-execute/scripts/transition_task.py`.
  - Delete: No files.
  - Touch points: `transition_task`, `apply_task_meta_defaults`, `state_dir`, CLI transition choices.
  - Validation: Transition fixture tasks through `draft -> refining -> refined -> planned -> wip` and manually to `blocked`; verify folder path and task frontmatter agree.
  - Notes: There is no dedicated blocked skill in scope; blocked remains manually assignable through tooling or direct metadata repair.

- [ ] Step 3 — Generate root and status-local indexes
  - Achieve: Rebuild the root `BACKLOG.md` plus one `BACKLOG_<status>.md` inside each status folder from canonical task records.
  - Create: Status-local index rendering helpers if they do not already fit existing index code.
  - Modify: `.agents/skills/backlog-core/scripts/backlog_lib.py`, `.agents/skills/backlog-core/scripts/backlog_tool.py`.
  - Delete: No files.
  - Touch points: `rebuild_active_index`, `rebuild_indexes`, `render_active_entry`, index path helpers.
  - Validation: Running the index sync against a fixture creates correct local indexes for `draft`, `refining`, `refined`, `planned`, `wip`, and `blocked`.
  - Notes: Root `BACKLOG.md` should remain the high-level map; local indexes make disk browsing human-readable.

### Milestone 3 — Migration and Doctor Tooling

- [ ] Step 1 — Rewrite backlog migration for canonical task files
  - Achieve: Convert existing `.task.draft.md`, `.task.refined.md`, and legacy `.task.md` layouts into the new canonical `<taskid>.task.md` model.
  - Create: New helper functions or fixture data for migration testing.
  - Modify: `.agents/skills/backlog-migrate/scripts/migrate_backlog.py`, `.agents/skills/backlog-migrate/SKILL.md`, shared helpers as needed.
  - Delete: No files during script development; migration execution may rename/remove obsolete files only after dry-run acceptance.
  - Touch points: Candidate file selection, metadata merge, content preservation, dry-run JSON output, write path.
  - Validation: Dry-run reports metadata moves and file renames; write mode on a temp fixture produces `<taskid>.task.md` and preserves user-authored content.
  - Notes: Metadata conflict rule is `refined -> task` because current skills wrote lifecycle metadata into refined files.

- [ ] Step 2 — Strip non-main artifact frontmatter during migration
  - Achieve: Remove frontmatter from refined, plan, notes, question, and clarify artifacts after task metadata is centralized, while preserving artifact body content.
  - Create: A reusable frontmatter-strip helper if useful.
  - Modify: `.agents/skills/backlog-migrate/scripts/migrate_backlog.py`, possibly `.agents/skills/backlog-core/scripts/backlog_lib.py`.
  - Delete: No files unless migration explicitly retires obsolete duplicate task artifacts after content has been preserved.
  - Touch points: `parse_frontmatter`, markdown write helpers, migration action reporting.
  - Validation: Fixture sidecars retain body text and lose YAML frontmatter; canonical task file receives any task-level metadata before stripping.
  - Notes: Be conservative with deletion. Metadata gets knocked out. User prose survives.

- [ ] Step 3 — Add `backlog-doctor`
  - Achieve: Provide a repo-local skill and script that can report and repair drift between canonical task status, physical folder, local status indexes, and root backlog index.
  - Create: `.agents/skills/backlog-doctor/SKILL.md`, `.agents/skills/backlog-doctor/scripts/doctor_backlog.py` or an equivalent shared CLI subcommand.
  - Modify: Shared backlog helpers if doctor logic belongs in `backlog-core`.
  - Delete: No files.
  - Touch points: Task discovery, status directory mapping, index rebuild helpers, CLI JSON report output.
  - Validation: Doctor dry-run reports misplaced fixture folders; repair mode moves them and rebuilds root/status indexes.
  - Notes: Doctor should be useful after manual edits, failed migrations, and future cross-repo copies.

### Milestone 4 — Skill Workflow Updates

- [ ] Step 1 — Update drafting, refining, and clarification instructions
  - Achieve: Align agent instructions with `<taskid>.task.md`, status transitions, sidecar-without-frontmatter rules, and per-status folder movement.
  - Create: No files unless a skill README is clearly needed.
  - Modify: `.agents/skills/backlog-draft/SKILL.md`, `.agents/skills/backlog-refine/SKILL.md`, `.agents/skills/backlog-clarify/SKILL.md`, draft/refine/clarify scripts as needed.
  - Delete: References that describe `.task.draft.md` as the desired main artifact.
  - Touch points: Scaffold input/output, question/clarify artifact rules, transition to `refining`, transition to `refined`.
  - Validation: The skills describe the same lifecycle and do not instruct agents to write lifecycle frontmatter to sidecars.
  - Notes: The current task may temporarily use old files while implementing the new world. That is allowed only as migration input.

- [ ] Step 2 — Update planning and execution instructions
  - Achieve: Make plan acceptance set status `planned`, execution set status `wip`, and both move the folder to the matching status subfolder through shared transition helpers.
  - Create: Nothing unless helper scripts need small wrappers.
  - Modify: `.agents/skills/backlog-plan/SKILL.md`, `.agents/skills/backlog-execute/SKILL.md`, related resolve/transition scripts.
  - Delete: Old `ready` terminology where it is no longer compatible.
  - Touch points: Acceptance semantics, transition commands, BACKLOG link conventions, notes creation guidance.
  - Validation: Skill docs and scripts agree that `planned` replaces `ready` for accepted plans.
  - Notes: Keep accepted-plan execution handoff behavior intact.

- [ ] Step 3 — Update completion, archive, and migration support instructions
  - Achieve: Keep historical flows compatible while using canonical task metadata and new status folders.
  - Create: No files.
  - Modify: `.agents/skills/backlog-complete/SKILL.md`, `.agents/skills/backlog-archive/SKILL.md`, `.agents/skills/backlog-migrate/SKILL.md`, any complete/archive scripts if present.
  - Delete: Stale `.task.refined.md`/`.task.draft.md` lifecycle ownership language.
  - Touch points: Completed/archived status handling, historical logs, artifact link rendering.
  - Validation: Completion/archive docs do not reintroduce split task metadata ownership.
  - Notes: Completed and archived redesign is not the main goal; adjust only enough to keep the new model coherent.

### Milestone 5 — Controlled Migration of the Current Backlog

- [ ] Step 1 — Run migration dry-run on the live backlog
  - Achieve: Produce a clear report of planned renames, metadata merges, sidecar frontmatter removals, folder moves, and index changes for `docs/backlog`.
  - Create: Optional temporary report under `/tmp` if useful.
  - Modify: Nothing in dry-run.
  - Delete: Nothing.
  - Touch points: `docs/backlog`, migration script output.
  - Validation: Dry-run exits cleanly and reports CO54 plus existing active tasks in a way that matches the refined task’s rules.
  - Notes: Stop if the report would touch unrelated dirty work unexpectedly.

- [ ] Step 2 — Apply migration and doctor repair
  - Achieve: Convert the active backlog to canonical task files, status folders, and local indexes.
  - Create: `BACKLOG_<status>.md` files in status folders.
  - Modify: Active task folders under `docs/backlog`, root `docs/backlog/BACKLOG.md`.
  - Delete: Obsolete duplicate task artifacts only when migration has preserved their task content and metadata in the canonical task file.
  - Touch points: Existing active backlog task files, status directories, root and local indexes.
  - Validation: `backlog-doctor` reports no drift after repair; every active task appears exactly once in the expected index.
  - Notes: Do not migrate completed/archived history unless required by accepted scope or script compatibility.

### Milestone 6 — Verification and Safety Checks

- [ ] Step 1 — Run focused script checks
  - Achieve: Prove the core model and migration behavior work without relying on manual inspection.
  - Create: Temporary fixture directories under `/tmp` or a narrow repo-local test fixture if justified.
  - Modify: Nothing outside test fixtures.
  - Delete: Temporary fixtures after use if they are not needed for debugging.
  - Touch points: `backlog-core`, `backlog-migrate`, `backlog-doctor`, status transition scripts.
  - Validation: Checks cover metadata parsing, refined-to-task metadata merge, filename migration, sidecar frontmatter removal, doctor dry-run, doctor repair, index rebuild, and at least one representative transition.
  - Notes: Prefer deterministic command output that future agents can rerun.

- [ ] Step 2 — Final repository verification
  - Achieve: Confirm the live repository is coherent after migration and docs updates.
  - Create: Nothing.
  - Modify: Nothing unless verification reveals a scoped fix.
  - Delete: Nothing.
  - Touch points: `docs/backlog/BACKLOG.md`, status-local indexes, active task folders, changed skill files.
  - Validation: `git status --short` is reviewed; no `CHANGELOG.md` changes; root/local indexes point to existing files; no task appears in multiple active statuses.
  - Notes: Run `npm run qa` only if app/TypeScript code was modified. Skill/script-only work should use focused script validation instead.
