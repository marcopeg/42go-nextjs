# Plan — Add Progressive Reveal and Played-Text Dimming to Reader Playback

## Goal

Add two persistent, independent playback-focus options that use the existing active sentence as the sole visual frontier, hide all later reader content when requested, and dim prior sentences without changing playback behavior.

## Milestones

### Milestone 1 — Extend the canonical playback preference contract

- [x] Step 1 — Add focus preferences and controller setters
  - Achieve: Extend the existing device-local playback preference model with two safe boolean values that default to off, sanitize legacy or malformed records, and update immediately through the playback controller.
  - Create: n/a
  - Modify: `playback-preferences.ts`, `types.ts`, `useReaderPlayback.ts`
  - Delete: n/a
  - Touch points: `ReaderPlaybackPreferences`, defaults, storage sanitizer, controller interface, preference commit callbacks.
  - Validation: Existing stored records load with both new options off; valid stored booleans survive sanitization; setters do not alter speech state.
  - Notes: Keep the existing `lingocafe.reader.playback-preferences.v1` record and backward-compatible field-level sanitization.

- [x] Step 2 — Add the two controls to Playback settings
  - Achieve: Show `Reveal as you listen` and `Dim previous sentences` as independent switches in the existing anchored playback settings panel.
  - Create: n/a
  - Modify: `BookReaderPlaybackSettings.tsx`
  - Delete: n/a
  - Touch points: Existing `PlaybackToggle` rows, switch semantics, compact mobile panel scrolling/height.
  - Validation: Each switch reflects canonical preference state, applies immediately, stays independent, and remains usable on mobile and desktop.
  - Notes: Reuse the existing switch row and semantic tokens. Do not create a second settings surface or preference store.

### Milestone 2 — Apply positional reveal and dimming to the reader

- [x] Step 3 — Implement reader focus presentation from the active sentence
  - Achieve: While playback is playing, delaying, or paused, use the active sentence DOM position to dim earlier sentences and visually hide every later sibling branch through the article boundary while preserving layout geometry. Restore prior DOM presentation on every update and cleanup.
  - Create: `reader-playback/focus-presentation.ts`
  - Modify: `BookPageReader.tsx`
  - Delete: n/a
  - Touch points: Stable `data-reader-sentence-id` wrappers, article ref, layout effect lifecycle, theme-aware opacity, native `hidden` behavior, translation targets.
  - Validation: Forward and backward active-sentence changes recompute the frontier; hidden content retains layout space but leaves pointer interaction, keyboard order, and the accessibility tree; disabling either option restores only its own presentation.
  - Notes: Keep this presentation-only. Do not add playback history or mutate book data.

- [x] Step 4 — Wire both responsive reader surfaces to the canonical playback state
  - Achieve: Pass focus activation and both preferences into desktop and mobile `BookPageReader` instances, with completed, idle, error, closed, unavailable, and no-active-sentence states showing the full page at normal contrast.
  - Create: n/a
  - Modify: `BookReaderSurfaces.tsx`
  - Delete: n/a
  - Touch points: Desktop/mobile reader prop wiring and status gating.
  - Validation: Playing/delay/paused states preserve the frontier; closing or completion restores content; continuous page transitions activate when the destination sentence becomes active.
  - Notes: Keep desktop and mobile behavior identical.

### Milestone 3 — Verify regression safety and visual hierarchy

- [x] Step 5 — Run focused static and behavioral checks
  - Achieve: Confirm preference compatibility, type correctness, lint compliance, and presentation cleanup paths before the full build.
  - Create: n/a
  - Modify: n/a unless validation exposes defects.
  - Delete: n/a
  - Touch points: New preference fields/setters, DOM restoration, seek preview and committed seek behavior, active highlight compatibility.
  - Validation: Focused ESLint and TypeScript pass; source inspection confirms every mutated DOM property is restored.
  - Notes: The repository has no browser DOM unit-test harness, so DOM behavior requires live verification.

- [x] Step 6 — Run QA and visually verify the reader
  - Achieve: Prove the production build and all four toggle combinations on the LingoCafe reader in mobile and desktop layouts, including pause, close, and seeking.
  - Create: n/a
  - Modify: Execution notes with results and any deviations.
  - Delete: n/a
  - Touch points: `npm run qa`, `https://lc42go.ngrok.app/`, light/dark reader themes, headings, summary/body boundary, translation interaction.
  - Validation: QA passes; normal, reveal-only, dim-only, and combined modes behave as accepted; closing playback restores the complete page.
  - Notes: `npm run qa` passes. The operator completed real-phone verification after the stable-geometry revision and accepted the final reveal, dimming, theme, and scroll behavior.

### Milestone 4 — Stabilize progressive-reveal scrolling

- [x] Step 7 — Give the reveal frontier its own scroll anchor
  - Achieve: When progressive reveal is active, suppress normal sentence-centering, add bottom space based on the reader scroll-container height, and pin the last visible sentence roughly 25–30% from the top by scrolling to the revealed content bottom.
  - Create: n/a
  - Modify: `focus-presentation.ts`, `BookPageReader.tsx`, `useReaderPlayback.ts`, `reader-playback-focus.test.mts`
  - Delete: n/a
  - Touch points: Reader article padding restoration, scroll-container bottom position, forward/backward seek preview, committed seek, guided scrolling, enable/disable cleanup.
  - Validation: The frontier owns scroll positioning only while reveal is enabled; disabling reveal restores full content without losing the active reading position; dim-only mode retains normal guided centering.
  - Notes: Added from operator testing on a real mobile phone.

- [x] Step 8 — Smooth and tighten frontier movement
  - Achieve: Animate reveal-owned scroll changes with a short eased transition and reduce dynamic bottom space from 70% to 55% of the available reader height.
  - Create: n/a
  - Modify: `focus-presentation.ts`, `reader-playback-focus.test.mts`
  - Delete: n/a
  - Touch points: Scroll animation cancellation, reduced-motion handling, bottom-padding calculation, enable/disable cleanup.
  - Validation: Normal frontier advances move over roughly 180 ms; reduced-motion mode remains immediate; cleanup cancels stale animation frames.
  - Notes: Added from the operator’s successful mobile functional retest. The manual animation implementation was superseded by Step 9 after it flickered on the real phone.

- [x] Step 9 — Replace manual animation with native smooth scrolling
  - Achieve: Remove the custom animation-frame loop and delegate reveal movement to the browser through `scrollTo({ behavior: "smooth" })`.
  - Create: n/a
  - Modify: `focus-presentation.ts`, `reader-playback-focus.test.mts`
  - Delete: n/a
  - Touch points: Native scroll invocation, reduced-motion behavior, cleanup cancellation.
  - Validation: No manual scroll interpolation remains; native smooth scrolling targets the revealed content bottom; cleanup stops an in-flight native scroll without changing the current position.
  - Notes: Added after real-phone testing found the custom animation fighting mobile scroll layout.

- [x] Step 10 — Preserve reader geometry and restore canonical auto-scroll
  - Achieve: Keep all future reader content in layout with CSS visibility and remove reveal-owned scrolling, dynamic padding, and progressive-reveal exceptions from the canonical guided-scroll flow.
  - Create: n/a
  - Modify: `focus-presentation.ts`, `BookPageReader.tsx`, `useReaderPlayback.ts`, `reader-playback-focus.test.mts`
  - Delete: n/a
  - Touch points: Future-branch visibility restoration, sentence advance, seek preview, committed seek, title/header scroll stability.
  - Validation: Reveal changes no layout measurements; normal and reveal modes use the same existing sentence-centering calls; no reveal-specific `scrollTo()` or padding remains.
  - Notes: Added after real-phone testing showed that collapsing future branches made the reader title and scroll container flicker even with native smooth scrolling.
