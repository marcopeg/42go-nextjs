# Plan — Make the LingoCafe Translation-Scope FAB a One-Tap Toggle

## Goal

Replace the two-step sentence/word floating selector in Conversations and Book Reader with one shared, 56 px-high pill FAB that visibly shows its active mode and switches scope immediately in one activation.

## Milestones

### Milestone 1 — Establish the shared one-tap control

- [x] Step 1 — Build the translation-scope pill FAB
  - Achieve: A shared client control that presents the translation icon and current `word`/`sentence` label, toggles its canonical scope in one click/tap/key activation, rolls its label upward on change, and exposes the current and next action accessibly.
  - Create: `src/components/ui/translation-scope-fab.tsx`.
  - Modify: None.
  - Delete: None.
  - Touch points: reader translation scope type, shared Button/Popover primitives, semantic tokens, reduced-motion contract.
  - Validation: Type-check through `npm run qa`; inspect source for button semantics, reduced-motion handling, and accurate accessible state.
  - Notes: Desktop hover and keyboard focus explain the next target mode without creating a second interaction step.

### Milestone 2 — Adopt the control in both reader surfaces

- [x] Step 2 — Replace the Conversation speed dial
  - Achieve: Conversations uses the shared pill in the existing left floating-action slot while preserving capability gates, safe-area positioning, and voiceover behavior.
  - Create: None.
  - Modify: `src/app/(app)/(lingocafe)/conversations/_components/ConversationTranslation.tsx`.
  - Delete: Translation-scope-specific `ExpandableFab` usage in that surface.
  - Touch points: `ConversationActionFab`, canonical conversation translation-scope callback.
  - Validation: Static/source test coverage and mobile visual check near 390 px.
  - Notes: Keep the existing right-side playback FAB unchanged.

- [x] Step 3 — Replace the Book Reader speed dial
  - Achieve: Book Reader uses the same shared pill in its existing mobile-left/desktop-right action-bar slots, preserving reader theme variables and playback gating.
  - Create: None.
  - Modify: `src/app/(app)/(lingocafe)/books/_components/BookReaderFloatingActionBar.tsx`.
  - Delete: Translation-scope-specific `ExpandableFab` action/menu styling in that surface.
  - Touch points: `BookReaderFloatingActionBar`, reader preference callback, reader themes.
  - Validation: Static/source test coverage plus reader visual check at mobile and desktop widths.
  - Notes: Keep `ExpandableFab` for the direct playback action and retain its generic menu capability untouched.

### Milestone 3 — Verify the mobile and desktop contract

- [x] Step 4 — Add regression coverage and run quality checks
  - Achieve: Automated coverage asserts both surfaces use the shared one-tap control, menu-specific translation options no longer appear, and the shared component preserves label, next-mode help, and motion/accessibility contracts.
  - Create: Focused source-level regression test if the existing test suite has no appropriate home.
  - Modify: Relevant LingoCafe reader/conversation tests.
  - Delete: None.
  - Touch points: LingoCafe reader and conversation test suites.
  - Validation: `npm run qa`; browser checks at about 390 px and desktop width in light/dark reader themes, including click/tap, keyboard focus, and reduced motion.
  - Notes: Added focused source-level coverage and ran lint plus the focused test. Full production build and live click verification are blocked by documented environment/toolchain constraints; see `TR73.notes.md`.
