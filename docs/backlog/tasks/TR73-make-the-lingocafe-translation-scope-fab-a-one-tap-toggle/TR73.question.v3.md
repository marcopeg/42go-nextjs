# Refinement Questions v3 — Make the LingoCafe Translation-Scope FAB a One-Tap Toggle

Answer in chat. The answer will be recorded in the canonical task before the next question.

## Questions

### 1. After-tap feedback

On mobile, after the FAB changes from `sentence` to `word` (or back), should it only update its visible label immediately, or also show a brief confirmation toast/message?

Answer:

Only update the label. Animate the label as a rolling transition upward into the next mode label.
