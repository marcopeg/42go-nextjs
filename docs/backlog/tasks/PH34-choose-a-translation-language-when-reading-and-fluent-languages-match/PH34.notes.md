# Execution Notes — Choose a Translation Language When Reading and Fluent Languages Match
**Task**: ./PH34.task.md
**Plan**: ./PH34.plan.md

## Decisions

- Use the existing LingoCafe fluent-language list and exclude the normalized reading language.
- A language selection immediately patches `ownLang`; successful persistence resumes the pending translation.
- Reject direct same-language requests before cache lookup.
- Share normalization and option filtering through a browser-safe LingoCafe helper used by both reader and backend code.

## Problems Encountered

- The normal `john` test account was blocked by first-run legal consent. No consent was accepted. Visual verification used the already-complete `admin` test profile instead.
- The browser automation's first native select interaction collapsed the popover after the save completed. A follow-up tap returned the new client-cached English translation, confirming the profile update and translation target. The selector, save state, persisted profile value, and translation result were all observed.

## Deviations From Plan

- Used the alternate seeded account for visual verification because the normal test account required legal consent.

## Additional Requests

- Removed the visible `Translate to` label from the reader popover while retaining it as the select's screen-reader-only accessible label.
- The operator withdrew the suspected mobile regression after identifying it as local behavior.

## Known Limitations

- The profile-save failure state is implemented and code-reviewed but was not forced against the shared test tunnel.

## Validation

- `npm run test:lingocafe-translation-language` passes: 3 tests, 0 failures.
- `npm run qa` passes, including ESLint, TypeScript, and the Next.js production build.
- Desktop browser verification confirmed the same-language prompt, Swedish exclusion, immediate English profile save, and English sentence translation.
- Mobile verification at 390 × 844 confirmed a 390 px popover with no horizontal overflow, square mobile edges, and a 40 px select.
- The test profile was restored to Swedish and the test browser session was logged out.
