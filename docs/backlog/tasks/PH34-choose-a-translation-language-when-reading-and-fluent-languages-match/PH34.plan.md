# Plan — Choose a Translation Language When Reading and Fluent Languages Match

## Goal

Replace same-language translation failures with an in-reader destination-language choice that persists `ownLang` and automatically resumes the pending translation.

## Milestones

### Milestone 1 — Establish shared language rules and backend protection

- [x] Step 1 — Add client-safe translation-language helpers
  - Achieve: Normalize language codes consistently and derive valid fluent-language destinations that exclude the reading language.
  - Create: `src/lib/lingocafe/translation-language.ts`.
  - Modify: Translation backend imports as needed.
  - Delete: Nothing.
  - Touch points: LingoCafe profile language options and translation route.
  - Validation: Focused unit tests cover normalization, equality, and destination filtering.
  - Notes: Keep helpers browser-safe; do not import server-only translation modules into the reader.

- [x] Step 2 — Reject same-language API requests early
  - Achieve: Return a specific client-safe validation response before cache lookup, event creation, or provider use.
  - Create: Focused regression coverage where practical.
  - Modify: `src/app/api/(lingocafe)/lingocafe/translate/route.ts`.
  - Delete: Nothing.
  - Touch points: Formal validation and cache short-circuit order.
  - Validation: Direct same-language request returns HTTP 400 with a destination-language message.
  - Notes: Preserve all existing authorization, page validation, cache, and provider behavior for differing languages.

### Milestone 2 — Add the in-reader save-and-translate flow

- [x] Step 1 — Model language-choice and save states
  - Achieve: Detect normalized source/destination equality before translation, retain the tapped word or sentence, and prevent translation/cache requests during conflict resolution.
  - Create: No new component unless extraction materially improves clarity.
  - Modify: `BookPageReader.tsx`.
  - Delete: Nothing.
  - Touch points: Translation state, request effect, page-change synchronization, sentence and word modes.
  - Validation: Differing-language taps remain unchanged; same-language taps enter language-choice state.
  - Notes: Keep the implementation local to the reader translation interaction.

- [x] Step 2 — Render and persist the destination choice
  - Achieve: Show a labelled native `Translate to` select using fluent-language options minus the reading language. Selection immediately patches `ownLang`, shows saving/error states, then resumes the pending translation without another action.
  - Create: Nothing unless a small pure helper is needed.
  - Modify: `BookPageReader.tsx`.
  - Delete: Nothing.
  - Touch points: Existing translation popover, `/api/profile` PATCH contract, active reader translation target.
  - Validation: Keyboard-accessible select; mobile and desktop geometry; failed saves keep the selector open and block translation.
  - Notes: Use reader-derived semantic surface variables and existing input/error conventions.

### Milestone 3 — Verify and document execution

- [x] Step 1 — Run automated quality gates
  - Achieve: Prove focused language behavior and repository integrity.
  - Create: Focused test file and package script only if useful.
  - Modify: Plan and execution notes.
  - Delete: Nothing.
  - Touch points: Node test runner, lint, production build.
  - Validation: Focused tests and `npm run qa` pass.
  - Notes: Record any unrelated warnings without broadening scope.

- [x] Step 2 — Visually verify the reader
  - Achieve: Confirm same-language prompt, filtering, immediate save-and-translate behavior, errors, and unchanged normal translation on LingoCafe.
  - Create: Nothing.
  - Modify: Execution notes.
  - Delete: Nothing.
  - Touch points: `https://lc42go.ngrok.app/`, normal user reader, mobile and desktop widths.
  - Validation: Browser inspection at desktop and approximately 390 px mobile width in light/dark context where practical.
  - Notes: Preserve and restore the test user's original profile language after verification.
