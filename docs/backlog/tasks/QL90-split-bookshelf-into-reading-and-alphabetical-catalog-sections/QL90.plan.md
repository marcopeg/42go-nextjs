# Plan — Split Bookshelf Into Reading and Alphabetical Catalog Sections

## Goal

Make the LingoCafe `/books` shelf show resumable books first in a `Currently Reading` section sorted by most recently read, then show the remaining `Catalog` sorted alphabetically by title, with existing card navigation preserved.

## Milestones

### Milestone 1 — Locate Shelf Data Flow

- [x] Step 1 — Confirm current reader payload and card navigation
  - Achieve: Verify the books page has the data needed to identify reading books and preserve existing click behavior.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `src/app/(app)/(lingocafe)/books/page.tsx`, `src/app/(app)/(lingocafe)/books/_components/BookCard.tsx`, `src/app/(app)/(lingocafe)/books/_components/book-types.ts`, `src/app/api/(lingocafe)/lingocafe/_lib/reader.ts`
  - Validation: Confirm `ReaderBook.readingAction.kind` and `readingAction.href` are available on every normalized book.
  - Notes: Do not change persistence or introduce a new reading-state signal unless the current payload proves insufficient.

### Milestone 2 — Split and Sort Books

- [x] Step 1 — Add deterministic bookshelf grouping
  - Achieve: Derive `currentlyReadingBooks` and `catalogBooks` from `data.books` without duplicates.
  - Create: Small local helper functions if useful, such as title comparison and reading-state predicates.
  - Modify: `src/app/(app)/(lingocafe)/books/page.tsx`, `src/app/api/(lingocafe)/lingocafe/_lib/reader.ts`, `src/app/(app)/(lingocafe)/books/_components/book-types.ts`
  - Delete: n/a
  - Touch points: The branch that currently renders one grid from `data.books`.
  - Validation: Books with `readingAction.kind === "resume"` and a string href appear only in the reading group; all others appear only in the catalog group.
  - Notes: Sort the reading group by progress `updated_at` descending with title as fallback. Sort the catalog by title using a stable, case-insensitive comparison. Keep the API language filter intact.

- [x] Step 2 — Stop relying on level-first API order for this surface
  - Achieve: Ensure `/books` presentation order is recency-first for reading books and alphabetical for catalog books regardless of the API's current `level`, then `title` order.
  - Create: n/a
  - Modify: `src/app/(app)/(lingocafe)/books/page.tsx`
  - Delete: n/a
  - Touch points: Derived arrays used by the shelf renderer.
  - Validation: The UI can render correctly even if the API returns level-first data.
  - Notes: Avoid changing the API order unless implementation shows another reader surface depends on the same returned order and the safer path is server-side sorting.

### Milestone 3 — Render Shelf Sections

- [x] Step 1 — Add a reusable section renderer for book grids
  - Achieve: Render section headings and existing book-card grids without duplicating fragile markup.
  - Create: Inline page-level render helper or small component if it keeps the page readable.
  - Modify: `src/app/(app)/(lingocafe)/books/page.tsx`
  - Delete: n/a
  - Touch points: Existing grid classes and `BookCard` mapping.
  - Validation: The existing responsive grid behavior, mobile density, loading state, onboarding form, empty state, and error display still work.
  - Notes: Keep styling restrained and aligned with the current shelf. This is a section split, not a page redesign.

- [x] Step 2 — Show `Currently Reading` only when needed
  - Achieve: Render `Currently Reading` first only when the reading group is non-empty; render `Catalog` below with the remaining books.
  - Create: n/a
  - Modify: `src/app/(app)/(lingocafe)/books/page.tsx`
  - Delete: n/a
  - Touch points: Non-loading, non-onboarding, non-empty catalog branch.
  - Validation: If no books are resumable, the page renders only the normal catalog view and does not show an empty or misleading reading section.
  - Notes: Use `Catalog` as the second section label from the refined task.

### Milestone 4 — Preserve Behavior and Verify

- [x] Step 1 — Validate navigation behavior
  - Achieve: Confirm reading books still link directly to the reader resume href and non-reading books still link to details.
  - Create: n/a
  - Modify: Prefer no `BookCard` changes unless needed.
  - Delete: n/a
  - Touch points: `BookCard` link logic and rendered sections.
  - Validation: Inspect rendered links or use a browser smoke test with the LingoCafe test user.
  - Notes: Existing `BookCard` already owns the navigation decision; grouping should not override it.

- [x] Step 2 — Run quality gates
  - Achieve: Catch lint, type, and build regressions.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: Whole project.
  - Validation: Run `npm run qa`.
  - Notes: `npm run qa` passed. Browser smoke verification was attempted, but Playwright/browser automation is not installed in this workspace.
