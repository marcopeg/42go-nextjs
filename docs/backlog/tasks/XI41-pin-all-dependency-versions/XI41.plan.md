# Plan — Pin All Dependency Versions

**TaskID**: XI41
**Status**: completed

## Goal

Convert every semver range in `package.json` to exact pins, regenerate `package-lock.json` so the lockfile matches those pins, and prove the repo still passes `yarn qa` with deterministic dependency resolution.

## Milestones

### Milestone 1 — Readiness Guardrail

- [x] Step 1 — Confirm XI41 is safe to run last
  - Achieve: Verify the dependency-upgrade sequence has finished and capture any blockers that would make pinning premature.
  - Create: None.
  - Modify: `.agents/backlog/XI41.pin-all-versions.md`, `.agents/backlog/XI41.pin-all-versions.notes.md`, `.agents/backlog/BACKLOG.md`.
  - Delete: None.
  - Touch points: `.agents/backlog/BACKLOG.md`, `.agents/backlog/UT41.eslint-10-upgrade.md`, `.agents/backlog/UT41.eslint-10-upgrade.notes.md`, `package.json`.
  - Validation: Backlog and task notes show XI41 still belongs at the end of the sequence; any unresolved upstream dependency work is documented before editing manifests.
  - Notes: UT41 is paused, but the repo's validated baseline is intentionally ESLint 9 today. Execution proceeds on that baseline with an explicit note that XI41 must be re-verified if UT41 later changes the dependency set.

### Milestone 2 — Pin the Manifest

- [x] Step 2 — Replace every semver range with an exact version
  - Achieve: Remove every leading `^` and `~` from `dependencies` and `devDependencies` without changing the numeric versions.
  - Create: None.
  - Modify: `package.json`.
  - Delete: None.
  - Touch points: `dependencies`, `devDependencies`, and any future dependency blocks that contain installable packages.
  - Validation: Search/grep over `package.json` returns zero remaining `^` or `~` version prefixes.
  - Notes: Exact pins already present for `next`, `eslint-config-next`, `@types/react`, and `@types/react-dom` were preserved. Cold reinstall testing exposed that `@dnd-kit/core` had to be corrected from `6.1.0` to `6.3.1` to satisfy `@dnd-kit/sortable@10.0.0` without relying on the previous lockfile.

- [x] Step 3 — Reconcile the overrides block with the final pins
  - Achieve: Ensure `overrides` remains explicitly pinned and still matches the pinned package versions where alignment is intentional.
  - Create: None.
  - Modify: `package.json`.
  - Delete: None.
  - Touch points: `overrides`, `@types/react`, `@types/react-dom`.
  - Validation: `package.json` shows only exact versions in `overrides`; any version shared with the main dependency graph matches deliberately.
  - Notes: `overrides` already used exact pins and did not require changes beyond verification.

### Milestone 3 — Lockfile Sync & Verification

- [x] Step 4 — Regenerate the lockfile from the pinned manifest
  - Achieve: Re-run installation so `package-lock.json` reflects the exact versions declared in `package.json`.
  - Create: None.
  - Modify: `package-lock.json`.
  - Delete: None.
  - Touch points: npm install resolution output, lockfile package metadata.
  - Validation: `npm install` completes successfully; `package-lock.json` diff contains only lockfile synchronization expected from pinning.
  - Notes: Verified with a stronger cold-start path: removed `node_modules`, `.next`, `tsconfig.tsbuildinfo`, and `package-lock.json`; cleared npm and Yarn caches; then reinstalled from `package.json` alone. The regenerated lockfile now records `@dnd-kit/core: 6.3.1` and the full pinned manifest.

- [x] Step 5 — Run final deterministic-build verification
  - Achieve: Prove the repo still lint-builds after the manifest and lockfile are pinned.
  - Create: None.
  - Modify: None unless verification exposes a dependency-resolution regression.
  - Delete: None.
  - Touch points: Terminal execution, `package.json`, `package-lock.json` only if a corrective sync is needed.
  - Validation: `yarn qa` passes, and a final grep/search confirms no semver ranges remain in `package.json`.
  - Notes: Cold `yarn qa` initially exposed real lint/type issues in the quicklist and theme-provider code paths; those were fixed in code and the final rerun passed. Remaining output matches the known baseline warnings around NFT tracing and repeated `next/headers` fallback messages.