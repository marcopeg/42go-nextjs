---
taskId: XI41
status: archived
createdAt: 2026-04-23T15:27:45+02:00
completedAt: 2026-04-23T14:55:49+02:00
compressedAt: 2026-07-29T18:45:00+02:00
compressedFromCommit: 27c91fcf1650c9cb49d27cb7c10a6484fbfe4329
---
# Pin All Dependency Versions

## Historical Summary

- Every dependency in package.json uses an exact version (no `^`, `~`, or `*`) - `overrides` section matches pinned versions - `npm run qa` passes

## Retrieval Anchors

- Task: `XI41` — Pin All Dependency Versions.
- Identifier: `accept + execute`.
- Identifier: `npm install`.
- Identifier: `package.json`.
- Identifier: `ERESOLVE`.
- Identifier: `@dnd-kit/sortable@10.0.0`.
- Identifier: `@dnd-kit/core@^6.3.0`.
- Identifier: `@dnd-kit/core@6.1.0`.
- Identifier: `yarn qa`.
- Identifier: `src/42go/config/ThemeProvider.tsx`.
- Identifier: `src/app/(app)/quicklists/[id]/page.tsx`.
- Identifier: `src/app/(app)/quicklists/[id]/info/page.tsx`.
- Identifier: `src/lib/quicklists/hooks/useQuicklistData.ts`.
- Path: `src/42go/config/ThemeProvider.tsx`.
- Path: `src/app/(app)/quicklists/[id]/page.tsx`.
- Path: `src/app/(app)/quicklists/[id]/info/page.tsx`.
- Path: `src/lib/quicklists/hooks/useQuicklistData.ts`.
- Source artifacts: `XI41.notes.md`, `XI41.plan.md`, `XI41.task.md`.

## Durable Outcome and Decisions

- Execution started after explicit `accept + execute` from the operator.
- XI41 proceeds on the currently validated ESLint 9 baseline even though UT41 remains paused.
- If UT41 later changes the dependency set, XI41 must be re-verified or rerun so pins stay aligned with the final manifest.
- Cold-install validation is required for this task; a warm lockfile-only `npm install` was not treated as sufficient proof.
- The manifest itself is now treated as authoritative, so dependency pins must satisfy peer constraints without relying on an older lockfile's resolved tree.

## Validation and Limitations

- No task-specific validation result was preserved beyond the completed/archived record.
- Limitations: A future UT41 resume could require XI41 to be rerun to re-pin any changed ESLint-related versions. Final QA still emits the known baseline warnings: the Turbopack NFT tracing warning for `next.config.ts` and repeated `Cannot access next/headers, falling back to environment matching only` build output.

## Task Relationships

### Supersedes

None identified.

### Superseded by

None identified.

### Related Tasks

None identified.

## Compression Provenance

Compressed 3 artifacts: `XI41.notes.md`, `XI41.plan.md`, `XI41.task.md`. Their exact pre-compression versions are recoverable from `compressedFromCommit` (`27c91fcf1650c9cb49d27cb7c10a6484fbfe4329`), for example:

```bash
git show 27c91fcf1650c9cb49d27cb7c10a6484fbfe4329:docs/backlog/tasks/XI41-pin-all-dependency-versions/XI41.task.md
```
