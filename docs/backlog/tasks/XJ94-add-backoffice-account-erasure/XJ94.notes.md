# Execution Notes - Add Backoffice Account Erasure
**Task**: ./XJ94.task.md
**Plan**: ./XJ94.plan.md

## Decisions

- Use a static server-only registry for app erasure handlers in this task.
- Keep existing unrelated package manifest changes untouched.

## Problems Encountered

- Browser smoke against `https://lc42go.ngrok.app/users` was blocked by the local LingoCafe admin account being forced through profile onboarding. The local database also needs the new `users:delete` grant seed applied before the delete controls become visible for that admin session.

## Deviations From Plan

- Did not add a new automated test file. Validation was performed with `npm run qa`; focused server behavior is isolated in `runAccountErasure` and app handlers for future direct tests.

## Additional Requests

- Fix the `/users` details side panel close animation so it slides out instead of disappearing abruptly.
- Use the delete button in the details panel to initiate the new delete user flow.
- Keep development seeds self-healing for `users:delete`, compact the delete confirmation UI, hide the target email outside the typed-confirmation placeholder, show the final confirmation as a nested small modal, and close the user drawer after deletion.
- Namespace concurrent user overlay keys, make dismissing the final confirmation cancel the whole delete flow, and align visible labels on "Delete account".
- Add shared neutral and destructive secondary button variants so Cancel/dismiss actions do not inherit app-primary hover colors and destructive actions remain red on hover.
- Rename the successful account deletion audit event to `user.deleted`.
- Security review hardening: verify the acting operator still exists in the current app inside the erasure transaction, register Quicklist erasure for the default app because it exposes Quicklists, and scope Quicklist cleanup through app-owned projects.
- Security review hardening: remove app-scoped email auth verification tokens and throttle rows for the target email because those auth tables are keyed by email and do not cascade from `auth.users`.
- Security review hardening: delete Quicklist invites addressed to the target email, not only invites created by the target user, because invite email addresses are not FK-backed.

## Known Limitations

- Manual browser verification of the final delete dialog is still pending after applying/seeding `users:delete` in the local dev database and completing the admin profile.
- The detail panel close-animation fix was implemented by preserving `detailUser` while the panel closes instead of clearing the panel content at close start.
