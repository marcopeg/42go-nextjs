# Refinement Questions v3 — Let readers mark a book as read

Answer inline under each question, then return to chat and say `next round`.

## Questions

### 1. What should happen to the existing `books_progress.completed_at` column?

Choose whether the migration should:

- copy any existing completion values into the new dedicated table and then drop the column
- leave the column in place but stop using it
- follow another explicit compatibility rule

Answer:

The book progress is at page level. We are adding a flag for the entire book, so it's a different thing. The book progress gets updated also while reading a page, while scrolling a page, and when changing a page. Those are internal reading information. Is that not the case?

### 2. What reading CTA should a completed book show on `/books/[bookId]`?

The page will also offer “Mark as unread.” For the reading CTA, choose one:

- keep `Continue reading` when progress exists, otherwise `Read now`
- always offer `Read again` from the first page
- hide the reading CTA while completed
- another explicit behavior

Answer:

Keep the "Continue reading" and add also the "Mark as unread."

### 3. How should the bottom `Completed` section be ordered?

Choose alphabetical title order, most recently marked read first, or another deterministic rule.

Answer:

e "last read comes first"
