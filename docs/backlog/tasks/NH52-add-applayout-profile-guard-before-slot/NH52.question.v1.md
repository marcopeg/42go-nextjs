# Refinement Questions v1 — LingoCafe Onboarding Follow-up

Answer inline under each question, then return to chat and say `next round`.

## Context

`NH52` has been repurposed from the future `app.profile.guard.before` platform slot to a LingoCafe onboarding task. `YA00` already shipped the first onboarding guard: a full-screen AppLayout layout guard that collects language, level, and consent for incomplete LingoCafe profiles.

## Questions

### 1. What concrete onboarding improvement should this task deliver?

Examples: visual polish, clearer copy, multi-step flow, first-book guidance, post-completion redirect, analytics, better loading/error states, consent UX, or something else.

Answer:

So right now, there is a small flickering because when it first loads the layout and then it loads the pop-up of the modal. So, there is a slight flickering that I don't like. 

### 2. Should the current full-screen `LingocafeOnboardingGuard` remain the base experience?

Say whether this task should polish the existing guard, replace it with a different surface, or add a follow-up step after it completes.

Answer:

no, the dark components should stay basically the same it's just to implement the before mechanic so that it can render instead of the up layout and I expect that when it goes away then the up layout is rendered underneath the best thing would be if it's possible to let a delay for the  outgoing animation of like a couple of tens of milliseconds, like 0.3, 300 milliseconds, so that there is time for the play out to render underneath and then the guard slides away because I still want it to be implemented as model.

### 3. Which users and routes should be affected?

Clarify whether this applies only to first-time/incomplete profiles across authenticated LingoCafe routes, also to users missing newly required consent, or only to specific routes such as `/books`.

Answer:

Same for the other; this should not be related to routes. It should be completely route-independent. This is a guard; it has nothing to do with routes. 

### 4. What fields or steps must onboarding collect now?

The current guard collects own language, reading language, reading level, and consent. List any additions, removals, ordering changes, or copy changes.

Answer:

The onboarding part is already implemented. This task is only about implementing the before slot and using the configuration. You should basically not even touch the lingocafe onboarding part. 

### 5. What should happen immediately after onboarding completion?

Examples: stay on the current route, redirect to `/books`, show a welcome screen, open the first suggested book, refresh route data only, or another behavior.

Answer:

One more time, this should not have anything to do with the routes. The route should already be Brooks because LingoCafé sets that as a default route after login. But this should have nothing to do with this model. I'm worried because you keep asking me this. 

### 6. Should this task add onboarding analytics/events?

The current flow relies on existing profile and consent APIs. State whether new events such as `onboarding-open`, `onboarding-complete`, or `onboarding-error` are required.

Answer:

No, this task has nothing to do with the onboarding. This is only creating the before layout slot. I'm extremely worried about all these questions because this is only preparing the slot before. Now we have the slot layout that the LingoCafe onboarding is using to render a pop-up over the layout; we are just creating the slot before so that we can render the same onboarding before. That's it. 

### 7. What must be visually verified before this task is accepted?

List the required desktop/mobile flows, test account state, routes, or screenshots. If the default `john` / `john` ngrok flow is enough, say so.

Answer:

If the user that logs in doesn't have a profile or it is not completed, then we should only see the onboarding component, nothing else. As soon as the component submits the answers and the profile is completed by the server, then the component goes away and we should see a play out and the default route for the configuration. In the case of Ringo Cafe, we should see the books. 
