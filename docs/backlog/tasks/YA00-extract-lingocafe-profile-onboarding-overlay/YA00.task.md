---
taskId: YA00
status: completed
createdAt: 2026-05-13T15:31:25+02:00
completedAt: 2026-05-13T16:58:56+02:00
group: lingocafe
compressedAt: 2026-07-29T18:55:00+02:00
compressedFromCommit: 27c91fcf1650c9cb49d27cb7c10a6484fbfe4329
---
# Extract LingoCafe Profile Onboarding Overlay

## Historical Summary

Create an AppLayout-level profile layout guard system that can be configured per app. The first concrete guard is a LingoCafe profile onboarding guard that detects incomplete profile state for authenticated LingoCafe users and opens a full-screen takeover popup on both mobile and desktop.

## Retrieval Anchors

- Task: `YA00` — Extract LingoCafe Profile Onboarding Overlay.
- Identifier: `execute it`.
- Identifier: `YA00`.
- Identifier: `loader`.
- Identifier: `AppLayout`.
- Identifier: `LingocafeOnboardingGuard`.
- Identifier: `useProfileController`.
- Identifier: `/api/profile`.
- Identifier: `ProfileConsent`.
- Identifier: `profile:complete`.
- Identifier: `/books`.
- Identifier: `42go-ui-modal`.
- Identifier: `presentation="panel"`.
- Path: `src/42go/components/ProfileBlock/index.ts`.
- Path: `src/42go/components/ProfileBlock/types.ts`.
- Path: `src/AppConfig.ts`.
- Path: `src/42go/layouts/app/AppLayout.tsx`.
- Path: `src/42go/layouts/app/types.ts`.
- Path: `src/app/(app)/(lingocafe)/_components/LingocafeOnboardingGuard.tsx`.
- Path: `src/42go/components/modal/Modal.tsx`.
- Path: `src/config/lingocafe/config.ts`.
- Source artifacts: `YA00.notes.md`, `YA00.plan.md`, `YA00.question.v1.md`, `YA00.question.v2.md`, `YA00.task.md`.

## Durable Outcome and Decisions

- Treat the operator's `execute it` as plan acceptance plus execution.
- Transitioned `YA00` from refined to planned to wip before source edits.
- Implemented the guard config with a `loader` option so LingoCafe can lazy-load the onboarding guard from `AppLayout`.
- Built `LingocafeOnboardingGuard` as the first profile layout guard. It uses `useProfileController`, existing `/api/profile`, shared `ProfileConsent`, and dispatches `profile:complete` after a complete save.
- Removed the `/books` inline onboarding form. `/books` now renders a plain incomplete-profile note and reloads book data on `profile:complete`.
- Adjusted the onboarding Modal to follow the `42go-ui-modal` full-screen overlay page recipe: `presentation="panel"`, `anchor="right"`, `size="full"`, and desktop width forced to the full viewport so no underlying layout strip remains visible.
- Removed the visible Modal header from the onboarding overlay and moved the title/copy into centered page content.
- Switched the onboarding Modal to `anchor="top"` with full desktop height so the panel exits upward when the profile becomes complete.

## Validation and Limitations

- `npm run lint` passed.
- `git diff --check` passed.
- `npm run qa` passed, including after the full-screen panel adjustment.
- Runtime HTTP smoke against `https://lc42go.ngrok.app/` passed: `/`, `/books`, and `/profile` returned 200.
- Authenticated ngrok smoke with `john` / `john` passed: `/api/auth/session` returned a LingoCafe session, `/api/profile` returned `isComplete: false`, and `/api/lingocafe/books` returned an empty `books` array for the incomplete profile.
- Limitations: The LingoCafe guard uses the backend `/api/profile` completeness result through `useProfileController`; no separate client AJV completeness implementation was added. Browser visual verification was not completed because Browser plugin actions were not exposed in this session and Playwright is not installed in the checkout.

## Task Relationships

### Supersedes

None identified.

### Superseded by

None identified.

### Related Tasks

None identified.

## Compression Provenance

Compressed 5 artifacts: `YA00.notes.md`, `YA00.plan.md`, `YA00.question.v1.md`, `YA00.question.v2.md`, `YA00.task.md`. Their exact pre-compression versions are recoverable from `compressedFromCommit` (`27c91fcf1650c9cb49d27cb7c10a6484fbfe4329`), for example:

```bash
git show 27c91fcf1650c9cb49d27cb7c10a6484fbfe4329:docs/backlog/tasks/YA00-extract-lingocafe-profile-onboarding-overlay/YA00.task.md
```
