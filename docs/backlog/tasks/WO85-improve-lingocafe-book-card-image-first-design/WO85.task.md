---
taskId: WO85
status: archived
createdAt: 2026-05-01T06:46:59+02:00
completedAt: 2026-05-01T07:16:42+02:00
group: lingocafe
compressedAt: 2026-07-29T18:45:00+02:00
compressedFromCommit: 27c91fcf1650c9cb49d27cb7c10a6484fbfe4329
---
# Improve LingoCafe Book Card Image-First Design

## Historical Summary

Each book card shows only the book image as the visible card surface. The card should have no visible border, while preserving rounded corners and the tall book-cover ratio.

## Retrieval Anchors

- Task: `WO85` — Improve LingoCafe Book Card Image-First Design.
- Identifier: `WO85`.
- Identifier: `BookCover`.
- Identifier: `object-cover`.
- Identifier: `object-contain`.
- Identifier: `BookCard`.
- Identifier: `BookTags`.
- Identifier: `http://localhost:3000/books`.
- Identifier: `book.author`.
- Identifier: `languageLevel`.
- Identifier: `book.lang`.
- Identifier: `book.level`.
- Identifier: `curl -I -s http://localhost:3000/books`.
- Path: `src/app/(app)/(lingocafe)/books/_components/BookCover.tsx`.
- Path: `src/app/api/(lingocafe)/lingocafe/_lib/reader.ts`.
- Path: `src/app/(app)/(lingocafe)/books/_components/BookCard.tsx`.
- Path: `src/app/(app)/(lingocafe)/books/_components/BookTags.tsx`.
- Path: `src/app/(app)/(lingocafe)/books/page.tsx`.
- Source artifacts: `WO85.notes.md`, `WO85.plan.md`, `WO85.task.md`.

## Durable Outcome and Decisions

- Treat the operator's "yes and execute" as plan acceptance plus execution approval.
- Move `WO85` from Drafts to WIP before code changes.
- Keep `BookCover` shared by catalog, detail, and reader surfaces, and add a narrow opt-in for catalog-specific image fill instead of changing the shared default.
- Render catalog images with `object-cover` while preserving the shared `BookCover` default as `object-contain`.
- Implement the catalog card overlays directly in `BookCard` so detail pages can keep using `BookTags` and full metadata independently.
- Use a responsive CSS grid with two mobile columns and wider breakpoint columns so mobile always shows at least two covers per row.

## Validation and Limitations

- Source inspection confirmed `BookCard` no longer renders `book.author`, `languageLevel`, `book.lang`, `book.level`, `SV`, `A2`, or `BookTags`.
- `curl -I -s http://localhost:3000/books` returned `HTTP/1.1 200 OK`.
- `npm run qa` passed. The build emitted the existing Turbopack/NFT warning in `next.config.ts` and repeated `next/headers` fallback logs during static generation.
- In-app browser verification passed after the profile was fixed: `/books` showed two image-only cover cards per row, readable top title overlays, bottom tags, and no author or SV/A2 badge.

## Task Relationships

### Supersedes

None identified.

### Superseded by

None identified.

### Related Tasks

None identified.

## Compression Provenance

Compressed 3 artifacts: `WO85.notes.md`, `WO85.plan.md`, `WO85.task.md`. Their exact pre-compression versions are recoverable from `compressedFromCommit` (`27c91fcf1650c9cb49d27cb7c10a6484fbfe4329`), for example:

```bash
git show 27c91fcf1650c9cb49d27cb7c10a6484fbfe4329:docs/backlog/tasks/WO85-improve-lingocafe-book-card-image-first-design/WO85.task.md
```
