# Plan — 42go Events Query Session

## Goal

Add `42go events query session`, backed by local Parquet analytics and app-scoped flat `.local/42go-stats/{app-id}/query_session_*.parquet` caches, so repeated runs return user session summaries without querying production PostgreSQL.

## Milestones

### Milestone 1 — Map Existing Event Query Patterns

- [x] Step 1 — Confirm command tree and help contract
  - Achieve: Identify the cleanest way to expose `42go events query session` beside `stats` and `users growth`.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `cli/src/fortytwogo_cli/events/cli.py`, `cli/src/fortytwogo_cli/events/query.py`, `cli/tests/test_cli.py`.
  - Validation: Existing help behavior is understood before edits; `42go events query stats` and `42go events query users growth` stay intact.
  - Notes: Added the singular `session` command under `42go events query`; existing `stats` and `users growth` command paths remain intact.

- [x] Step 2 — Confirm cache conventions from `users growth`
  - Achieve: Reuse or mirror source fingerprinting, safe app-id handling, schema versioning, cache status, reset semantics, and app-scoped directory conventions.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `cli/src/fortytwogo_cli/events/users_growth.py`, `.local/42go-stats/{app-id}/query_users_growth_metrics.parquet`.
  - Validation: The plan for session cache shape is consistent with existing aggregate cache behavior.
  - Notes: Reused the `users_growth` app-scoped cache pattern, source fingerprints, schema version checks, safe app-id validation, and reset behavior. Chose JSON for nested session payloads.

### Milestone 2 — Build Session Analytics Core

- [x] Step 3 — Add session data model and cache helpers
  - Achieve: Define typed Python structures for raw event rows, session rows, app results, source fingerprints, cache paths, and command options.
  - Create: `cli/src/fortytwogo_cli/events/sessions.py`.
  - Modify: n/a
  - Delete: n/a
  - Touch points: `SourceFileFingerprint`, `safe_app_id`, and cache state patterns from `users_growth.py`.
  - Validation: Module imports cleanly; cache paths resolve under `.local/42go-stats/{app-id}/`; unsafe app IDs are rejected.
  - Notes: Added `sessions.py` with session/event/result dataclasses, cache path helper, schema version, and state matching.

- [x] Step 4 — Read session source events from local Parquet
  - Achieve: Load raw events from `.local/42go-events/events/parquet/*.parquet`, excluding rows with missing `user_id` or missing `event_at`.
  - Create: DuckDB fetch helper in `sessions.py`.
  - Modify: n/a
  - Delete: n/a
  - Touch points: `read_parquet(?)`, columns `id`, `created_at`, `app_id`, `user_id`, `event_at`, `name`, `data`, `meta`.
  - Validation: Fixture proves events are ordered deterministically by app, user, `event_at`, `created_at`, and `id`.
  - Notes: DuckDB fetch reads raw event columns, excludes missing `user_id`/`event_at`, includes every event name, and sorts deterministically.

- [x] Step 5 — Cluster sessions by app/user and one-hour gaps
  - Achieve: Build session rows where a gap greater than one hour starts a new session and a gap equal to or less than one hour extends the current session.
  - Create: clustering helper in `sessions.py`.
  - Modify: n/a
  - Delete: n/a
  - Touch points: parsed timestamp handling, event serialization, duration calculation.
  - Validation: Tests cover same-user continuation, exact one-hour continuation, greater-than-one-hour split, different-user split, and different-app split.
  - Notes: Implemented app/user clustering with one-hour inclusive continuation; session id, duration seconds, and full chronological `events[]` are covered by tests.

### Milestone 3 — Persist App-Scoped Session Caches

- [x] Step 6 — Write aggregate cache files and metadata
  - Achieve: Persist session data and state metadata under flat `.local/42go-stats/{app-id}/` files.
  - Create: cache writer/reader helpers in `sessions.py`.
  - Modify: n/a
  - Delete: n/a
  - Touch points: PyArrow/DuckDB Parquet session subtables, JSON state metadata.
  - Validation: First run creates cache files; a second run can read them and produce identical session data.
  - Notes: Updated per operator request to write `query_session_sessions.parquet`, `query_session_events.parquet`, and `query_session_state.parquet` flat under `.local/42go-stats/{app-id}/`.

- [x] Step 7 — Implement no-new-data cache path
  - Achieve: Detect unchanged source Parquet inputs and load cached sessions instead of recomputing all history.
  - Create: state matching logic in `sessions.py`.
  - Modify: cache read workflow.
  - Delete: n/a
  - Touch points: source path, size, and mtime fingerprints from local Parquet files.
  - Validation: Test proves the second run reports or behaves as cache-backed without recomputing.
  - Notes: Matching source fingerprints load cached sessions; output filters do not narrow app-wide cache processing.

- [x] Step 8 — Implement reset and changed-source rebuild
  - Achieve: Rebuild session caches when source fingerprints change, and force a full fresh rebuild when `--reset` is passed.
  - Create: reset workflow in `sessions.py`.
  - Modify: aggregate refresh workflow.
  - Delete: app-scoped session cache contents only when `--reset` is passed.
  - Touch points: `.local/42go-stats/{app-id}/query_session_*.parquet`.
  - Validation: Tests prove reset rebuilds from all available Parquet data and changed source data can extend or replace the last session for a user.
  - Notes: Reset removes only the session cache directory. Changed source fingerprints rebuild from all local Parquet, preserving tail-session extension correctness.

### Milestone 4 — Wire CLI and Output

- [x] Step 9 — Add `query session` command
  - Achieve: Expose `42go events query session` with `--archive-dir`, `--reset`, `--user-id`, `--app-id`, `--limit`, and `--format`.
  - Create: n/a
  - Modify: `cli/src/fortytwogo_cli/events/cli.py`.
  - Delete: n/a
  - Touch points: Typer command registration, output-format validation, error handling.
  - Validation: Nested help shows the command and all flags; existing query commands still work.
  - Notes: Added `--archive-dir`, `--reset`, `--user-id`, `--app-id`, `--limit`, and `--format`; output filters run after cache processing.

- [x] Step 10 — Format human-readable console output
  - Achieve: Print compact session summaries, most recent first, limited to 20 sessions by default.
  - Create: formatter in `sessions.py`.
  - Modify: command handler output.
  - Delete: n/a
  - Touch points: duration formatting, event count, app/user/session timestamps.
  - Validation: Smoke output shows app, user, session id, start, end, human duration, and event count without dumping full `events[]`.
  - Notes: Text output prints compact newest-first summaries with human-readable duration and event counts; default limit is 20.

- [x] Step 11 — Add JSON output
  - Achieve: Support `--format json`, returning full returned session items with `events[]` and duration seconds.
  - Create: JSON serialization helper in `sessions.py`.
  - Modify: command output branch.
  - Delete: n/a
  - Touch points: raw event serialization, datetime strings, cache status metadata.
  - Validation: JSON parses cleanly and honors `--user-id`, `--app-id`, and `--limit` after cache processing.
  - Notes: `--format json` returns full returned session items, including full `events[]` arrays and duration seconds.

### Milestone 5 — Tests and Documentation

- [x] Step 12 — Add focused session tests
  - Achieve: Cover clustering, app isolation, user isolation, all-event inclusion, ignored missing user IDs, output filters, limit behavior, cache first run, cached second run, reset rebuild, and changed-source tail extension.
  - Create: `cli/tests/test_sessions.py`.
  - Modify: shared test fixtures only if useful.
  - Delete: n/a
  - Touch points: temporary Parquet fixtures, DuckDB, PyArrow, Typer command runner.
  - Validation: CLI tests pass and prove the task acceptance criteria.
  - Notes: Added `cli/tests/test_sessions.py` covering clustering, app/user isolation, ignored missing users, filters, limits, cache reuse, reset, and tail extension.

- [x] Step 13 — Update CLI and skill docs
  - Achieve: Document command path, flags, cache path, one-hour threshold, output formats, reset behavior, and filter semantics.
  - Create: n/a
  - Modify: `.agents/skills/42go-cli/SKILL.md`, `.agents/skills/42go-events-export/SKILL.md`, `.agents/skills/42go-events-export/README.md`.
  - Delete: n/a
  - Touch points: command help text and local analytics docs.
  - Validation: Docs mention `42go events query session`, `--reset`, `--user-id`, `--app-id`, `--limit`, and `--format json`.
  - Notes: Updated `42go-cli` and `42go-events-export` skill docs plus README command notes.

### Milestone 6 — Verification Gates

- [x] Step 14 — Verify command help and local smoke behavior
  - Achieve: Prove the command is discoverable and works against the current local Parquet archive.
  - Create: n/a
  - Modify: help strings if gaps appear.
  - Delete: n/a
  - Touch points: `42go events query --help`, `42go events query session --help`, local `.local/42go-events`.
  - Validation: Local command run creates flat `.local/42go-stats/{app-id}/query_session_*.parquet` files, prints human-readable summaries, supports JSON output, uses cache on second run, and rebuilds on `--reset`.
  - Notes: Verified help, text output, JSON output, cached second run, and reset rebuild against local `.local/42go-events`; generated `.local` cache files remain untracked.

- [x] Step 15 — Run CLI tests
  - Achieve: Catch Python regressions in event analytics.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `cli/tests`.
  - Validation: `.local/42go-events/.venv/bin/python -m pytest cli/tests` passes, or the repository’s available CLI test command passes.
  - Notes: `.local/42go-events/.venv/bin/python -m pytest cli/tests` passed with 29 tests.

- [x] Step 16 — Run repository QA
  - Achieve: Confirm the broader project still passes its standard gate.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: TypeScript/Next.js build and lint surface.
  - Validation: `npm run qa` passes.
  - Notes: `npm run qa` passed, including ESLint and production build.
