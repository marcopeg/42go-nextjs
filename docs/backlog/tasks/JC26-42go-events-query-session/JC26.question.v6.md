# Refinement Questions v6 — 42go Events Query Session

Answer inline under the question, then return to chat and say `next round`.

## Questions

### 1. Should sessions be grouped only by app/user, or also by device/browser?

Pick one: app/user only, or app/user/device.

Answer:
Segregate caches/results by app the same way as `query users`. Within each app, group sessions by `user_id` plus the time clustering rule. Do not split by device/browser.
