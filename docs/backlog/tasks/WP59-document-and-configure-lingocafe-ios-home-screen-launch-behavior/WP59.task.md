---
taskId: WP59
status: archived
createdAt: 2026-05-01T16:48:30+00:00
completedAt: 2026-05-02T06:20:24+02:00
compressedAt: 2026-07-29T18:45:00+02:00
compressedFromCommit: 27c91fcf1650c9cb49d27cb7c10a6484fbfe4329
---
# Document and Configure Lingocafe iOS Home Screen Launch Behavior

## Historical Summary

On iOS Home Screen relaunch, logged-in Lingocafe users should land in the Lingocafe app flow, not the public home page.

## Retrieval Anchors

- Task: `WP59` — Document and Configure Lingocafe iOS Home Screen Launch Behavior.
- Identifier: `public.pwa`.
- Identifier: `startUrl: "/books"`.
- Identifier: `contents/default/docs/pwa/README.md`.
- Identifier: `/books`.
- Identifier: `BookCard`.
- Identifier: `readingAction.href`.
- Identifier: `next/link`.
- Identifier: `start_url: "/books"`.
- Identifier: `start_url: "/quicklists"`.
- Identifier: `public/images/icons`.
- Identifier: `/images/icons/...`.
- Identifier: `npm run qa`.
- Path: `contents/default/docs/pwa/README.md`.
- Path: `public/images/icons`.
- Path: `src/app/manifest.ts`.
- Path: `src/app/layout.tsx`.
- Path: `src/42go/pwa/HeadTags.tsx`.
- Path: `src/42go/pwa/types.ts`.
- Path: `src/config/lingocafe/config.ts`.
- Path: `src/config/quicklist/config.ts`.
- Source artifacts: `WP59.notes.md`, `WP59.plan.md`, `WP59.task.md`.

## Durable Outcome and Decisions

- Accepted WP59 and moved it from draft to WIP before implementation.
- Confirmed the runtime fix stays config-only: no authenticated `/` redirect will be added.
- Added Lingocafe `public.pwa` with `startUrl: "/books"` so the generated manifest launches installed Lingocafe into the books flow.
- Added dedicated PWA documentation at `contents/default/docs/pwa/README.md` and linked it from the docs sidebar.
- Fixed a narrow pre-existing `/books` type error found by QA: `BookCard` now narrows `readingAction.href` to a string before passing it to `next/link`.
- Live manifest smoke checks returned Lingocafe `start_url: "/books"` and Quicklist `start_url: "/quicklists"`.

## Validation and Limitations

- No task-specific validation result was preserved beyond the completed/archived record.
- Limitations: Custom Lingocafe icon assets are not available yet and remain future work. The configured default icon path still needs a real asset before a polished iOS install; WP59 intentionally does not create custom icon artwork. `npm run qa` still reports an existing Turbopack NFT warning for `next.config.ts` and repeated static generation messages about falling back when `next/headers` is unavailable. The command exits successfully.

## Task Relationships

### Supersedes

None identified.

### Superseded by

None identified.

### Related Tasks

None identified.

## Compression Provenance

Compressed 3 artifacts: `WP59.notes.md`, `WP59.plan.md`, `WP59.task.md`. Their exact pre-compression versions are recoverable from `compressedFromCommit` (`27c91fcf1650c9cb49d27cb7c10a6484fbfe4329`), for example:

```bash
git show 27c91fcf1650c9cb49d27cb7c10a6484fbfe4329:docs/backlog/tasks/WP59-document-and-configure-lingocafe-ios-home-screen-launch-behavior/WP59.task.md
```
