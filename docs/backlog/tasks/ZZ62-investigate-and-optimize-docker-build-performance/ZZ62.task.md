---
taskId: ZZ62
status: archived
createdAt: 2026-05-02T07:41:42+00:00
completedAt: 2026-05-03T09:55:18+02:00
group: build
compressedAt: 2026-07-29T18:55:00+02:00
compressedFromCommit: 27c91fcf1650c9cb49d27cb7c10a6484fbfe4329
---
# Investigate and Optimize Docker Build Performance

## Historical Summary

A clear build-time profile exists for local production-image builds, with the slowest stages identified and ranked. Safe local-first optimizations are implemented and validated. Future CI/GitHub tag publishing guidance is documented separately enough to become its own task.

## Retrieval Anchors

- Task: `ZZ62` — Investigate and Optimize Docker Build Performance.
- Identifier: `ZZ62`.
- Identifier: `arm64`.
- Identifier: `docker buildx ls`.
- Identifier: `linux/arm64`.
- Identifier: `linux/amd64`.
- Identifier: `linux/amd64/v2`.
- Identifier: `make prod.build`.
- Identifier: `docker build`.
- Identifier: `--no-cache`.
- Identifier: `make prod.build.light`.
- Identifier: `make publish VERSION=0.0.20`.
- Identifier: `docker buildx build --platform linux/amd64,linux/arm64 --push`.
- Path: `docs/backlog/wip/ZZ62-investigate-and-optimize-docker-build-performance/ZZ62.plan.md`.
- Path: `docs/articles/PRODUCTION_DEPLOYMENT.md`.
- Source artifacts: `ZZ62.notes.md`, `ZZ62.plan.md`, `ZZ62.task.md`.

## Durable Outcome and Decisions

- Accepted plan moved `ZZ62` into WIP.
- Local machine architecture is `arm64`.
- Docker client is Darwin arm64; Docker server is Linux arm64.
- `docker buildx ls` shows the active builder supports `linux/arm64`, `linux/amd64`, and `linux/amd64/v2`.
- Current `make prod.build` runs plain `docker build` with `--no-cache`; on this setup it targets the arm64 Docker server default.
- Current `make prod.build.light` runs plain cached `docker build`; on this setup it also targets the arm64 Docker server default.
- Current `make publish VERSION=0.0.20` runs `docker buildx build --platform linux/amd64,linux/arm64 --push`, publishing both server and ARM images.
- Baseline no-cache build took 76.07s before adding `--load`; dependency install stages were about 26s (`npm ci --omit=dev`) and 31s (`npm ci`).

## Validation and Limitations

- No task-specific validation result was preserved beyond the completed/archived record.
- Limitations: Publish commands are being dry-run unless registry credentials and push intent are explicitly available.

## Task Relationships

### Supersedes

None identified.

### Superseded by

None identified.

### Related Tasks

None identified.

## Compression Provenance

Compressed 3 artifacts: `ZZ62.notes.md`, `ZZ62.plan.md`, `ZZ62.task.md`. Their exact pre-compression versions are recoverable from `compressedFromCommit` (`27c91fcf1650c9cb49d27cb7c10a6484fbfe4329`), for example:

```bash
git show 27c91fcf1650c9cb49d27cb7c10a6484fbfe4329:docs/backlog/tasks/ZZ62-investigate-and-optimize-docker-build-performance/ZZ62.task.md
```
