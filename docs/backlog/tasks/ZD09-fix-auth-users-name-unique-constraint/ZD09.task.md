---
taskId: ZD09
status: archived
createdAt: 2026-05-02T08:24:18+02:00
completedAt: 2026-05-03T08:19:09+02:00
group: auth
compressedAt: 2026-07-29T18:55:00+02:00
compressedFromCommit: 27c91fcf1650c9cb49d27cb7c10a6484fbfe4329
---
# Fix Auth Users Name Unique Constraint

## Historical Summary

`auth.users` should distinguish between credential username and display name. The credential username should remain unique per app when present. The display name should be allowed to duplicate.

## Retrieval Anchors

- Task: `ZD09` — Fix Auth Users Name Unique Constraint.
- Identifier: `knex/migrations/20240320_auth.js`.
- Identifier: `migrate.sql`.
- Identifier: `auth.users.name`.
- Identifier: `username`.
- Identifier: `npm run qa`.
- Identifier: `make migrate.rebuild`.
- Identifier: `backlog-execute`.
- Identifier: `resolve_task.py`.
- Identifier: `docs/cv/`.
- Identifier: `make migrate`.
- Identifier: `make seed`.
- Identifier: `knex/seeds/20260427150500_lingocafe_test_users.js`.
- Path: `knex/migrations/20240320_auth.js`.
- Path: `docs/cv/`.
- Path: `knex/seeds/20260427150500_lingocafe_test_users.js`.
- Path: `knex/migrations/20260502083000_auth_username.js`.
- Path: `src/42go/auth/lib/providers/get-providers.ts`.
- Path: `src/42go/auth/lib/callbacks.ts`.
- Path: `knex/seeds/20240523_default_test_users.js`.
- Path: `knex/seeds/20240522_init_auth_data.js`.
- Source artifacts: `ZD09.notes.md`, `ZD09.plan.md`, `ZD09.task.md`, `migrate.sql`.

## Durable Outcome and Decisions

- Accepted the plan and moved the task to WIP on 2026-05-03.
- Kept the in-place edit to `knex/migrations/20240320_auth.js` because the operator explicitly wants a local destructive rebuild path.
- Replaced the superseded Knex migration with a root-level `migrate.sql` manual production patch.
- Split app test users into dedicated seed files for default, Quicklist, and LingoCafe.
- Kept OAuth profile handling unchanged; OAuth still writes provider real names into `auth.users.name` and does not require `username`.
- Corrected the LingoCafe seed path to use LingoCafe-scoped Jane for demo profile data, matching the earlier OG12 decision that John/Jane are LingoCafe app users.
- `npm run qa` passed after the migration, auth, SQL patch, and seed split changes.
- The operator confirmed the local path works.

## Validation and Limitations

- No task-specific validation result was preserved beyond the completed/archived record.
- Limitations: Production was patched manually with `migrate.sql`; this is intentionally outside Knex migration tracking. `migrate.sql` only aligns the auth-users structure for this change. It assumes the deployed database has the other expected migrations already applied.

## Task Relationships

### Supersedes

None identified.

### Superseded by

None identified.

### Related Tasks

None identified.

## Compression Provenance

Compressed 4 artifacts: `ZD09.notes.md`, `ZD09.plan.md`, `ZD09.task.md`, `migrate.sql`. Their exact pre-compression versions are recoverable from `compressedFromCommit` (`27c91fcf1650c9cb49d27cb7c10a6484fbfe4329`), for example:

```bash
git show 27c91fcf1650c9cb49d27cb7c10a6484fbfe4329:docs/backlog/tasks/ZD09-fix-auth-users-name-unique-constraint/ZD09.task.md
```
