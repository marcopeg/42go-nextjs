# Plan — Remove Google Fonts Build Network Dependency

## Goal

Remove the `next/font/google` build path, keep typography visually acceptable through a system font stack, document the strategy, and prove the build no longer needs Google Fonts network access after dependencies are installed.

## Milestones

### Milestone 1 — Confirm Font Dependency Surface

- [x] Step 1 — Re-run focused font source scan
  - Achieve: Confirm the complete Google Fonts and font-loading surface before editing.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `src/app/layout.tsx`, `src/app/tailwind.css`, `src/app/(app)/(lingocafe)/books/_components/reader-preferences.ts`, app/public layout files, CSS files, theme assets.
  - Validation: Run `rg` for `next/font`, `next/font/google`, `fonts.googleapis.com`, `fonts.gstatic.com`, Google Fonts `@import`, and font-family declarations that imply external loading.
  - Notes: Existing refinement already found `src/app/layout.tsx` as the confirmed `next/font/google` path.

- [x] Step 2 — Confirm reader fonts are web-safe
  - Achieve: Verify the LingoCafe reader only offers web-safe stacks and needs no additional font assets.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `src/app/(app)/(lingocafe)/books/_components/reader-preferences.ts`.
  - Validation: Inspect `READER_FONT_OPTIONS` and record the exact stacks in implementation notes.
  - Notes: Do not redesign reader typography.

### Milestone 2 — Replace App Shell Google Font With System Stack

- [x] Step 1 — Remove `next/font/google` from root layout
  - Achieve: Stop Next.js from trying to fetch Google Fonts during production build.
  - Create: n/a
  - Modify: `src/app/layout.tsx`.
  - Delete: The `Inter` import, `Inter(...)` call, and `inter.variable` usage.
  - Touch points: Root `<html>` class handling and comments near app shell font setup.
  - Validation: `rg "next/font/google|Inter\\(" src/app/layout.tsx` returns no matches.
  - Notes: Preserve metadata, PWA head tags, providers, toaster, and body `font-sans` behavior.

- [x] Step 2 — Map `font-sans` to a deterministic local/fallback stack
  - Achieve: Keep shared UI typography stable without generated Google font variables.
  - Create: n/a
  - Modify: `src/app/tailwind.css`.
  - Delete: Dependency on a Google-generated `--font-inter` variable.
  - Touch points: Tailwind `@theme inline` font tokens.
  - Validation: Build CSS still compiles, and `font-sans` resolves to a local Inter token with system fallbacks.
  - Notes: This step was first completed with a pure system stack, then superseded by Milestone 5 after the operator requested locally vendored Inter.

### Milestone 3 — Document The Font Strategy

- [x] Step 1 — Add theming documentation note
  - Achieve: Document why the platform avoids build-time Google Fonts.
  - Create: n/a
  - Modify: `docs/articles/THEMING.md`.
  - Delete: n/a
  - Touch points: Theme stylesheet/font section near app theme assets.
  - Validation: Documentation states the app shell uses system fonts by default, app-specific custom fonts must be local/self-hosted if added later, and runtime Google CDN links are not the default strategy.
  - Notes: Mention Codex/sandbox build compatibility explicitly.

- [x] Step 2 — Record rejected alternatives
  - Achieve: Make the tradeoff clear for future agents.
  - Create: n/a
  - Modify: `docs/articles/THEMING.md`.
  - Delete: n/a
  - Touch points: Same documentation section.
  - Validation: Note covers vendored font files as a future option and Google CDN links as a runtime network dependency.
  - Notes: Keep docs short. Chuck Norris does not write a novel to explain `font-family`.

### Milestone 4 — Verify Build Independence

- [x] Step 1 — Verify source has no build-time Google Fonts path
  - Achieve: Prove the code no longer asks Next.js to fetch Google Fonts.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `src`, `public`, `contents`, `docs/articles`, `docs/memory-bank`, `next.config.*`, `package.json`.
  - Validation: `rg -n "next/font/google|fonts\\.googleapis|fonts\\.gstatic|@import\\s+url\\(['\\\"]?https?://fonts|Google Fonts" ...` returns no live build-path matches except task/history documentation.
  - Notes: Documentation mentions are acceptable; live imports, CSS imports, and head links are not.

- [x] Step 2 — Run install and production build
  - Achieve: Confirm normal verification passes in the sandbox.
  - Create: n/a
  - Modify: generated build artifacts only.
  - Delete: n/a
  - Touch points: `package-lock.json` only if `npm install` legitimately changes it.
  - Validation: Run `npm install` and `npm run build`; report exact pass/fail results.
  - Notes: If install needs network and sandbox blocks it, request escalation according to repo rules. If build fails for a different external dependency, record the next blocker.

- [x] Step 3 — Run final QA gate
  - Achieve: Catch lint/type/build regressions from the source and doc changes.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: Whole repo quality gate.
  - Validation: Run `npm run qa`; report exact result.
  - Notes: `npm run qa` includes build in this repo, but keep the explicit build result visible because FN30 is build-focused.

### Milestone 5 — Vendor Inter Locally

- [x] Step 1 — Add local Inter assets and license
  - Achieve: Preserve consistent Inter typography without build-time or runtime Google Fonts network access.
  - Create: `src/app/fonts/inter/Inter-Variable.ttf`, `src/app/fonts/inter/Inter-Italic-Variable.ttf`, `src/app/fonts/inter/OFL.txt`, `src/app/fonts/inter/METADATA.pb`, `src/app/fonts/inter/README.md`.
  - Modify: n/a
  - Delete: n/a
  - Touch points: `src/app/fonts/inter`.
  - Validation: Confirm font files, metadata, and OFL license are present.
  - Notes: Added after operator clarified they prefer locally vendored Google font files over OS-dependent system typography.

- [x] Step 2 — Wire Inter through `next/font/local`
  - Achieve: Let Next optimize local Inter assets while avoiding Google Fonts fetching.
  - Create: n/a
  - Modify: `src/app/layout.tsx`, `src/app/tailwind.css`.
  - Delete: n/a
  - Touch points: Root layout font variable and Tailwind `font-sans` token.
  - Validation: Build passes and source scan has no `next/font/google` or Google Fonts CDN usage.
  - Notes: `font-sans` now uses `var(--font-inter)` with system fallbacks.
