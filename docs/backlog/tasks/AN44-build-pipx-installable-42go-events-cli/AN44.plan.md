# Plan — Build pipx-installable 42Go Events CLI

## Goal

Create a pipx-installable Python CLI package under `cli/` that exposes the `42go` command, moves event export/query logic into CLI-owned modules, provides agent-navigable help, updates `make events` to call `42go events pull`, and documents the command tree in a new `42go-cli` skill.

## Milestones

### Milestone 1 — Establish the CLI Package

- [x] Step 1 — Scaffold `cli/` package metadata
  - Achieve: Add a pipx-installable Python package with distribution name `42go-cli` and console script `42go`.
  - Create: `cli/pyproject.toml`, `cli/src/fortytwogo_cli/__init__.py`, `cli/src/fortytwogo_cli/__main__.py`, `cli/src/fortytwogo_cli/cli.py`.
  - Modify: n/a
  - Delete: n/a
  - Touch points: `/Users/marcopeg/dv/ImageMagik/pyproject.toml`, `/Users/marcopeg/dv/LingoCafe/books/pyproject.toml`, `/Users/marcopeg/dv/LingoCafe/cli/pyproject.toml`.
  - Validation: `python -m pip install -e cli` or equivalent local install exposes a runnable `42go` command in the active environment.
  - Notes: Implemented `42go-cli` with import package `fortytwogo_cli`; the console script remains `42go`.

- [x] Step 2 — Build root help behavior
  - Achieve: Running bare `42go` prints root help, and `42go --help` shows command discovery text.
  - Create: Root Typer app with `no_args_is_help=True`, version option, and clear help descriptions.
  - Modify: `cli/src/fortytwogo_cli/cli.py`.
  - Delete: n/a
  - Touch points: Typer command setup patterns from ImageMagik and LingoCafe CLI examples.
  - Validation: `42go`, `42go --help`, and `42go --version` work after local install.
  - Notes: Bare `42go`, `42go --help`, and `42go --version` work. Root no-arg help exits with code 0.

### Milestone 2 — Move Event Export Logic Into CLI Modules

- [x] Step 3 — Create event archive and dependency helpers
  - Achieve: Centralize archive path resolution, `.env` loading, dependency import errors, Parquet globbing, and local file utilities.
  - Create: `cli/src/fortytwogo_cli/events/paths.py`, `cli/src/fortytwogo_cli/events/dependencies.py`, optional `cli/src/fortytwogo_cli/events/__init__.py`.
  - Modify: n/a
  - Delete: n/a
  - Touch points: `.agents/skills/42go-events-export/scripts/export_events.py`, `.agents/skills/42go-events-export/scripts/analyze_events.py`.
  - Validation: Unit or smoke checks can resolve `.local/42go-events`, find monthly Parquet files, and report clean dependency errors when DuckDB/PyArrow/psycopg are missing.
  - Notes: Added `paths.py` and `dependencies.py`; defaults preserve `EVENTS_ANALYTICS_DIR` and `.local/42go-events`.

- [x] Step 4 — Port exporter implementation
  - Achieve: Move the current production event pull implementation into CLI-owned Python modules.
  - Create: `cli/src/fortytwogo_cli/events/pull.py`.
  - Modify: Existing exporter logic as needed while preserving cursor semantics and archive layout.
  - Delete: n/a initially; old script cleanup happens after CLI parity is proven.
  - Touch points: `fetch_rows`, row normalization, monthly CSV/Parquet merge, de-dupe by `id`, smoke read, `state.json`, `manifest.jsonl`, `inflight.json`.
  - Validation: `42go events pull --help` works; dry-run or limited run path behaves like the old exporter without corrupting existing files.
  - Notes: Moved exporter logic to `cli/src/fortytwogo_cli/events/pull.py`. Read-only dry-run against `EVENTS_DATABASE_URL` succeeded outside sandbox.

- [x] Step 5 — Wire `42go events pull`
  - Achieve: Expose the pull logic through the command tree.
  - Create: `cli/src/fortytwogo_cli/events/cli.py`.
  - Modify: `cli/src/fortytwogo_cli/cli.py`.
  - Delete: n/a
  - Touch points: command options for `--archive-dir`, `--limit`, `--run-id`, `--dry-run`.
  - Validation: `42go events --help`, `42go events pull --help`, and a safe `42go events pull --dry-run` run successfully when `EVENTS_DATABASE_URL` is configured.
  - Notes: `42go events --help`, `42go events pull --help`, and `42go events pull --dry-run --limit 1` passed. Sandbox DNS blocks Neon, so dry-run validation used approved network access.

### Milestone 3 — Add Parquet Stats Query

- [x] Step 6 — Create Parquet stats service
  - Achieve: Read local monthly Parquet files with DuckDB and compute high-level archive stats.
  - Create: `cli/src/fortytwogo_cli/events/query.py`.
  - Modify: n/a
  - Delete: n/a
  - Touch points: `.local/42go-events/events/parquet/*.parquet`, DuckDB `read_parquet`, event columns from `events.events`.
  - Validation: On the current local archive, the service reports total rows, distinct event ids, covered months, app count, user count, event-name count, timestamp ranges, and per-month counts.
  - Notes: Added `query.py`; stats read Parquet through DuckDB and report 981 events across 2 months in the current archive.

- [x] Step 7 — Wire `42go events query stats`
  - Achieve: Add human-readable stats output under `42go events query stats`.
  - Create: query sub-app command wiring in `cli/src/fortytwogo_cli/events/cli.py` or a dedicated `events/query_cli.py`.
  - Modify: `cli/src/fortytwogo_cli/events/query.py`.
  - Delete: n/a
  - Touch points: output labels, no-file behavior, archive-dir option, per-month table.
  - Validation: `42go events query --help`, `42go events query stats --help`, and `42go events query stats` work against `.local/42go-events`.
  - Notes: `42go events query --help`, `42go events query stats --help`, and `42go events query stats` passed with human-readable output and per-month counts.

### Milestone 4 — Replace Make and Clean Old Script Surface

- [x] Step 8 — Make `make events` call the CLI
  - Achieve: Preserve the Make interface while routing execution through `42go events pull`.
  - Create: n/a
  - Modify: `Makefile`.
  - Delete: n/a
  - Touch points: current `events` target, local editable CLI install assumptions.
  - Validation: `make events` invokes the CLI path and preserves current operator behavior.
  - Notes: `make -n events` confirms the target calls global `42go`, falls back to `.local/42go-events/.venv/bin/42go`, and otherwise prints a pipx install hint.

- [x] Step 9 — Retire or convert old event scripts
  - Achieve: Remove disconnected implementation ownership from `.agents/skills/42go-events-export/scripts/`.
  - Create: optional thin compatibility shims only if needed for existing skill docs during transition.
  - Modify: `.agents/skills/42go-events-export/scripts/export_events.py`, `.agents/skills/42go-events-export/scripts/analyze_events.py`, `.agents/skills/42go-events-export/README.md`, `.agents/skills/42go-events-export/SKILL.md`.
  - Delete: old implementation code from script files if the CLI fully owns it.
  - Touch points: 42go events export skill docs and any references to direct script invocation.
  - Validation: Repository search shows no primary docs still instruct operators to run old scripts directly instead of `42go events pull` or `42go events query stats`.
  - Notes: Old scripts are compatibility wrappers only. Primary skill docs now point to `42go events pull` and `42go events query stats`.

### Milestone 5 — Document Agent Navigation

- [x] Step 10 — Create the `42go-cli` skill
  - Achieve: Add an agent skill that documents the CLI command index and directs agents to command help.
  - Create: `.agents/skills/42go-cli/SKILL.md`.
  - Modify: n/a
  - Delete: n/a
  - Touch points: implemented CLI help text, command tree, skill conventions in `.agents/skills/*/SKILL.md`.
  - Validation: Skill includes a flat list for `42go`, `42go events`, `42go events pull`, `42go events query`, and `42go events query stats`, with brief reasons to use each.
  - Notes: Added `.agents/skills/42go-cli/SKILL.md` with flat command index and help contract.

- [x] Step 11 — Verify help completeness
  - Achieve: Prove an agent can discover the CLI from help alone.
  - Create: n/a
  - Modify: CLI help strings if gaps are found.
  - Delete: n/a
  - Touch points: `42go`, `42go --help`, `42go events --help`, `42go events pull --help`, `42go events query --help`, `42go events query stats --help`.
  - Validation: Each command explains what it does, relevant options, defaults, and environment variables when applicable.
  - Notes: Verified root, events, pull, query, and stats help output. Bare root/events/query commands also print help with exit code 0.

### Milestone 6 — Packaging and Quality Gates

- [x] Step 12 — Verify pipx install path
  - Achieve: Prove the CLI can be installed with pipx from the repo-local `cli/` package.
  - Create: n/a
  - Modify: packaging metadata if pipx exposes issues.
  - Delete: n/a
  - Touch points: `cli/pyproject.toml`, package data, entry point.
  - Validation: `pipx install ./cli` or an equivalent local pipx command installs `42go`; `42go --help` runs from that install.
  - Notes: `pipx install --force ./cli` passed and exposed global `42go`. Added `pytz` after global DuckDB stats exposed the missing dependency.

- [x] Step 13 — Run focused CLI checks
  - Achieve: Catch Python syntax, import, command, and stats regressions.
  - Create: tests under `cli/tests/` if lightweight and useful.
  - Modify: `cli/pyproject.toml` test config if needed.
  - Delete: n/a
  - Touch points: command runner tests, stats query service, archive path helpers.
  - Validation: CLI tests or smoke commands pass, including local stats against existing Parquet files.
  - Notes: Added focused tests under `cli/tests/`; `.local/42go-events/.venv/bin/python -m pytest cli/tests` passed with 4 tests.

- [x] Step 14 — Run repository QA
  - Achieve: Ensure the Next.js repository still passes its required gate.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `npm run qa`.
  - Validation: `npm run qa` passes.
  - Notes: `npm run qa` passed, including ESLint and production build.
