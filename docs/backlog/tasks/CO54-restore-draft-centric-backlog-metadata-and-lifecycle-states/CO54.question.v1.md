# Refinement Questions v1 — Restore Draft-Centric Backlog Metadata and Lifecycle States

Answer inline under each question, then return to chat and say `next round`.

## Questions

### 1. How should `blocked` fit into the new lifecycle?

Choose one or describe another rule: keep `blocked` as a full status alongside `draft`, `refining`, `refined`, `planned`, and `wip`; make it an orthogonal field such as `blocked: true` / `blockedReason` / `reviewAfter` while status remains the underlying lifecycle state; or leave blocked behavior unchanged for now and explicitly exclude it from this task.

Answer:

blocked can be manually attributed to a task. there is no skill for this.

### 2. Should the visible backlog folders and `BACKLOG.md` sections be renamed to match the new statuses?

For example, should the system create/use `refining/`, `refined/`, and `planned/` folders plus matching sections, or should it keep the current physical folders/sections such as `drafts`, `ready`, and `wip` while the draft file status becomes authoritative?

Answer:

I'd say we should create a new skill `backlog-doctor` that when executed analyzes each task's status and moves it to the correct folder so that each status value has its own folder.

make sure the other skills always move the task's folder in the appropriate status sub-folder.

this will give the phisical backlog on disk a more human approachable look.

each subfolder should then have the BACKLOG\_{status}.md with the local index of the tasks.

the `backlog-doctor` should be able to verify and rework the status folders and relative indexes so to reflect the task's status as reported into the .draft.md file.

### 3. What frontmatter, if any, may remain on non-draft artifacts?

Should `.task.refined.md`, `.plan.md`, `.notes.md`, `.question.vN.md`, and `.clarify.vN.md` keep only artifact-local fields such as `taskId`, `createdAt`, `updatedAt`, and `round`, or should some of them have no frontmatter at all? Confirm that task lifecycle fields like `status`, `reviewedAt`, `plannedAt`, `startedAt`, `completedAt`, and `reviewAfter` must be removed from every non-draft artifact.

Answer:

none, i want all the frontmatter attribute centralized in the .draft.md task - that will become the source of truth as initial task and lifecycle attributes, plus any other frontmatter information that is relative to the task itself. it can also use custom attributes if the users wants to.

make sure the `backlog-migrate` fixes existing backlog tasks to this.

### 4. How should migration resolve conflicts between draft metadata and refined or legacy task metadata?

Choose one or refine the rule: prefer the non-draft value when the draft is missing the field; prefer the value with the newest `updatedAt`; prefer the draft unless explicitly empty; or stop and report any conflict where both files have different non-empty lifecycle values.

Answer:

the refined metadata should me reflected into the draft because the current skills write there. so merge "refined -> draft"

### 5. Should this implementation update only the repo-local skills, or also the global reusable skill copies?

The current draft constrains the work to `.agents/skills/backlog-*` in this repository. Because you want to apply the migration to other codebases, should this task also update `/Users/marcopeg/.agents/skills/backlog-*`, or should it keep the repo-local implementation portable and leave global installation/copying as a separate step?

Answer:

only repo local skills, i will then copy manually.

## BIG CHANGE:

I don't want to keep the ".draft.md" suffix, i want the main task file to be only {id}.task.md.

Make sure the skills reflect this and that the migration and the doctor skill can handle this.
