# Plan — Show the Standard Mobile More Item to LingoCafe Backoffice Users

## Goal

Let LingoCafe backoffice users open the existing mobile drawer through the standard bottom-bar More item, without touching the Profile page or duplicating navigation behavior.

## Milestones

### Milestone 1 — Add declarative policy to the standard More item

- [x] Step 1 — Extend MobileBottomNav's existing More configuration
  - Achieve: Let AppConfig conditionally show the existing More item using the standard 42Go policy shape.
  - Create: Nothing.
  - Modify: `src/AppConfig.ts` and `MobileBottomNav.tsx`.
  - Delete: Nothing.
  - Touch points: Existing More trigger, `ProtectComponent`, flex distribution, and drawer callback.
  - Validation: TypeScript/build checks plus focused contracts for policy wiring and existing drawer reuse.
  - Notes: Keep drawer ownership and close behavior unchanged inside `AppLayout`.

### Milestone 2 — Configure LingoCafe

- [x] Step 2 — Apply a backoffice policy to the standard More item
  - Achieve: Show **More** only to LingoCafe mobile sessions with the app-scoped `backoffice` role.
  - Create: Nothing.
  - Modify: `src/config/lingocafe/config.ts`.
  - Delete: All superseded Profile toolbar/context work.
  - Touch points: LingoCafe `app.menu.mobile.more.policy`.
  - Validation: Focused tests cover config placement, role policy, mobile-only class, accessible label, toolbar placement, and drawer-opener use.
  - Notes: This leaves only three product-code touch points and no Profile-specific implementation.

### Milestone 3 — Verify behavior and preserve regressions

- [x] Step 3 — Run automated and visual verification
  - Achieve: Prove the trigger opens the established menu without changing desktop, ordinary-user, other-app, or existing navigation behavior.
  - Create: A focused Node test and execution notes.
  - Modify: Plan progress and task notes.
  - Delete: Nothing.
  - Touch points: LingoCafe profile at approximately 390 px, desktop profile, Users and Notifications drawer entries, test scripts if needed.
  - Validation: Focused test command, `npm run qa`, and browser checks through the LingoCafe tunnel when available.
  - Notes: Automated coverage, lint, TypeScript, and a native webpack production build passed. Browser verification reached the current app at 390 px, but both documented test credential pairs were rejected by the active database; no seed or restore was performed. The broader communications suite also has one unrelated stale `PlainList` source assertion. The final Turbopack QA rerun was environment-blocked by its helper-port sandbox restriction after an earlier full QA pass.
