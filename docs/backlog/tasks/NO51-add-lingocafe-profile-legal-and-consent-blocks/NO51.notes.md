# Execution Notes — Add LingoCafe Profile Legal and Consent Blocks
**Task**: ./NO51.task.md
**Plan**: ./NO51.plan.md

## Decisions

- Treat the operator's "yes and implement" as plan acceptance plus execution.
- Move `NO51` from Drafts to WIP before source changes.
- Store consent/acknowledgement evidence only in `lingocafe.profiles.data`.
- Store consent evidence as readable `consent_{key}` objects with `value`, `date`, `version`, `source`, and `method` subkeys.
- Do not collect IP address, User-Agent, screen resolution, operating system, or fingerprinting-style datapoints by default.
- Add placeholder LingoCafe Terms and Privacy public pages in AppConfig so consent links resolve now, while clearly noting the copy should be reviewed before production.
- Change profile block persistence from parallel to sequential so the required Terms and Privacy block can create or update legal acknowledgement data before language and optional-consent blocks persist.
- Keep server-side validation stricter than the UI: creating or completing a LingoCafe profile without Terms and Privacy now returns a validation error.

## Problems Encountered

- The execution skill's `resolve_task.py` is not present locally; task resolution was already confirmed through the planning skill resolver.
- Running the transition script without an absolute `--backlog-root` looked in the skill directory. Retried with the repository backlog path and the WIP transition succeeded.

## Deviations From Plan

- `ProfilePageRenderer` now persists profile blocks sequentially instead of in parallel. This was needed to preserve the app-specific profile-block model while allowing `LingoCafeTermsPrivacy` to satisfy server-side profile-creation requirements before the other LingoCafe blocks save.

## Additional Requests

- Refactor consent evidence away from flat `consent_{key}_{param}` fields into readable JSONB objects such as `consent_mkt.value`, `consent_mkt.date`, and matching objects for Terms, Privacy, and Early Birds.

## Known Limitations

- The legal copy added for `/terms` and `/privacy` is placeholder copy and should be replaced or reviewed before production use.

## Validation

- `npm run lint` passed.
- `npm run build` passed.
- `npm run qa` passed.
- `curl -I -s -H 'Host: read.lingocafe.app' http://localhost:3000/profile` returned 200.
- `curl -I -s -H 'Host: read.lingocafe.app' http://localhost:3000/books` returned 200.
- `curl -H 'Host: read.lingocafe.app' http://localhost:3000/terms` rendered `LingoCafe Terms and Conditions`.
- Authenticated credentials smoke as LingoCafe `john` verified `POST /api/lingocafe/profile` rejects a language-only profile create with 400 until Terms and Privacy are present.
- Authenticated credentials smoke as LingoCafe `john` verified a full onboarding payload returns 200, marks `profile.isComplete: true`, stores `consent_terms`, `consent_privacy`, `consent_mkt`, and `consent_alpha` objects, and stores unchecked optional values as `false` with dates.
- Repeated the authenticated full onboarding payload after the nested JSONB refactor; response contains nested consent objects only, a no-op repeat kept the existing ISO dates unchanged, and the `profile-update` event count stayed unchanged at 3.
