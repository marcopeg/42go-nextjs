---
taskId: OG12
status: archived
createdAt: 2026-04-25T06:43:53+02:00
completedAt: 2026-04-27T14:57:33+02:00
group: lingocafe
compressedAt: 2026-07-29T18:15:00+02:00
compressedFromCommit: 27c91fcf1650c9cb49d27cb7c10a6484fbfe4329
---
# LingoCafe Data Model and Knex Migrations

## Historical Summary

The project has a clear LingoCafe domain model that covers the core reading-app entities and relationships, the design is documented well enough to guide implementation decisions, and the required Knex migrations exist so the schema can be created through the standard migration flow.

## Retrieval Anchors

- Task: `OG12` — LingoCafe Data Model and Knex Migrations.
- Identifier: `events`.
- Identifier: `book_id`.
- Identifier: `page_id`.
- Identifier: `(created_at, id)`.
- Identifier: `events.id`.
- Identifier: `PK(created_at, id)`.
- Identifier: `auth.users`.
- Identifier: `lingocafe.profiles`.
- Identifier: `lingocafe.books_progress`.
- Identifier: `target_level`.
- Identifier: `data`.
- Identifier: `target_level = a2`.
- Path: `knex/migrations/`.
- Path: `docs/backlog/drafts/OG12-lingocafe-data-model-and-knex-migrations/OG12.task.md`.
- Path: `knex/migrations/20260427150000_lingocafe_schema.js`.
- Path: `knex/seeds/`.
- Path: `knex/seeds/20240522_init_auth_data.js`.
- Source artifacts: `OG12.notes.md`, `OG12.plan.md`, `OG12.task.md`.

## Durable Outcome and Decisions

- The raw `events` stream will keep `book_id` and `page_id` as nullable text references with no foreign keys so historical events survive disruptive re-exports.
- The partitioned `events` table will use a composite primary key on `(created_at, id)` because PostgreSQL requires the partition key to participate in parent-level primary-key enforcement.
- Seed data will intentionally be asymmetric: John is a fresh Lingocafe user with no profile or progress, while Jane is the returning demo user with a Swedish-learning profile and saved progress.
- The Lingocafe seed uses recognisable titles with fully original demo page text so the repository stays safe to distribute.

## Validation and Limitations

- No task-specific validation result was preserved beyond the completed/archived record.
- Limitations: No additional implementation limitations were discovered during this session. Event analytics beyond the raw stream still belong to later tasks.

## Task Relationships

### Supersedes

None identified.

### Superseded by

None identified.

### Related Tasks

None identified.

## Compression Provenance

Compressed 3 artifacts: `OG12.notes.md`, `OG12.plan.md`, `OG12.task.md`. Their exact pre-compression versions are recoverable from `compressedFromCommit` (`27c91fcf1650c9cb49d27cb7c10a6484fbfe4329`), for example:

```bash
git show 27c91fcf1650c9cb49d27cb7c10a6484fbfe4329:docs/backlog/tasks/OG12-lingocafe-data-model-and-knex-migrations/OG12.task.md
```
