# Execution Notes — Add Theme Selector to LingoCafe Reader Preferences

**Task**: ./IU71.task.md
**Plan**: ./IU71.plan.md

## Decisions

- Use the shared theme provider as the single source of truth and persistence path.
- Mirror the existing profile selector’s tab semantics, options, icons, and selection styling inside the reader panel.

## Problems Encountered

- The configured public LingoCafe verification URL (`https://lc42go.ngrok.app/`) returned `ERR_NGROK_3200`; the endpoint is offline, so reader-panel browser verification could not run.

## Deviations From Plan

None so far.

## Additional Requests

None.

## Known Limitations

- Visual verification of the reader panel remains pending until the public test endpoint is available again.

## Validation

- `npm run qa` passed (ESLint and production build).
