# Plan — Add Theme Selector to LingoCafe Reader Preferences

## Goal

Expose the existing global light/dark/system theme preference in the LingoCafe reader panel, using the profile-page tab pattern directly below the preview and above font size.

## Milestones

### Milestone 1 — Add the reader theme control

- [x] Step 1 — Reuse the shared theme state in the reader preferences panel
  - Achieve: Read the current global theme and update it through the existing shared theme provider from the panel.
  - Create: No new storage or API surface.
  - Modify: `src/app/(app)/(lingocafe)/books/_components/BookReaderPreferencesPanel.tsx`
  - Delete: Nothing.
  - Touch points: `useTheme`, `ThemeValue`, profile-page theme option labels/icons, and the reader panel’s settings order.
  - Validation: Switching Auto, Light, and Dark immediately changes the global theme and preserves the existing `localStorage` persistence path.
  - Notes: Keep the control local unless a small shared presentation extraction has a clear benefit.

- [x] Step 2 — Match the profile tab UI and panel ordering
  - Achieve: Render an accessible Theme tablist after the preview and before Font size, with the profile control’s Auto, Light, and Dark options and selected styling.
  - Create: No new route, modal, or reader preference schema.
  - Modify: `src/app/(app)/(lingocafe)/books/_components/BookReaderPreferencesPanel.tsx`
  - Delete: Nothing.
  - Touch points: Reader panel spacing, sticky preview, tab semantics, responsive styling, and existing font-size section.
  - Validation: The Theme control is first after the preview on mobile and desktop; font size and remaining controls are unchanged.
  - Notes: Theme changes must remain immediate and independent of any save action.

### Milestone 2 — Verify reader integration

- [ ] Step 3 — Validate profile mapping and quality gate
  - Achieve: Confirm theme changes select the existing `light`, `dark`, or `system` reader preference profile and do not regress reader settings.
  - Create: No persistent reader state.
  - Modify: Only if validation exposes a necessary integration issue.
  - Delete: Nothing.
  - Touch points: Reader page theme-profile calculation, panel preview, and existing preferences controls.
  - Validation: Run `npm run qa`; browser-check all three theme options and reader control ordering.
  - Notes: `npm run qa` passed. The public LingoCafe test URL returned `ERR_NGROK_3200` (offline), so the browser verification remains pending.
