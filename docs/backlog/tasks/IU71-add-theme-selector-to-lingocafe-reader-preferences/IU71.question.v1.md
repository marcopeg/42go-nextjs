# Refinement Questions v1 — Add Theme Selector to LingoCafe Reader Preferences

Answer inline under each question, then return to chat and say `next round`.

## Questions

### 1. Should the reader-panel control change the same global, persisted app theme as the profile-page selector (the current draft assumption), rather than only changing the reader canvas?

Answer:

yes, it is equivalent to the one in the profile. they do the same thing from two different places