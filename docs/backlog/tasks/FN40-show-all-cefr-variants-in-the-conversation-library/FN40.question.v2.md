# Refinement Questions v2 — Show all CEFR variants in the conversation library

Answer inline under each question, then return to chat and say `next round`.

## Questions

### 1. When one scenario/variant has multiple language and CEFR versions, should the row present a direct button for each combined choice (for example, `SV A1`, `EN B1`), with the chooser showing the same labels?

Answer:
Yes for levels only: each conversation row must show one direct button for every available CEFR version of that conversation. Keep the current target-language filter unchanged; a learner studying Swedish sees Swedish conversations only.
