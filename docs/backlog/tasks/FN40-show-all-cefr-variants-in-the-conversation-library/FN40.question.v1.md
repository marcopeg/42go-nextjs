# Refinement Questions v1 — Show all CEFR variants in the conversation library

Answer inline under each question, then return to chat and say `next round`.

## Questions

### 1. Should the conversation library continue to show only the profile's target language and remove only the CEFR-level filter, or should it show every available corpus language too?

Answer:
Show every available corpus language too. The profile target level must have no impact on conversation visibility.
