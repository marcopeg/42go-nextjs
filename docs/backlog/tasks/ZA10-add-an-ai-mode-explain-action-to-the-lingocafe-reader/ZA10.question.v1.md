# Refinement Questions v1 — Add an AI Mode explain action to the LingoCafe reader

Answer inline under each question, then return to chat and say `next round`.

## Questions

### 1. Where should the Explain action appear: only after the normal translation result has loaded, or immediately when the reader selects a word or sentence?

Answer: Show it only after the normal translation result has loaded. It is the third and last action in the translation popover. The first action is the scope-specific selection playback (`Play word` or `Play sentence`); the second starts the complete audiobook/voiceover and needs shorter, clearer wording than `Start audiobook from here`.
