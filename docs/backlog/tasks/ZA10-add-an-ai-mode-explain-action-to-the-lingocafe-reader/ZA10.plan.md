# Plan — Add an AI Mode explain action to the LingoCafe reader

## Goal

Add a third, final Explain action to the resolved reader translation popover. It launches Google AI Mode with a complete language-learning prompt while preserving the reader and differentiating selection playback from audiobook playback.

## Milestones

### Milestone 1 — Define the explain handoff

- [x] Step 1 — Build prompt and navigation utilities
  - Achieve: Deterministic, unit-testable Google AI Mode URL generation for sentence and word translation scopes.
  - Create: A reader-local or LingoCafe utility for prompt construction and navigation-target selection.
  - Modify: Reader translation types only as needed to retain selected text, enclosing sentence, reading language, and own-language label.
  - Delete: None.
  - Touch points: `BookPageReader`, reader translation helpers/tests, canonical LingoCafe language options.
  - Validation: Cover sentence and word prompt content, full analysis instructions, language labels, special characters, and normal-browser new-tab behavior.
  - Notes: Verify the current supported Google AI Mode query/deep-link contract before finalizing the URL.

### Milestone 2 — Add the reader action

- [x] Step 2 — Wire Explain into the resolved translation popover
  - Achieve: Present the three actions in the fixed order `Play word`/`Play sentence`, `Start audiobook`, and `Explain`; only enable Explain when the active selection has a successful translation result.
  - Create: The Explain action callback and any concise action/icon presentation needed by the existing popover pattern.
  - Modify: `ReaderTranslationPopover`, `BookPageReader`, and focused component tests.
  - Delete: None.
  - Touch points: Current pronunciation/audiobook actions, active translation state, popover interaction containment, keyboard labels.
  - Validation: Exercise both translation scopes, action order, correct labels, click/keyboard use, and preservation of normal selection/popover behavior.
  - Notes: Reuse established action styling; do not introduce a bespoke overlay.

### Milestone 3 — Handle fullscreen/PWA and verify

- [x] Step 3 — Apply platform-safe handoff behavior and complete verification
  - Achieve: Normal browser launches use a new tab/window. Fullscreen/installed/PWA uses a supported managed/in-app handoff when available, with a safe external fallback and no reader-state loss.
  - Create: Focused navigation tests or feature detection tests where the platform abstraction supports them.
  - Modify: Handoff utility and reader action wiring as needed.
  - Delete: None.
  - Touch points: PWA/fullscreen detection, browser navigation APIs, existing reader mobile behavior.
  - Validation: Unit tests plus responsive browser checks; run `npm run qa`.
  - Notes: Platform browser security owns whether true embedded/in-app presentation is available, especially on iOS; never iframe Google as a workaround.

### Milestone 4 — Expand explain context across readers

- [x] Step 4 — Add book paragraph and conversation-window context
  - Achieve: Give sentence explanations their enclosing paragraph and conversation explanations the active plus up to two surrounding rounds, including speaker labels.
  - Create: Optional prompt context input shared by book and conversation readers.
  - Modify: `BookPageReader`, `ConversationTranslatableText`, `ConversationReaderPage`, and AI Mode prompt tests.
  - Delete: None.
  - Touch points: Book selection state, conversation turn rendering, shared translation popover handoff.
  - Validation: Unit tests cover paragraph and nearby-turn prompt construction; lint passes.
  - Notes: Word explanations retain sentence context; conversation windows clamp naturally at the first and last rounds.
