---
taskId: PR58
status: archived
createdAt: 2026-04-27T15:41:36+02:00
completedAt: 2026-04-27T16:38:33+02:00
group: lingocafe
compressedAt: 2026-07-29T18:25:00+02:00
compressedFromCommit: 27c91fcf1650c9cb49d27cb7c10a6484fbfe4329
---
# Show Read Now or Continue Reading on Book Details

## Historical Summary

The book details API call also fetches the authenticated user's `books_progress` record for the selected book, if one exists.

## Retrieval Anchors

- Task: `PR58` — Show Read Now or Continue Reading on Book Details.
- Identifier: `PR58`.
- Identifier: `/books/<bookId>/<pageId>`.
- Identifier: `/books/[bookId]`.
- Identifier: `progress_bps`.
- Identifier: `BookInfoContent`.
- Identifier: `nils-holgersson-sv-a2`.
- Identifier: `p02`.
- Identifier: `progress_bps = 4200`.
- Identifier: `little-prince-en-a2`.
- Identifier: `p01`.
- Identifier: `npm run qa`.
- Identifier: `string`.
- Path: `src/app/(app)/(lingocafe)/books/_components/book-types.ts`.
- Path: `src/app/api/(lingocafe)/lingocafe/_lib/reader.ts`.
- Path: `src/app/api/(lingocafe)/lingocafe/books/[bookId]/route.ts`.
- Path: `src/app/(app)/(lingocafe)/books/[bookId]/page.tsx`.
- Path: `src/app/(app)/(lingocafe)/books/_components/BookReadingAction.tsx`.
- Path: `src/app/(app)/(lingocafe)/books/_components/BookInfoContent.tsx`.
- Source artifacts: `PR58.notes.md`, `PR58.plan.md`, `PR58.task.md`.

## Durable Outcome and Decisions

- Accepted plan execution moved `PR58` from draft to WIP.
- Use `/books/<bookId>/<pageId>` as the read-page link shape because it extends the existing `/books/[bookId]` detail route and matches the dependent read UI task direction.
- Use `progress_bps` as the resume query parameter name because the operator asked to pass `progress_bps` as a URL parameter.
- Kept the action component local to `BookInfoContent` because the UI is small and only used by the book detail surface.
- Verified seeded data has a resume case for `nils-holgersson-sv-a2` at `p02` with `progress_bps = 4200`, and a start case for `little-prince-en-a2` at first page `p01`.
- `npm run qa` passed after the type fix.

## Validation and Limitations

- No task-specific validation result was preserved beyond the completed/archived record.
- Limitations: This task intentionally does not create the read page UI or read route implementation. The action links point to the future read-page route shape; the route itself belongs to `ZE11`. QA still prints the existing Turbopack NFT warning and static `next/headers` fallback messages, but the command exits successfully.

## Task Relationships

### Supersedes

None identified.

### Superseded by

None identified.

### Related Tasks

None identified.

## Compression Provenance

Compressed 3 artifacts: `PR58.notes.md`, `PR58.plan.md`, `PR58.task.md`. Their exact pre-compression versions are recoverable from `compressedFromCommit` (`27c91fcf1650c9cb49d27cb7c10a6484fbfe4329`), for example:

```bash
git show 27c91fcf1650c9cb49d27cb7c10a6484fbfe4329:docs/backlog/tasks/PR58-show-read-now-or-continue-reading-on-book-details/PR58.task.md
```
