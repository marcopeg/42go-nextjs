# Refinement Questions v1 — Store Database Dumps Under .local

Answer inline under each question, then return to chat and say `next round`.

## Questions

### 1. What should the new default dump directory be?

The task currently suggests `.local/42go-dumps/`. Another tidy option is `.local/42go-backup/dumps/`, which keeps future backup-related local files grouped together.

Answer:

`.local/42go-backups/`

### 2. Should bare filename restore search legacy `knex/dumps` after the new `.local` directory?

For example, `make restore from=20260509T161705Z.dump.light.sql` could first try the new `.local` dump directory, then try `knex/dumps` for old files. Explicit paths like `make restore from=knex/dumps/file.sql` would keep working either way.

Answer:

no, just search in the new folder (`.local/42go-backups/`). no backward compatibility needed.

### 3. Should `knex/dumps/.gitkeep` remain in the repository?

If `knex/dumps` is no longer the generated dump home, we can leave the folder alone for backward compatibility, remove the `.gitkeep`, or keep the old ignore rule only as a legacy guard.

Answer:

no, remove it.

ALSO: update the gitignore and dockerignore appropriately
