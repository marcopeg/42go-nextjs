# Refinement Questions v1 - Show App QR Code in Mobile Landscape

Answer inline under the question, then return to chat and say `next round`.

## Questions

### 1. Should the QR config live at `app.qr` or top-level `qr`?

Answer:
Use top-level `qr`.
