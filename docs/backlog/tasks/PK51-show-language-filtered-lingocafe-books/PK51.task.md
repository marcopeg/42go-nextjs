---
taskId: PK51
status: archived
createdAt: 2026-04-27T14:38:15+02:00
completedAt: 2026-04-27T15:31:56+02:00
group: lingocafe
compressedAt: 2026-07-29T18:15:00+02:00
compressedFromCommit: 27c91fcf1650c9cb49d27cb7c10a6484fbfe4329
---
# Show Language-Filtered LingoCafe Books

## Historical Summary

When a user lands on `/books`, the app makes one API call that returns both the current user's LingoCafe profile and the list of books to display.

## Retrieval Anchors

- Task: `PK51` — Show Language-Filtered LingoCafe Books.
- Identifier: `backlog-execute ma68, pk51`.
- Identifier: `lingocafe.profiles`.
- Identifier: `lingocafe.books`.
- Identifier: `/api/lingocafe/books`.
- Identifier: `/api/lingocafe/profile`.
- Identifier: `own_lang`.
- Identifier: `target_lang`.
- Identifier: `target_level`.
- Identifier: `set-state-in-effect`.
- Identifier: `/books`.
- Identifier: `npm run qa`.
- Identifier: `languages.levels`.
- Path: `src/app/(app)/(lingocafe)/books/page.tsx`.
- Path: `src/app/api/(lingocafe)/lingocafe/...`.
- Path: `src/app/api/(lingocafe)/lingocafe/_lib/reader.ts`.
- Path: `src/AppConfig.ts`.
- Path: `src/config/lingocafe/config.ts`.
- Path: `src/app/api/books/route.ts`.
- Path: `src/app/(app)/books/page.tsx`.
- Source artifacts: `PK51.notes.md`, `PK51.plan.md`, `PK51.task.md`.

## Durable Outcome and Decisions

- Treat the operator's `backlog-execute ma68, pk51` request as plan acceptance plus execution.
- Use the existing `lingocafe.profiles` and `lingocafe.books` tables for the reader catalog.
- Return language options from `/api/lingocafe/books` and `/api/lingocafe/profile` so the client can render onboarding without component-local option arrays.
- Keep the language option lists as static API-local constants for now, per operator request.
- Use the current authenticated session user ID for profile reads/writes and never accept a browser-supplied user ID.
- Treat a profile as complete only when `own_lang`, `target_lang`, and `target_level` are all present.
- Keep the page in one of two states only: profile completion form or book list.

## Validation and Limitations

- No task-specific validation result was preserved beyond the completed/archived record.

## Task Relationships

### Supersedes

None identified.

### Superseded by

None identified.

### Related Tasks

None identified.

## Compression Provenance

Compressed 3 artifacts: `PK51.notes.md`, `PK51.plan.md`, `PK51.task.md`. Their exact pre-compression versions are recoverable from `compressedFromCommit` (`27c91fcf1650c9cb49d27cb7c10a6484fbfe4329`), for example:

```bash
git show 27c91fcf1650c9cb49d27cb7c10a6484fbfe4329:docs/backlog/tasks/PK51-show-language-filtered-lingocafe-books/PK51.task.md
```
