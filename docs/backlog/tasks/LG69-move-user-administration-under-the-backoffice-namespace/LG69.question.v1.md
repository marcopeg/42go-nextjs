# Refinement Questions v1 — Move User Administration Under the Backoffice Namespace

Answer inline under each question, then return to chat and say `next round`.

## Questions

### 1. What should happen when an operator opens the legacy `/users` page?

Recommendation: keep an indefinite permanent redirect to `/backoffice/users`. This preserves bookmarks while making the canonical location unambiguous. If you want removal instead, state whether it should return 404 immediately or after a defined compatibility period.

Answer:

no need to handle backward compatibility. but update the entry in the desktop menu.

### 2. What compatibility behavior should the legacy `/api/users` endpoint have?

Recommendation: keep a temporary protected alias that invokes the same handlers and policies as `/api/backoffice/users`, without relying on an HTTP redirect for PATCH or DELETE. State the concrete removal trigger, such as “remove in the next breaking release,” or choose immediate removal.

Answer:

no bacward compatibility required. just update to the desired final url shape and code location.

### 3. Are any consumers outside this repository known to call `/api/users`?

List them if they exist. If none are known, answer `No known external consumers`; planning will treat repository-owned callers as the complete migration inventory.

Answer:

no.

### 4. Should this task introduce a shared `/backoffice` layout or navigation shell?

Recommendation: no. Keep the existing `AppLayout` behavior on each management page, colocate users beside notifications under the namespace, and leave a shared backoffice shell to a separate task. Choose `yes` only if LG69 must also define shared UI, breadcrumbs, or navigation behavior.

Answer:

no, keep the current
