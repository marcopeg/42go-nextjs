# Plan — Move User Administration Under the Backoffice Namespace

## Goal

Move the existing user-administration page and API to the established backoffice namespace, update every repository-owned caller and navigation entry, remove the legacy routes, and preserve the current policies and behavior.

## Milestones

### Milestone 1 — Move the routes without changing behavior

- [x] Step 1 — Relocate the user-administration page
  - Achieve: Serve the existing client-only user-management workflow at `/backoffice/users`.
  - Create: `src/app/(app)/backoffice/users/page.tsx`.
  - Modify: Fetch targets inside the moved page.
  - Delete: `src/app/(app)/users/page.tsx`.
  - Touch points: Existing `AppLayout` page policy and list/details/edit/delete workflow.
  - Validation: Confirm the page keeps `page:users`, session, `backoffice`, and `users:list` requirements and uses `/api/backoffice/users`.
  - Notes: Do not introduce a shared backoffice shell or redesign the page.

- [x] Step 2 — Relocate the user-management API
  - Achieve: Serve GET, PATCH, and DELETE at `/api/backoffice/users` with unchanged behavior and authorization.
  - Create: `src/app/api/backoffice/users/route.ts`.
  - Modify: No business logic beyond its route location.
  - Delete: `src/app/api/users/route.ts`.
  - Touch points: User listing, updates, account erasure, current-app resolution, and operation-specific route policies.
  - Validation: Confirm explicit `api:users`, session, `backoffice`, and existing exact grants remain on every handler.
  - Notes: Do not add a legacy alias or repair the separate `users:edit` authorization issue.

### Milestone 2 — Update repository-owned navigation and documentation

- [x] Step 3 — Replace all repository-owned legacy route references
  - Achieve: Point every AppConfig menu and caller to the new canonical routes.
  - Create: n/a.
  - Modify: `src/config/lingocafe/config.ts`, `src/config/default/config.ts`, and moved page fetch URLs.
  - Delete: n/a.
  - Touch points: LingoCafe desktop/bottom menu, default desktop/top menu, default mobile menu, GET/PATCH/DELETE browser fetches.
  - Validation: Repository search finds no user-administration `/users` or `/api/users` route reference outside historical backlog artifacts and internal module imports.
  - Notes: Imports under `@/42go/users/` are module paths, not legacy HTTP routes.

- [x] Step 4 — Document user administration in the backoffice namespace
  - Achieve: Record the canonical user-management page/API and preserved policy boundary.
  - Create: n/a.
  - Modify: `docs/memory-bank/ARCHITECTURE.md` and `docs/memory-bank/FEATURES.md`.
  - Delete: n/a.
  - Touch points: Backoffice route namespace and user-administration capability documentation.
  - Validation: Documentation names `/backoffice/users` and `/api/backoffice/users` and states that namespacing does not replace authorization.
  - Notes: Do not modify `CHANGELOG.md`.

### Milestone 3 — Prove route and policy integrity

- [x] Step 5 — Add focused route contract tests
  - Achieve: Guard the new paths, fetch targets, menu links, explicit policies, and absence of legacy route files.
  - Create: `tests/backoffice-users-routes.test.mts`.
  - Modify: `package.json` with a targeted test script.
  - Delete: n/a.
  - Touch points: Node test runner and source-level route contract checks.
  - Validation: `npm run test:backoffice-users-routes` passes.
  - Notes: Tests should fail if legacy route files return or policy declarations drift.

- [x] Step 6 — Run final verification
  - Achieve: Verify source inventory, targeted route contracts, lint, and production build.
  - Create: n/a.
  - Modify: Plan checkboxes and `LG69.notes.md`.
  - Delete: n/a.
  - Touch points: `rg`, targeted test script, and `npm run qa`.
  - Validation: No legacy HTTP references remain, targeted tests pass, and `npm run qa` passes.
  - Notes: Record any environmental warning or deviation without weakening the gate.
