# Execution Notes — Move User Administration Under the Backoffice Namespace
**Task**: ./LG69.task.md
**Plan**: ./LG69.plan.md

## Decisions

- The operator's `plan and execute` instruction accepted the newly created plan and authorized immediate execution.
- Legacy `/users` and `/api/users` routes will be removed without redirects, aliases, or a deprecation period.
- Existing per-page `AppLayout` behavior and route policies remain unchanged.

## Problems Encountered

- The live Next.js development client logged one framework performance-measure error with a negative timestamp during browser navigation. The users page rendered and operated normally, and the message originated from Next.js development instrumentation rather than the moved route.

## Deviations From Plan

- n/a

## Additional Requests

## Known Limitations

- Direct unauthenticated smoke checks prove `/api/backoffice/users` returns `401`, `/api/users` returns `404`, and the QuickList app fails closed with `404`. The authenticated LingoCafe browser smoke check loaded the full users list at desktop and 390 px widths. Destructive or mutating user actions were not invoked during verification.

## Validation

- `npm run test:backoffice-users-routes` — passed, 4 tests.
- `npm run qa` — passed; production route output includes `/backoffice/users` and `/api/backoffice/users` and excludes the legacy routes.
- Repository route inventory — no legacy `/users` or `/api/users` references remain in application source or current documentation.
- Browser smoke check — LingoCafe admin session loaded `/backoffice/users`; desktop navigation points to the canonical URL; 390 px geometry had no horizontal page overflow.
