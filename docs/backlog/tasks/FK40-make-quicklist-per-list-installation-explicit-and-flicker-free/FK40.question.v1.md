# Refinement Questions v1 — Make QuickList Per-List Installation Explicit and Flicker-Free

Answer inline under each question, then return to chat and say `next round`.

## Questions

### 1. Installer route and return journey

Should the deliberate installation document be the authenticated route `/quicklists/:projectId/install`, reached only from the list settings page, with a clear explanation and a normal back link to that list’s settings?

Answer:

yes

### 2. Manifest exposure outside the installer

For a normal browser visit or refresh of `/quicklists/:projectId`, should the page use the base QuickList manifest and reserve the list-specific manifest exclusively for the installer document? This makes the installer boundary unambiguous, but the installed-list launch flow must be verified carefully on supported browsers.

Answer:

yes

### 3. Delivery validation

Is the required release validation matrix Chromium desktop plus iPhone/iPad Safari, or must Android Chromium be included in this task before it can ship?

Answer:

i will do the manual testing on iphone and android

