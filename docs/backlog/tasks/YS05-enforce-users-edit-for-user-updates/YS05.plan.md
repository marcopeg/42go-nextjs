# Plan — Enforce users:edit for User Updates

## Goal

Make every backoffice user-update path require exact `users:edit` authorization, provision development data consistently, and provide a safe operator-run SQL artifact for existing databases.

## Milestones

### Milestone 1 — Enforce the server authorization boundary

- [x] Step 1 — Protect all user-update operations with `users:edit`
  - Achieve: Make `update-user`, `reset-profile`, and `reset-consent` inaccessible to list-only operators while preserving list and delete policies.
  - Create: n/a
  - Modify: `src/app/api/backoffice/users/route.ts`
  - Delete: n/a
  - Touch points: `PATCH` route policy; existing `GET` and `DELETE` policies.
  - Validation: Focused route-policy tests assert exact grants and unchanged route namespace.
  - Notes: Server enforcement remains authoritative and app-scoped.

### Milestone 2 — Align provisioning without automatic production data changes

- [x] Step 1 — Align default and LingoCafe development seeds
  - Achieve: Ensure both supported development/test app seeds define `users:edit` and assign it to their app-scoped backoffice roles.
  - Create: n/a
  - Modify: `knex/seeds/20240523_default_test_users.js`; LingoCafe seed only if inspection finds drift.
  - Delete: n/a
  - Touch points: `auth.grants`; `auth.roles_grants`.
  - Validation: Deterministic seed-source tests cover both apps.
  - Notes: Do not add a Knex data migration.

- [x] Step 2 — Add the operator-run SQL artifact
  - Achieve: Let the operator manually provision existing databases without altering unrelated role/grant mappings.
  - Create: `docs/backlog/tasks/YS05-enforce-users-edit-for-user-updates/YS05.provision-users-edit.sql`
  - Modify: YS05 task documentation if artifact details require clarification.
  - Delete: n/a
  - Touch points: Idempotent grant insert; existing app-scoped backoffice mappings; verification and rollback guidance.
  - Validation: Static tests inspect transaction boundaries, conflict-safe inserts, scope, and absence of destructive statements.
  - Notes: Create the file but never execute it.

### Milestone 3 — Apply standard client policy gating

- [x] Step 1 — Hide unauthorized edit and reset controls
  - Achieve: Omit edit, reset-profile, and reset-consent controls unless the session passes exact `users:edit`.
  - Create: n/a
  - Modify: `src/app/(app)/backoffice/users/page.tsx`
  - Delete: n/a
  - Touch points: Detail-panel edit action; row-menu edit and reset actions; `ProtectComponent` policy constants.
  - Validation: Focused static tests assert standard policy usage and silent loading/error omission.
  - Notes: Preserve existing styling, responsive layout, and server-side enforcement.

### Milestone 4 — Verify and document the completed boundary

- [x] Step 1 — Expand authorization regression coverage
  - Achieve: Cover exact list/edit/delete separation, supported app seeds, route namespace, and operator SQL safety.
  - Create: n/a
  - Modify: `tests/backoffice-users-routes.test.mts`; `package.json` only if a separate focused test command is necessary.
  - Delete: n/a
  - Touch points: List-only, edit-capable, mixed-grant, unauthenticated, non-backoffice, wrong-app policy construction and no-write behavior where the existing harness permits.
  - Validation: `npm run test:backoffice-users-routes`.
  - Notes: Prefer behavioral assertions where practical and deterministic source assertions for wiring boundaries.

- [x] Step 2 — Update authorization documentation and run final validation
  - Achieve: Document distinct `users:list`, `users:edit`, and `users:delete` responsibilities and leave the task ready for completion review.
  - Create: `docs/backlog/tasks/YS05-enforce-users-edit-for-user-updates/YS05.notes.md`
  - Modify: Relevant authorization/operator documentation; YS05 plan progress.
  - Delete: n/a
  - Touch points: Backoffice user administration documentation and execution notes.
  - Validation: Focused test plus `npm run qa`; inspect the final diff for accidental migration or legacy-route changes.
  - Notes: Do not modify `CHANGELOG.md`.
