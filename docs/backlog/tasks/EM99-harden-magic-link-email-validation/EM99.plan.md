# Plan — Harden Magic Link Email Validation

## Goal

Add a static shared auth-grade email validator for magic-link login, block invalid/alias/disposable inputs before email auth starts, document the policy, and verify it with focused tests plus QA.

## Milestones

### Milestone 1 — Research and Policy Shape

- [x] Step 1 — Confirm provider alias and disposable-domain sources
  - Achieve: Record implementation-facing research for major provider alias behavior and disposable-domain list strategy.
  - Create: No new runtime files yet.
  - Modify: `EM99.notes.md` during execution.
  - Delete: Nothing.
  - Touch points: Google Gmail dots docs, Microsoft plus-addressing docs, Fastmail plus/subdomain docs, Proton plus-alias docs, Yahoo disposable address docs, Apple iCloud alias docs, maintained disposable-domain repositories.
  - Validation: Notes capture source URLs and the chosen v1 policy boundaries.
  - Notes: Privacy relays remain allowed. Research and v1 policy decisions recorded in `EM99.notes.md`.

### Milestone 2 — Shared Validator

- [x] Step 2 — Implement reusable email validation utility
  - Achieve: Central validator normalizes accepted email addresses and rejects invalid syntax, plus aliases, Gmail dot aliases, selected provider alias patterns, and disposable domains.
  - Create: A client/server-safe validator module and any small static domain/policy data files.
  - Modify: Existing email normalization utility if needed.
  - Delete: Nothing.
  - Touch points: `src/42go/auth/lib/email/`, possible reusable `src/42go/lib/` location if better after code inspection.
  - Validation: Focused validator tests pass.
  - Notes: Implemented in `src/42go/auth/lib/email/validation.ts`; public callers surface only generic invalid state.

### Milestone 3 — Auth Route and UI Guards

- [x] Step 3 — Apply validation to magic-link request surfaces
  - Achieve: UI blocks invalid email on Enter/click without network request; app-owned server routes reject invalid input before throttle, token lookup, or sender path.
  - Create: No separate validator.
  - Modify: `IdentifierLogin.tsx`, `/api/auth/email/request`, `/api/auth/email/verify-code`, and existing auth email utilities.
  - Delete: Nothing.
  - Touch points: `src/42go/auth/components/login-strategies/IdentifierLogin.tsx`, `src/app/api/auth/email/request/route.ts`, `src/app/api/auth/email/verify-code/route.ts`.
  - Validation: Source inspection confirms `/api/auth/email/request` validates before throttle and `/api/auth/email/verify-code` validates before token lookup.
  - Notes: Did not modify NextAuth internals; existing `normalizeEmailIdentifier` now delegates to the shared validator.

### Milestone 4 — Documentation

- [x] Step 4 — Document policy and maintenance
  - Achieve: Public and developer docs explain the validation policy, known limitations, privacy relay allowance, and periodic disposable-domain review.
  - Create: No new public page unless existing docs lack a home.
  - Modify: `contents/default/docs/authentication/README.md`, `docs/articles/EMAIL_AUTH.md`, and `.agents/skills/42go-auth/SKILL.md`.
  - Delete: Nothing.
  - Touch points: Existing auth documentation only.
  - Validation: Docs include source URLs and maintenance expectation.
  - Notes: Keep user-facing copy generic.

### Milestone 5 — Tests and QA

- [x] Step 5 — Add targeted tests and run gates
  - Achieve: Unit tests cover accepted addresses, syntax failures, plus aliases, Gmail dot aliases, disposable domains, privacy relays, and normalization. Route/UI guard behavior is covered to the extent practical without a full browser test.
  - Create: Focused Node test file/script if the repo lacks a working Jest runtime.
  - Modify: `package.json` only for a narrow test script if needed.
  - Delete: Nothing.
  - Touch points: `package.json`, test files/scripts.
  - Validation: `npm run test:email-validation` passes; `npm run qa` passes.
  - Notes: Added a focused Node built-in test script with the module-type warning disabled for clean output.
