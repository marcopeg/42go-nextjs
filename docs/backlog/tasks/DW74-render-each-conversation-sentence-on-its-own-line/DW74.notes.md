# Execution Notes — Render Each Conversation Sentence on Its Own Line
**Task**: ./DW74.task.md
**Plan**: ./DW74.plan.md

## Decisions

- Extended the shared sentence-display utility rather than adding a conversation-only splitter.
- Each display segment now retains Markdown `source` for visual rendering and plain `text` for translation and playback.
- `sentenceLines` defaults to `false`; the exact round renderer enables it in one line. Titles and descriptions retain inline flow.

## Problems Encountered

- The first Markdown-source mapper assigned leading bold delimiters to the preceding sentence. Focused tests exposed it; delimiter ownership is now mapped to the correct source fragment.
- Browser screenshot capture timed out. The live mobile reader was still verified through DOM, accessibility tree, computed display mode, line height, and element geometry.

## Deviations From Plan

- None.

## Additional Requests

- None.

## Known Limitations

- No user-facing preference is added. A future preference can pass the existing `sentenceLines` prop.
- `npm run qa` passes with the existing unrelated lint warning in `src/app/(app)/layout.tsx` about `window.location.href`.
