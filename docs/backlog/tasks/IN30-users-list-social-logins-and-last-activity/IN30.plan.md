# Plan — Users List Social Logins and Last Activity

## Goal

Extend the `/users` backoffice page so each listed user shows login-method icons and latest activity, and can open a read-only detail panel, without exposing passwords, OAuth secrets, or event payloads.

## Milestones

### Milestone 1 — Database Query Support

- [x] Step 1 — Add lookup indexes
  - Achieve: Make account-provider aggregation and latest-event lookup efficient for the `/api/users` list response.
  - Create: A new Knex migration after `20260602120000_email_auth_app_scope.js`.
  - Modify: n/a
  - Delete: n/a
  - Touch points: `knex/migrations/`
  - Validation: Run `make migrate.status` or equivalent migration status check if the local database is available; otherwise verify migration syntax by inspection and `npm run qa`.
  - Notes: Prefer `auth.accounts (app_id, user_id, provider)` and `events.events (app_id, user_id, event_at DESC, created_at DESC, id DESC)` unless implementation discovers an existing equivalent index.
  - Result: Added `20260611132500_users_list_lookup_indexes.js` with the planned account and event lookup indexes.

### Milestone 2 — Users API Enrichment

- [x] Step 1 — Extend response types
  - Achieve: Define a safe response contract with `loginMethods`, `lastActivity`, `hasPassword`, and `consent` where needed for the detail panel.
  - Create: n/a
  - Modify: `src/app/api/users/route.ts`
  - Delete: n/a
  - Touch points: `UserRow`, list response shaping, client-facing field names.
  - Validation: TypeScript build through `npm run qa`.
  - Notes: Never return `auth.users.password`; return only a boolean signal needed to infer credentials login.
  - Result: Extended the users response with `consent`, `hasPassword`, `loginMethods`, and `lastActivity`.

- [x] Step 2 — Aggregate provider links
  - Achieve: Fetch provider names from `auth.accounts` scoped by both `app_id` and `user_id`, grouped per listed user.
  - Create: n/a
  - Modify: `src/app/api/users/route.ts`
  - Delete: n/a
  - Touch points: `listUsers`
  - Validation: Confirm users with multiple account rows get multiple providers without duplicate user rows.
  - Notes: Do not include `account_id`, tokens, scopes, session state, or provider secret fields in the response.
  - Result: Added a distinct provider lookup scoped by `app_id` and listed user IDs.

- [x] Step 3 — Fetch latest activity
  - Achieve: Add one latest event per listed user using `events.events`, scoped by `app_id` and `user_id`, ordered primarily by `event_at DESC`.
  - Create: n/a
  - Modify: `src/app/api/users/route.ts`
  - Delete: n/a
  - Touch points: `listUsers`
  - Validation: Confirm users with no events get `lastActivity: null`.
  - Notes: Use a set-based query such as `DISTINCT ON (user_id)` or a lateral join. No browser-side full-event reduction. No N+1 API pattern.
  - Result: Added a `DISTINCT ON (user_id)` latest-event query ordered by `event_at DESC, created_at DESC, id DESC`.

### Milestone 3 — Users List Rendering

- [x] Step 1 — Update client models
  - Achieve: Match the `/api/users` response shape in the `/users` client page.
  - Create: n/a
  - Modify: `src/app/(app)/users/page.tsx`
  - Delete: n/a
  - Touch points: `AppUser`, response parsing, rendering props.
  - Validation: TypeScript build through `npm run qa`.
  - Notes: Keep browser fetch with `credentials: 'same-origin'`.
  - Result: Updated the `AppUser` model to match the enriched API payload.

- [x] Step 2 — Render login-method icons
  - Achieve: Show visible, accessible provider/login-method icons in each row.
  - Create: Small local render helpers inside `page.tsx`, unless the implementation naturally justifies extracting them.
  - Modify: `src/app/(app)/users/page.tsx`
  - Delete: n/a
  - Touch points: table columns or identity cell, loading skeleton width, empty states.
  - Validation: Visual check on desktop and mobile widths.
  - Notes: Support GitHub, Google, unknown providers, credentials login, and magic-link/email fallback. Prefer existing lucide icons where provider-specific icons are unavailable.
  - Result: Added accessible row icons for providers, credentials, magic-link/email, and unknown providers.

- [x] Step 3 — Render last activity
  - Achieve: Show latest activity date/time in the row, with event name available on hover/focus.
  - Create: Local helper or reuse existing date display behavior if it fits.
  - Modify: `src/app/(app)/users/page.tsx`
  - Delete: n/a
  - Touch points: table header, row cells, loading skeletons, empty state.
  - Validation: Verify no-event users show `Never`; hover/focus exposes event name.
  - Notes: Keep text compact and avoid layout shifts.
  - Result: Added a last-activity column with date display and event-name disclosure via title/focusable control.

### Milestone 4 — Read-Only Detail Panel

- [x] Step 1 — Add selected-user detail state
  - Achieve: Open a right-anchored read-only detail panel from whole-row click and from the row menu.
  - Create: `UserDetailPanel` in `src/app/(app)/users/page.tsx`, or a local section in the existing file if simpler.
  - Modify: `src/app/(app)/users/page.tsx`
  - Delete: n/a
  - Touch points: row `onClick`, keyboard access, dropdown menu actions, selected-user state.
  - Validation: Clicking the three-dot menu does not trigger the row detail panel.
  - Notes: Add `View user details` as the first dropdown item.
  - Result: Added row and menu entry paths into a shared `UserDetailPanel`.

- [x] Step 2 — Format detail content
  - Achieve: Display user identity, email, username, name, image, profile, consent, feature flags, created/updated dates, login methods, and latest activity in readable sections.
  - Create: Small display helpers for JSON blocks and field rows if needed.
  - Modify: `src/app/(app)/users/page.tsx`
  - Delete: n/a
  - Touch points: shared `Modal` panel, field formatting.
  - Validation: Detail panel does not show `appId`, password hashes, OAuth tokens, or provider secret fields.
  - Notes: Keep `appId` out of the panel even if it remains in the client model for internal use.
  - Result: Added structured detail sections for identity, login, activity, profile, consent, and feature flags.

- [x] Step 3 — Add panel actions
  - Achieve: Provide `Edit` and placeholder `Delete` actions in the detail panel.
  - Create: n/a
  - Modify: `src/app/(app)/users/page.tsx`
  - Delete: n/a
  - Touch points: existing `openEditUser`, existing edit panel state, `window.confirm` or existing confirmation pattern.
  - Validation: `Edit` opens the existing edit panel for the same user. Confirmed delete shows `User deletion not yet implemented` and performs no API call.
  - Notes: The delete button should be a red ghost action and must not implement real deletion.
  - Result: Added `Edit` and red ghost placeholder `Delete` actions.

- [x] Step 4 — Email verification affordance
  - Achieve: Represent email verification with a clear icon and reveal verification date for verified users on hover/focus/tap.
  - Create: Local display helper if needed.
  - Modify: `src/app/(app)/users/page.tsx`
  - Delete: n/a
  - Touch points: list or detail panel verification display.
  - Validation: Verified and unverified users are visually distinct and accessible.
  - Notes: Use icons from the existing stack instead of emoji where practical.
  - Result: Added shield-style verification icons with accessible labels and click disclosure for verified dates.

### Milestone 5 — Verification

- [x] Step 1 — Run automated quality gate
  - Achieve: Catch lint, type, and build regressions.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: project root
  - Validation: `npm run qa`
  - Notes: If local DB is required for extra checks and unavailable, report that clearly.
  - Result: `npm run qa` passed. `make migrate.status` worked, then `make migrate` applied the new index migration locally.

- [ ] Step 2 — Visual smoke test LingoCafe users
  - Achieve: Verify the changed `/users` experience in the target app.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `https://lc42go.ngrok.app/users`
  - Validation: Log in with `john` / `john`, open `/users`, verify row icons, last activity display, menu item, row detail panel, edit action, and delete placeholder.
  - Notes: Check row/menu click separation carefully. Then check it again.
  - Result: Blocked during safe verification because `john` is intercepted by the required LingoCafe profile onboarding overlay. I did not accept terms or submit consent on behalf of the user.
