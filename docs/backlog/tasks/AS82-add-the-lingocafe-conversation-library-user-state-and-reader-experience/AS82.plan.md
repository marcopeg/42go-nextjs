# Plan — Add the LingoCafe conversation database foundation

## Goal

Deliver and prove the migration-derived conversation storage and publication contract that gates YE24 and QT31.

## Milestones

### Milestone 1 — Schema and contract

- [x] Step 1 — Add the conversation migration
  - Achieve: Create normalized content, localization, relationship, round, and user-state tables with the refined constraints and indexes.
  - Create: One additive Knex migration.
  - Modify: n/a.
  - Delete: n/a.
  - Touch points: `knex/migrations/`.
  - Validation: Static migration tests plus development migration up/down/up.
  - Notes: Preserve historical migrations.

- [x] Step 2 — Publish the standalone data contract
  - Achieve: Make schema and safe importer semantics derivable from 42go-nextjs alone.
  - Create: `docs/lingocafe/CONVERSATIONS_DATA_MODEL.md`.
  - Modify: n/a.
  - Delete: n/a.
  - Touch points: accumulated migrations and source YAML schemas.
  - Validation: Cross-check every documented key/check/FK/index against migration source.
  - Notes: Document explicit visibility and ownership boundaries.

### Milestone 2 — Fixture and user-state lifecycle

- [x] Step 3 — Add a surgical development fixture
  - Achieve: Provide fixed reserved rows for local schema/API development without a books checkout.
  - Create: One deterministic Knex seed.
  - Modify: Seed cleanup only if required for FK-safe development initialization.
  - Delete: n/a.
  - Touch points: `knex/seeds/`.
  - Validation: Repeat seed and verify no broad delete/user-state mutation.
  - Notes: Full corpus remains YE24.

- [x] Step 4 — Extend account erasure
  - Achieve: Explicitly delete and report conversation reads and stars.
  - Create: Targeted regression assertions.
  - Modify: LingoCafe account-erasure handler and tests.
  - Delete: n/a.
  - Touch points: `src/config/lingocafe/account-erasure.server.ts`, `tests/`.
  - Validation: Source/behavior tests and QA.
  - Notes: Leave pre-existing books-completed debt out of scope.

### Milestone 3 — Proof and handoff

- [x] Step 5 — Verify constraints and migration lifecycle
  - Achieve: Prove invalid scopes, statuses, levels, duplicate realizations, cross-scenario actors, and blank rounds fail.
  - Create: Targeted schema tests/probes.
  - Modify: package test scripts if useful.
  - Delete: n/a.
  - Touch points: tests, development PostgreSQL.
  - Validation: up/down/up, migration status, fixture counts, unchanged book-state counts.
  - Notes: Record exact results in execution notes.

- [x] Step 6 — Run repository quality gate and independent review
  - Achieve: Finish AS82 with a stable contract ready for YE24.
  - Create: Execution notes.
  - Modify: Plan checkboxes and notes with results/deviations.
  - Delete: n/a.
  - Touch points: scoped diff and full repository QA.
  - Validation: targeted tests, `npm run qa`, `git diff --check`, adversarial review.
  - Notes: Fix material findings before completion.
