# Plan — Add a Play Action to the Reader Translation Popover

## Goal

Add capability-aware sentence and word listening actions to the LingoCafe translation popover, preserving the refined playback state matrix, isolated word pronunciation, privacy-safe analytics, and responsive reader styling.

## Milestones

### Milestone 1 — Extend translation-aware playback orchestration

- [x] Step 1 — Add controller actions for sentence and word translation playback
  - Achieve: Expose explicit controller methods for restarting/starting sentence playback from a translation and for isolated one-word pronunciation.
  - Create: n/a
  - Modify: `reader-playback/types.ts`, `reader-playback/useReaderPlayback.ts`
  - Delete: n/a
  - Touch points: playback status refs, auto-pause reasons, speech provider generation, manual pause recovery, translation dismissal.
  - Validation: TypeScript through `npm run qa`; code review against the refined playback-state matrix.
  - Notes: Reuse the single speech provider. Do not create a second playback engine.

- [x] Step 2 — Add sentence/word analytics discrimination
  - Achieve: Emit `audio.play` with `type: "sentence"` for continuous sentence playback and `type: "word"` for each isolated word pronunciation.
  - Create: n/a
  - Modify: `reader-playback/useReaderPlayback.ts`
  - Delete: n/a
  - Touch points: existing event payloads and word utterance start callback.
  - Validation: Static review confirms only `language`, `book_id`, `page_id`, and non-sensitive `type` are emitted.
  - Notes: Never include source text, translated text, word IDs, or sentence IDs.

### Milestone 2 — Build and wire the split translation action

- [x] Step 3 — Add the split-surface listening action
  - Achieve: Render translation content on the left and a full-height divided action on the right, with Play icon plus `Listen from here` or `Play word`.
  - Create: n/a
  - Modify: `BookPageReader.tsx`
  - Delete: n/a
  - Touch points: loading/success/error states, source label placement, mobile edge-to-edge layout, keyboard focus, pointer behavior.
  - Validation: Inspect mobile and desktop layout, focus state, semantic button label, light/dark reader themes.
  - Notes: Keep the existing sentence-anchored popover. The shared Modal cannot preserve this inline anchoring contract.

- [x] Step 4 — Wire playback state and actions through both reader surfaces
  - Achieve: Show actions only when full playback capability is available, hide them during active speech when translation auto-pause is disabled, close sentence popovers on action, and keep word popovers open.
  - Create: n/a
  - Modify: `BookPageReader.tsx`, `BookReaderSurfaces.tsx`
  - Delete: n/a
  - Touch points: desktop/mobile reader props, translation open-state effects, outside-tap detection.
  - Validation: Static state-matrix review and authenticated reader smoke verification.
  - Notes: Treat clicks inside the translation popover as internal so `Play word` does not trigger outside dismissal.

### Milestone 3 — Validate and document

- [x] Step 5 — Run repository quality gates
  - Achieve: Produce a clean lint and production build.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: full repository QA.
  - Validation: `npm run qa`.
  - Notes: Do not start a second development server.

- [x] Step 6 — Verify reader behavior and record execution results
  - Achieve: Check sentence and word translation actions on mobile and desktop, including loading/error states, auto-paused playback, manual pause, repeated word playback, outside dismissal, and unavailable capability.
  - Create: `QN48.notes.md`
  - Modify: `QN48.plan.md`, `QN48.notes.md`
  - Delete: n/a
  - Touch points: `https://lc42go.ngrok.app/`, `/books`, reader translation scopes.
  - Validation: Visual/manual verification or a documented external blocker.
  - Notes: The operator accepted completion after taking ownership of final device testing.
