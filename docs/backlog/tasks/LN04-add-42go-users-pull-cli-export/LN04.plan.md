# Plan — Add 42go Users Pull CLI Export

## Goal

Add a `42go users pull` CLI command that refreshes local auth user/account Parquet datasets, excludes secrets, and runs before analytics aggregations in `42go update`.

## Milestones

### Milestone 1 — Auth Export Model and Paths

- [x] Step 1 — Inspect CLI conventions
  - Achieve: Confirm current Typer command, dependency, path, state, and test patterns.
  - Create: n/a
  - Modify: n/a
  - Delete: n/a
  - Touch points: `cli/src/fortytwogo_cli/cli.py`, `cli/src/fortytwogo_cli/events/*`, `cli/tests/*`
  - Validation: Existing tests reveal how to inject temp archive roots and mock dependencies.
  - Notes: Keep this read-only.
- [x] Step 2 — Add auth stats paths
  - Achieve: Resolve `.local/42go-stats/_data/auth_users.parquet`, `.local/42go-stats/_data/auth_accounts.parquet`, and metadata/state paths.
  - Create: Auth export path helpers if the existing events path helpers are too event-specific.
  - Modify: Reuse dependency loaders from `events.dependencies`.
  - Delete: n/a
  - Touch points: new `cli/src/fortytwogo_cli/users/paths.py` or equivalent.
  - Validation: Unit tests can resolve paths under a temporary root.
  - Notes: Do not partition v1.

### Milestone 2 — Pull and Merge Engine

- [x] Step 1 — Implement source queries
  - Achieve: Fetch users and accounts changed after stored high-water marks.
  - Create: Auth user/account query helpers.
  - Modify: n/a
  - Delete: n/a
  - Touch points: new `cli/src/fortytwogo_cli/users/pull.py`.
  - Validation: Tests assert SQL/output shape or monkeypatch fetch helpers.
  - Notes: Users order by `(updated_at, created_at, id)`; accounts order by `(updated_at, created_at, app_id, account_id)`.
- [x] Step 2 — Implement Parquet merge/write
  - Achieve: Read existing Parquet, merge fetched rows, de-dupe by identity, write atomically, smoke-read output.
  - Create: Normalizers, schemas, merge helpers, metadata writer.
  - Modify: n/a
  - Delete: n/a
  - Touch points: new `cli/src/fortytwogo_cli/users/pull.py`.
  - Validation: Tests cover new rows, updated rows, reset, approved fields, and excluded secrets.
  - Notes: Metadata advances only after successful writes and smoke reads.

### Milestone 3 — CLI Wiring

- [x] Step 1 — Add `42go users pull`
  - Achieve: Register a `users` Typer app with a `pull` command and useful output.
  - Create: `cli/src/fortytwogo_cli/users/cli.py`, package init if needed.
  - Modify: `cli/src/fortytwogo_cli/cli.py`.
  - Delete: n/a
  - Touch points: Typer app registration and help text.
  - Validation: CLI runner tests assert command discoverability and output.
  - Notes: Match existing CLI error handling style.
- [x] Step 2 — Integrate into `42go update`
  - Achieve: Run auth users pull before book/event aggregation commands.
  - Create: n/a
  - Modify: `cli/src/fortytwogo_cli/cli.py`.
  - Delete: n/a
  - Touch points: `update()`.
  - Validation: Tests assert ordering and hard failure when users pull fails.
  - Notes: Failure stops update like a brick wall.

### Milestone 4 — Verification and Task Notes

- [x] Step 1 — Add focused tests
  - Achieve: Cover cursor filtering, merge/upsert, reset, field allowlists, secret exclusion, atomic validation, CLI help, and update ordering/failure.
  - Create: `cli/tests/test_users_pull.py` or equivalent.
  - Modify: Existing CLI tests if needed.
  - Delete: n/a
  - Touch points: `cli/tests/`.
  - Validation: CLI test suite passes.
  - Notes: Prefer monkeypatching fetches over requiring a live Postgres database.
- [x] Step 2 — Run validation
  - Achieve: Prove the implementation with tests and applicable repo QA.
  - Create: n/a
  - Modify: Plan checkbox progress and execution notes.
  - Delete: n/a
  - Touch points: `LN04.plan.md`, `LN04.notes.md`.
  - Validation: Run CLI tests; run `npm run qa` only if non-CLI repo code changes.
  - Notes: Record any skipped validation plainly.
