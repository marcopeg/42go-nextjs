# Plan — Improve Backlog Draft Skill Scaffolding

## Goal

Move deterministic draft creation for `backlog-draft` into a repo-local Python scaffold script, then rewrite the skill so agents prepare structured content, run the scaffold, verify the result, and report TaskID plus draft path.

## Milestones

### Milestone 1 — Inspect Current Backlog Helpers

- [x] Step 1 — Confirm reusable helper surface
  - Achieve: Identify which existing `backlog-core` functions can support draft scaffolding without duplication.
  - Create: Nothing.
  - Modify: Nothing.
  - Delete: Nothing.
  - Touch points: `.agents/skills/backlog-core/scripts/backlog_lib.py`, `.agents/skills/backlog-core/scripts/backlog_tool.py`, `.agents/skills/backlog-draft/scripts/generate_task_id.py`.
  - Validation: Local code inspection confirms the scaffold can import or reuse helper logic for slug, frontmatter, filenames, and index sync.
  - Notes: Confirmed `backlog_lib.py` exposes reusable helpers for draft filenames, slugification, timestamping, frontmatter writing, relative links, and active index rebuilding. Kept the existing `docs/backlog` storage model.

### Milestone 2 — Add Draft Scaffold Script

- [x] Step 1 — Implement script-owned draft creation
  - Achieve: Add a Python scaffold command that accepts structured task content and creates a complete draft task.
  - Create: `.agents/skills/backlog-draft/scripts/scaffold_draft.py`.
  - Modify: Nothing unless import paths or shared helpers need a small adjustment.
  - Delete: Nothing.
  - Touch points: TaskID generation, slug creation, frontmatter writing, standard body section rendering, `docs/backlog/BACKLOG.md` sync.
  - Validation: Script can run from the repository root and create a draft folder, draft file, and backlog entry.
  - Notes: Added `scaffold_draft.py` and kept `generate_task_id.py` as the narrow ID helper.

- [x] Step 2 — Define structured input and JSON output
  - Achieve: Support JSON input for task title, group, and standard section content; output JSON with at least `taskId`, `taskFolder`, and `draftPath`.
  - Create: Nothing beyond script code.
  - Modify: `.agents/skills/backlog-draft/scripts/scaffold_draft.py`.
  - Delete: Nothing.
  - Touch points: JSON stdin or input-file parsing, omitted section fallback to `information is missing`, output contract.
  - Validation: Dry-run or temp-root run proves missing sections are filled predictably and output is parseable JSON.
  - Notes: Implemented JSON file/stdin input, section aliases, omitted-section placeholders, and JSON output including `taskId`, `taskFolder`, `draftPath`, and `absoluteDraftPath`.

### Milestone 3 — Rewrite Skill Workflow

- [x] Step 1 — Move trigger guidance into frontmatter description
  - Achieve: Improve skill picking by putting direct draft/refine trigger wording into the `description`.
  - Create: Nothing.
  - Modify: `.agents/skills/backlog-draft/SKILL.md`.
  - Delete: Nothing.
  - Touch points: Skill frontmatter description.
  - Validation: Description clearly covers direct task drafting, rough capture, and draft-and-refine requests.
  - Notes: Description now carries draft/new-task/capture/draft-and-refine trigger language.

- [x] Step 2 — Replace scaffold-heavy body with operational flow
  - Achieve: Shrink `SKILL.md` by removing duplicate deterministic scaffold details and documenting the script-owned versus agent-owned boundary.
  - Create: Nothing.
  - Modify: `.agents/skills/backlog-draft/SKILL.md`.
  - Delete: Nothing.
  - Touch points: Drafting rules, related-work lookup, scaffold invocation, verification, refinement handoff.
  - Validation: A future agent can follow the skill without manually recreating frontmatter, filenames, or index entries.
  - Notes: Skill body now defines workflow, agent-owned work, script-owned work, scaffold input/output, and chaining while preserving no-confirmation draft behavior and draft-and-refine chaining.

### Milestone 4 — Verify and Document Execution

- [x] Step 1 — Run safe scaffold verification
  - Achieve: Prove the new scaffold creates valid files, index entries, and JSON output without damaging the real backlog.
  - Create: Temporary test backlog under `/private/tmp` or another safe writable location.
  - Modify: No persistent repo files except plan/notes progress.
  - Delete: Temporary files may be removed after validation if useful.
  - Touch points: `scaffold_draft.py`, generated temp `BACKLOG.md`, generated temp draft file.
  - Validation: Command output is valid JSON, file exists, frontmatter is correct, standard sections exist, and the temp index links the task once.
  - Notes: Verified against `/private/tmp/hw55-backlog-smoke` and `/private/tmp/hw55-backlog-smoke-missing`; no throwaway tasks were created in the real backlog.

- [x] Step 2 — Update execution notes and progress
  - Achieve: Record decisions, validation, and any limitations.
  - Create: `docs/backlog/wip/HW55-improve-backlog-draft-skill-scaffolding/HW55.notes.md` after WIP transition.
  - Modify: `HW55.plan.md`.
  - Delete: Nothing.
  - Touch points: Plan checkboxes, execution notes.
  - Validation: Plan accurately reflects completed work and notes summarize the final state.
  - Notes: Plan and notes were updated with implementation decisions, smoke-test results, and the passing `npm run qa` gate. Leave final completion to `backlog-complete`.
