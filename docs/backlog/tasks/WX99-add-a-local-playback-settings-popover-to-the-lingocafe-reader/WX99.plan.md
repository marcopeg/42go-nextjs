# Plan — Add a Local Playback Settings Popover to the LingoCafe Reader

## Goal

Replace the reader player's speed-cycling button with an anchored gear-and-speed settings popover that controls and locally persists playback speed, word highlighting, sentence highlighting, translation auto-pause, and settings auto-pause, with the accepted pause/resume and current-sentence restart semantics.

## Milestones

### Milestone 1 — Define Device-Local Playback Preferences

- [x] Step 1 — Add a typed, versioned playback-preferences store
  - Achieve: Centralize defaults, validation, local-storage loading, and local-storage persistence for speed plus the four positive boolean preferences.
  - Create: `src/app/(app)/(lingocafe)/books/_components/reader-playback/playback-preferences.ts`.
  - Modify: None expected outside the new module in this step.
  - Delete: Nothing.
  - Touch points: `READER_PLAYBACK_SPEEDS`; browser `localStorage`; default values `1`, `true`, `true`, `true`, `true`.
  - Validation: Missing storage, malformed JSON, unsupported speed values, non-boolean fields, and storage exceptions all return safe defaults without throwing; valid partial legacy objects default every missing boolean to `true`.
  - Notes: Use a dedicated versioned key such as `lingocafe.reader.playback-preferences.v1`; do not mix playback behavior into theme-specific reader preferences or playback-position memory.
  - Notes: Implemented `lingocafe.reader.playback-preferences.v1` with defensive parsing and positive boolean defaults.

- [x] Step 2 — Extend the playback controller contract
  - Achieve: Expose the current preferences and explicit mutation/settings-lifecycle methods required by the UI instead of `cycleSpeed`.
  - Create: No new file expected.
  - Modify: `src/app/(app)/(lingocafe)/books/_components/reader-playback/types.ts`.
  - Delete: Remove the obsolete `cycleSpeed` controller member after all consumers migrate.
  - Touch points: `ReaderPlaybackController`; speed setter; boolean preference setters; controlled settings-open callback.
  - Validation: TypeScript requires all controller consumers to use the new explicit interface and prevents unsupported speed values where practical.
  - Notes: Keep speech sequencing state separate from visual highlight visibility so disabling a highlight never removes the active sentence needed by playback.
  - Notes: Added explicit settings state, speed setter, four boolean setters, and settings lifecycle control; removed `cycleSpeed`.

### Milestone 2 — Implement Playback Preference Semantics

- [x] Step 3 — Load, apply, and persist preferences in `useReaderPlayback`
  - Achieve: Initialize playback from sanitized local preferences, update settings immediately, and persist each accepted change without server/profile writes.
  - Create: No additional file expected.
  - Modify: `src/app/(app)/(lingocafe)/books/_components/reader-playback/useReaderPlayback.ts`.
  - Delete: Remove `getNextPlaybackSpeed` usage and the cycle-only speed handler.
  - Touch points: `speedRef`; active word boundaries; active sentence state; local-storage helper; controller return shape.
  - Validation: Reload restores all five values; missing/invalid storage produces `1×` and four `true` booleans; storage errors leave playback functional.
  - Notes: Preference state changes immediately in the local controller. No `storage` event or cross-tab synchronization is required.
  - Notes: Controller state and refs now initialize from and persist through the dedicated local store.

- [x] Step 4 — Add deterministic settings pause/resume and speed-restart behavior
  - Achieve: Make settings opening/closing coordinate with device speech exactly as refined, including delayed inter-sentence playback.
  - Create: No new file expected.
  - Modify: `src/app/(app)/(lingocafe)/books/_components/reader-playback/useReaderPlayback.ts`.
  - Delete: Nothing.
  - Touch points: `statusRef`; `pausedFromDelayRef`; provider pause/resume/cancel; active sentence index; settings-auto-pause ownership ref; speed-changed-while-open ref.
  - Validation: When settings auto-pause is `true`, opening pauses only active playing/delay states; closing resumes only playback paused by settings; highlight-only changes resume the current utterance; a speed change restarts the same sentence on close. When settings auto-pause is `false`, opening/closing does not alter playback and a speed change immediately restarts the current sentence.
  - Notes: Never auto-resume playback that was manually paused before settings opened. Keep progress and active sentence stable across the interaction.
  - Notes: Implemented shared auto-pause reason ownership so overlapping settings/translation pauses resume only after every active reason clears.

- [x] Step 5 — Make translation auto-pause configurable
  - Achieve: Preserve today's translation pause/resume flow when enabled and bypass it entirely when disabled.
  - Create: No new file expected.
  - Modify: `src/app/(app)/(lingocafe)/books/_components/reader-playback/useReaderPlayback.ts`.
  - Delete: Nothing.
  - Touch points: `setTranslationPaused`; `pausedByTranslationRef`; persisted `autoPauseOnTranslation` preference.
  - Validation: Opening/closing a translation pauses/resumes only when the preference is `true`; switching it off prevents both automatic actions without affecting the translation highlight or popover.
  - Notes: Preserve ownership checks so translation never resumes playback that it did not pause.
  - Notes: Translation open state now participates in the shared pause-reason model and responds immediately when its toggle changes.

### Milestone 3 — Build the Anchored Player Settings UI

- [x] Step 6 — Add the shared anchored popover primitive
  - Achieve: Provide a repository-styled Radix Popover wrapper with portal/content/anchor behavior, focus handling, Escape dismissal, and outside-click dismissal.
  - Create: `src/components/ui/popover.tsx`.
  - Modify: None expected outside the new primitive in this step.
  - Delete: Nothing.
  - Touch points: Installed `@radix-ui/react-popover`; shared `cn` utility; popover color tokens and mobile collision padding.
  - Validation: The controlled primitive toggles from its trigger, closes on outside click and Escape, and positions within the mobile viewport.
  - Notes: Use Radix Popover instead of the shared `Modal` because the accepted UI must be anchored to the gear, remain compact/non-modal, and close when the same trigger is clicked again. `Modal` makes the background inert and expands to full-screen on mobile, so it cannot meet those interaction requirements.
  - Notes: Added a shared styled wrapper around the already-installed Radix Popover dependency.

- [x] Step 7 — Create the playback settings content
  - Achieve: Render a compact five-row control surface with the four speed choices, four positive switches, and an explicit neutral close control.
  - Create: `src/app/(app)/(lingocafe)/books/_components/BookReaderPlaybackSettings.tsx`.
  - Modify: No other file expected in this step.
  - Delete: Nothing.
  - Touch points: `READER_PLAYBACK_SPEEDS`; accessible button-group/radio semantics; `role="switch"` with `aria-checked`; close icon; touch target sizing.
  - Validation: Current speed and every boolean state are visually and semantically clear; controls are keyboard-operable; changes do not close the popover; close control is neutrally styled.
  - Notes: Keep app-specific settings content outside shared UI primitives. Use positive labels exactly: `Word highlighting`, `Sentence highlighting`, `Auto-pause on translation`, and `Auto-pause on settings`.
  - Notes: Added direct speed choices, four accessible switches, and a neutral close control. Operator later added `0.25×` as the first of five supported speeds.

- [x] Step 8 — Replace the speed button with the controlled gear-and-speed popover
  - Achieve: Wire the new settings component into the floating playback controls and route every open/close path through the controller lifecycle.
  - Create: No new file expected.
  - Modify: `src/app/(app)/(lingocafe)/books/_components/BookReaderPlaybackControls.tsx`.
  - Delete: Remove the old click-to-cycle speed button behavior.
  - Touch points: `Settings` icon plus current `speed×`; controlled Popover `open` state; trigger re-click; explicit close; outside click; Escape; `playback.setSettingsOpen`.
  - Validation: Trigger opens and re-click closes; outside click, Escape, and close handle close; all paths invoke identical resume/restart semantics; the popover remains open after every setting change.
  - Notes: Keep the existing player bar dimensions usable on narrow mobile screens and respect safe-area positioning.
  - Notes: Player now uses controller-owned popover state so playback/page closure cannot leave stale open UI state.

### Milestone 4 — Apply Independent Highlight Rendering

- [x] Step 9 — Gate playback sentence and word treatments without losing playback state
  - Achieve: Make all four highlight combinations render correctly while preserving translation highlighting, active sentence identity, guided scrolling, and boundary processing.
  - Create: No new file expected.
  - Modify: `src/app/(app)/(lingocafe)/books/_components/BookReaderSurfaces.tsx`, `src/app/(app)/(lingocafe)/books/_components/BookPageReader.tsx` only if the cleanest prop boundary requires it.
  - Delete: Nothing.
  - Touch points: `playbackSentenceId`; `playbackWordRange`; `activeSentenceId`; translation `activeSentenceId`; desktop and mobile reader surface call sites.
  - Validation: Both `true` shows sentence and best-effort word; word `false` shows sentence only; sentence `false` shows word only; both `false` shows neither. Translation selection styling remains unchanged in every combination.
  - Notes: Prefer suppressing render inputs at the reader-surface boundary while keeping controller state intact.
  - Notes: Word range is gated at the surface boundary; sentence styling has a separate render flag while the active sentence ID remains intact.

### Milestone 5 — Verify Behavior and Close the Task

- [x] Step 10 — Run static and production quality gates
  - Achieve: Catch lint, type, and production-build regressions across the new controller contract and UI primitive.
  - Create: No new file expected.
  - Modify: Fix only issues caused by this task.
  - Delete: Nothing.
  - Touch points: `npm run qa`; changed-file diff; generated build registries if the build touches them.
  - Validation: `npm run qa` passes; no profile, API, database, auth, or unrelated reader-preference changes appear in the task diff.
  - Notes: Preserve unrelated user work in the dirty worktree.
  - Notes: Focused ESLint and TypeScript checks passed; `npm run qa` then passed twice after the final cleanup, including the Next.js production build and all 35 static pages.

- [x] Step 11 — Perform focused LingoCafe browser verification
  - Achieve: Confirm the accepted mobile player UX and speech behavior in the live LingoCafe test environment.
  - Create: Record results in `WX99.notes.md` during execution.
  - Modify: Fix verified task regressions and update plan checkboxes/notes as work completes.
  - Delete: Nothing.
  - Touch points: `https://lc42go.ngrok.app/`; `john` / `john`; `/books`; mobile-sized viewport; device/browser TTS availability.
  - Validation: Verify gear plus speed, five-row popover, every dismissal route, four highlighting combinations, both auto-pause toggles, mid-sentence highlight continuation, speed-change current-sentence restart, page reload persistence, malformed-storage fallback where practical, and existing pause/seek/close/translation behavior.
  - Notes: If the remote environment has not picked up local code or its speech engine lacks word-boundary support, record the limitation precisely and use source/runtime checks for the unavailable portion rather than claiming it passed.
  - Notes: The ngrok root and `/books` both returned HTTP 200. In-app browser setup failed before a tab could open (`Cannot redefine property: process`), so visual, authenticated, speech, and local-storage verification remain pending and are not claimed as passed.
  - Notes: Operator testing exposed a nested-dialog portal issue: the trigger rendered, but the body-level popover did not. The popover now portals into the nearest reader modal content, and `npm run qa` passes after the fix; operator re-verification remains pending.
  - Notes: A second operator test still showed no trigger response. The Radix Trigger/custom Button composition was removed; a native button now toggles controlled open state directly, shows an active background when open, and uses Radix only for anchoring/dismissal. QA passes after this change; operator re-verification remains pending.
  - Notes: A third test still showed no tap response. The gear now uses the exact direct shared-Button interaction pattern of the former working speed button; Radix anchors to a separate wrapper, and the mobile-risky `display: contents` wrapper is gone. QA passes; operator re-verification remains pending.
  - Notes: The alert/direct-pause test proved click delivery and exposed the invisible Radix dismiss layer consuming Play taps. Radix has been removed from this feature. A direct fixed panel now mirrors the proven control-bar geometry, sits immediately above it at maximum z-index, and dismisses through document pointer/Escape listeners without a blocking backdrop; operator re-verification remains pending.
  - Notes: Screenshot verification exposed two simultaneously mounted settings instances: the hidden desktop instance treated taps inside the visible mobile panel as outside taps and closed the shared state. Hidden instances no longer register dismissal listeners. The panel now uses the player's own fixed containing block, matching its full width and `max-w-2xl`, sitting 0.5rem above it with an opaque background. `npm run qa` passes; operator re-verification remains pending.
  - Notes: Follow-up mobile verification showed the panel aligned horizontally with an opaque background, but mobile Safari dropped the Tailwind arbitrary calculated bottom rule and used the panel's static position below the player. The 0.5rem-above-player offset is now an explicit inline style; operator re-verification remains pending.
  - Notes: Positioning is operator-confirmed. The settings panel and audio bar now share one outer-surface class for identical background, border, rounded corners, blur, and a restrained shadow. The temporary alert/direct-pause diagnostic path was removed, restoring controller-owned settings pause/resume behavior; operator re-verification of resume remains pending.
  - Notes: Operator confirmed the interaction and placement, then identified residual text bleed from the shared 95% background. Both player surfaces now use a fully opaque background with no backdrop blur; final operator confirmation remains pending.
  - Notes: Operator confirmed the finished surface and added `0.25×` as the first speed choice. The shared typed speed list, storage validation, and five-column selector now include it; final operator confirmation remains pending.
  - Notes: Operator completed the mobile verification loop and confirmed the placement, controls, pause/resume behavior, styling, and solid background. The final `0.25×` addition passed the full lint, TypeScript, and production build gate.
  - Notes: Follow-up mobile verification showed the generated five-column utility falling back to a single-column grid. The speed selector now declares an explicit five-column grid template, preserving all five speeds on one horizontal line.
