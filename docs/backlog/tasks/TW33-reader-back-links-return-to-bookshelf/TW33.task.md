---
taskId: TW33
status: archived
createdAt: 2026-05-04T08:21:41+02:00
completedAt: 2026-05-04T08:39:48+02:00
group: lingocafe
compressedAt: 2026-07-29T18:25:00+02:00
compressedFromCommit: 27c91fcf1650c9cb49d27cb7c10a6484fbfe4329
---
# Reader Back Links Return To Bookshelf

## Historical Summary

On mobile, the reader's visible header back link returns to `/books`.

## Retrieval Anchors

- Task: `TW33` — Reader Back Links Return To Bookshelf.
- Identifier: `TW33`.
- Identifier: `/books/[bookId]`.
- Identifier: `/books`.
- Identifier: `bookDetailsHref`.
- Identifier: `bookshelfHref`.
- Identifier: `backHref`.
- Identifier: `Back to books`.
- Identifier: `docs/backlog`.
- Identifier: `Back to book details`.
- Identifier: `npm run qa`.
- Identifier: `src/app/(app)/(lingocafe)/books/[bookId]/[pageId]/page.tsx`.
- Identifier: `AppLayout`.
- Path: `docs/backlog`.
- Path: `src/app/(app)/(lingocafe)/books/[bookId]/[pageId]/page.tsx`.
- Path: `src/app/(app)/(lingocafe)/books/_components/BookReaderSurfaces.tsx`.
- Source artifacts: `TW33.notes.md`, `TW33.plan.md`, `TW33.task.md`.

## Durable Outcome and Decisions

- Treat the operator's "yes and execute it" as plan acceptance plus execution approval.
- Move `TW33` from Drafts to WIP before source changes.
- Keep desktop/AppLayout reader back navigation pointed at `/books/[bookId]`.
- Change only the mobile reader header back link target to `/books`.
- Split the read page hrefs into `bookDetailsHref` and `bookshelfHref` instead of reusing one shared `backHref`.
- Update the mobile reader header link accessible label to `Back to books`.
- Operator expanded the rule after initial execution: desktop/AppLayout reader back navigation should also go to `/books`.
- Removed the separate `bookDetailsHref` and now pass `bookshelfHref` to AppLayout, mobile reader, and desktop reader.

## Validation and Limitations

- `npm run qa` passed.
- `npm run qa` passed again after applying the bookshelf rule to desktop.
- Limitations: Static verification passed for the first pass: mobile receives `/books`, desktop/AppLayout receive `bookDetailsHref`, and desktop keeps `Back to book details`. Expanded static verification passed: AppLayout, mobile reader, and desktop reader all receive `/books`; both reader labels say `Back to books`. Browser smoke was not run in this execution pass.

## Task Relationships

### Supersedes

None identified.

### Superseded by

None identified.

### Related Tasks

None identified.

## Compression Provenance

Compressed 3 artifacts: `TW33.notes.md`, `TW33.plan.md`, `TW33.task.md`. Their exact pre-compression versions are recoverable from `compressedFromCommit` (`27c91fcf1650c9cb49d27cb7c10a6484fbfe4329`), for example:

```bash
git show 27c91fcf1650c9cb49d27cb7c10a6484fbfe4329:docs/backlog/tasks/TW33-reader-back-links-return-to-bookshelf/TW33.task.md
```
