# Plan — Add Multi-App In-App Notifications and Admin Management

## Goal

Deliver an app-scoped, extensible communication system with normalized PostgreSQL storage, protected backoffice authoring, reusable in-app delivery, qualified display tracking, and authenticated notification history.

## Milestones

### Milestone 1 — Establish the communication domain and schema

- [x] Step 1 — Add normalized communication storage
  - Achieve: Store app-scoped communications, audience membership, user state, and append-only display events with lifecycle and same-app safety.
  - Create: Reversible Knex migration in `knex/migrations/`.
  - Modify: RBAC seeds and schema documentation.
  - Delete: Nothing.
  - Touch points: `42go_data`, `auth.users`, app-scoped roles and grants.
  - Validation: Migration status/rebuild where available and schema-focused automated tests.
  - Notes: Hard deletion cascades dependent data; creator deletion sets `created_by` to null.

- [x] Step 2 — Build typed validation and query services
  - Achieve: Centralize kinds, templates, limits, audience rules, lifecycle transitions, eligibility, response validation, and history ordering.
  - Create: Shared communication domain modules under `src/42go/communications/`.
  - Modify: Shared exports as required.
  - Delete: Nothing.
  - Touch points: Knex, Zod, sanitized Markdown, server-derived app/user identity.
  - Validation: Unit tests for lifecycle, templates, polls, inputs, URLs, and eligibility helpers.
  - Notes: Published semantic fields are immutable; email remains authorable but unconsumed.

### Milestone 2 — Expose secure user and backoffice APIs

- [x] Step 1 — Add authenticated user notification APIs
  - Achieve: List eligible in-app items, append qualified displays, submit idempotent reactions/responses, and paginate user history.
  - Create: `/api/notifications` route handlers.
  - Modify: Feature configuration/types.
  - Delete: Nothing.
  - Touch points: `protectRoute`, session identity, app resolution, `api:notifications`.
  - Validation: Route/service tests for app isolation, audience modes, schedule boundaries, and response validation.
  - Notes: Every endpoint derives app and user server-side.

- [x] Step 2 — Add protected backoffice APIs
  - Achieve: List, create, inspect, edit, publish, abort, and delete communications plus current-app audience user lookup.
  - Create: `/api/backoffice/notifications` route handlers.
  - Modify: Grant definitions/seeds.
  - Delete: Nothing.
  - Touch points: Backoffice role, exact notification grants, lazy details aggregates.
  - Validation: Authorization and lifecycle API tests.
  - Notes: Strong title confirmation is enforced when collected user data exists.

### Milestone 3 — Build reusable user delivery and history UI

- [x] Step 1 — Implement the shared notification container
  - Achieve: Fetch once on mount, display one item at a time, record qualified visibility, validate user interactions, animate queue progress, and render nothing when empty.
  - Create: Container/presentation/hooks under `src/42go/components/Notifications/`.
  - Modify: Shared component exports and Markdown/media rendering.
  - Delete: Nothing.
  - Touch points: IntersectionObserver, Page Visibility API, reduced motion, same-origin fetch.
  - Validation: Component tests for empty/queue states and visibility timing.
  - Notes: No polling, focus refetch, reconnect refetch, push, or SSR data fetching.

- [x] Step 2 — Add placements and authenticated history page
  - Achieve: Default profile placement, explicit LingoCafe/QuickList placements, and `/notifications` active/history experience with ten-item Load more pagination.
  - Create: Client-only `(app)/notifications` page.
  - Modify: AppConfig contract, profile, books, QuickList pages/configs.
  - Delete: Nothing.
  - Touch points: `page:notifications`, `showInProfile`, `showHistoryLink`.
  - Validation: App-specific rendering checks and feature-flag behavior.
  - Notes: History link appears only when both configuration and page feature permit it.

### Milestone 4 — Build the mobile-first backoffice

- [x] Step 1 — Implement notification list and date/status presentation
  - Achieve: Render a client-only newest-first management list with kind, lifecycle, audience, status-aware dates, and concise content.
  - Create: `/backoffice/notifications` page and shared backoffice components.
  - Modify: Consolidate `DisplayDate` into one accessible future-aware implementation.
  - Delete: Duplicate `DisplayDate` implementation.
  - Touch points: `AppLayout`, semantic Panels, Lucide icons, browser locale/timezone.
  - Validation: Keyboard, mobile, desktop, light, and dark visual checks.
  - Notes: List rows avoid aggregate subqueries.

- [x] Step 2 — Implement authoring and details panels
  - Achieve: Create immutable kinds, edit drafts, select audiences, publish/abort, inspect lazy-loaded metrics/history, and safely delete.
  - Create: Responsive editors, audience picker, details and delete flows.
  - Modify: Backoffice page components.
  - Delete: Nothing.
  - Touch points: Shared Modal panel, grants, current-app user filtering.
  - Validation: Mobile authoring, focus handling, destructive confirmation, and per-kind field tests.
  - Notes: Published records are read-only except abort and delete.

### Milestone 5 — Verify, document, and close the task

- [x] Step 1 — Complete automated and visual verification
  - Achieve: Cover domain invariants, security boundaries, rendering behavior, and responsive UX.
  - Create: Focused tests and any test fixtures.
  - Modify: Test scripts only if required.
  - Delete: Nothing.
  - Touch points: Node test suite, `npm run qa`, ngrok/browser verification.
  - Validation: All focused tests and `npm run qa` pass.
  - Notes: Record environmental limitations instead of weakening checks.

- [x] Step 2 — Publish operator and developer documentation
  - Achieve: Explain schema, lifecycle, routes, APIs, AppConfig, grants, authoring kinds, validation, tracking, `/backoffice`, and extension points.
  - Create: Communication feature documentation.
  - Modify: Memory Bank feature/architecture indexes where appropriate.
  - Delete: Nothing.
  - Touch points: `docs/articles`, `docs/memory-bank`.
  - Validation: Documentation matches accumulated migrations and shipped API/UI behavior.
  - Notes: User-admin relocation and `users:edit` remain LG69 and YS05.

## Plan Notes

- The operator’s instruction to “plan and execute this task” is accepted-plan authorization for this workflow.
- Implementation will preserve the existing users routes and authorization behavior; OE92 only adds the agreed notification domain.
- All milestones completed. The first-release backoffice list is bounded to the newest 100 root rows; per-message aggregates remain lazy in details.
- Visual QA found and fixed immediate history expansion after the final active acknowledgement.
