# Execution Notes — Add Multi-App In-App Notifications and Admin Management
**Task**: ./OE92.task.md
**Plan**: ./OE92.plan.md

## Decisions

- The operator’s “plan and execute” instruction accepted the milestone plan and authorized immediate execution.
- Communication remains the extensible storage domain; notification remains the user-facing route and backoffice vocabulary.
- Existing user administration relocation and `users:edit` correction remain separate tasks LG69 and YS05.
- `"42go_data"` uses normalized root, audience, user-state, and append-only display-event tables with composite same-app references.
- Published records are immutable; abort preserves evidence; hard delete cascades it.
- Visual verification used a temporary published fixture and temporary test-user backoffice memberships. All temporary rows were removed afterward.

## Problems Encountered

- The repository-local backlog-core path referenced by the generic skill is absent. The installed shared skill path was used instead.
- The first sandboxed QA build hit a Turbopack helper-process loopback restriction. The approved unrestricted retry passed.
- The configured ngrok endpoint was offline. It was reopened temporarily against the managed dev server for visual verification, then stopped.
- Visual QA found that history did not open immediately after the final active acknowledgement. Queue-length reporting was corrected and reverified.

## Deviations From Plan

- The admin list uses a bounded newest-first first release query rather than adding an admin-side pagination control. Per-message aggregates remain lazy in details as required.

## Additional Requests

None.

## Known Limitations

- Published email rows are intentionally unconsumed; no sender exists in this task.
- Direct asset upload and communication export remain future work.

## Verification

- `make migrate.redo` passed after the final migration changes.
- `npm run test:communications` passed 11 tests.
- `npm run qa` passed lint, TypeScript, production compilation, and route generation.
- Browser verification covered desktop backoffice empty state and kind menu, desktop/mobile poll editor, profile placement, Markdown, history link, acknowledgement transition, and automatic history expansion at 390px.
