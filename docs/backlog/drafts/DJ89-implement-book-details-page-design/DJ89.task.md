---
taskId: DJ89
status: draft
createdAt: 2026-04-28T08:06:01+02:00
updatedAt: 2026-04-28T11:23:21+02:00
group: lingocafe
reviewedAt: 2026-04-28T11:12:57+02:00
---

# Implement Book Details Page Design

## Elevator's Pitch

Adapt the LingoCafe book details page to match the design attached to this task, with deliberate layouts for both mobile and desktop.

## Business Gain

The book details page is where a reader decides whether to start or continue reading. It should feel polished, focused, and trustworthy on every viewport. Chuck Norris does not inspect a rough detail page. The detail page inspects itself first.

## Current State

The LingoCafe book detail route already exists at `/books/[bookId]`.

Completed work established the canonical route, browser-side detail loading, cover rendering, metadata, description, mobile full-screen presentation, desktop main-page navigation, back navigation, and `book-info` event tracking.

The existing UI was built to make the flow functional. It now needs to be adapted to the supplied visual design.

## Desired State

The book details page matches the attached design closely on desktop and mobile.

![target design](./book-card-design.png)

Desktop should use the designed detail-page layout as the main authenticated app page: app sidebar stays visible, the content uses a breadcrumb row, cover and detail metadata sit side by side, the primary reading action is prominent, and the page continues with "About this book" plus an `Innehåll` section.

Mobile should keep the current full-screen detail container behavior and focus the redesign on the content inside it: top bar with back button and "Book details" title, large cover at the top, compact metadata, full-width primary reading action, secondary icon actions, description preview with "Read more", and a shortened "Contents" list with "Show all chapters".

## Definition of Success

The `/books/[bookId]` detail surface visually matches the attached design on desktop and mobile, including the book metadata presentation and chapter list shown in the design, without regressing data loading, cover fallback behavior, back navigation, reading action behavior, event tracking, or authenticated app layout policy.

## Additional Context

The operator asked: "implement books details page design. Use the designed that is attached to this task (I will add the link to the image before review) and adapt the boo details page for both mobile and desktop"

The intended wording is assumed to be "book details page."

The design reference is attached in this task folder as `book-card-design.png`.

The design shows:

- A desktop details page inside the existing app shell, with sidebar navigation and breadcrumb `Books > <book title>`.
- A large `2:3` cover on the left and book metadata on the right.
- A language/level pill such as `SV / B2`.
- Title, subtitle/adaptation text, tags, adapted-by/source-author notes, and compact metadata for level, language, and pages.
- A prominent black primary "Start reading" button with book icon.
- Secondary icon buttons for bookmark, favorite, and more actions.
- An "About this book" section.
- A table-of-contents section with chapter rows, page counts, chevrons, and a "show all chapters" control.
- A mobile full-screen details view with top bar, cover-first layout, carousel dots under the cover, full-width primary action, secondary action buttons, compact about text, and a shorter visible table of contents.

Use basic English placeholder copy for interface labels for now. App-level i18n will be implemented later.

Related existing work:

- `IL68` introduced the canonical `/books/[bookId]` detail route and book-info view.
- `DG66` introduced cover URL resolution, placeholder fallback, and stable cover rendering.
- `PR58` introduced the "Read now" or "Continue reading" action state on book details.
- `ZE11` introduced the reading route that the detail action points to.
- `NN45` is a separate draft for the books shelf design and should remain scoped to `/books` catalog cards.
- `VU18` is a separate draft for the book page read design and should remain scoped to `/books/[bookId]/[pageId]`.

## Assumptions

The implementation should update the existing LingoCafe book detail page and its route-local components rather than replacing the route or data flow.

The attached design will be the source of truth for visual details once its link is added.

The task targets the book details page, not the books shelf/card design or the page read view.

The attached design requires a table-of-contents area. For this first release, it can use basic English placeholder labels and available `lingocafe.books_pages` data where practical.

Secondary bookmark, favorite, and more action buttons should be UI-only for the first release. Build them so they are easy to comment out or remove because the functionality is not part of this release. This should stay aligned with the same instruction in `NN45`.

Mobile should keep the current full-screen container and route behavior. This task should focus on redesigning the content inside that container.

## Constraints

Follow the authenticated app-page convention for routes under `(app)`: client component, `AppLayout`, and browser-side fetching with `credentials: "same-origin"`.

Preserve `/books/[bookId]` as the canonical detail route.

Preserve the existing LingoCafe API paths and response behavior unless the design requires data that is not currently exposed.

Preserve desktop main-page navigation and mobile full-screen presentation behavior from `IL68`.

Do not rebuild the mobile container/shell; keep the existing full-screen container and adapt the content layout inside it.

Preserve `book-info` event tracking.

Preserve cover URL and placeholder fallback behavior from `DG66`.

Preserve reading action behavior from `PR58`.

If table-of-contents data is added to the detail API, keep it under the existing LingoCafe detail endpoint and validate it server-side.

Do not reintroduce horizontal overflow on mobile.

Run `npm run qa` after implementation.

## Acceptance Criteria

- [ ] The task contains a link to the attached design before review.
- [ ] `/books/[bookId]` visually matches the attached design on desktop.
- [ ] `/books/[bookId]` visually matches the attached design on mobile.
- [ ] Desktop shows the existing app sidebar and a breadcrumb path similar to `Books > <book title>`.
- [ ] Desktop lays out the cover and metadata side by side.
- [ ] Mobile shows a top bar with a back button and `Book details` title.
- [ ] Mobile lays out the cover first, followed by compact metadata and actions.
- [ ] Desktop keeps the book detail as main page navigation.
- [ ] Mobile keeps the book detail as a full-screen detail presentation above the app UI.
- [ ] Mobile keeps the existing full-screen container behavior and only redesigns the content inside it.
- [ ] The detail page still renders cover, title, author, tags, description, and reading action where available.
- [ ] The detail page renders a combined language/level badge like the design when those fields are available.
- [ ] The detail page renders compact metadata for level, language, and page count when available.
- [ ] The detail page renders the designed secondary action buttons as UI-only controls that are easy to comment out or remove.
- [ ] The detail page uses basic English placeholder labels because app-level i18n is out of scope.
- [ ] The detail page renders a "Contents" table-of-contents section using available real book-page data where practical.
- [ ] Table-of-contents rows link to the corresponding read pages when those rows come from real book-page data.
- [ ] Mobile limits the initially visible description and table-of-contents rows, with expansion controls matching the design.
- [ ] Cover fallback behavior still works for missing or failed cover assets.
- [ ] The back button still returns to `/books`.
- [ ] Opening a valid book detail still records a `book-info` event.
- [ ] The reading action still points to the correct read or continue-reading route.
- [ ] Loading, 404, and error states remain clear and non-crashing.
- [ ] Mobile layout has no horizontal scroll caused by the detail page.
- [ ] Text, metadata, badges, cover, and actions fit their containers at common mobile and desktop widths.
- [ ] The implementation keeps accessibility basics intact for buttons, links, focus states, and readable contrast.
- [ ] `npm run qa` passes after implementation.

## Dos

- Do use the attached design as the visual source of truth once it is added.
- Do adapt the existing book detail page and route-local components.
- Do keep the work separate from the books shelf/card design task.
- Do keep the work separate from the book page read design task.
- Do preserve the established route, API, cover, tracking, and reading-action behavior.
- Do verify desktop and mobile responsive behavior.
- Do keep dimensions and wrapping stable so the detail page does not shift or overflow unpredictably.
- Do use existing Tailwind/shadcn and LingoCafe component patterns where practical.
- Do source table-of-contents rows from existing book-page data where practical.
- Do use basic English UI labels for now.
- Do make non-functional secondary actions easy to comment out or remove before release.
- Do keep the existing mobile full-screen container and focus on the detail content.

## Don'ts

- Don't start implementation before the design link is attached or otherwise available.
- Don't redesign the `/books` shelf in this task.
- Don't redesign the `/books/[bookId]/[pageId]` read view in this task.
- Don't change the canonical detail route away from `/books/[bookId]`.
- Don't remove the mobile full-screen detail behavior unless the design explicitly requires a replacement.
- Don't break book-info tracking or reading action links.
- Don't add new API data fields unless the design cannot be implemented with the current payload.
- Don't introduce decorative styling that conflicts with the attached design.
- Don't fake table-of-contents rows with hardcoded chapter titles.
- Don't implement persistence for bookmark or favorite actions unless explicitly confirmed as in scope.
- Don't implement app-level i18n in this task.
- Don't rebuild the mobile full-screen shell/container.

## Open Questions

Q: What is the link or file path for the attached design image?
A: `docs/backlog/drafts/DJ89-implement-book-details-page-design/book-card-design.png`

Q: Does the design require new data fields beyond the current detail payload?
A: Likely yes for the `Innehåll` table-of-contents section and page/chapter counts. The current `ReaderBookInfo` payload has book metadata and `readingAction`, but not a list of book pages.

Q: Should the mobile design keep the existing full-screen overlay-style detail exactly, or adapt it into a different full-screen pattern shown by the design?
A: Keep the current full-screen container behavior. Focus on adapting the content inside the container.

Q: Are loading, error, and missing-book states covered by the design?
A: No. Preserve the current clear non-crashing states and keep any styling changes minimal.

Q: Should bookmark, favorite, and more buttons be functional in this task, hidden, or rendered as inactive visual controls?
A: Build the UI only and make it easy to comment out or remove. The functionality is not part of the first release.

Q: Should the `Innehåll` section be implemented now with real clickable chapter/page rows, or deferred to a separate task?
A: Implement the visual section now using basic English placeholder labels. Use available real book-page data where practical; app-level i18n comes later.

## Related to

- [NN45: Implement Books Shelf Design](../NN45-implement-books-shelf-design/NN45.task.md)
- [VU18: Implement Books Page Read Design](../VU18-implement-books-page-read-design/VU18.task.md)
- [IL68: Open Book Info Page From Books List](../../completed/IL68-open-book-info-page-from-books-list/IL68.task.md)
- [DG66: Display Book Covers](../../completed/DG66-display-book-covers/DG66.task.md)
- [PR58: Show Read Now or Continue Reading on Book Details](../../completed/PR58-show-read-now-or-continue-reading-on-book-details/PR58.task.md)
- [ZE11: Book Page Read UI](../../completed/ZE11-book-page-read-ui/ZE11.task.md)
