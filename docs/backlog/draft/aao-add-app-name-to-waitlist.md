# Add the app name into the waitlist table to track what app the user is waiting for [aao]

Add a new column to the waitlist table to store the app name. This will allow tracking which app each user is waiting for.

## Goals

- [ ] Add `app_id` column to the waitlist table
- [ ] Update backend logic to handle the new column
- [ ] Update any relevant documentation

## Acceptance Criteria

- [ ] Waitlist table includes an `app_id` column
- [ ] App name is stored when a user joins the waitlist
- [ ] Documentation is updated to reflect the change
