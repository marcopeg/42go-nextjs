---
taskId: QS23
status: draft
createdAt: 2026-05-12T14:52:30+02:00
updatedAt: 2026-05-12T14:52:30+02:00
group: security
---

# Fix Docker Security Gate Findings

## Elevator's Pitch

Resolve Docker release security gate findings before publishing production images.

## Business Gain

Publishing should fail before vulnerable images, leaked local artifacts, or risky production compose settings reach Docker Hub or CapRover.

## Current State

- The security gate reported 10 findings, including 4 blocking findings.
- Sanitized report: /private/tmp/42go-security-check-current-findings.md
- MEDIUM: .dockerignore is missing release-safety exclusions (.dockerignore)
- MEDIUM: Sensitive local artifacts exist in the repository tree, including database dump files under `db-backups/` and `knex/dumps/` (source tree)
- HIGH: Production compose uses env_file (docker-compose.prod.yml)
- MEDIUM: Production compose exposes PostgreSQL on the host (docker-compose.prod.yml)
- LOW: Production compose config includes host-gateway (docker-compose.prod.yml)
- LOW: Production compose bind-mounts contents (docker-compose.prod.yml)
- HIGH: Container image has high or critical vulnerabilities (trivy image)
- HIGH: Docker configuration has high or critical misconfigurations (trivy config)
- HIGH: Repository secret scan found sensitive-looking material (trivy fs secret)
- MEDIUM: Runtime image contains source maps (docker image contents)

## Desired State

The Docker security gate passes with no blocking findings and can be used before publish/deploy work.

## Definition of Success

- `npm run qa` passes.
- `trivy image`, `trivy config`, and source secret scans pass or have documented accepted exceptions.
- Runtime image has no env files, npmrc files, SQL/dump artifacts, docs/backlog files, or agent files.
- Production compose exposes only required ports and uses explicit runtime environment variables.

## Additional Context

- Generated by `.agents/skills/42go-docker-security-check/scripts/run_security_check.py`.
- Image scanned: `42go-next:latest`.
- Secret values are intentionally omitted from this task.

## Assumptions

information is missing

## Constraints

- Do not paste secret values or raw environment files into the backlog.
- Keep fixes compatible with the existing Dockerfile and multi-app runtime.

## Acceptance Criteria

- [ ] Run `python3 .agents/skills/42go-docker-security-check/scripts/run_security_check.py --build --image 42go-next:latest --draft --fail-on-findings`.
- [ ] Blocking findings are fixed or explicitly documented as accepted risk.
- [ ] `npm run qa` passes after fixes.
- [ ] The sanitized security report is linked from task notes or output.

## Dos

information is missing

## Don'ts

information is missing

## Open Questions

information is missing

## Related to

- TK89 Docker Image Security Review
