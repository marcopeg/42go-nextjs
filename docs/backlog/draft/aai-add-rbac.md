# Add RBAC to the users system [aai]

Add guards that are similare to the configuration-based feature flags to protect pages and apis or maybe even single components by defining the database-driven RBAC system.

The database structure is already present (look at migrations) and we can even look at the old project how we implemented it there and find inspiration.
